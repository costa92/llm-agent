name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: go mod tidy (drift check)
        run: |
          go mod tidy
          # llm-agent is stdlib-only, so go.sum may not exist. Use
          # `git status --porcelain` (handles missing files cleanly)
          # instead of `git diff --exit-code` (fatals on absent paths).
          drift=$(git status --porcelain go.mod go.sum 2>/dev/null)
          if [ -n "$drift" ]; then
            echo "go mod tidy changed files — commit tidy changes first"
            echo "$drift"
            git --no-pager diff -- go.mod go.sum || true
            exit 1
          fi
      - name: examples go mod tidy (drift check)
        run: |
          cd examples
          go mod tidy
          drift=$(git status --porcelain go.mod go.sum 2>/dev/null)
          if [ -n "$drift" ]; then
            echo "examples go mod tidy changed files — commit tidy changes first"
            echo "$drift"
            git --no-pager diff -- go.mod go.sum || true
            exit 1
          fi
      - name: go vet
        run: go vet ./...
      - name: go build
        run: go build ./...
      - name: go test
        run: go test ./...
      - name: examples — vet + build
        run: |
          cd examples
          go vet ./...
          go build ./...
