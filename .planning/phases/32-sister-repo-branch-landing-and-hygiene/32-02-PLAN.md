---
phase: 32-sister-repo-branch-landing-and-hygiene
plan: 02
type: execute
wave: 1
repo: llm-agent-customer-support
depends_on: []
files_modified:
  - "(git state — llm-agent-customer-support main branch)"
requirements: [ECO-02]
must_haves:
  truths:
    - "llm-agent-customer-support local main is in sync with origin/main (the CI fix is already merged there)"
    - "main builds and tests green"
    - "no tag is cut and nothing is pushed in this slice"
  artifacts:
    - path: "(git: llm-agent-customer-support local main)"
      provides: "a main branch that reflects the repo's true current state"
      contains: "in sync with origin/main; stale local branches pruned"
---

<objective>
Bring `llm-agent-customer-support`'s local checkout onto a current
`main`. The CI-governance fix the v1.1 SUMMARY flagged is **already
merged to `origin/main`** (PR #4) — so this slice is sync + verify +
local-branch hygiene, **not a merge**. ECO-02, KE-4.

**Local git work only — no push, no tag.**
</objective>

<context>
- Repo: `/tmp/llm-agent-customer-support`. The local checkout is parked on
  `fix/pr-governance-auto-merge-permissions`.
- Re-audit against `origin` (correcting the v1.1 SUMMARY): that fix branch
  is **already merged to `origin/main`** —
  `2ffccce Merge pull request #4 from
  costa92/fix/pr-governance-auto-merge-permissions`. The local checkout is
  simply 1 commit behind `origin/main`. **No merge is needed.**
- Stale local branches to prune: `docs/link-governance-guides`,
  `fix/pr-governance-auto-merge-permissions`,
  `fix/released-function-call-compat`.
- `llm-agent-customer-support` is intentionally demo-grade — v1.1 aligns
  its dependencies (Phase 33), it does not productionize it. This slice
  only lands the branch state.
- See `32-RESEARCH.md` (re-audit + Decision 2).
</context>

<tasks>
1. `cd /tmp/llm-agent-customer-support && git fetch origin`.
2. Switch to a current `main`:
   `git checkout main && git merge --ff-only origin/main`. If `--ff-only`
   fails (local `main` diverged), report it and stop — do not force.
3. Confirm there is genuinely nothing to merge — verify the CI fix is on
   `main`: `git log --oneline | grep -i 'auto-merge\|governance'` shows
   the PR #4 merge. Record this in the SUMMARY (it corrects the SUMMARY's
   "2 unmerged commits" claim).
4. Verify `main` builds and tests green:
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`.
   (The `llm-agent-otel`/`llm-agent-providers` dep bumps are **Phase 33**
   — verify against the current `go.mod`.)
5. Prune stale **local** branches: `git branch -d` (or `-D` if confirmed
   stale) `docs/link-governance-guides`,
   `fix/pr-governance-auto-merge-permissions`,
   `fix/released-function-call-compat`. Do **not** delete remote
   branches — list them in the SUMMARY for the operator.
6. Run every command in `<verify>`.

Out of scope:
- Any merge — the fix is already on `origin/main`.
- `git push`, `git tag` — milestone-close / Phase 33.
- The `llm-agent-otel`/`llm-agent-providers` dep bumps — Phase 33.
- Productionizing the demo — a standing non-goal.
</tasks>

<verify>
- local `main` is in sync with `origin/main`:
  `cd /tmp/llm-agent-customer-support && git log --oneline origin/main..main; git log --oneline main..origin/main` (both empty)
- the CI fix is present on `main`:
  `cd /tmp/llm-agent-customer-support && git log --oneline | grep -qi 'pull request #4\|auto-merge' && echo FIX-PRESENT`
- `main` builds + tests green:
  `cd /tmp/llm-agent-customer-support && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /tmp/llm-agent-customer-support && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- no tag created:
  `cd /tmp/llm-agent-customer-support && git tag --points-at HEAD` (empty)
- stale local branches gone:
  `cd /tmp/llm-agent-customer-support && git branch | grep -E 'docs/link-governance|fix/pr-governance|fix/released-function' && echo STILL-THERE || echo PRUNED`
- working tree clean:
  `cd /tmp/llm-agent-customer-support && git status --short` (empty)
</verify>

<acceptance>
- `llm-agent-customer-support`'s local `main` is in sync with
  `origin/main`; the CI-governance fix is confirmed already present.
- `main` builds and `go test ./...` passes.
- Stale local branches are pruned; remote branches untouched and listed
  for the operator.
- No tag was cut; nothing was pushed.
- All `<verify>` commands pass.
</acceptance>
