---
phase: 32-sister-repo-branch-landing-and-hygiene
plan: 01
type: execute
wave: 1
repo: llm-agent-otel
depends_on: []
files_modified:
  - "(git state — llm-agent-otel main branch)"
requirements: [ECO-02]
must_haves:
  truths:
    - "llm-agent-otel main carries the otelrag RAG-wrapping feature"
    - "the merged main builds and tests green"
    - "no tag is cut and nothing is pushed in this slice"
  artifacts:
    - path: "(git: llm-agent-otel local main)"
      provides: "a main branch that reflects the repo's true current state"
      contains: "the 4-commit otelrag feature merged; in sync with origin/main otherwise"
---

<objective>
Land the stranded `otelrag` RAG-wrapping feature on `llm-agent-otel`'s
`main`: sync local `main` to `origin/main`, merge
`feat/otelrag-wrap-rag-system` into `main`, verify the merged `main`
builds and tests green, and prune stale local branches. ECO-02, KE-4.

**Local git work only — no push, no tag.**
</objective>

<context>
- Repo: `/tmp/llm-agent-otel`. The local checkout is on
  `feat/otelrag-wrap-rag-system`.
- Relative to `origin/main`, that branch is 5 commits ahead (the 4-commit
  `otelrag` feature — `057bd68`/`40b0fce`/`12b647e`/`4ddbc4c` — plus a
  governance-workflow fix `d982ad4`) and 3 behind (the same governance
  fix + an idempotency fix, already on `origin/main` via PR #3).
- Local `main` is stale (4 commits behind `origin/main`).
- A trial merge was verified clean: `git merge-tree --write-tree
  origin/main feat/otelrag-wrap-rag-system` exits 0, zero conflicts.
- Stale local branches to prune: `docs/link-governance-guides`,
  `fix/pr-governance-auto-merge-permissions`.
- See `32-RESEARCH.md` (re-audit + Decision 1 — the git-write boundary).
</context>

<tasks>
1. `cd /tmp/llm-agent-otel && git fetch origin` — refresh remote refs.
2. Sync local `main` to `origin/main`:
   `git checkout main && git merge --ff-only origin/main`. If `--ff-only`
   fails (local `main` diverged), report it and stop — do not force.
3. Merge the feature branch:
   `git merge --no-ff feat/otelrag-wrap-rag-system` into `main` with a
   clear merge-commit message
   (e.g. `Merge feat/otelrag-wrap-rag-system: otelrag RAG-wrapping`).
   If the merge reports any conflict (the trial said it will not), **stop
   and surface it** — do not hand-resolve without reporting.
4. Verify the merged `main`:
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1` — both green.
   (The dep bump rag `v0.3.0`→`v1.0.0` is **Phase 33**, not here — verify
   against the current `go.mod`.)
5. Prune stale **local** branches: `git branch -d docs/link-governance-guides`
   and `git branch -d fix/pr-governance-auto-merge-permissions` (use
   `-D` only if `-d` refuses because the branch is merged-state-ambiguous
   and you have confirmed it is stale). Do **not** delete `origin/*`
   remote branches — list them in the SUMMARY for the operator instead.
6. Run every command in `<verify>`.

Out of scope:
- `git push` — the merged `main` stays local; the push is a
  milestone-close operator action.
- `git tag` — Phase 33.
- The `llm-agent-rag` dep bump (`v0.3.0`→`v1.0.0`) — Phase 33.
- Deleting remote branches.
</tasks>

<verify>
- `main` carries the otelrag feature:
  `cd /tmp/llm-agent-otel && git log --oneline -8 main | grep -q otelrag && echo FEATURE-LANDED`
- the merged `main` builds + tests green:
  `cd /tmp/llm-agent-otel && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /tmp/llm-agent-otel && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- `main` is ahead of `origin/main` (the merge landed locally, unpushed):
  `cd /tmp/llm-agent-otel && git log --oneline origin/main..main | head`
- nothing pushed, no tag created:
  `cd /tmp/llm-agent-otel && git tag --points-at HEAD` (empty)
- stale local branches gone:
  `cd /tmp/llm-agent-otel && git branch | grep -E 'docs/link-governance|fix/pr-governance' && echo STILL-THERE || echo PRUNED`
- working tree clean (the merge is committed, nothing dangling):
  `cd /tmp/llm-agent-otel && git status --short` (empty)
</verify>

<acceptance>
- `llm-agent-otel`'s local `main` contains the merged `otelrag`
  RAG-wrapping feature and is otherwise in sync with `origin/main`.
- The merged `main` builds and `go test ./...` passes.
- Stale local branches are pruned; remote branches are untouched and
  listed for the operator.
- No tag was cut; nothing was pushed.
- All `<verify>` commands pass.
</acceptance>
