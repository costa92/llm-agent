---
phase: 32-sister-repo-branch-landing-and-hygiene
plan: 03
type: execute
wave: 1
repo: llm-agent-providers
depends_on: []
files_modified:
  - "(git state — llm-agent-providers local branches)"
requirements: [ECO-02]
must_haves:
  truths:
    - "llm-agent-providers main is current with origin/main and carries the deepseek/minimax adapters"
    - "main builds and tests green"
    - "no tag is cut and nothing is pushed in this slice"
  artifacts:
    - path: "(git: llm-agent-providers local main)"
      provides: "a confirmed-current main, stale local branches pruned"
      contains: "in sync with origin/main; deepseek + minimax adapters present"
---

<objective>
Confirm `llm-agent-providers`'s `main` reflects its true current state —
current with `origin/main`, building and testing green, with the
deepseek/minimax adapters present — and prune stale local branches. This
is a **verification slice**, not a merge: the repo is already clean on
`main`. ECO-02, KE-4.

**Local git work only — no push, no tag, no merge, no rebase.**
</objective>

<context>
- Repo: `/tmp/llm-agent-providers`. Already on `main`, **in sync with
  `origin/main`** (0 ahead / 0 behind).
- The deepseek + minimax adapters are present on `main`
  (`5b946b4 feat: add minimax adapter`, `c9dbcb4 feat: add deepseek
  adapter`) — real feature work past `v0.1.1`, untagged. Tagging is
  Phase 33.
- Stale local branches to prune: `docs/link-governance-guides`,
  `verify/pr-governance-owner-20260513`.
- KE-1: this slice ships no new work and invents none — `providers` needs
  no merge, no rebase, no tag here. Just confirm + local hygiene.
- See `32-RESEARCH.md` (Decision 2 — confirm-only).
</context>

<tasks>
1. `cd /tmp/llm-agent-providers && git fetch origin`.
2. Confirm `main` is current:
   `git checkout main` then `git log --oneline origin/main..main` and
   `git log --oneline main..origin/main` — both must be empty. If `main`
   has diverged, report it and stop.
3. Confirm the deepseek/minimax adapters are present on `main`:
   `ls deepseek minimax` and
   `git log --oneline | grep -E 'deepseek|minimax'`.
4. Verify `main` builds and tests green:
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1`.
5. Prune stale **local** branches: `git branch -d` (or `-D` if confirmed
   stale) `docs/link-governance-guides` and
   `verify/pr-governance-owner-20260513`. Do **not** delete remote
   branches — list them in the SUMMARY for the operator.
6. Run every command in `<verify>`.

Out of scope:
- Any merge or rebase — `main` is already current.
- `git push`, `git tag` — Phase 33 tags `providers v0.2.0`.
- The optional `llm-agent` dep bump — Phase 33.
</tasks>

<verify>
- `main` current with `origin/main`:
  `cd /tmp/llm-agent-providers && git log --oneline origin/main..main; git log --oneline main..origin/main` (both empty)
- deepseek + minimax present:
  `cd /tmp/llm-agent-providers && test -d deepseek && test -d minimax && echo ADAPTERS-PRESENT`
- `main` builds + tests green:
  `cd /tmp/llm-agent-providers && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /tmp/llm-agent-providers && GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1`
- no tag created:
  `cd /tmp/llm-agent-providers && git tag --points-at HEAD` (empty)
- stale local branches gone:
  `cd /tmp/llm-agent-providers && git branch | grep -E 'docs/link-governance|verify/pr-governance' && echo STILL-THERE || echo PRUNED`
- working tree clean:
  `cd /tmp/llm-agent-providers && git status --short` (empty)
</verify>

<acceptance>
- `llm-agent-providers`'s `main` is confirmed current with `origin/main`,
  builds and tests green, with the deepseek + minimax adapters present.
- Stale local branches are pruned; remote branches untouched and listed
  for the operator.
- No merge, no rebase, no tag, no push.
- All `<verify>` commands pass.
</acceptance>
