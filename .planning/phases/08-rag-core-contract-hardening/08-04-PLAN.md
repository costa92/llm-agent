---
phase: 08-rag-core-contract-hardening
plan: 04
type: execute
wave: 4
repo: llm-agent
depends_on: [08-03]
files_modified:
  - rag/
autonomous: true
requirements: [RAG-CORE-04]
must_haves:
  truths:
    - "the core rag facade preserves historical behavior while consuming the richer standalone contract"
    - "tool responses can surface richer provenance without forcing a breaking API change"
  artifacts:
    - path: "rag/"
      provides: "compatibility-aligned rag facade"
      contains: "wrapper search/ask/tool updates"
---

> Archived planning record only.
> Do not use as current development guide.
> Current development follows live code and `llm-agent-rag` current docs.


<objective>
Align the core `llm-agent/rag` compatibility facade and tool surface with the
new standalone contract, preserving the historical API shape while moving
correctness and diagnostics ownership into `llm-agent-rag`.
</objective>

<tasks>
- Update the core wrapper to consume the new standalone filter and answer
  structures.
- Remove any now-redundant adapter-side patching that the standalone store or
  standalone ask path correctly owns.
- Extend tool responses to expose richer citations or diagnostics where safe.
- Add or update regression tests in the core repo so the compatibility layer
  remains stable for existing callers.
</tasks>

<verify>
- `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./rag -count=1`
- `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
