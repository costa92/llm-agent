---
phase: 08-rag-core-contract-hardening
plan: 03
type: execute
wave: 3
repo: llm-agent-rag
depends_on: [08-02]
files_modified:
  - rag/
  - prompt/
autonomous: true
requirements: [RAG-CORE-03]
must_haves:
  truths:
    - "standalone ask results expose machine-readable citations and diagnostics"
    - "retrieval trace data is stable enough for later eval and feedback phases"
  artifacts:
    - path: "rag/"
      provides: "rich answer envelope"
      contains: "citations + diagnostics + trace"
---

<objective>
Evolve the standalone `Ask(...)` result into a richer answer envelope with
citations, provenance, and retrieval diagnostics while preserving the existing
core values of pluggable prompting and model generation.
</objective>

<tasks>
- Add additive citation and diagnostics fields to `rag.Answer`.
- Define a minimal retrieval trace structure covering query inputs, effective
  filters, and selected hits.
- Populate the richer answer envelope from the standalone ask path.
- Add or extend tests so citations and diagnostics are stable and predictable.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./rag ./prompt ./examples -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
