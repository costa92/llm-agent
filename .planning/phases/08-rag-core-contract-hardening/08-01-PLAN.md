---
phase: 08-rag-core-contract-hardening
plan: 01
type: execute
wave: 1
repo: llm-agent-rag
depends_on: []
files_modified:
  - store/
  - rag/
autonomous: true
requirements: [RAG-CORE-01]
must_haves:
  truths:
    - "the standalone default store enforces real metadata filtering rather than leaving correctness to adapter-side post-processing"
    - "namespace and metadata filters behave deterministically in the reference backend"
  artifacts:
    - path: "store/"
      provides: "reference filter-aware store behavior"
      contains: "search filtering + stats/test coverage"
---

> Archived planning record only.
> Do not use as current development guide.
> Current development follows live code and `llm-agent-rag` current docs.

<objective>
Implement real metadata filtering in `llm-agent-rag`'s default store so the
standalone core owns correct retrieval semantics instead of relying on the core
compatibility facade to patch behavior after the fact.
</objective>

<tasks>
- Expand the store query/filter behavior as needed for deterministic metadata
  matching in the default in-memory backend.
- Implement metadata filtering in `store.InMemoryStore.Search(...)`.
- Add or extend tests for namespace isolation, metadata filter matching, and
  combined namespace-plus-filter search behavior.
- Ensure any stats or helper behavior remains coherent after the filter logic
  lands.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./store ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
