---
phase: 08-rag-core-contract-hardening
plan: 02
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [08-01]
files_modified:
  - store/
  - rag/
autonomous: true
requirements: [RAG-CORE-02]
must_haves:
  truths:
    - "mandatory security trimming is distinct from optional caller filters"
    - "security filters cannot be bypassed by omission or override"
  artifacts:
    - path: "store/store.go"
      provides: "dual-filter query contract"
      contains: "caller filters + mandatory security filters"
---

<objective>
Add non-bypassable security-filter plumbing to the standalone retrieval
contract so future ACL/tenant trimming has an explicit home instead of being
smuggled through ordinary metadata filters.
</objective>

<tasks>
- Extend the standalone store and retrieval query contracts to distinguish
  caller-visible filters from mandatory security filters.
- Update the in-memory store to enforce both filter sets together.
- Add tests that prove security filters are always applied, even when caller
  filters are empty or conflicting.
- Keep the API expansion narrow enough that later phases can build on it
  without another redesign.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./store ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
