---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 01
type: execute
wave: 1
repo: llm-agent-rag
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-rag commit + tag v1.0.1, pushed)"
requirements: [ECO-05]
must_haves:
  truths:
    - "llm-agent-rag/go.mod requires llm-agent v0.5.0"
    - "llm-agent-rag is tagged v1.0.1 on master and pushed; no replace directive"
    - "llm-agent-rag tests pass after the bump"
---

<objective>
Refresh `llm-agent-rag`'s back-edge to `llm-agent v0.5.0`, commit, tag
`v1.0.1`, push. This is the precondition for the strict dep-currency gate
(34-02) firing green across all 5 repos. ECO-05.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag`,
  branch `master`, currently at tag `v1.0.0`.
- `go.mod` today: `require github.com/costa92/llm-agent v0.4.0` — one
  major + one minor stale of core's current `v0.5.0`.
- This is the post-research **Decision Update** in
  `34-RESEARCH.md` lines 15-47 (operator-confirmed 2026-05-20, Option B
  "re-tag rag"). The decision rejects the original "exempt rag from the
  gate" option in favor of strict-equality across all 5 repos.
- KE-2 (rag's frozen public API) is **not** violated: a `go.mod` back-edge
  bump moves no exported symbol. `v1.0.1` is a patch tag — additive /
  chore-only semantics within `v1.x`.
- **Trade-off acknowledged**: `v1.0.0` was cut 2026-05-21 as the freeze
  point and `v1.0.1` lands the day after. The audit doc (34-04) documents
  this explicitly.
- `llm-agent-rag` is **public** since Phase 33 — `GOPRIVATE` is no longer
  required for fetching `costa92/*` modules; the default `GOPROXY` serves
  them. Use `GOWORK=off` to bypass any local workspace file.
- **Operator-gated step**: `git push origin master` and `git push origin
  v1.0.1` MUST be surfaced for explicit operator confirmation before the
  executor runs them. The local commit + annotated tag are not
  operator-gated — only the push to `origin` is. Per standing rule, the
  executor never pushes unilaterally.
- See `34-RESEARCH.md` §Decision Update; §Slice breakdown.
</context>

<tasks>
1. `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag`,
   confirm on `master`, `git status --short` (clean), `git fetch origin --tags`.
2. Bump the back-edge:
   `GOWORK=off go get github.com/costa92/llm-agent@v0.5.0`
   then `GOWORK=off go mod tidy`.
3. Confirm `go.mod` now requires `github.com/costa92/llm-agent v0.5.0` and
   carries **no `replace` directive**.
4. Verify green:
   `GOWORK=off GOCACHE=/tmp/go-build go vet ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1` —
   all green. If a bump breaks anything (e.g. the cross-repo `contract`
   gate), **stop and surface it** — do not amend.
5. Commit: `git add go.mod go.sum` then `git commit -m "chore: bump
   llm-agent back-edge to v0.5.0"`.
6. Tag: `git tag -a v1.0.1 -m "v1.0.1 — back-edge bump to llm-agent
   v0.5.0; no public API change (KE-2)"`.
7. **Operator gate**: surface the upcoming `git push` invocations and
   wait for explicit confirmation. Once confirmed:
   `git push origin master` then `git push origin v1.0.1`.
8. Run every command in `<verify>`.

Out of scope:
- Any public-API change in `llm-agent-rag` — KE-2 keeps the frozen
  surface untouched; this slice is back-edge only.
- The umbrella CI gate itself — slice 34-02.
- Cross-repo verification — slice 34-03.
</tasks>

<verify>
- `go.mod` bumped:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && grep -q 'github.com/costa92/llm-agent v0.5.0' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- build + test green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1`
- cross-repo contract test (load-bearing for the back-edge bump — named explicitly so a silent skip is auditable):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test -short -count=1 ./contract/... && echo CONTRACT-OK`
- tag points at HEAD:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && git tag --points-at HEAD | grep -q v1.0.1 && echo TAG-OK`
- tag pushed:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && git ls-remote --tags origin v1.0.1 | grep -q refs/tags/v1.0.1 && echo TAG-PUSHED`
- master pushed:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && git log --oneline origin/master..master | wc -l` (0)
- working tree clean:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-rag && git status --short` (empty)
</verify>

<acceptance>
- `llm-agent-rag` `go.mod` requires `github.com/costa92/llm-agent v0.5.0`; no `replace`.
- `llm-agent-rag` builds and `go test -short ./...` passes after the bump.
- `llm-agent-rag` is committed, tagged `v1.0.1` (annotated), and `master` + the tag are pushed to `origin`.
- No exported symbol of `llm-agent-rag` changed (KE-2 honored — back-edge only).
- All `<verify>` commands pass.
</acceptance>
