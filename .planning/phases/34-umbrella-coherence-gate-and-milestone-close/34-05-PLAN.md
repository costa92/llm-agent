---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 05
type: execute
wave: 5
repo: llm-agent-providers
depends_on: ["34-01", "34-02", "34-03", "34-04"]
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-providers commit + tag v0.2.1, merged via PR — main is protected)"
requirements: [ECO-05]
must_haves:
  truths:
    - "llm-agent-providers/go.mod requires llm-agent v0.5.1"
    - "the bumped repo builds and tests green"
    - "llm-agent-providers is tagged v0.2.1 on the merged commit and pushed; no replace directive"
---

<objective>
Cascade slice 4/4 (Wave 5) — providers transitive bump. The other 3
cascade slices (Waves 2-4) refreshed everything pinning rag@v1.0.1.
`llm-agent-providers` doesn't pin rag but DOES pin `llm-agent`, which
moved to `v0.5.1` in Wave 2; without this slice, the dep-currency gate
(Wave 6) would flag providers' stale `llm-agent v0.5.0` pin. ECO-05.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers`,
  branch `main` (verified clean against origin after Phase 32).
- `go.mod` today: `require github.com/costa92/llm-agent v0.5.0`. No
  rag/otel/customer-support deps (providers depends on core only).
- This gap was discovered DURING Wave 5 execution: Wave 2 cascaded core
  to v0.5.1 to refresh rag's back-edge, and providers' core pin then
  became stale by the strict-equality gate's rule. The operator chose
  `"冲到底(加 Wave 4.5)"` 2026-05-20 to bump providers too.
- **Branch protection is on** for `providers` `main` (same shape as
  otel/customer-support: `required_status_checks = go + governance`,
  `enforce_admins: true`, `allow_force_pushes: false`). Use the PR-merge
  flow (Phase 33-03 + Wave 3/4 precedent).
- No rag dep means the CI's `go mod tidy` won't touch rag; PR CI is
  simpler than otel's.
- This bump is **transitive-only** — no behavioral change in providers'
  adapter code. The tag is `v0.2.1` (patch — chore-only semantics in v0.x).
- No `CHANGELOG.md` — annotated tag message carries the release note.
- **Operator-gated** at PR creation + at tag push (same shape as Waves 3-4).
- See `34-RESEARCH.md` "Decision Update 2 — cascade complete" (post-cascade
  audit will list providers as the 4th cascade slice).
</context>

<tasks>
1. `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers`,
   confirm on `main`, `git fetch origin`, working tree clean.
2. Create a feature branch: `git checkout -b chore/v1.1-cascade-bump`.
3. Bump core: `GOWORK=off go get github.com/costa92/llm-agent@v0.5.1` then
   `GOWORK=off go mod tidy`.
4. Confirm `go.mod` requires `llm-agent v0.5.1`; no `replace`; no
   accidental rag dep introduced.
5. Verify green locally:
   `GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1`.
   All adapter packages (deepseek, minimax, openai, anthropic, ollama)
   must pass. If anything red, **stop and surface**.
6. Stage + commit: `git add go.mod go.sum && git commit -m "chore: bump
   to llm-agent v0.5.1 (v1.1 transitive cascade)"`.
7. Push the feature branch:
   `git push -u origin chore/v1.1-cascade-bump`.
8. Open PR: `gh pr create --title "chore: v1.1 cascade — llm-agent
   v0.5.1" --body "Final cascade slice for v1.1 Phase 34 Wave 5.
   Transitive core bump (providers has no rag dep). Refreshes core pin
   from v0.5.0 to v0.5.1. No code change." --base main --head
   chore/v1.1-cascade-bump`. Capture the PR number.
9. **Operator gate (1/2)**: surface the PR number + URL + initial CI run
   IDs. Wait for orchestrator/operator to confirm CI green.
10. After confirm: `gh pr merge <PR#> --merge --delete-branch` (or
    accept `auto-merge-owner` bot's race per Waves 3-4 precedent).
    Capture merge commit SHA.
11. `git fetch origin && git checkout main && git pull --ff-only origin
    main`. Confirm `main` at the merge commit.
12. Tag: `git tag -a v0.2.1 -m "v0.2.1 — v1.1 transitive cascade:
    llm-agent v0.5.1"`.
13. **Operator gate (2/2)**: surface `git push origin v0.2.1` and wait
    for explicit confirmation. Do NOT push unilaterally.
14. Run every command in `<verify>` except the tag-pushed line —
    `PRE-PUSH-DEFERRED`.
</tasks>

<verify>
- `go.mod` bumped:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers && grep -q 'github.com/costa92/llm-agent v0.5.1' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- no rag dep leaked:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers && ! grep -q 'github.com/costa92/llm-agent-rag' go.mod && echo NO-RAG-DEP`
- build + test green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1 && echo PROVIDERS-GREEN`
- tag points at HEAD:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers && git tag --points-at HEAD | grep -q v0.2.1 && echo TAG-OK`
- main pushed (post-PR-merge):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers && git log --oneline origin/main..main | wc -l` (0)
- tag pushed (PRE-PUSH-DEFERRED until operator confirms):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-providers && git ls-remote --tags origin v0.2.1 | grep -q refs/tags/v0.2.1 && echo TAG-PUSHED`
</verify>

<acceptance>
- `llm-agent-providers` `go.mod` requires `llm-agent v0.5.1`; no `replace`; no rag dep introduced.
- The repo builds and `go test -short ./...` passes after the bump (all 5 adapters).
- PR was opened, CI was green, PR was merged (either manually or auto-merge-owner).
- `llm-agent-providers` is tagged `v0.2.1` (annotated) on the merge commit; push is operator-gated.
- All `<verify>` commands pass (excluding the PRE-PUSH-DEFERRED tag-push line).
</acceptance>
