---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 07
type: execute
wave: 7
repo: llm-agent
depends_on: ["34-01", "34-02", "34-03", "34-04", "34-05", "34-06"]
files_modified:
  - .github/workflows/umbrella.yml
  - scripts/dep-currency-check.sh
requirements: [ECO-04]
must_haves:
  truths:
    - "umbrella.yml has a new dep-currency job that calls scripts/dep-currency-check.sh"
    - "scripts/dep-currency-check.sh is the gate's single source of truth (CI + local)"
    - "gate proves it fires on a synthetic stale dep (in dry-run test)"
    - "gate passes against current 5-repo coordinated tag set"
  artifacts:
    - path: ".github/workflows/umbrella.yml"
      provides: "the umbrella CI workflow with the dep-currency gate step calling the script"
      contains: "Dependency-currency gate"
    - path: "scripts/dep-currency-check.sh"
      provides: "the stdlib-only bash gate body — runnable from CI and locally"
      contains: "Dependency-currency gate PASSED"
---

<objective>
Add a strict dep-currency gate to `umbrella.yml` that fails when any
sister `go.mod` pins a sibling at a version older than that sibling's
latest published release tag. Closes the `test.yml` (stale dep, invisible)
vs `umbrella.yml` (rag master, hides staleness) blind spot that let the
v1.1 drift accumulate silently. ECO-04.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. The gate lives in `.github/workflows/umbrella.yml`.
- Implementation: the bash sketch in `34-RESEARCH.md` §"34-01 implementation
  sketch" lines 216-291 is the source of truth. The step runs **before**
  the existing `go work init` step so failures land before any build.
- Loop set: all 5 repos — `llm-agent`, `llm-agent-rag`, `llm-agent-otel`,
  `llm-agent-providers`, `llm-agent-customer-support`. Per the
  post-research **Decision Update + Decision Update 2 (post-Wave-1 cascade)**,
  rag is **not** exempted; Waves 2-4 (the cascade) brought core +
  otel + customer-support current with rag v1.0.1 so the strict loop
  fires green on the cascaded state:
  - `llm-agent v0.5.1` (rag pin: v1.0.1)
  - `llm-agent-rag v1.0.1` (back-edge: llm-agent v0.5.0 — wait, that was rag-v1.0.1's commit; rag does not pin a cascaded sibling)
  - `llm-agent-otel v0.2.1` (rag pin: v1.0.1; core pin: v0.5.1)
  - `llm-agent-providers v0.2.0` (core pin: v0.5.1 — wait, was this bumped?  NO — providers stays at v0.2.0; it pins llm-agent v0.4.0 still per Phase 33-03)
  - `llm-agent-customer-support v0.2.1` (rag: v1.0.1, core: v0.5.1, otel: v0.2.1, providers: v0.2.0)
  **Validate carefully**: `llm-agent-providers` was NOT re-tagged in v1.1
  Phase 34 because it has no rag dep — but its existing `go.mod` still
  pins `llm-agent v0.4.0`. **Confirm during task 4 (live positive)
  whether the strict gate flags providers as stale on the core dep.** If
  yes, this is a real finding that the cascade missed; surface and stop.
- Mechanism: `git ls-remote --tags https://github.com/costa92/<repo>`,
  filtered to plain `vX.Y.Z` tags (strip `^{}` peeled refs and prereleases),
  sorted `sort -V`, `tail -1` for the latest. Reference: 34-RESEARCH §Decision 2.
- Strictness: equality with the latest plain `vX.Y.Z` release tag. Per
  34-RESEARCH §Decision 3.
- Behavior: hard-fail (block merge). Per 34-RESEARCH §Decision 4.
- Inline-vs-separate-script: **this slice ships BOTH**. The bash body lives
  in `scripts/dep-currency-check.sh` (the single source of truth, stdlib-only
  bash, runnable in CI and locally); `umbrella.yml`'s step is a one-line
  `bash scripts/dep-currency-check.sh`. This resolves 34-RESEARCH §Open
  question 2 affirmatively and matches the `scripts/workspace.sh` precedent.
  Single-source eliminates the YAML-extraction fragility 34-03 verify would
  otherwise face.
- Negative test (red-fire) and positive test (green-pass) MUST both run
  locally before commit. The synthetic stale edit is in the working tree
  only; never committed.
- Core stays stdlib-only — this slice adds bash + YAML, no Go files
  change. The `python3` parser already used by `release-precheck.yml` is
  not required here.
- **Operator-gated**: commit + push are deferred to explicit operator
  confirmation per the standing rule. The slice's `<verify>` runs locally.
- See `34-RESEARCH.md` §"The blind spot the gate closes" and §Decisions 1-4.
</context>

<tasks>
1. `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
   confirm on `main`, `git status --short` (clean modulo `.planning/`).
2. Create `scripts/dep-currency-check.sh` with the bash body from
   34-RESEARCH lines 224-290 verbatim — `#!/usr/bin/env bash`,
   `set -euo pipefail`, the `REPOS` map, the `git ls-remote --tags` loop
   that builds `LATEST`, the per-sister `go.mod` parse loop, the strict
   `pinned != latest` comparison, the readable `::error::` annotation
   (works in CI; readable plain text locally), the final exit-status
   check. Loop set is all 5 repos (`llm-agent-rag` is **in**, not
   exempted — 34-01 made the back-edge current). Skip-self check is
   `[ "$sister" = "$r" ] && continue`. The script discovers sibling
   checkouts via `ECOSYSTEM_ROOT=${ECOSYSTEM_ROOT:-$(cd "$(dirname "$0")/.." && cd .. && pwd)}`
   so it runs identically from CI (where the umbrella workflow checks
   out all 5 siblings adjacent) and locally. `chmod +x scripts/dep-currency-check.sh`.
3. Edit `.github/workflows/umbrella.yml`: insert a new step
   `name: Dependency-currency gate (ECO-04 / KE-6)` **after** the
   `Verify scripts/workspace.sh is byte-identical ...` step and **before**
   the `Initialize go.work pointing all 5 modules ...` step. The step's
   `run:` body is a single line: `bash scripts/dep-currency-check.sh`.
   Single source of truth — CI calls the same script local runs do.
4. Sanity-test the gate locally — positive (green) case. From the
   umbrella checkout's parent:
   `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem &&
   bash llm-agent/scripts/dep-currency-check.sh`. Expected: every sibling
   pin line prints `OK: <sister> -> <r> <v> (current)` and the final
   line is `Dependency-currency gate PASSED — all sibling pins current.`
   Exit 0.
5. Sanity-test the gate locally — negative (red) case. Temporarily edit
   `llm-agent-otel/go.mod` in the working tree to downgrade its `llm-agent`
   pin from `v0.5.0` to `v0.4.0` (do **not** `go mod tidy`, do **not**
   commit, do **not** `git add`). Re-run the script. Expected:
   exit 1 with `::error::llm-agent-otel pins github.com/costa92/llm-agent
   at v0.4.0 but latest is v0.5.0` followed by `Dependency-currency gate
   FAILED — see annotations above.`
6. Restore the synthetic edit: `cd
   /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel
   && git checkout -- go.mod`. Re-run the script and confirm green again.
7. Stage **only** the gate artifacts:
   `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent &&
   git add .github/workflows/umbrella.yml scripts/dep-currency-check.sh`.
   Confirm `git diff --cached --stat` shows only those two files.
8. **Operator gate**: surface the upcoming
   `git commit -m "ci(umbrella): add dependency-currency gate for sibling
   repos"` and `git push origin main` and wait for explicit confirmation
   before running them. Per standing rule, the executor does not commit
   or push unilaterally.
9. Run every command in `<verify>`.

Out of scope:
- Per-sister-repo dep-currency gates. Out of v1.1 scope by RESEARCH §Decision 1.
- Any Go code change in core — this is CI YAML + stdlib bash only.
</tasks>

<verify>
- script exists, is executable, and starts with the bash shebang:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && test -x scripts/dep-currency-check.sh && head -1 scripts/dep-currency-check.sh | grep -q '^#!/usr/bin/env bash' && echo SCRIPT-OK`
- script references the 5 repos:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for r in llm-agent llm-agent-rag llm-agent-otel llm-agent-providers llm-agent-customer-support; do grep -q "\"$r\"" scripts/dep-currency-check.sh || { echo "MISSING $r"; exit 1; }; done && echo REPOS-OK`
- umbrella.yml step calls the script:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q 'Dependency-currency gate' .github/workflows/umbrella.yml && grep -q 'bash scripts/dep-currency-check.sh' .github/workflows/umbrella.yml && echo GATE-STEP-OK`
- step ordering — gate runs before `go work init`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && awk '/Dependency-currency gate/{a=NR}/Initialize go.work/{b=NR}END{print (a>0 && b>0 && a<b) ? "ORDER-OK" : "ORDER-FAIL"}' .github/workflows/umbrella.yml`
- YAML still parses (no syntax break):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/umbrella.yml"))' && echo YAML-OK`
- live positive: script exits 0 on current 5-repo state:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem && bash llm-agent/scripts/dep-currency-check.sh && echo GATE-GREEN-LIVE`
- working tree of sister repos is clean (synthetic negative test restored):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel && git status --short` (empty)
- core stays stdlib-only (no Go change leaked):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff --stat HEAD -- '*.go' go.mod go.sum | wc -l` (0)
</verify>

<acceptance>
- `scripts/dep-currency-check.sh` exists, is executable, is stdlib-only bash, and is the gate's single source of truth.
- `.github/workflows/umbrella.yml` contains the `Dependency-currency gate (ECO-04 / KE-6)` step calling `bash scripts/dep-currency-check.sh`, placed before `Initialize go.work`.
- The script's loop covers all 5 ecosystem repos and uses `git ls-remote --tags` + `sort -V` + strict equality per 34-RESEARCH §Decisions 2-3.
- Negative test (synthetic stale pin) drove the script to exit 1 with a readable `::error::` line; positive test (current state) exited 0 with `Dependency-currency gate PASSED`.
- The working tree of every other repo is clean — no synthetic edit leaked.
- Commit + push are operator-gated and not performed by the executor.
- All `<verify>` commands pass.
</acceptance>
