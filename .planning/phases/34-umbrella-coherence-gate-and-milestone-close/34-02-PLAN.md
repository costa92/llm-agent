---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 02
type: execute
wave: 2
repo: llm-agent (core)
depends_on: ["34-01"]
files_modified:
  - go.mod
  - go.sum
  - CHANGELOG.md
  - "(git: llm-agent commit + tag v0.5.1, pushed direct to main — no branch protection)"
requirements: [ECO-05]
must_haves:
  truths:
    - "llm-agent/go.mod requires llm-agent-rag v1.0.1"
    - "the bumped core builds and tests green (incl. the 7 facade tests Phase 31 fixed)"
    - "llm-agent is tagged v0.5.1 and pushed; no replace directive"
    - "CHANGELOG.md records the v0.5.1 entry"
---

<objective>
Cascade slice 1/3 (Wave 2). Bump the core `llm-agent` rag pin from
`v1.0.0 → v1.0.1` and cut `v0.5.1`. Without this, the dep-currency gate
(34-05) fires red on core; downstream otel + customer-support can't be
re-current with both rag and core simultaneously otherwise. ECO-05.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. **No branch protection** — direct push to `main` is the
  established pattern (Phase 33-01 precedent).
- `go.mod` today: `require github.com/costa92/llm-agent-rag v1.0.0`. Only
  one ecosystem dep (rag); no other consumers inside the umbrella.
- Wave 1 pushed `llm-agent-rag v1.0.1` — a chore-only back-edge bump in
  rag, no public-API change. Core's `go get` against v1.0.1 must build +
  test green; the Phase 31 facade-test repair stays intact (7 tests).
- Cascade origin: the operator chose "冲到底(完整连锁)" 2026-05-20 when
  Wave 2 surfaced the v1.0.1 forward-drift on 3 consumers. This slice and
  34-03 + 34-04 are the cascade.
- Coordinated tag set after this slice: core `v0.5.1` + rag `v1.0.1`;
  otel + cs still on `v0.2.0` (next slices fix that).
- `CHANGELOG.md` needs a `[v0.5.1]` entry — a 2-3 line patch-release note:
  "Bump `llm-agent-rag` dep to `v1.0.1` (back-edge refresh; no public-API
  change)."
- `go get` over SSH — rag is now public, no `GOPRIVATE` needed.
- **Operator-gated:** the commit, the tag, and the push are operator-gated.
  Stage + commit happen locally; the executor STOPS at the push step and
  surfaces the local commit SHA + tag + tag-message for confirm.
- See `34-RESEARCH.md` "Decision Update" and Phase 33 patterns
  (33-01-PLAN style; this slice direct-pushes per that precedent).
</context>

<tasks>
1. `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
   confirm on `main`, `git fetch origin`, working tree clean (modulo
   `.planning/` which is the in-flight phase tree — that's expected).
2. Bump: `GOWORK=off go get github.com/costa92/llm-agent-rag@v1.0.1`
   then `GOWORK=off go mod tidy`.
3. Confirm `go.mod` now `require github.com/costa92/llm-agent-rag v1.0.1`,
   no `replace`, and **no new non-stdlib modules entered** (`go list -deps
   -f '{{if .Module}}{{.Module.Path}}{{end}}' ./... | sort -u` should
   contain only stdlib + `github.com/costa92/llm-agent` + `github.com/costa92/llm-agent-rag`).
4. Verify green:
   `GOWORK=off GOCACHE=/tmp/go-build go vet ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1`. The
   7 Phase-31 facade tests must stay green. If anything red, **stop and
   surface**.
5. Append a `[v0.5.1] - 2026-05-20` entry to `CHANGELOG.md` directly under
   `[Unreleased]` (or directly under the file header if there's no
   Unreleased section). Three lines: section header, one bullet
   "Bump `llm-agent-rag` to `v1.0.1` (back-edge refresh, no public-API
   change)", blank line.
6. Stage **only** the 3 file changes:
   `git add go.mod go.sum CHANGELOG.md`. Confirm
   `git diff --cached --stat` shows only those 3 files.
7. Commit (slice-level atomic commit per established GSD workflow):
   `git commit -m "chore: bump llm-agent-rag to v1.0.1"`.
   Capture the new HEAD SHA.
8. Tag: `git tag -a v0.5.1 -m "v0.5.1 — cascade bump: llm-agent-rag v1.0.1
   (back-edge refresh, no public API change)"`. Confirm `git tag
   --points-at HEAD | grep v0.5.1`.
9. **Operator gate**: surface the upcoming pushes
   `git push origin main` and `git push origin v0.5.1` and PAUSE for
   explicit operator confirmation. Per standing rule, the executor does
   not push unilaterally. Do NOT push.
10. Run every command in `<verify>` except the two that require the push
    to have happened — flag those as `PRE-PUSH-DEFERRED`.
</tasks>

<verify>
- `go.mod` bumped:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q 'github.com/costa92/llm-agent-rag v1.0.1' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- stdlib-only preserved (no new non-stdlib module):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -deps -f '{{if .Module}}{{.Module.Path}}{{end}}' ./... | sort -u | grep -vE '^(github\.com/costa92/llm-agent(-rag)?$|$)' | wc -l` (0)
- build + test green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go build ./... && GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1 && echo CORE-GREEN`
- CHANGELOG entry:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q '\[v0.5.1\]' CHANGELOG.md && echo CHANGELOG-OK`
- tag points at HEAD:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git tag --points-at HEAD | grep -q v0.5.1 && echo TAG-OK`
- tag pushed (PRE-PUSH-DEFERRED until operator confirms; orchestrator re-runs):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git ls-remote --tags origin v0.5.1 | grep -q refs/tags/v0.5.1 && echo TAG-PUSHED`
- main pushed (PRE-PUSH-DEFERRED):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git log --oneline origin/main..main | wc -l` (0 after push)
</verify>

<acceptance>
- `llm-agent` `go.mod` requires `github.com/costa92/llm-agent-rag v1.0.1`; no `replace`.
- Core stays stdlib-only (no new non-stdlib module appears in the dep list).
- Core builds and `go test -short ./...` passes; the 7 Phase-31 facade tests stay green.
- `CHANGELOG.md` records the `[v0.5.1]` entry.
- `llm-agent` is committed locally, tagged `v0.5.1` (annotated); push is operator-gated.
- All `<verify>` commands pass (excluding the two PRE-PUSH-DEFERRED lines).
</acceptance>
