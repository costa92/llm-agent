---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 06
type: execute
wave: 6
repo: llm-agent-customer-support
depends_on: ["34-01", "34-02", "34-03", "34-04", "34-05"]
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-customer-support commit + tag v0.2.2, merged via PR — main is protected)"
requirements: [ECO-05]
must_haves:
  truths:
    - "llm-agent-customer-support/go.mod requires llm-agent-providers v0.2.1 (and the other v1.1 cascaded pins)"
    - "the bumped repo builds and tests green"
    - "llm-agent-customer-support is tagged v0.2.2 on the merged commit and pushed"
---

<objective>
Cascade follow-up slice (Wave 6) — fix the topological-order miss in
Wave 4. Wave 4 bumped customer-support BEFORE providers was re-tagged
(Wave 5); customer-support's `providers v0.2.0` pin is therefore stale
against providers' new `v0.2.1`. Re-bump providers in customer-support,
cut `v0.2.2`. This is the LAST cascade slice — no other repo pins
customer-support, so the chain ends here. ECO-05.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support`,
  branch `main` at `4480246` (Wave 4 merge).
- `go.mod` today (post-Wave-4):
  - `require github.com/costa92/llm-agent v0.5.1` (current — Wave 2)
  - `require github.com/costa92/llm-agent-rag v1.0.1` (current — Wave 1)
  - `require github.com/costa92/llm-agent-otel v0.2.1` (current — Wave 3)
  - `require github.com/costa92/llm-agent-providers v0.2.0` (**STALE — Wave 5 moved providers to v0.2.1**)
- The topological-order error: I planned the cascade as
  `rag → core → otel → cs → providers`. Correct order is
  `rag → core → providers → otel → cs` (customer-support last, so it
  sees the final upstream state). The fix is one extra slice for
  customer-support; no other repo pins customer-support.
- **Branch protection on**; PR-merge flow (same shape as Waves 3-4).
- This bump touches one line in `go.mod`: providers v0.2.0 → v0.2.1.
- Tag is `v0.2.2` (the next patch after v0.2.1; v0.2.1 was Wave 4's tag).
- This is a recorded finding in the v1.1 audit (slice 34-09) under
  Trade-offs / Process insight: strict-equality + diamond DAG = topology-sensitive
  cascade order.
- **Operator-gated** at PR creation + at tag push.
</context>

<tasks>
1. `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support`,
   confirm on `main` at `4480246`, `git fetch origin`, working tree clean.
2. Create feature branch: `git checkout -b chore/v1.1-cascade-followup`.
3. Bump providers:
   `GOWORK=off go get github.com/costa92/llm-agent-providers@v0.2.1` then
   `GOWORK=off go mod tidy`.
4. Confirm `go.mod` now requires `llm-agent-providers v0.2.1` and the
   other 3 sibling pins are unchanged (`llm-agent v0.5.1`,
   `llm-agent-rag v1.0.1`, `llm-agent-otel v0.2.1`). No `replace`.
5. Verify green locally:
   `GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1`.
   If anything red, **stop and surface**.
6. Stage + commit: `git add go.mod go.sum && git commit -m "chore: bump
   llm-agent-providers to v0.2.1 (v1.1 cascade follow-up — topological-order
   fix)"`.
7. Push branch: `git push -u origin chore/v1.1-cascade-followup`.
8. Open PR: `gh pr create --title "chore: v1.1 cascade follow-up — providers
   v0.2.1" --body "Topological-order fix for Phase 34 cascade. Wave 4
   bumped customer-support before Wave 5 retagged providers; this slice
   refreshes customer-support's providers pin. No code change." --base
   main --head chore/v1.1-cascade-followup`. Capture PR number.
9. **Operator gate (1/2)**: surface PR + URL + CI run IDs. Wait for confirm.
10. After confirm: `gh pr merge <PR#> --merge --delete-branch` (or accept
    auto-merge-owner's race). Capture merge commit SHA.
11. `git fetch origin && git checkout main && git pull --ff-only origin
    main`. Confirm `main` at merge commit.
12. Tag: `git tag -a v0.2.2 -m "v0.2.2 — v1.1 cascade follow-up:
    llm-agent-providers v0.2.1"`.
13. **Operator gate (2/2)**: surface `git push origin v0.2.2` and wait.
14. Run `<verify>` minus PRE-PUSH-DEFERRED.
</tasks>

<verify>
- `go.mod` providers bumped:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && grep -q 'github.com/costa92/llm-agent-providers v0.2.1' go.mod && echo PROVIDERS-OK`
- other 3 sibling pins unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && grep -q 'github.com/costa92/llm-agent v0.5.1' go.mod && grep -q 'github.com/costa92/llm-agent-rag v1.0.1' go.mod && grep -q 'github.com/costa92/llm-agent-otel v0.2.1' go.mod && echo OTHERS-UNCHANGED`
- no `replace`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- build + test green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1 && echo CS-GREEN`
- tag points at HEAD:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && git tag --points-at HEAD | grep -q v0.2.2 && echo TAG-OK`
- main in sync (post-PR-merge):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && git log --oneline origin/main..main | wc -l` (0)
- tag pushed (PRE-PUSH-DEFERRED):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && git ls-remote --tags origin v0.2.2 | grep -q refs/tags/v0.2.2 && echo TAG-PUSHED`
</verify>

<acceptance>
- `llm-agent-customer-support` `go.mod` requires `llm-agent-providers v0.2.1`; other 3 sibling pins unchanged; no `replace`.
- The repo builds and `go test -short ./...` passes after the bump.
- PR was opened, CI green, PR merged via PR-merge flow.
- `llm-agent-customer-support` is tagged `v0.2.2` (annotated) on the merge commit; push is operator-gated.
- All `<verify>` commands pass (excluding the PRE-PUSH-DEFERRED tag-push line).
- The cascade chain is **closed**: no other repo pins customer-support; no further re-bump propagates.
</acceptance>
