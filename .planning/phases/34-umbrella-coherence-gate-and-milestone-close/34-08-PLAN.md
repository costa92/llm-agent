---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 07
type: execute
wave: 7
repo: all-5 (verification only)
depends_on: ["34-01", "34-02", "34-03", "34-04", "34-05", "34-06"]
files_modified: []
requirements: [ECO-05]
must_haves:
  truths:
    - "every repo builds clean against the cascaded coordinated tag set"
    - "every repo's tests pass under GOWORK=off"
    - "no replace directives anywhere on tagged branches"
    - "the new dep-currency gate runs green on the cascaded state"
  artifacts:
    - path: ".planning/phases/34-umbrella-coherence-gate-and-milestone-close/34-06-RESULTS.md"
      provides: "the per-repo build/test log, the coordinated tag table, and the gate exit code — cited by 34-07"
      contains: "Coordinated tag set; Build/test results; Gate exit code"
---

<objective>
Prove the v1.1 coordinated tag set is internally consistent end-to-end:
all 5 repos build + test green under `GOWORK=off`, every tagged branch is
`replace`-free, and the dep-currency gate (from 34-02) runs green against
the live state. No code changes. The deliverable is `34-06-RESULTS.md`,
the evidence base the audit doc (34-04) cites. ECO-05.
</objective>

<context>
- Verification surface: all 5 repos under
  `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/<repo>/`.
- Expected post-Phase-33 + post-34-01 tag set:
  - `llm-agent` `v0.5.0`
  - `llm-agent-rag` `v1.0.1` (bumped by 34-01)
  - `llm-agent-otel` `v0.2.0`
  - `llm-agent-providers` `v0.2.0`
  - `llm-agent-customer-support` `v0.2.0`
- Mirrors the 34-RESEARCH §"34-02 verification protocol" lines 304-345
  bash, extended for the rag re-tag (`v1.0.0` → `v1.0.1`).
- `GOPRIVATE` is **not** required — all 5 repos are public; the default
  `GOPROXY` serves them. `GOWORK=off` mirrors how `test.yml` runs in CI.
- The dep-currency gate from 34-02 is exercised here against the live
  ecosystem to confirm the coordinated state passes (the same script
  must exit 0 from the ecosystem root with all 5 sister checkouts).
- This slice is **verification-only**: zero `git add`, zero commits, zero
  pushes, zero edits to any source file. The only file written is
  `34-06-RESULTS.md` inside the planning tree.
- See `34-RESEARCH.md` §"34-02 verification protocol" and §Slice breakdown.
</context>

<tasks>
1. Confirm every repo is on the expected branch at the expected tag:
   for each `r` in `llm-agent llm-agent-rag llm-agent-otel
   llm-agent-providers llm-agent-customer-support`,
   `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/$r &&
   git rev-parse HEAD && git describe --tags --exact-match HEAD
   2>/dev/null || echo "(no tag exactly on HEAD)"`.
   Expect: `llm-agent v0.5.0`, `llm-agent-rag v1.0.1`, otel `v0.2.0`,
   providers `v0.2.0`, customer-support `v0.2.0`. If any HEAD is past
   its tag, **stop and surface it** — the audit cannot proceed against
   drifted HEADs.
2. Per-repo clean build + vet + test under `GOWORK=off`:
   for each of the 5 repos,
   `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/$r &&
   GOWORK=off GOCACHE=/tmp/go-build go vet ./... &&
   GOWORK=off GOCACHE=/tmp/go-build go build ./... &&
   GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1`.
   Capture exit code + a short summary line per repo. If any repo fails,
   **stop and surface it** — the audit cannot file PASS.
3. Confirm zero `replace` directives anywhere on the tagged branches:
   for each of the 5 repos,
   `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/$r &&
   if grep -E '^replace|^[[:space:]]+replace' go.mod >/dev/null 2>&1; then
   echo "FAIL: $r has replace directives"; exit 1; else echo "OK: $r no
   replace"; fi`.
4. Run the dep-currency gate from 34-02 against the live ecosystem.
   Extract the bash from `.github/workflows/umbrella.yml`'s
   `Dependency-currency gate` step into `/tmp/dep-currency-check.sh`,
   then run from the ecosystem root:
   `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem &&
   bash /tmp/dep-currency-check.sh`. Capture the full output. Expected:
   exit 0, every line `OK: <sister> -> <r> <v> (current)`, final line
   `Dependency-currency gate PASSED — all sibling pins current.` Delete
   the scratch file after.
5. Write `34-06-RESULTS.md` at
   `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/34-umbrella-coherence-gate-and-milestone-close/34-06-RESULTS.md`
   with these sections:
   - **Header** — verification date, ecosystem path, who ran it.
   - **Coordinated tag set** — markdown table `| Repo | Branch | HEAD SHA
     | Tag at HEAD |` with one row per repo, filled from task 1.
   - **Per-repo build/test results** — markdown table `| Repo | vet |
     build | test -short | Test summary |`, filled from task 2.
   - **`replace` directive scan** — one line per repo from task 3,
     each `OK: <repo> no replace` or the failure.
   - **Dep-currency gate run** — the literal output from task 4 in a
     fenced block, plus the recorded exit code (must be 0).
   - **Verdict** — one line: `✅ PASS — coordinated tag set internally
     consistent across all 5 repos.` (Or, if any check failed, the
     specific failure and STOP.)
6. Run every command in `<verify>`.

Out of scope:
- Any code change in any repo — verification only.
- Cutting any new tag — Phase 33 + 34-01 already cut the coordinated set.
- Writing the milestone audit doc — slice 34-04.
- Committing `34-06-RESULTS.md` — it lives in the planning tree;
  committing is the operator's milestone-close move at the end of 34-04.
</tasks>

<verify>
- results doc exists and has every required section:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && test -f .planning/phases/34-umbrella-coherence-gate-and-milestone-close/34-06-RESULTS.md && for s in 'Coordinated tag set' 'Per-repo build/test results' 'replace directive scan' 'Dep-currency gate run' 'Verdict'; do grep -q "$s" .planning/phases/34-umbrella-coherence-gate-and-milestone-close/34-06-RESULTS.md || { echo "MISSING $s"; exit 1; }; done && echo RESULTS-OK`
- coordinated tag set is what we expect:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem && for r in llm-agent:v0.5.0 llm-agent-rag:v1.0.1 llm-agent-otel:v0.2.0 llm-agent-providers:v0.2.0 llm-agent-customer-support:v0.2.0; do repo="${r%:*}"; want="${r#*:}"; got=$(cd "$repo" && git describe --tags --exact-match HEAD 2>/dev/null || echo none); [ "$got" = "$want" ] || { echo "FAIL: $repo at $got, want $want"; exit 1; }; done && echo TAGS-OK`
- every repo passes vet + build + test -short:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem && for r in llm-agent llm-agent-rag llm-agent-otel llm-agent-providers llm-agent-customer-support; do ( cd "$r" && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go build ./... && GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1 ) || { echo "FAIL: $r"; exit 1; }; done && echo ALL-GREEN`
- no `replace` anywhere:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem && for r in llm-agent llm-agent-rag llm-agent-otel llm-agent-providers llm-agent-customer-support; do grep -E '^replace|^[[:space:]]+replace' "$r/go.mod" && { echo "FAIL: $r has replace"; exit 1; } || true; done && echo NO-REPLACE-ANYWHERE`
- dep-currency gate is green on live state (single source of truth — 34-02 ships the script):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem && bash llm-agent/scripts/dep-currency-check.sh && echo GATE-GREEN`
- working trees stay clean (verification only — no edits leaked):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem && for r in llm-agent-rag llm-agent-otel llm-agent-providers llm-agent-customer-support; do dirty=$(cd "$r" && git status --short); [ -z "$dirty" ] || { echo "FAIL: $r dirty"; exit 1; }; done && echo TREES-CLEAN`
</verify>

<acceptance>
- All 5 repos confirmed at the expected coordinated tags.
- All 5 repos pass `go vet ./...`, `go build ./...`, `go test -short ./...` under `GOWORK=off`.
- Zero `replace` directives across the 5 repos.
- The dep-currency gate (from 34-02's umbrella.yml) exits 0 against the live ecosystem.
- `34-06-RESULTS.md` exists with all required sections and the `✅ PASS` verdict; it is the evidence 34-04's audit doc cites.
- No source-code edits leaked into any sister repo's working tree.
- All `<verify>` commands pass.
</acceptance>
