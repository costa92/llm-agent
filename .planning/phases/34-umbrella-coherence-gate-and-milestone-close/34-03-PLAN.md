---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 03
type: execute
wave: 3
repo: llm-agent-otel
depends_on: ["34-01", "34-02"]
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-otel commit + tag v0.2.1, merged via PR — main is protected)"
requirements: [ECO-05]
must_haves:
  truths:
    - "llm-agent-otel/go.mod requires llm-agent-rag v1.0.1 and llm-agent v0.5.1"
    - "the bumped repo builds and tests green"
    - "llm-agent-otel is tagged v0.2.1 on the merged commit and pushed; no replace directive"
---

<objective>
Cascade slice 2/3 (Wave 3). Bump `llm-agent-otel` to consume the new
rag `v1.0.1` AND the new core `v0.5.1`, cut `v0.2.1`. ECO-05.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel`,
  branch `main` (verified clean against origin after Phase 32).
- `go.mod` today: `require github.com/costa92/llm-agent v0.5.0` +
  `require github.com/costa92/llm-agent-rag v1.0.0`. Both stale after
  Waves 1-2.
- **Branch protection is on** for `otel` `main` (`required_status_checks
  = go + governance`, `enforce_admins: true`) — direct `git push origin
  main` will be rejected. Use the **PR-merge flow** (Phase 33-02 precedent):
  branch → push → open PR → wait for CI green → merge → push the tag
  against the merge commit.
- Cross-repo CI: otel's `go` job calls `go mod tidy (drift check)` against
  `llm-agent-rag` (now PUBLIC since 2026-05-21) — no auth issues.
- `otelrag` package depends on the rag SDK; the upgrade exercises that
  package's build path.
- No `CHANGELOG.md` — the annotated tag message carries the release note.
- **Operator-gated** at PR creation + at tag push. Stage + commit locally;
  push branch; open PR via `gh pr create`; surface PR number and CI state;
  wait for merge confirmation; then surface the tag-push step.
- See `34-RESEARCH.md` "Decision Update", Phase 33-02 PR-merge precedent.
</context>

<tasks>
1. `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel`,
   confirm on `main`, `git fetch origin`, working tree clean.
2. Create a feature branch: `git checkout -b chore/v1.1-cascade-bump`.
3. Bump both deps:
   `GOWORK=off go get github.com/costa92/llm-agent-rag@v1.0.1`
   `GOWORK=off go get github.com/costa92/llm-agent@v0.5.1`
   then `GOWORK=off go mod tidy`.
4. Confirm `go.mod` now requires `llm-agent-rag v1.0.1` and
   `llm-agent v0.5.1`; no `replace`.
5. Verify green locally:
   `GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1`.
   If anything red, **stop and surface**.
6. Stage + commit: `git add go.mod go.sum && git commit -m "chore: bump to
   llm-agent v0.5.1 + llm-agent-rag v1.0.1 (v1.1 cascade)"`.
7. Push the feature branch:
   `git push -u origin chore/v1.1-cascade-bump`.
8. Open PR: `gh pr create --title "chore: v1.1 cascade — llm-agent v0.5.1
   + llm-agent-rag v1.0.1" --body "Cascade bump for v1.1 Phase 34 Wave 3.
   Picks up rag v1.0.1 (Wave 1) and core v0.5.1 (Wave 2). No code change
   in otelrag or otelmodel; back-edge refresh only." --base main --head
   chore/v1.1-cascade-bump`. Capture the PR number.
9. **Operator gate (1/2)**: surface the PR number + URL + initial CI run
   IDs. Wait for explicit operator confirmation that CI is green and the
   PR can be merged.
10. After operator confirms PR is mergeable:
    `gh pr merge <PR#> --merge --delete-branch`. Capture the merge commit
    SHA on `main`.
11. `git fetch origin && git checkout main && git pull --ff-only origin
    main`. Confirm `main` is at the merge commit SHA.
12. Tag: `git tag -a v0.2.1 -m "v0.2.1 — v1.1 cascade: llm-agent v0.5.1 +
    llm-agent-rag v1.0.1"`. Confirm `git tag --points-at HEAD | grep v0.2.1`.
13. **Operator gate (2/2)**: surface the upcoming
    `git push origin v0.2.1` and wait for explicit confirmation. Do NOT
    push the tag unilaterally.
14. Run every command in `<verify>` except the tag-pushed line —
    `PRE-PUSH-DEFERRED`.
</tasks>

<verify>
- `go.mod` bumped both:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel && grep -q 'github.com/costa92/llm-agent v0.5.1' go.mod && grep -q 'github.com/costa92/llm-agent-rag v1.0.1' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- build + test green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1 && echo OTEL-GREEN`
- tag points at HEAD:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel && git tag --points-at HEAD | grep -q v0.2.1 && echo TAG-OK`
- main pushed (post-PR-merge):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel && git log --oneline origin/main..main | wc -l` (0)
- tag pushed (PRE-PUSH-DEFERRED until operator confirms):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel && git ls-remote --tags origin v0.2.1 | grep -q refs/tags/v0.2.1 && echo TAG-PUSHED`
</verify>

<acceptance>
- `llm-agent-otel` `go.mod` requires `llm-agent v0.5.1` + `llm-agent-rag v1.0.1`; no `replace`.
- The repo builds and `go test -short ./...` passes after the bump.
- PR was opened, CI was green, PR was merged via `gh pr merge --merge --delete-branch`.
- `llm-agent-otel` is tagged `v0.2.1` (annotated) on the merge commit; push is operator-gated.
- All `<verify>` commands pass (excluding the PRE-PUSH-DEFERRED tag-push line).
</acceptance>
