---
phase: 34-umbrella-coherence-gate-and-milestone-close
plan: 04
type: execute
wave: 4
repo: llm-agent-customer-support
depends_on: ["34-01", "34-02", "34-03"]
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-customer-support commit + tag v0.2.1, merged via PR — main is protected)"
requirements: [ECO-05]
must_haves:
  truths:
    - "llm-agent-customer-support/go.mod requires llm-agent-rag v1.0.1, llm-agent v0.5.1, llm-agent-otel v0.2.1, llm-agent-providers v0.2.0"
    - "the bumped repo builds and tests green"
    - "llm-agent-customer-support is tagged v0.2.1 on the merged commit and pushed; no replace directive"
---

<objective>
Cascade slice 3/3 (Wave 4). Bump `llm-agent-customer-support` to consume
the new rag `v1.0.1`, core `v0.5.1`, and otel `v0.2.1`. Cut `v0.2.1`. The
final cascade slice — after this, all 5 repos are coordinated and the
gate (34-05) can fire green on the live state. ECO-05.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support`,
  branch `main` (verified clean against origin after Phase 32).
- `go.mod` today:
  - `require github.com/costa92/llm-agent v0.5.0`
  - `require github.com/costa92/llm-agent-rag v1.0.0`
  - `require github.com/costa92/llm-agent-otel v0.2.0`
  - `require github.com/costa92/llm-agent-providers v0.2.0`
  All except `providers` are stale after Waves 1-3. `providers` stays at
  `v0.2.0` (it has no rag dep — not part of the cascade).
- **Branch protection is on** for `customer-support` `main`
  (`required_status_checks = go + governance`, `enforce_admins: true`,
  `allow_force_pushes: true`) — use the PR-merge flow (Phase 33-04
  precedent). PR-merge required even with force-push allowed.
- Cross-repo CI: customer-support's `go` job calls `go mod tidy` against
  the upstream tags — rag is public, no auth issues.
- This is the demo-grade reference service; v1.1 aligns its *deps* only.
- No `CHANGELOG.md` — the annotated tag message carries the release note.
- **Operator-gated** at PR creation + at tag push (same shape as 34-03).
- See `34-RESEARCH.md` "Decision Update", Phase 33-04 precedent.
</context>

<tasks>
1. `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support`,
   confirm on `main`, `git fetch origin`, working tree clean.
2. Create a feature branch: `git checkout -b chore/v1.1-cascade-bump`.
3. Bump three deps (providers stays at v0.2.0):
   `GOWORK=off go get github.com/costa92/llm-agent-rag@v1.0.1`
   `GOWORK=off go get github.com/costa92/llm-agent@v0.5.1`
   `GOWORK=off go get github.com/costa92/llm-agent-otel@v0.2.1`
   then `GOWORK=off go mod tidy`.
4. Confirm `go.mod` requires `llm-agent-rag v1.0.1`, `llm-agent v0.5.1`,
   `llm-agent-otel v0.2.1`, and **still** `llm-agent-providers v0.2.0`
   (this slice does NOT bump providers — providers has no rag dep and was
   unaffected by Waves 1-3). No `replace`.
5. Verify green locally:
   `GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1`.
   If anything red, **stop and surface**.
6. Stage + commit: `git add go.mod go.sum && git commit -m "chore: bump to
   llm-agent v0.5.1 + llm-agent-otel v0.2.1 + llm-agent-rag v1.0.1 (v1.1
   cascade)"`.
7. Push the feature branch:
   `git push -u origin chore/v1.1-cascade-bump`.
8. Open PR: `gh pr create --title "chore: v1.1 cascade — three-way dep
   refresh" --body "Final cascade slice for v1.1 Phase 34 Wave 4. Picks
   up rag v1.0.1, core v0.5.1, otel v0.2.1. providers stays at v0.2.0.
   No code change." --base main --head chore/v1.1-cascade-bump`. Capture
   the PR number.
9. **Operator gate (1/2)**: surface the PR number + URL + initial CI run
   IDs. Wait for explicit operator confirmation that CI is green.
10. After operator confirms:
    `gh pr merge <PR#> --merge --delete-branch`. Capture the merge commit SHA.
11. `git fetch origin && git checkout main && git pull --ff-only origin
    main`. Confirm `main` is at the merge commit SHA.
12. Tag: `git tag -a v0.2.1 -m "v0.2.1 — v1.1 cascade: llm-agent v0.5.1 +
    llm-agent-otel v0.2.1 + llm-agent-rag v1.0.1"`.
13. **Operator gate (2/2)**: surface `git push origin v0.2.1` and wait
    for explicit confirmation. Do NOT push unilaterally.
14. Run every command in `<verify>` except the tag-pushed line —
    `PRE-PUSH-DEFERRED`.
</tasks>

<verify>
- `go.mod` bumped (all three) + providers untouched:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && grep -q 'github.com/costa92/llm-agent v0.5.1' go.mod && grep -q 'github.com/costa92/llm-agent-rag v1.0.1' go.mod && grep -q 'github.com/costa92/llm-agent-otel v0.2.1' go.mod && grep -q 'github.com/costa92/llm-agent-providers v0.2.0' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- build + test green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && go build ./... && go test -short ./... -count=1 && echo CS-GREEN`
- tag points at HEAD:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && git tag --points-at HEAD | grep -q v0.2.1 && echo TAG-OK`
- main pushed (post-PR-merge):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && git log --oneline origin/main..main | wc -l` (0)
- tag pushed (PRE-PUSH-DEFERRED):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-customer-support && git ls-remote --tags origin v0.2.1 | grep -q refs/tags/v0.2.1 && echo TAG-PUSHED`
</verify>

<acceptance>
- `llm-agent-customer-support` `go.mod` requires `llm-agent v0.5.1` + `llm-agent-rag v1.0.1` + `llm-agent-otel v0.2.1` + `llm-agent-providers v0.2.0`; no `replace`.
- The repo builds and `go test -short ./...` passes after the bump.
- PR was opened, CI was green, PR was merged via `gh pr merge --merge --delete-branch`.
- `llm-agent-customer-support` is tagged `v0.2.1` (annotated) on the merge commit; push is operator-gated.
- All `<verify>` commands pass (excluding the PRE-PUSH-DEFERRED tag-push line).
</acceptance>
