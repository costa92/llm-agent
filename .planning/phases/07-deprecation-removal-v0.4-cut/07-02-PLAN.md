---
phase: 07-deprecation-removal-v0.4-cut
plan: 02
type: execute
repo: llm-agent
depends_on: [07-01]
files_modified:
  - rag/
  - context/
  - bench/
  - rl/
requirements: [DEPRC-01]
---

<objective>
Remove all remaining legacy-surface usage from runtime packages inside the core
repo while keeping tests green, without deleting the deprecated public symbols
yet.
</objective>

<tasks>
- Migrate `rag/` from `llm.Client` + `GenerateRequest/Response` to
  `llm.ChatModel` + `Request/Response`.
- Migrate `context/` compression helpers to the new surface.
- Migrate `bench/` judge and win-rate evaluation helpers to the new surface.
- Migrate `rl/trainer_proxy.go` model-loading seam off `llm.Client`.
- Update affected package-local tests as needed.
</tasks>

<verify>
- `go test ./rag ./context ./bench ./rl -count=1`
- `go test ./...`
</verify>

