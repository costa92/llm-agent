# Phase 7 Plan 05

Date: 2026-05-13
Repo set: `llm-agent-providers`, `llm-agent-otel`, `llm-agent-customer-support`
Depends on: `07-04`

## Objective

Prepare the sister repos for the final Phase 7 release cut by bumping their
declared core dependency from the old pre-release line to the intended final
`llm-agent v0.4.0` tag and aligning their release notes/docs accordingly.

## Tasks

1. Update `go.mod` in each sister repo from `github.com/costa92/llm-agent v0.3.0-pre.2`
   to `github.com/costa92/llm-agent v0.4.0`.
2. Update repo-local README versioning language to reflect that the repos are
   already code-compatible with the removed-compatibility core and are now
   waiting only on the coordinated final tag publication.
3. Re-run local 4-repo workspace verification with the updated sister-repo
   `go.mod` files.
4. Record any remaining release blocker explicitly; if none, leave only the
   actual publish/tag action as pending operator work.

## Verify

- `GOWORK=/tmp/phase7-v04-audit/go.work go test ./...` in:
  - `/tmp/llm-agent-providers`
  - `/tmp/llm-agent-otel`
  - `/tmp/llm-agent-customer-support`
