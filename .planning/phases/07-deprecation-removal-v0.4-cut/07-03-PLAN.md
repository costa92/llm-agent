# Phase 7 Plan 03

Date: 2026-05-12
Repo: `llm-agent`
Depends on: `07-02`

## Objective

Complete `DEPRC-02` and `DEPRC-03` inside the core repo by removing the
deprecated v0.2 compatibility surface (`llm.Client`, `llm.LegacyClient`,
`GenerateRequest`, `GenerateResponse`, `StreamChunk`, `StreamUsage`) after all
internal code has already migrated to `llm.ChatModel`.

## Scope

- remove `llm/legacy.go`
- move any still-needed shared definitions that currently live only in the
  compatibility file to the new-surface files
- update package docs, tests, examples, and top-level docs so they no longer
  teach or reference the removed surface
- leave sister-repo bumps and tag coordination to the later release step

## Tasks

1. Convert remaining in-repo tests/examples/helpers from compatibility response
   types to `llm.Response`.
2. Relocate `FinishReason` definition/constants off the soon-to-be-deleted
   compatibility file.
3. Delete `llm/legacy.go` and update `llm/llm_test.go` away from alias tests.
4. Rewrite comments/docs that still describe `llm.Client` as active usage.
5. Re-run full test coverage and update Phase 7 summaries/state.

## Verify

- `go test ./...`
- `rg -n "llm\\.Client|LegacyClient|GenerateRequest|GenerateResponse|GenerateStream|StreamChunk|StreamUsage" .`
