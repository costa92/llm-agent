# Phase 7 Plan 04

Date: 2026-05-13
Repo set: `llm-agent`, `llm-agent-providers`, `llm-agent-otel`, `llm-agent-customer-support`
Depends on: `07-03`

## Objective

Verify that the sister repos already compile and test cleanly against the
post-compat-removal core, then reduce the remaining Phase 7 work to coordinated
version/tagging only.

## Tasks

1. Build an isolated local `go.work` linking:
   - local core repo
   - `/tmp/llm-agent-providers`
   - `/tmp/llm-agent-otel`
   - `/tmp/llm-agent-customer-support`
2. Run full repo tests in each sister repo against the current core checkout.
3. If any compile/runtime break appears, patch the affected sister repo and
   rerun verification.
4. If all repos pass unchanged, record that `DEPRC-04` has no code delta and
   only needs coordinated version/tagging follow-through.

## Verify

- `GOWORK=/tmp/phase7-v04-audit/go.work go test ./...` in:
  - `/tmp/llm-agent-providers`
  - `/tmp/llm-agent-otel`
  - `/tmp/llm-agent-customer-support`
