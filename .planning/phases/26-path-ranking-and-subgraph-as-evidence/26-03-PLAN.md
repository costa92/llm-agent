---
phase: 26-path-ranking-and-subgraph-as-evidence
plan: 03
type: execute
wave: 3
repo: llm-agent-rag
depends_on: [26-01, 26-02]
files_modified:
  - examples/graphrag_path_example_test.go
  - docs/graphrag.md
requirements: [RAG-GRAPH4-02]
must_haves:
  truths:
    - "the path-evidence feature is demonstrated end-to-end by a deterministic worked example"
    - "a reader can learn how to enable path ranking and read the evidence subgraph from the docs"
  artifacts:
    - path: "examples/graphrag_path_example_test.go"
      provides: "deterministic path-ranking worked example"
      contains: |
        Example_graphRAGPaths with a stable Output
---

<objective>
Ship a deterministic worked example for path-ranked subgraph evidence and
document it in `docs/graphrag.md`. Completes RAG-GRAPH4-02 and Phase 26.
</objective>

<context>
- `graph.WeightedPathRanker` / `RankedPath` (26-01) and the opt-in
  `GraphRetriever.PathRanker` field + `GraphTrace.Paths`/`EvidenceSubgraph`
  (26-02).
- `examples/graphrag_example_test.go` (v0.7) and
  `examples/graphrag_global_example_test.go` (v0.8) are the worked-example
  templates — `DictionaryEntityExtractor` + in-memory store + scripted
  model, a stable `// Output:`.
- `docs/graphrag.md` is the GraphRAG doc (Tier-1 + Tier-3 sections after
  v0.8's 25-03).
</context>

<tasks>
1. `examples/graphrag_path_example_test.go` — `Example_graphRAGPaths`:
   a fully deterministic end-to-end wiring — `DictionaryEntityExtractor`,
   an in-memory store, import a small fixed corpus whose entities form a
   known multi-hop connection, a `GraphRetriever` with a
   `graph.WeightedPathRanker` set, run retrieval, and print the top ranked
   path (its entity sequence) and the evidence-subgraph size — with a
   stable `// Output:`. No live model.
2. `docs/graphrag.md` — add a **path-ranked evidence** subsection under the
   Tier-1 / `GraphRetriever` material: how to set `GraphRetriever.PathRanker`
   (`graph.WeightedPathRanker`), what `GraphTrace.Paths` and
   `EvidenceSubgraph` carry, that path mode is opt-in and additive (chunk
   retrieval is unchanged when off), and a short code snippet. Cross-check
   every type/method name against the shipped 26-01/02 code.
3. Run every command in `<verify>`.

Out of scope:
- Any code change beyond the example test — the feature shipped in 26-01/02.
- DRIFT — Phase 27.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./examples/... -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- confirm no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
- core facade smoke (run from the core repo `llm-agent`):
  `GOWORK=off go vet ./rag/... && go test ./rag/...`
</verify>

<acceptance>
- `Example_graphRAGPaths` runs deterministically with a stable `// Output:`.
- `docs/graphrag.md` documents path-ranked evidence — enabling it, reading
  `GraphTrace.Paths`/`EvidenceSubgraph`, the opt-in/additive contract.
- Every type/method name in the doc matches the shipped code.
- No new module dependency; all `<verify>` commands pass.
</acceptance>
