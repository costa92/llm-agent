---
phase: 10-retrieval-policies-hybrid-recall-and-context-packing
plan: 03
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [10-01, 10-02]
files_modified:
  - retrieve/
  - rag/
  - adapter/llmagent/
autonomous: true
requirements: [RAG-RETRIEVE-02, RAG-RETRIEVE-03]
must_haves:
  truths:
    - "MQE and HyDE are configured through standalone retrieval policy inputs rather than adapter-only helper loops"
    - "callers can enable query expansion through SearchOptions without losing backward-compatible retrieval behavior"
    - "multi-query expansion results merge deterministically inside the retrieval layer"
  artifacts:
    - path: "retrieve/"
      provides: "policy-aware LLM query expansion and multi-query merge orchestration"
      contains: "LLMExpansionPreprocessor + VariantRetriever"
    - path: "rag/"
      provides: "SearchOptions plumbing for MQE and HyDE controls"
      contains: "policy-layer request propagation"
---

<objective>
Move MQE and HyDE under the standalone retrieval policy layer so query
expansion is part of reusable retrieval orchestration instead of adapter-side
manual loops.
</objective>

<tasks>
- Extend standalone search/retrieval option structs with MQE and HyDE controls.
- Add a model-aware retrieval preprocessor that generates expanded query
  variants.
- Add a retrieval wrapper that runs and merges multiple query variants through a
  base retriever.
- Update the `llm-agent` adapter to delegate MQE/HyDE behavior to the
  standalone retrieval path.
- Add tests covering policy-layer expansion and deterministic multi-query merge
  behavior.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
