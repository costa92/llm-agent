---
phase: 10-retrieval-policies-hybrid-recall-and-context-packing
plan: 04
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - rerank/
  - pack/
  - rag/
autonomous: true
requirements: [RAG-RETRIEVE-04]
must_haves:
  truths:
    - "standalone ask flow has a default rerank stage and a budget-aware context packing stage"
    - "callers can enable rerank and control prompt evidence budget without bypassing standalone abstractions"
    - "final prompt evidence selection is machine-traceable"
  artifacts:
    - path: "rerank/"
      provides: "default reranker seam and heuristic implementation"
      contains: "Reranker interface + heuristic reranking"
    - path: "pack/"
      provides: "budget-aware context packing seam"
      contains: "GreedyTokenPacker + traceable evidence selection"
---

<objective>
Add a default rerank and context-packing path so standalone RAG prompt assembly
is token-budget-aware and explainable.
</objective>

<tasks>
- Add a reranker seam and one default heuristic reranker.
- Add a context packer seam and one default token-budget-aware packer.
- Wire rerank plus packing into `rag.Ask(...)`.
- Extend diagnostics and trace fields so final prompt evidence is inspectable.
- Add tests covering rerank promotion, packing drops, truncation, and custom
  packer overrides.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./rerank ./pack ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
