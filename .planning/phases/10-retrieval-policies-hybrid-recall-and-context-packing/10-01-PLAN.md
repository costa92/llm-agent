---
phase: 10-retrieval-policies-hybrid-recall-and-context-packing
plan: 01
type: execute
wave: 1
repo: llm-agent-rag
depends_on: []
files_modified:
  - rag/
  - retrieve/
autonomous: true
requirements: [RAG-RETRIEVE-01, RAG-RETRIEVE-03]
must_haves:
  truths:
    - "retrieval orchestration is separated from the dense algorithm path"
    - "query preprocessing is additive and traceable"
  artifacts:
    - path: "retrieve/"
      provides: "policy seam for query preprocessing and retrieval"
      contains: "request types + preprocessor + dense retriever"
---

<objective>
Introduce the first standalone retrieval-policy seam: query preprocessing plus
default dense retrieval behind an explicit retriever contract, while preserving
current `Retrieve(...)` behavior for callers.
</objective>

<tasks>
- Add a `QueryPreprocessor` seam with a no-op default.
- Add a `Retriever` seam and a default dense retriever implementation.
- Route `rag.System.Retrieve(...)` through the new policy layer.
- Record enough trace data to show the effective query used by retrieval.
- Add tests proving default behavior is preserved.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./rag ./retrieve -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
