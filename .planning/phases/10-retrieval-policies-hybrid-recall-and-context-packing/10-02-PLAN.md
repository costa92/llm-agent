---
phase: 10-retrieval-policies-hybrid-recall-and-context-packing
plan: 02
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [10-01]
files_modified:
  - retrieve/
autonomous: true
requirements: [RAG-RETRIEVE-01]
must_haves:
  truths:
    - "standalone retrieval can run lexical-only or hybrid retrieval in addition to dense retrieval"
    - "hybrid fusion is deterministic and preserves filter/security semantics"
  artifacts:
    - path: "retrieve/"
      provides: "lexical and hybrid retriever implementations"
      contains: "token-overlap recall + fusion"
---

<objective>
Add the first non-dense retrieval modes to the standalone policy layer:
lexical retrieval plus a simple deterministic hybrid fusion path.
</objective>

<tasks>
- Add a lexical retriever using token overlap against stored chunk content.
- Add a hybrid retriever that fuses dense and lexical results.
- Preserve namespace, metadata filter, and security filter semantics.
- Add tests covering lexical-only and hybrid retrieval behavior.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
