---
phase: 03-native-tool-calling-agent-refactor
plan: 05
type: execute
wave: 2
repo: llm-agent
depends_on: [01, 02, 03, 04]
files_modified:
  - agent/
  - llm/
autonomous: true
requirements: [CORE-05, CONF-04]
must_haves:
  truths:
    - "Core agent constructors consume ChatModel plus optional ToolCaller capability"
    - "Fallback-capable agents choose native tools or scratchpad once at construction time"
    - "FunctionCall agent fails fast when ToolCaller is unavailable"
  artifacts:
    - path: "agent/"
      provides: "constructor refactor across agent paradigms"
      contains: "NewReActAgent"
    - path: "agent/"
      provides: "fast-fail construction for native-only agent"
      contains: "NewFunctionCallAgent"
---

<objective>
Refactor the core agents so they bind against `llm.ChatModel` and branch on the
`ToolCaller` capability surface. Preserve scratchpad fallback where appropriate
and fail early where native tools are required.
</objective>

<context>
@.planning/phases/03-native-tool-calling-agent-refactor/03-CONTEXT.md
@.planning/phases/03-native-tool-calling-agent-refactor/03-RESEARCH.md
@.planning/phases/03-native-tool-calling-agent-refactor/03-PATTERNS.md
@agent/
@llm/
</context>

<tasks>
- Audit existing agent constructors and replace provider-specific assumptions
  with `ChatModel` + capability assertions.
- Add native-tool fast paths for fallback-capable agents and preserve the
  scratchpad path when `ToolCaller` is absent.
- Make `NewFunctionCallAgent` fail at construction time with a clear
  model-specific error when native tools are unavailable.
- Add regression tests covering both native-tool and fallback paths.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./agent/... ./llm/...`
- `GOCACHE=/tmp/go-build go build ./agent/... ./llm/...`
</verify>
