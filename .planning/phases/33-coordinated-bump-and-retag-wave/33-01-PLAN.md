---
phase: 33-coordinated-bump-and-retag-wave
plan: 01
type: execute
wave: 1
repo: llm-agent
depends_on: []
files_modified:
  - CHANGELOG.md
  - "(git: llm-agent commit + tag v0.5.0, pushed)"
requirements: [ECO-03]
must_haves:
  truths:
    - "the core llm-agent is committed and tagged v0.5.0 with the Phase-31 facade re-alignment"
    - "the v0.5.0 commit excludes the .planning/ tree"
    - "the core stays stdlib-only and carries no replace directive"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "the core v0.5.0 release notes"
      contains: "## [v0.5.0]; the llm-agent-rag v0.1.4→v1.0.0 bump + facade fix"
---

> Archived planning record only.
> Do not use as current development guide.
> Current development follows live code and `llm-agent-rag` current docs.

<objective>
Commit the Phase-31 core RAG-facade re-alignment and cut `llm-agent
v0.5.0` — the first tag in the v1.1 coordinated wave. Commit, tag, push.
ECO-03.
</objective>

<context>
- Repo: the CORE repo `/home/hellotalk/code/go/src/github.com/costa92/llm-agent`,
  branch `main`.
- The working tree holds Phase 31's uncommitted facade change (`go.mod`
  now `llm-agent-rag v1.0.0`, `go.sum`, `rag/doc.go`, `rag/rag.go`,
  `rag/store.go`) **plus** a large uncommitted `.planning/` tree. This
  slice commits **only the code + CHANGELOG** — the `.planning/` tree is
  the separate milestone-close commit.
- `CHANGELOG.md` has an `## [Unreleased]` section (older post-v0.4
  RAG-compat maintenance — the `v0.1.2→v0.1.4` bump). v0.5.0 ships that
  *and* the Phase-31 work.
- Core is at tag `v0.4.0`; the core has no separate changelog-commit
  convention — `v0.5.0` is tagged on the single facade+changelog commit.
- Downstream slices 33-02/03/04 bump against `llm-agent v0.5.0` — it must
  be **pushed** for `go get` to resolve it.
- See `33-RESEARCH.md` Decisions 1, 4.
</context>

<tasks>
1. `CHANGELOG.md` — rename `## [Unreleased]` to `## [v0.5.0] - 2026-05-21`.
   Under its `### Changed`, append the Phase-31 re-alignment:
   - bumped `github.com/costa92/llm-agent-rag` from `v0.1.4` to `v1.0.0`
     (the standalone SDK's frozen v1.0 API);
   - repaired the core `rag/` compatibility facade for the v1.0.0 store
     contract — `storeAdapter` now enumerates via a real list route
     (`*InMemoryStore.ListDocuments` + an optional `lister` interface +
     an id-index fallback) instead of a `nil`-vector similarity search,
     which v1.0.0's stricter `store.InMemoryStore.Search` rejected;
   - the core remains stdlib-only; the public `rag` facade API is
     unchanged.
   Keep the existing `[Unreleased]`-section bullets (they are part of
   v0.5.0). Add a fresh empty `## [Unreleased]` header above `[v0.5.0]`
   if the changelog's convention keeps one.
2. Verify green before committing:
   `GOWORK=off go vet ./... && GOWORK=off go build ./... && GOWORK=off go test ./... -count=1`.
3. Confirm no `replace` directive: `grep -n '^replace\|^\sreplace' go.mod`
   (must be empty).
4. Commit **only the code + changelog** — explicit paths, NOT `.planning/`:
   `git add go.mod go.sum rag/doc.go rag/rag.go rag/store.go CHANGELOG.md`
   then `git commit -m "feat: align rag facade with llm-agent-rag v1.0.0"`.
   Verify `git status --short .planning/` still shows the planning tree
   uncommitted (it must — that is the close commit).
5. Tag: `git tag v0.5.0` on the commit just made.
6. Push: `git push origin main` then `git push origin v0.5.0`.
7. Run every command in `<verify>`.

Out of scope:
- Committing the `.planning/` tree — that is the milestone-close commit.
- The sister-repo bumps — slices 33-02/03/04.
</tasks>

<verify>
- `CHANGELOG.md` has the entry:
  `grep -q '## \[v0.5.0\]' CHANGELOG.md && echo CHANGELOG-OK`
- the commit excludes `.planning/`:
  `git show --stat HEAD | grep -q '\.planning/' && echo PLANNING-LEAKED || echo PLANNING-EXCLUDED`
- `v0.5.0` tag exists on HEAD:
  `git tag --points-at HEAD | grep -q v0.5.0 && echo TAG-OK`
- pushed: `git log --oneline origin/main..main | wc -l` (0 — main pushed)
  and `git ls-remote --tags origin v0.5.0 | grep -q v0.5.0 && echo TAG-PUSHED`
- no `replace`: `! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- core still stdlib-only:
  `GOWORK=off go list -deps -f '{{if .Module}}{{.Module.Path}}{{end}}' ./rag | sort -u`
  (only `github.com/costa92/llm-agent` + `…/llm-agent-rag`)
- `GOWORK=off go test ./... -count=1` green
</verify>

<acceptance>
- `llm-agent` is committed (code + `CHANGELOG.md [v0.5.0]` only, no
  `.planning/`), tagged `v0.5.0`, and `main` + the tag are pushed.
- The core builds + tests green, stays stdlib-only, carries no `replace`.
- The `.planning/` tree remains uncommitted (the milestone-close commit).
- All `<verify>` commands pass.
</acceptance>
