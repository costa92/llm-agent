---
phase: 33-coordinated-bump-and-retag-wave
plan: 03
type: execute
wave: 2
repo: llm-agent-providers
depends_on: ["33-01"]
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-providers commit + tag v0.2.0, pushed)"
requirements: [ECO-03]
must_haves:
  truths:
    - "llm-agent-providers consumes llm-agent v0.5.0"
    - "the bumped repo builds and tests green"
    - "llm-agent-providers is tagged v0.2.0 and pushed; no replace directive"
---

<objective>
Bump `llm-agent-providers` onto `llm-agent v0.5.0` and cut `v0.2.0` —
capturing the untagged deepseek/minimax adapter work. Commit, tag, push.
ECO-03.
</objective>

<context>
- Repo: `/tmp/llm-agent-providers`, branch `main` (current with
  `origin/main` after Phase 32).
- `go.mod` today: `require llm-agent v0.4.0`; no `llm-agent-rag` dep.
- `providers` does not use the core `rag` facade — only the `llm`
  package, untouched by Phase 31 — so the `llm-agent → v0.5.0` bump is
  low-risk; it is done for ecosystem coherence (v1.1's purpose).
- `main` already carries the deepseek + minimax adapters (untagged past
  `v0.1.1`); `v0.2.0` captures them.
- 33-01 has pushed `llm-agent v0.5.0` — fetchable.
- No `CHANGELOG.md` — the annotated tag message carries the release note.
- `go get` fetches over SSH — pass `GOPRIVATE=github.com/costa92/*`
  inline.
- See `33-RESEARCH.md` Decisions 2-3.
</context>

<tasks>
1. `cd /tmp/llm-agent-providers`, confirm on `main`, `git fetch origin`.
2. Bump: `GOPRIVATE=github.com/costa92/* GOWORK=off go get
   github.com/costa92/llm-agent@v0.5.0` then
   `GOPRIVATE=github.com/costa92/* GOWORK=off go mod tidy`.
3. Confirm `go.mod` requires `llm-agent v0.5.0` and has **no `replace`**.
4. Verify green:
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1` — all
   green. If a bump breaks anything, **stop and surface it**.
5. Commit: `git add go.mod go.sum` then
   `git commit -m "chore: bump to llm-agent v0.5.0"`.
6. Tag: `git tag -a v0.2.0 -m "v0.2.0 — ecosystem alignment: deepseek + minimax adapters; llm-agent v0.5.0"`.
7. Push: `git push origin main` then `git push origin v0.2.0`.
8. Run every command in `<verify>`.

Out of scope:
- `core` / `otel` / `customer-support` — slices 33-01/02/04.
- Adding any new adapter — v1.1 is alignment, not features (KE-1).
</tasks>

<verify>
- `go.mod` bumped:
  `cd /tmp/llm-agent-providers && grep -q 'llm-agent v0.5.0' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /tmp/llm-agent-providers && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- build + test green:
  `cd /tmp/llm-agent-providers && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /tmp/llm-agent-providers && GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1`
- tagged + pushed:
  `cd /tmp/llm-agent-providers && git tag --points-at HEAD | grep -q v0.2.0 && echo TAG-OK`
  `cd /tmp/llm-agent-providers && git ls-remote --tags origin v0.2.0 | grep -q v0.2.0 && echo TAG-PUSHED`
  `cd /tmp/llm-agent-providers && git log --oneline origin/main..main | wc -l` (0)
- working tree clean: `cd /tmp/llm-agent-providers && git status --short` (empty)
</verify>

<acceptance>
- `llm-agent-providers` `go.mod` requires `llm-agent v0.5.0`; no `replace`.
- The repo builds and `go test -short ./...` passes after the bump.
- `llm-agent-providers` is committed, tagged `v0.2.0`, and `main` + the
  tag are pushed.
- All `<verify>` commands pass.
</acceptance>
