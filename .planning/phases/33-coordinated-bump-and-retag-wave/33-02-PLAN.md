---
phase: 33-coordinated-bump-and-retag-wave
plan: 02
type: execute
wave: 2
repo: llm-agent-otel
depends_on: ["33-01"]
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-otel commit + tag v0.2.0, pushed)"
requirements: [ECO-03]
must_haves:
  truths:
    - "llm-agent-otel consumes llm-agent v0.5.0 and llm-agent-rag v1.0.0"
    - "the bumped repo builds and tests green"
    - "llm-agent-otel is tagged v0.2.0 and pushed; no replace directive"
---

<objective>
Bump `llm-agent-otel` onto the current sibling tags and cut `v0.2.0`:
`llm-agent v0.4.0 → v0.5.0` and `llm-agent-rag v0.3.0 → v1.0.0`. Commit,
tag, push. ECO-03.
</objective>

<context>
- Repo: `/tmp/llm-agent-otel`, branch `main` (Phase 32 merged the
  `otelrag` feature into it; the merge is unpushed).
- `go.mod` today: `require llm-agent v0.4.0` + `llm-agent-rag v0.3.0`.
- **Both bumps are required and inseparable** (`33-RESEARCH.md`
  Decision 2): `otelrag` wraps the core `*rag.System` facade — bumping
  `llm-agent-rag → v1.0.0` without bumping `llm-agent → v0.5.0` would make
  Go's MVS compile core `v0.4.0`'s pre-Phase-31 facade against rag
  `v1.0.0` and hit the dimension-mismatch bug Phase 31 fixed.
- 33-01 has tagged + pushed `llm-agent v0.5.0`; `llm-agent-rag v1.0.0` was
  pushed at the v1.0 close — both are fetchable.
- `llm-agent-otel` has no `CHANGELOG.md` — the annotated tag message
  carries the release note.
- `go get` fetches over SSH — pass `GOPRIVATE=github.com/costa92/*`
  inline on the `go get`/`go mod tidy` invocations.
- See `33-RESEARCH.md` Decisions 1-3.
</context>

<tasks>
1. `cd /tmp/llm-agent-otel`, confirm on `main`, `git fetch origin`.
2. Bump both dependencies:
   `GOPRIVATE=github.com/costa92/* GOWORK=off go get github.com/costa92/llm-agent@v0.5.0 github.com/costa92/llm-agent-rag@v1.0.0`
   then `GOPRIVATE=github.com/costa92/* GOWORK=off go mod tidy`.
3. Confirm `go.mod` now requires `llm-agent v0.5.0` + `llm-agent-rag
   v1.0.0` and carries **no `replace` directive**.
4. Verify green:
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1` — all green.
   If a bump breaks the build/tests, **stop and surface it** — do not tag
   a broken repo.
5. Commit the bump: `git add go.mod go.sum` then
   `git commit -m "chore: bump to llm-agent v0.5.0 + llm-agent-rag v1.0.0"`.
6. Tag: `git tag -a v0.2.0 -m "v0.2.0 — ecosystem alignment: otelrag RAG-wrapping; llm-agent v0.5.0 + llm-agent-rag v1.0.0"`.
7. Push: `git push origin main` then `git push origin v0.2.0`.
8. Run every command in `<verify>`.

Out of scope:
- The core `llm-agent` tag — slice 33-01 (done).
- `providers` / `customer-support` — slices 33-03 / 33-04.
</tasks>

<verify>
- `go.mod` bumped:
  `cd /tmp/llm-agent-otel && grep -q 'llm-agent v0.5.0' go.mod && grep -q 'llm-agent-rag v1.0.0' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /tmp/llm-agent-otel && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- build + test green:
  `cd /tmp/llm-agent-otel && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /tmp/llm-agent-otel && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- tagged + pushed:
  `cd /tmp/llm-agent-otel && git tag --points-at HEAD | grep -q v0.2.0 && echo TAG-OK`
  `cd /tmp/llm-agent-otel && git ls-remote --tags origin v0.2.0 | grep -q v0.2.0 && echo TAG-PUSHED`
  `cd /tmp/llm-agent-otel && git log --oneline origin/main..main | wc -l` (0)
- working tree clean: `cd /tmp/llm-agent-otel && git status --short` (empty)
</verify>

<acceptance>
- `llm-agent-otel` `go.mod` requires `llm-agent v0.5.0` +
  `llm-agent-rag v1.0.0`; no `replace`.
- The repo builds and `go test ./...` passes after the bump.
- `llm-agent-otel` is committed, tagged `v0.2.0`, and `main` + the tag
  are pushed.
- All `<verify>` commands pass.
</acceptance>
