---
phase: 33-coordinated-bump-and-retag-wave
plan: 04
type: execute
wave: 3
repo: llm-agent-customer-support
depends_on: ["33-02", "33-03"]
files_modified:
  - go.mod
  - go.sum
  - "(git: llm-agent-customer-support commit + tag v0.2.0, pushed)"
requirements: [ECO-03]
must_haves:
  truths:
    - "llm-agent-customer-support consumes llm-agent v0.5.0, llm-agent-otel v0.2.0, llm-agent-providers v0.2.0"
    - "the bumped repo builds and tests green"
    - "llm-agent-customer-support is tagged v0.2.0 and pushed; no replace directive"
---

<objective>
Bump `llm-agent-customer-support` onto every current sibling tag and cut
`v0.2.0` — the last tag in the v1.1 coordinated wave. Commit, tag, push.
ECO-03.
</objective>

<context>
- Repo: `/tmp/llm-agent-customer-support`, branch `main` (current after
  Phase 32).
- `go.mod` today: `require llm-agent v0.4.0` + `llm-agent-otel v0.1.0` +
  `llm-agent-providers v0.1.0` — all three stale.
- This is the **last slice of the wave**: it depends on 33-02 (otel
  `v0.2.0` pushed) and 33-03 (providers `v0.2.0` pushed); core `v0.5.0`
  was pushed by 33-01.
- `customer-support` is intentionally demo-grade — v1.1 aligns its
  *dependencies* only; it is not productionized.
- No `CHANGELOG.md` — the annotated tag message carries the release note.
- `go get` fetches over SSH — pass `GOPRIVATE=github.com/costa92/*`
  inline.
- See `33-RESEARCH.md` Decisions 1-3.
</context>

<tasks>
1. `cd /tmp/llm-agent-customer-support`, confirm on `main`,
   `git fetch origin`.
2. Bump all three sibling deps:
   `GOPRIVATE=github.com/costa92/* GOWORK=off go get
   github.com/costa92/llm-agent@v0.5.0
   github.com/costa92/llm-agent-otel@v0.2.0
   github.com/costa92/llm-agent-providers@v0.2.0`
   then `GOPRIVATE=github.com/costa92/* GOWORK=off go mod tidy`.
3. Confirm `go.mod` now requires `llm-agent v0.5.0`,
   `llm-agent-otel v0.2.0`, `llm-agent-providers v0.2.0`, and carries
   **no `replace` directive**.
4. Verify green:
   `GOWORK=off GOCACHE=/tmp/go-build go build ./...` and
   `GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1` — all
   green. If a bump breaks anything, **stop and surface it**.
5. Commit: `git add go.mod go.sum` then `git commit -m "chore: bump to
   llm-agent v0.5.0 + llm-agent-otel v0.2.0 + llm-agent-providers v0.2.0"`.
6. Tag: `git tag -a v0.2.0 -m "v0.2.0 — ecosystem alignment: current on
   llm-agent v0.5.0, llm-agent-otel v0.2.0, llm-agent-providers v0.2.0"`.
7. Push: `git push origin main` then `git push origin v0.2.0`.
8. Run every command in `<verify>`.

Out of scope:
- `core` / `otel` / `providers` — slices 33-01/02/03.
- Productionizing the demo — a standing non-goal.
</tasks>

<verify>
- `go.mod` bumped — all three:
  `cd /tmp/llm-agent-customer-support && grep -q 'llm-agent v0.5.0' go.mod && grep -q 'llm-agent-otel v0.2.0' go.mod && grep -q 'llm-agent-providers v0.2.0' go.mod && echo BUMP-OK`
- no `replace`:
  `cd /tmp/llm-agent-customer-support && ! grep -E '^replace|^[[:space:]]+replace' go.mod && echo NO-REPLACE`
- build + test green:
  `cd /tmp/llm-agent-customer-support && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /tmp/llm-agent-customer-support && GOWORK=off GOCACHE=/tmp/go-build go test -short ./... -count=1`
- tagged + pushed:
  `cd /tmp/llm-agent-customer-support && git tag --points-at HEAD | grep -q v0.2.0 && echo TAG-OK`
  `cd /tmp/llm-agent-customer-support && git ls-remote --tags origin v0.2.0 | grep -q v0.2.0 && echo TAG-PUSHED`
  `cd /tmp/llm-agent-customer-support && git log --oneline origin/main..main | wc -l` (0)
- working tree clean: `cd /tmp/llm-agent-customer-support && git status --short` (empty)
</verify>

<acceptance>
- `llm-agent-customer-support` `go.mod` requires `llm-agent v0.5.0`,
  `llm-agent-otel v0.2.0`, `llm-agent-providers v0.2.0`; no `replace`.
- The repo builds and `go test -short ./...` passes after the bump.
- `llm-agent-customer-support` is committed, tagged `v0.2.0`, and `main`
  + the tag are pushed.
- All four repos now consume current sibling tags — the wave is complete.
- All `<verify>` commands pass.
</acceptance>
