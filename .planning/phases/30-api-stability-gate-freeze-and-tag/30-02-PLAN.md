---
phase: 30-api-stability-gate-freeze-and-tag
plan: 02
type: execute
wave: 2
repo: llm-agent-rag
depends_on: ["30-01"]
files_modified:
  - .github/workflows/test.yml
requirements: [RAG-API-05]
must_haves:
  truths:
    - "the snapshot gate runs in CI on every push and PR"
    - "the build-tagged adapter/llmagent is built and tested in CI"
    - "the replace-directive rejection covers the release branch"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "CI that runs the API-snapshot gate and exercises the llmagent adapter"
      contains: "go test ./... (runs the snapshot test); a -tags llmagent build+test step"
---

<objective>
Wire the API-stability gate into CI and close the `adapter/llmagent`
coverage gap: add a `-tags llmagent` build/test step to `test.yml`,
confirm `release-precheck.yml` covers the release branch, and run the
final full-suite verification. RAG-API-05.
</objective>

<context>
- The snapshot gate from 30-01 is an ordinary `go test` in
  `internal/apisnapshot` — `test.yml`'s existing `go test ./...` step
  **already runs it**. No new workflow is needed; this slice makes the
  coverage explicit and closes the adapter gap.
- `test.yml` today: a core module-boundary `rg` check, then
  `go vet ./...`, `go build ./...`, `go test ./...` — all with
  `GOWORK: off`. It runs **nothing** with `-tags llmagent`, so
  `adapter/llmagent` is never built or tested in CI.
- `adapter/llmagent` imports the core `github.com/costa92/llm-agent`
  module — a `-tags llmagent` CI step needs that module resolvable
  (`setup-go` with the committed `go.sum` handles it; document it in the
  step name/comment).
- `release-precheck.yml` triggers on `release/**` and rejects `replace`
  directives — the glob already covers a `release/v1.0` branch.
- See `30-RESEARCH.md` Decision 2.
</context>

<tasks>
1. `.github/workflows/test.yml` — after the existing `go test ./...` step,
   add a step that exercises the build-tagged adapter:
   - `go build -tags llmagent ./...`
   - `go test -tags llmagent ./adapter/...`
   Name the step so it is clear it is the one place CI touches the core
   `llm-agent` dependency. Keep `GOWORK: off` (already the workflow env).
2. Optionally add an explicitly-named step `API snapshot gate` running
   `go test ./internal/apisnapshot/...` — purely for CI-log visibility
   (the gate also runs inside `go test ./...`). If added, keep it; it is
   not required for correctness.
3. Confirm `release-precheck.yml`'s `release/**` trigger covers a
   `release/v1.0` branch — it does (glob match). No change unless a gap is
   found; record the confirmation in `30-02-SUMMARY.md`.
4. Run every command in `<verify>` — this includes the final full-suite
   verification for the milestone.

Out of scope:
- The snapshot generator itself — slice 30-01.
- The `CHANGELOG.md` entry — slice 30-03.
- Cutting the `v1.0.0` tag — operator action at milestone-close.
- Creating a `release/v1.0` branch — not needed; tagging follows the
  established tag-on-the-work-commit convention.
</tasks>

<verify>
- `test.yml` is valid YAML and has the new step:
  `cd /tmp/llm-agent-rag && python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/test.yml")); print("YAML-OK")'`
  `cd /tmp/llm-agent-rag && grep -q 'tags llmagent' .github/workflows/test.yml && echo LLMAGENT-STEP-OK`
- `release-precheck.yml` covers release branches:
  `cd /tmp/llm-agent-rag && grep -q 'release/\*\*' .github/workflows/release-precheck.yml && echo PRECHECK-OK`
- the snapshot gate is reachable via the standard test run:
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./internal/apisnapshot/ -count=1`
- final full-suite verification (the milestone gate):
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build -tags llmagent ./...`
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test -tags llmagent ./adapter/... -count=1`
- `cd /tmp/llm-agent-rag && gofmt -l . | (grep -v vendor || true)` (no output)
- no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
</verify>

<acceptance>
- `test.yml` runs the API-snapshot gate (via `go test ./...`) and a
  `-tags llmagent` build+test step exercising `adapter/llmagent`.
- `release-precheck.yml` is confirmed to cover `release/**`.
- The full suite — `go vet`/`go build`/`go test ./...` plus the
  `-tags llmagent` build and adapter test — is green; the repo is
  `gofmt`-clean.
- No new module dependency; all `<verify>` commands pass.
</acceptance>
