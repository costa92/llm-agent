---
phase: 30-api-stability-gate-freeze-and-tag
plan: 03
type: execute
wave: 2
repo: llm-agent-rag
depends_on: ["30-01"]
files_modified:
  - CHANGELOG.md
requirements: [RAG-API-06]
must_haves:
  truths:
    - "CHANGELOG.md has a [v1.0.0] entry framing the API freeze and the compatibility promise"
    - "the entry lists the Phase-28 renames as the final breaking changes"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "the v1.0.0 release notes"
      contains: |
        ## [v1.0.0]; API-freeze framing; the eval renames + ragkit
        doc-anchor as breaking changes; compatibility-promise note
---

<objective>
Write the `CHANGELOG.md` `[v1.0.0]` entry — framing v1.0.0 as the API
freeze + the Go-module compatibility promise, and listing the Phase-28
renames as the final breaking changes. RAG-API-06.

The `v1.0.0` **tag is not cut in this slice** — it is an operator action
at milestone-close.
</objective>

<context>
- `CHANGELOG.md` uses Keep-a-Changelog format; the latest entry is
  `## [v0.6.0] - 2026-05-20` with a prose intro + `### Added` sections.
- v1.0.0 is **not a feature release** — it is the API freeze. The entry's
  framing must say so.
- The Phase-28 breaking changes to list:
  - `eval.Evaluator` → `eval.RetrievalEvaluator`
  - `eval.Result` → `eval.RetrievalResult`
  - the `ragkit` root package repurposed as a documented doc-anchor (its
    `doc.go` comment rewritten; no symbol change, but worth noting).
- The additive / non-breaking v1.0 work to mention: full package +
  exported-symbol doc-comment coverage; `docs/compatibility.md` (the
  written compatibility promise); `docs/api-audit-v1.0.md` (the audit);
  the `api/v1.snapshot.txt` API-stability gate; the repo made
  `gofmt`-clean.
- No new dependency, no behavior change anywhere in v1.0.
- See `30-RESEARCH.md` Decision 3; `docs/compatibility.md` (Phase 29).
</context>

<tasks>
1. `CHANGELOG.md` — add a new `## [v1.0.0] - 2026-05-21` entry above
   `## [v0.6.0]`. Structure (Keep-a-Changelog):
   - **Intro prose** — v1.0.0 freezes the `llm-agent-rag` public API and
     adopts the Go import-compatibility promise: within `v1.x` the API is
     additive-only; breaking changes require `/v2`. Link
     `docs/compatibility.md`. State plainly: not a feature release — no
     new features, no behavior change, no new dependency.
   - **`### Changed` (breaking)** — list, marked as the **final** breaking
     changes before the freeze:
     - `eval.Evaluator` → `eval.RetrievalEvaluator` and `eval.Result` →
       `eval.RetrievalResult` (symmetry with the prefixed answer-path
       evaluators `GlobalEvaluator`/`DriftEvaluator`/`TriadEvaluator`);
     - the `ragkit` root package comment rewritten to document the root
       as a deliberate doc-anchor (callers import the sub-packages).
   - **`### Added`** — `docs/compatibility.md` (the compatibility policy);
     `docs/api-audit-v1.0.md` (the exported-surface audit); complete
     package + exported-symbol doc-comment coverage; the
     `api/v1.snapshot.txt` exported-surface snapshot gate (a stdlib
     `go test`-time diff that fails any unintended API change,
     complementing the cross-repo `contract` gate).
   - Optionally a short note that the repo is now `gofmt`-clean.
2. Keep the existing `[v0.6.0]` and earlier entries unchanged.
3. Run every command in `<verify>`.

Out of scope:
- Cutting the `v1.0.0` git tag — operator action at milestone-close (the
  tag sits on the milestone work commit; this `CHANGELOG` commit lands one
  commit past it, per the v0.3.0→v0.6.0 convention).
- Any code or workflow change.
</tasks>

<verify>
- the entry exists and is framed as a freeze:
  `cd /tmp/llm-agent-rag && grep -q '## \[v1.0.0\]' CHANGELOG.md && echo ENTRY-OK`
  `cd /tmp/llm-agent-rag && grep -qi 'freeze\|compatibility promise' CHANGELOG.md && echo FRAMING-OK`
- the renames are listed:
  `cd /tmp/llm-agent-rag && grep -q 'RetrievalEvaluator' CHANGELOG.md && grep -q 'RetrievalResult' CHANGELOG.md && echo RENAMES-OK`
- the compatibility doc is linked:
  `cd /tmp/llm-agent-rag && grep -q 'compatibility.md' CHANGELOG.md && echo LINK-OK`
- earlier entries intact:
  `cd /tmp/llm-agent-rag && grep -q '## \[v0.6.0\]' CHANGELOG.md && echo HISTORY-OK`
- no code touched — build/test still green:
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
</verify>

<acceptance>
- `CHANGELOG.md` has a `## [v1.0.0]` entry framed as the API freeze +
  compatibility promise, linking `docs/compatibility.md`.
- It lists the `eval` renames and the `ragkit` doc-anchor change as the
  final breaking changes, and the additive v1.0 doc/gate work.
- Earlier changelog entries are unchanged; no code or workflow changed.
- No new module dependency; all `<verify>` commands pass.
</acceptance>
