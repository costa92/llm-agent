---
phase: 35-budget-and-cancellation-context
plan: 04
type: execute
wave: 4
repo: llm-agent (core)
depends_on: ["35-01", "35-02", "35-03"]
files_modified:
  - simple_test.go
  - react_test.go
  - reflection_test.go
  - plan_solve_test.go
  - function_call_test.go
  - budget_integration_test.go
requirements: [CC-1]
must_haves:
  truths:
    - "every agent paradigm (Simple/ReAct/Reflection/PlanSolve/FunctionCall) has at least one new budget-enforcement test"
    - "one cross-paradigm test (budget_integration_test.go) asserts uniform behavior across all 5 paradigms"
    - "core stays stdlib-only — go list -deps ./... shows zero third-party modules outside the rag back-edge (which itself is stdlib-only)"
    - "go test -race ./... is green repo-wide"
    - "go.mod and go.sum are unchanged from pre-35 state"
    - "no edit to any paradigm production .go file (simple.go / react.go / reflection.go / plan_solve.go / function_call.go) — KC-5"
artifacts:
  - path: "simple_test.go"
    provides: "TestSimple_BudgetExhaustion test added"
  - path: "react_test.go"
    provides: "TestReAct_BudgetExhaustion test added"
  - path: "reflection_test.go"
    provides: "TestReflection_BudgetExhaustion test added"
  - path: "plan_solve_test.go"
    provides: "TestPlanSolve_BudgetExhaustion test added"
  - path: "function_call_test.go"
    provides: "TestFunctionCall_BudgetExhaustion test added"
  - path: "budget_integration_test.go"
    provides: "TestAllParadigms_BudgetUniformity — cross-paradigm test asserting all 5 propagate identical sentinel + partial-result shape"
---

<objective>
Wide integration test that 35-02's chokepoint edit propagates correctly
through all 5 agent paradigms. Add one budget-enforcement test per
paradigm (5 tests) plus a single cross-paradigm test (`TestAllParadigms_BudgetUniformity`)
that asserts they all behave identically when the budget exhausts: same
sentinel (`budget.ErrCallsExceeded`), wrapping `budget.ErrBudgetExceeded`,
with the paradigm-specific partial result preserved. Then run the
stdlib-only exit gate and the repo-wide race test — both must be green.
Phase 35 closes when this slice closes. CC-1.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. Depends on 35-01 (`budget` package), 35-02 (chokepoint
  edit), 35-03 (example exists + runs).
- **Repo layout is FLAT (verified — see `35-RESEARCH.md` §"Chokepoint
  discovery").** The 5 paradigm files live at the repo root in
  `package agents`:
  - `simple.go` + `simple_test.go`
  - `react.go` + `react_test.go`
  - `reflection.go` + `reflection_test.go`
  - `plan_solve.go` + `plan_solve_test.go`
  - `function_call.go` + `function_call_test.go`
  There is **no `agents/react/` subdirectory** — that's a legacy
  convention from other Go repos. The user-prompt's `files_modified`
  listed `agents/react/react_test.go` etc.; we use the actual repo paths
  (`react_test.go` at the root). This is a deviation from the prompt and
  is recorded in the summary.
- The `budget_integration_test.go` (a NEW file at the repo root) lives
  in `package agents` so it can import all 5 paradigm constructors
  directly. It is NOT in a separate `agents/integration_test/` package —
  the integration test runs in the same package as the paradigms for
  direct access to the unexported helpers if needed (we do not expect to
  need unexported access, but the same-package shape avoids one extra
  hop).
- The chokepoint edit from 35-02 means a single test pattern applies to
  every paradigm:
  ```
  ctx, _ := budget.WithBudget(context.Background(), budget.Budget{MaxCalls: N})
  // build paradigm with ScriptedLLM scripted for > N turns
  result, err := paradigm.Run(ctx, "input")
  // assert errors.Is(err, budget.ErrCallsExceeded)
  // assert errors.Is(err, budget.ErrBudgetExceeded)
  // assert result == agents.Result{} (paradigms early-return zero Result on chokepoint error)
  // assert tracker.Snapshot().Calls == N (the cap; denied attempt did not mutate the tracker)
  ```
  **Important:** every paradigm in this repo returns `agents.Result{}` (zero value) on any `generateFromPrompt` error — verified at `react.go:106`, `reflection.go:81/94/110`, `plan_solve.go:81/102/119`. Tests therefore assert zero-Result + sentinel + tracker Snapshot — NOT partial-result preservation. (Adding partial-result preservation would require editing paradigm production code, which KC-5 forbids in this milestone.)
  N is paradigm-specific: SimpleAgent needs 1 call to finish, so N=0
  (impossible to set — `MaxCalls: 0 = no cap`); use a manual loop. ReAct
  needs ≥3 calls in a typical scratchpad cycle, so set N=2 and script 4
  turns. Reflection has 3 phases (initial draft, critique, revise) so
  N=2 cuts the revise. PlanSolve has plan + per-step + synthesis (≥3
  calls); N=2 cuts the synthesis. FunctionCall typically needs 2 calls
  (tool-call then post-tool); N=1 cuts the second. Document the
  paradigm-specific N choice in each test's comment.
- **SimpleAgent** is the odd one out — it makes exactly 1 LLM call per
  Run. To exercise the budget against SimpleAgent, the test calls
  `agent.Run(ctx, ...)` in a manual loop of 3 iterations with
  `MaxCalls: 2`; the 3rd Run returns `ErrCallsExceeded` because the
  tracker is shared across Runs via ctx. This proves cross-Run
  enforcement — a property real Supervisor-style code (Phase 37) will
  rely on.
- **Cross-paradigm uniformity test** (`TestAllParadigms_BudgetUniformity`):
  a `paradigms` table with 5 rows, each row a factory closure that takes
  a `llm.ChatModel` and returns an `agents.Agent`. For each: install the
  same `Budget{MaxCalls: 2}`, run, assert the err sentinel matches.
  Asserts identical sentinel + identical tracker `Snapshot().Calls`
  (the cap) — not Result shape (each paradigm has its own
  `Result.Answer` shape, and on this error path all return zero
  `agents.Result{}`).
- **Stdlib-only exit gate** (the load-bearing milestone proof, even
  though the milestone closes in Phase 38, not here):
  ```
  GOWORK=off go list -deps -f '{{if .Module}}{{.Module.Path}}{{end}}' ./... \
    | sort -u \
    | grep -vE '^(github\.com/costa92/llm-agent(-rag)?|^$)' \
    | wc -l
  ```
  Must return `0`. Any non-zero output means a third-party module
  slipped in.
- The repo-wide `-race` test is a separate gate from the
  budget-package-only race test in 35-01. Here we race the WHOLE repo —
  some existing paradigm tests may have their own concurrency that is
  unaffected by budget; we want to confirm the chokepoint edit did not
  introduce a regression.
- The slice does **not** edit any paradigm production file. All edits
  are in `*_test.go` files (extending) or the new
  `budget_integration_test.go` (creating). KC-5 verification is via
  `git status -- '*.go' ':!*_test.go'` showing zero changes outside
  `agent_chatmodel.go` (which was edited in 35-02).
- See `35-RESEARCH.md` §"Chokepoint discovery" + §"Decision 3" + §"Slice
  breakdown" 35-04.
</context>

<tasks>
1. Read each existing paradigm test file end-to-end to learn the
   constructor + ScriptedLLM wiring style for each:
   - `simple_test.go`
   - `react_test.go`
   - `reflection_test.go`
   - `plan_solve_test.go`
   - `function_call_test.go`
   Match the existing voice — naming, helper-builder pattern, error
   assertion style. Do not refactor.
2. For each paradigm, append ONE new test:
   - **`simple_test.go` :: `TestSimple_BudgetExhaustion`**:
     - `ctx, _ := budget.WithBudget(context.Background(), budget.Budget{MaxCalls: 2})`.
     - Build a SimpleAgent with a `ScriptedLLM` returning 3 fixed
       responses (each `Usage.TotalTokens: 10`).
     - Call `agent.Run(ctx, "input-N")` three times in a loop. First two
       succeed; the third returns `(_, err)` where
       `errors.Is(err, budget.ErrCallsExceeded)` and
       `errors.Is(err, budget.ErrBudgetExceeded)` both true.
     - Pull the tracker via `t, ok := budget.From(ctx)`; assert
       `ok == true && t.Snapshot().Calls == 2` (the denied attempt did
       not mutate state).
   - **`react_test.go` :: `TestReAct_BudgetExhaustion`**:
     - `ctx, _ := budget.WithBudget(ctx, budget.Budget{MaxCalls: 2})`.
     - Build a ReActAgent with `ScriptedLLM` scripted for 4+ turns
       (thought/action/observation pattern — match the existing
       `react_test.go` test data).
     - Call `agent.Run(ctx, "input")` once.
     - Assert `errors.Is(err, budget.ErrCallsExceeded)`.
     - Assert `result == agents.Result{}` (paradigm early-returns zero Result on chokepoint error — verified at `react.go:106`).
     - Pull tracker via `t, ok := budget.From(ctx)`; assert `ok && t.Snapshot().Calls == 2` (the cap; the denied 3rd attempt did not mutate state).
   - **`reflection_test.go` :: `TestReflection_BudgetExhaustion`**:
     - `Budget{MaxCalls: 2}` — cuts before the revise phase.
     - Script 3 responses (draft / critique / would-be-revise).
     - Assert `errors.Is(err, budget.ErrCallsExceeded)`.
     - Assert `result == agents.Result{}` (`reflection.go:81/94/110` all return zero Result on error).
     - Assert tracker `Snapshot().Calls == 2`.
   - **`plan_solve_test.go` :: `TestPlanSolve_BudgetExhaustion`**:
     - `Budget{MaxCalls: 2}` — cuts before synthesis (call 3).
     - Script 3 responses (plan / per-step / would-be-synthesis).
     - Assert `errors.Is(err, budget.ErrCallsExceeded)`.
     - Assert `result == agents.Result{}` (`plan_solve.go:81/102/119`).
     - Assert tracker `Snapshot().Calls == 2`.
   - **`function_call_test.go` :: `TestFunctionCall_BudgetExhaustion`**:
     - `Budget{MaxCalls: 1}` — cuts before the post-tool LLM call.
     - Script 2 responses (tool-binding call + post-tool synthesis).
     - Use a tool that records its invocation in a closure-captured counter (so the test can prove tool dispatched before deny without depending on Result shape).
     - The agent should make the first call, dispatch tool, then on the second LLM call get denied.
     - Assert `errors.Is(err, budget.ErrCallsExceeded)`.
     - Assert `result == agents.Result{}` (zero on chokepoint error).
     - Assert tool counter == 1 (tool ran during the first LLM call's response handling, BEFORE the second chokepoint enter).
     - Assert tracker `Snapshot().Calls == 1`.
3. Create `budget_integration_test.go` at the repo root, `package agents`:
   - Imports: `context`, `errors`, `testing`, `time` (if needed), the
     `budget` package (`github.com/costa92/llm-agent/budget`), the `llm`
     package, and the local ScriptedLLM (the canonical one from
     `scriptedllm_test.go`).
   - **`TestAllParadigms_BudgetUniformity`** — a table-driven test:
     ```
     paradigms := []struct {
         name    string
         build   func(model llm.ChatModel) agents.Agent
         needs   int // calls the paradigm uses before MaxCalls cuts
     }{
         { "simple",       buildSimple,       1 },
         { "react",        buildReAct,        3 },
         { "reflection",   buildReflection,   3 },
         { "plansolve",    buildPlanSolve,    3 },
         { "functioncall", buildFunctionCall, 2 },
     }
     ```
     For each row: set `Budget{MaxCalls: row.needs - 1}` (forces a deny
     before the paradigm naturally finishes), run, assert
     `errors.Is(err, budget.ErrCallsExceeded)` AND
     `errors.Is(err, budget.ErrBudgetExceeded)`. The test does NOT
     assert `Result` shape (each paradigm returns its own — and on this
     error path all return zero anyway); it DOES assert
     `tracker.Snapshot().Calls == row.needs - 1` — the cap, proving the
     denied attempt did not mutate the tracker. (Per-Run agent counters
     in `Result.Usage.LLMCalls` are NOT used as assertion targets: they
     are independent of the tracker per Q4 of research AND are zero on
     this error path since paradigms early-return zero Result.)
   - For SimpleAgent (`needs: 1`, `MaxCalls: 0` is "no cap"): special-
     case the row by calling `agent.Run` twice in the same ctx with
     `Budget{MaxCalls: 1}`. The second call denies. **This is the
     cross-Run enforcement proof — load-bearing for Phase 37 Supervisor**
     (the tracker survives across `agent.Run` calls within a single ctx).
     Document the special case inline; do NOT omit SimpleAgent from the
     table.
4. Run every command in `<verify>`. The repo-wide `-race` and the
   stdlib-only gate are the load-bearing checks; everything else is a
   pre-condition.

Out of scope:
- Any edit to a paradigm production file (`simple.go` etc.) — KC-5
  absolute. All edits are test-side.
- Any new test infrastructure / mock — reuse `ScriptedLLM`
  (`scriptedllm_test.go`).
- Testing `MaxTokens` / `MaxWall` / `MaxCost` at the paradigm level —
  35-02's `agent_chatmodel_test.go` covers those dimensions at the
  chokepoint; this slice is paradigm-coverage for `MaxCalls` (the most
  load-bearing dimension for cross-paradigm uniformity). Other
  dimensions are integration-tested at the chokepoint level (35-02) and
  unit-tested at the package level (35-01); paradigm-coverage for them
  would be redundant.
- A `budget.Wrap` decorator test — deferred to v1.3.
- Any update to `.planning/STATE.md` / `.planning/ROADMAP.md` — Phase
  38 (milestone close) owns the planning-tree refresh.
- A `go test -bench` run — out of v1.2 scope.
</tasks>

<verify>
- all 5 paradigm tests pass:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -run 'TestSimple_BudgetExhaustion|TestReAct_BudgetExhaustion|TestReflection_BudgetExhaustion|TestPlanSolve_BudgetExhaustion|TestFunctionCall_BudgetExhaustion' . -count=1 && echo PARADIGMS-OK`
- cross-paradigm uniformity test passes:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -run 'TestAllParadigms_BudgetUniformity' . -count=1 && echo UNIFORMITY-OK`
- repo-wide vet + test:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && echo REPO-GREEN`
- repo-wide race (the load-bearing chokepoint-integration race check):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race ./... -count=1 && echo REPO-RACE-OK`
- stdlib-only exit gate (the load-bearing milestone proof):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -deps -f '{{if .Module}}{{.Module.Path}}{{end}}' ./... | sort -u | grep -vE '^(github\.com/costa92/llm-agent(-rag)?)$' | grep -v '^$' | wc -l` (0)
- go.mod / go.sum unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- go.mod go.sum | wc -l` (0)
- no edits to paradigm production .go files (KC-5 — `:!*_test.go`):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- simple.go react.go reflection.go plan_solve.go function_call.go agent.go | wc -l` (0)
- no edits to validated llm public types (KC-5):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- llm/types.go llm/chatmodel.go | wc -l` (0)
- budget_integration_test.go exists at repo root with the cross-paradigm test:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && test -f budget_integration_test.go && grep -q 'TestAllParadigms_BudgetUniformity' budget_integration_test.go && echo INTEGRATION-FILE-OK`
- every paradigm test file gained a budget-test (grep gate):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for p in simple react reflection plan_solve function_call; do grep -q "BudgetExhaustion" ${p}_test.go || { echo "MISSING budget test in ${p}_test.go"; exit 1; }; done && echo BUDGET-TESTS-PRESENT`
</verify>

<acceptance>
- All 5 paradigm test files (`simple_test.go`, `react_test.go`, `reflection_test.go`, `plan_solve_test.go`, `function_call_test.go`) carry at least one new `Test*_BudgetExhaustion` test that exercises `MaxCalls` exhaustion and asserts the sentinel propagates.
- `budget_integration_test.go` exists at the repo root and ships `TestAllParadigms_BudgetUniformity` — a table-driven cross-paradigm uniformity check.
- `go test ./... -count=1` and `go test -race ./... -count=1` both green repo-wide.
- The stdlib-only exit gate (`go list -deps ./... | grep -vE 'costa92/llm-agent(-rag)?' | grep -v ^$ | wc -l`) returns 0.
- `go.mod` and `go.sum` unchanged from pre-35 state.
- No edit to any paradigm production .go file (`simple.go`, `react.go`, `reflection.go`, `plan_solve.go`, `function_call.go`, `agent.go`) — KC-5 honored.
- No edit to any validated public type (`llm.ChatModel`, `llm.Response`, `llm.Usage`, `agents.Agent`, `agents.Result`, `agents.Usage`) — KC-5 honored.
- All `<verify>` commands pass.
</acceptance>
</content>
</invoke>