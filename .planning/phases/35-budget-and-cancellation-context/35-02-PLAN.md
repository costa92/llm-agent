---
phase: 35-budget-and-cancellation-context
plan: 02
type: execute
wave: 2
repo: llm-agent (core)
depends_on: ["35-01"]
files_modified:
  - agent_chatmodel.go
  - agent_chatmodel_test.go
requirements: [CC-1]
must_haves:
  truths:
    - "generateFromPrompt calls budget.From(ctx) and charges Tracker.Charge BEFORE and AFTER every model.Generate call"
    - "MaxCalls counts attempts — the pre-call charge fires before the LLM round-trip; a denied call never reaches the network"
    - "MaxTokens / MaxCost (if a Cost field surfaces) are charged post-call from response.Usage"
    - "ErrBudgetExceeded (and its dim-specific sentinels) propagates to all 5 agent paradigms through the existing return path"
    - "no budget set = byte-identical behavior to pre-35 (the From(ctx) → (nil, false) path is unaltered)"
    - "function signature of generateFromPrompt is unchanged — body-only edit (KC-5)"
artifacts:
  - path: "agent_chatmodel.go"
    provides: "edited generateFromPrompt with pre/post Charge, no signature change"
  - path: "agent_chatmodel_test.go"
    provides: "table-driven integration tests using ScriptedLLM — pre-call deny, post-call deny, no-budget passthrough, response+sentinel return, race"
---

<objective>
Surgical edit of `agent_chatmodel.go::generateFromPrompt` (line 10) — the
one helper every agent paradigm × 10 call sites funnels through. Add
pre-call `Tracker.Charge(Usage{Calls: 1})` and post-call
`Tracker.Charge(Usage{Tokens: resp.Usage.TotalTokens})` against the
ctx-keyed tracker installed by 35-01's `budget.WithBudget`. Function
signature stays byte-identical (KC-5). Add `agent_chatmodel_test.go`
proving: 5-paradigm coverage via `ScriptedLLM`, pre-call deny short-circuits
the network, post-call deny returns BOTH the response AND the sentinel,
no-budget path is unaffected, and the chokepoint is race-clean. CC-1.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. Depends on 35-01 (the `budget` package must exist and
  expose `From`, `Charge`, `Usage`, sentinels).
- The chokepoint, verified at `agent_chatmodel.go:10-18`:
  ```
  func generateFromPrompt(ctx context.Context, model llm.ChatModel, systemPrompt, prompt string) (llm.Response, error) {
      req := llm.Request{Messages: []llm.Message{{Role: "user", Content: prompt}}}
      if systemPrompt != "" { req.SystemPrompt = systemPrompt }
      return model.Generate(ctx, req)
  }
  ```
  All 5 paradigms × 10 call sites enter through here (verified —
  `simple.go:52`, `react.go:104,160`, `reflection.go:79,92,108`,
  `plan_solve.go:79,100,117`, `function_call.go:81`). One edit covers
  all paradigms; **no edit to any paradigm file** (KC-5).
- **Operator-confirmed Q2 (2026-05-20):** `MaxCalls` counts attempts.
  Charge `Calls: 1` BEFORE the network round-trip; a deny prevents the
  call. Document the semantic at the call-site comment too — it is the
  load-bearing decision of this slice.
- Token counts come from `llm.Response.Usage.TotalTokens` (verified —
  `llm/types.go:65,68`). All providers + `ScriptedLLM` populate
  `Response.Usage` — no estimator needed (`35-RESEARCH.md` §"Decision 2").
- Wall-clock budget is **already plumbed** by 35-01's `WithBudget`:
  when `Budget.MaxWall > 0`, the returned ctx carries
  `context.WithDeadline`. Providers honor `ctx.Done()` via the
  `http.Client` they own — zero new surface here. The chokepoint does
  nothing extra for wall-clock; it just calls `model.Generate(ctx, req)`
  and the deadline fires naturally as `ctx.Err() == context.DeadlineExceeded`.
- **Cost charging — defer in v1.2.** `llm.Response.Usage` carries no
  `Cost` field today (verified — `llm/types.go` shows
  `InputTokens/OutputTokens/TotalTokens/Source`, no Cost). The chokepoint
  charges only `Tokens` post-call. `Budget.MaxCost` plumbing is in place
  (caller can charge externally if they have a CostMapper), but the
  chokepoint does not derive Cost from a missing field. Recorded as a
  known v1.2 gap with v1.3 follow-up (Estimator + CostMapper). This must
  be cited in the slice's commit message / `agent_chatmodel.go` inline
  comment.
- **Post-call deny semantics.** When the token cap fires AFTER the
  response (the response itself has been generated; the tracker now
  sees the post-call total exceeds `MaxTokens`), the chokepoint returns
  `(resp, cerr)` — both the response AND the sentinel. The next call
  through the chokepoint will then be denied pre-call on the Calls or
  Tokens dimension. This matches `35-RESEARCH.md` §"Decision 3" exactly.
  Callers that want to ignore the response on post-call deny can use
  `errors.Is(err, budget.ErrBudgetExceeded)` to discriminate; callers
  that want to log the partial response can read `resp` first.
- **Why the chokepoint, not a `budget.Wrap` decorator:** the chokepoint
  gives 100% paradigm coverage with one edit; a decorator duplicates
  enforcement at a different layer and would balloon to ~40-60% of
  `otelmodel.Wrap`'s capability-preservation body. Decorator is a v1.3
  follow-up per `35-RESEARCH.md` §"Decision 3". Out of this slice.
- Public surface unchanged: `llm.ChatModel`, `agents.Agent`, `agents.Result`,
  `agents.Usage`, `llm.Response`, `llm.Usage` — all KC-5 untouched. The
  `agents.Usage.LLMCalls++` increment in each paradigm file (e.g.,
  `react.go:108`, `reflection.go:83`) stays — that is the per-Run
  agent-side counter, independent of the tracker (Q4 of research). **Do
  NOT touch those lines.**
- See `35-RESEARCH.md` §"Chokepoint discovery" + §"Decision 3" + §"Open
  questions" Q2/Q4.
</context>

<tasks>
1. Read the current `agent_chatmodel.go` end-to-end (35 lines).
   Identify exactly the body of `generateFromPrompt` (lines 10-18). The
   signature stays. The two helpers below it (`nativeToolCaller`,
   `toolCapabilityError`) are unaltered.
2. Edit `agent_chatmodel.go`:
   - Add import `"github.com/costa92/llm-agent/budget"` to the import
     block.
   - Inside `generateFromPrompt`, after `req` is built and before
     `model.Generate`, insert:
     ```
     // Pre-call charge: MaxCalls counts attempts (Q2 — operator-confirmed
     // 2026-05-20). A denied call never reaches the network.
     t, hasBudget := budget.From(ctx)
     if hasBudget {
         if err := t.Charge(budget.Usage{Calls: 1}); err != nil {
             return llm.Response{}, err
         }
     }
     ```
   - After `resp, err := model.Generate(ctx, req)` (split the current
     `return` into the explicit `resp, err := ...` form), branch on err:
     - If `err != nil`, return `resp, err` as-is. ctx.Err() from
       `MaxWall` surfaces here naturally; nothing to add.
     - If `err == nil` and `hasBudget`, charge the token total:
       ```
       if cerr := t.Charge(budget.Usage{Tokens: resp.Usage.TotalTokens}); cerr != nil {
           return resp, cerr // post-call deny returns BOTH (Decision 3)
       }
       ```
     - Final `return resp, nil`.
   - Add an inline comment above the post-call charge: `// Cost is NOT
     charged here in v1.2 — llm.Response.Usage has no Cost field. v1.3
     adds CostMapper.` (Recorded gap.)
   - Function signature **unchanged**: still
     `func generateFromPrompt(ctx context.Context, model llm.ChatModel, systemPrompt, prompt string) (llm.Response, error)`.
3. Write `agent_chatmodel_test.go` in `package agents` (so it can
   construct `ScriptedLLM` and call `generateFromPrompt` directly).
   - Helper at top: `func scripted(responses []llm.Response) llm.ChatModel`
     wrapping the canonical `ScriptedLLM` from `scriptedllm_test.go`.
     Each response has `Usage.TotalTokens` populated so the post-call
     charge has something to count.
   - **`TestGenerateFromPrompt_NoBudget_Passthrough`**: call
     `generateFromPrompt(context.Background(), scripted([...]), "", "hi")`
     N=5 times; assert no error on any call; assert the response is
     byte-identical to what `ScriptedLLM` was configured to return.
     Proves "zero behavior change when no budget set".
   - **`TestGenerateFromPrompt_MaxCalls_PreCallDeny`**: install
     `budget.WithBudget(ctx, budget.Budget{MaxCalls: 3})`; call the
     chokepoint 4 times; first 3 succeed; 4th returns
     `budget.ErrCallsExceeded` AND `resp == llm.Response{}` (the zero
     value — proving no network round-trip happened). Use a script-call
     counter on `ScriptedLLM` (the canonical mock should already expose
     this; if not, add an unexported counter on the test-local wrapper);
     assert the counter == 3, NOT 4.
   - **`TestGenerateFromPrompt_MaxTokens_PostCallDeny_ReturnsBoth`**:
     `Budget{MaxTokens: 100}`. Script 3 responses, each
     `Usage.TotalTokens = 60`. Call 1 succeeds (cumulative tokens = 60).
     Call 2 succeeds at the network level (tokens generated) but returns
     `(resp, ErrTokensExceeded)` — `resp.Usage.TotalTokens == 60` (the
     response IS valid), AND `errors.Is(err, budget.ErrBudgetExceeded)` is
     true. Call 3 returns `(llm.Response{}, ErrTokensExceeded)` on the
     pre-call... no wait — the post-call charge from call 2 mutated
     `Snapshot().Tokens` to 120; call 3's pre-call `Charge(Calls: 1)`
     succeeds (no MaxCalls); the next thing that happens is the network
     round-trip, then the post-call `Charge(Tokens: 60)` denies again.
     So call 3 also returns `(resp, ErrTokensExceeded)` with a valid
     resp. Document this explicitly in the test comment — it is the
     v1.2 "tokens-as-proxy is post-hoc" behavior; v1.3 with Estimator
     can deny pre-call on tokens.
   - **`TestGenerateFromPrompt_MaxWall_ContextDeadline`**:
     `Budget{MaxWall: 50 * time.Millisecond}`. Script a single response
     but have the test's `ScriptedLLM` wrapper sleep 200ms before
     returning. Call the chokepoint; assert err is non-nil AND
     `errors.Is(err, context.DeadlineExceeded)` is true. This proves
     wall-clock fires via the ctx-deadline path (no new surface).
   - **`TestGenerateFromPrompt_Concurrent_Race`**: `Budget{MaxCalls: 50}`.
     Spawn 20 goroutines, each calling `generateFromPrompt` 10 times
     against the SAME tracker (same `ctx` shared across goroutines).
     After joining, the sum of successful calls plus denied calls must
     equal 200; the count of successful calls must be exactly 50 (the
     cap). Must pass under `go test -race`.
4. Run the 5-paradigm regression. Read each paradigm's existing test file
   to confirm none changed (`simple_test.go`, `react_test.go`,
   `reflection_test.go`, `plan_solve_test.go`, `function_call_test.go`).
   Then run `GOWORK=off go test ./... -count=1` — the whole repo green.
5. Run every command in `<verify>`.

Out of scope:
- Any edit to a paradigm file (`simple.go`, `react.go`, `reflection.go`,
  `plan_solve.go`, `function_call.go`) — KC-5 absolute; the chokepoint
  alone gives the coverage.
- Any edit to `agents.Usage`, `agents.Result`, `llm.Usage`,
  `llm.Response`, `llm.ChatModel` — KC-5.
- Wiring budget into `bench/`, `rag/`, `context/compress.go` — `35-RESEARCH.md`
  §"Out of scope" (those aren't agent paradigms; v1.3 `budget.Wrap`
  decorator addresses them uniformly).
- `Cost` charging — deferred (v1.3 with CostMapper); recorded as inline
  comment in `agent_chatmodel.go`.
- Adding a new `StreamEvent.Kind` for budget exhaustion — `35-RESEARCH.md`
  §"Decision 4"; K1's typed union stays locked.
- Wide paradigm-level integration tests — that's 35-04's scope.
</tasks>

<verify>
- function signature unchanged (byte-equality on the prototype line):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q '^func generateFromPrompt(ctx context.Context, model llm.ChatModel, systemPrompt, prompt string) (llm.Response, error) {' agent_chatmodel.go && echo SIGNATURE-OK`
- budget is imported:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q '"github.com/costa92/llm-agent/budget"' agent_chatmodel.go && echo BUDGET-IMPORTED`
- pre + post Charge sites present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -c 't.Charge(budget.Usage' agent_chatmodel.go` (>= 2)
- Q2 attempt-counting comment present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q -i 'attempts' agent_chatmodel.go && echo Q2-COMMENT-OK`
- v1.3 cost-deferral comment present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q -E '(Cost.*NOT|v1\.3.*CostMapper|CostMapper)' agent_chatmodel.go && echo COST-DEFER-COMMENT-OK`
- vet + build green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go build ./... && echo VET-BUILD-OK`
- repo-wide test green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && echo TEST-OK`
- chokepoint-specific tests + race:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race -run 'TestGenerateFromPrompt' . -count=1 && echo CHOKEPOINT-RACE-OK`
- go.mod / go.sum unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- go.mod go.sum | wc -l` (0)
- no edits to paradigm files (KC-5 check):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- simple.go react.go reflection.go plan_solve.go function_call.go agent.go | wc -l` (0)
- no edits to validated llm public types (KC-5 check):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- llm/types.go llm/chatmodel.go | wc -l` (0)
</verify>

<acceptance>
- `agent_chatmodel.go::generateFromPrompt` body now invokes `budget.From(ctx)` and charges pre-call (`Calls: 1`) and post-call (`Tokens: resp.Usage.TotalTokens`).
- Function signature is byte-identical to pre-slice (KC-5 honored).
- No paradigm file (`simple.go`, `react.go`, `reflection.go`, `plan_solve.go`, `function_call.go`) was edited.
- No validated public type (`llm.ChatModel`, `agents.Agent`, `agents.Usage`, `agents.Result`, `llm.Response`, `llm.Usage`) was edited.
- `agent_chatmodel_test.go` exercises: no-budget passthrough, MaxCalls pre-call deny (verifies no-network-round-trip), MaxTokens post-call deny (verifies `(resp, err)` both returned), MaxWall via `context.DeadlineExceeded`, concurrent charges under `-race`.
- Pre-call deny prevents the LLM call (the ScriptedLLM counter does NOT advance on the denied attempt).
- `go vet ./...` + `go test ./... -count=1` + `go test -race -run 'TestGenerateFromPrompt' .` all green.
- `go.mod` / `go.sum` unchanged.
- All `<verify>` commands pass.
</acceptance>
</content>
</invoke>