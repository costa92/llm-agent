---
phase: 35-budget-and-cancellation-context
plan: 03
type: execute
wave: 3
repo: llm-agent (core)
depends_on: ["35-01", "35-02"]
files_modified:
  - examples/06-budget/main.go
  - examples/06-budget/main_test.go
  - examples/06-budget/README.md
  - examples/README.md
requirements: [CC-1]
must_haves:
  truths:
    - "examples/06-budget/main.go runs to completion deterministically using ScriptedLLM"
    - "the example demonstrates MaxCalls (pre-call deny) + MaxTokens (post-call deny) + MaxWall (ctx.DeadlineExceeded)"
    - "go test ./examples/06-budget/... is part of the standard test suite and passes without network"
    - "examples/06-budget/README.md walks a reader through the Budget struct, the chokepoint enforcement, and wall-clock composition"
    - "the example uses the canonical ScriptedLLM (per CLAUDE.md) — no external provider"
artifacts:
  - path: "examples/06-budget/main.go"
    provides: "a runnable, deterministic budget demo wiring ScriptedLLM + ReActAgent + budget.WithBudget"
  - path: "examples/06-budget/main_test.go"
    provides: "deterministic test of the example (captures stdout, asserts the budget-exhaustion trail)"
  - path: "examples/06-budget/README.md"
    provides: "60-line walk-through — Budget struct, chokepoint, wall-clock, cancellation"
  - path: "examples/README.md"
    provides: "updated examples index listing 06-budget alongside 01-05"
---

<objective>
Ship a deterministic, network-free, runnable budget example at
`examples/06-budget/`. The example uses the canonical `ScriptedLLM` mock
(per CLAUDE.md), wires `budget.WithBudget` into a ReAct agent, exercises
`MaxCalls` (pre-call deny), `MaxTokens` (post-call deny), and `MaxWall`
(ctx-deadline cancellation), and prints a transcript that proves the
chokepoint enforces each dimension. A companion `main_test.go` captures
stdout and asserts the transcript so the example is in the standard
`go test ./...` suite. A `README.md` walks the reader through the
plumbing. CC-1.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. Depends on 35-01 (`budget` package) and 35-02 (chokepoint
  integration).
- **Existing examples layout — verified:**
  ```
  examples/
    01-simple-agent/main.go
    02-tool-use/main.go
    03-pipeline/main.go
    04-state-graph/main.go
    05-fanout/main.go
    scriptedllm/
    go.mod
    README.md
  ```
  Numbering convention uses **descriptive prefixes** (`01-simple-agent`,
  `02-tool-use`, ...). `06-budget/` is the next free slot — names match
  the request prompt. Each example today ships a single `main.go` (no
  `main_test.go` in any of 01-05; verified). **Adding `main_test.go` is
  a deliberate divergence** to honor the prompt's "deterministic
  example_test.go runs in standard test suite" requirement; the test
  pattern follows the canonical Go `Example_*`/`os.Pipe`-stdout-capture
  approach and is recorded as a precedent that other examples may adopt
  later (out of this slice).
- The `examples/scriptedllm/` directory exists as a shared mock for the
  examples — reuse it. Examples have their own `examples/go.mod`
  (verified — a separate module to keep the core's `go.mod` free of the
  example-only paths). The new `06-budget/main.go` imports from
  `github.com/costa92/llm-agent` (core) and `github.com/costa92/llm-agent/budget`
  (the new package from 35-01) — both already in `examples/go.mod` once
  the example imports them and `go mod tidy` runs.
- The canonical ScriptedLLM populates `Response.Usage` (verified —
  `llm/scripted.go:139` shows `usage := Usage{InputTokens, OutputTokens, TotalTokens, Source}`).
  This is what the post-call charge counts against `MaxTokens`.
- The example demonstrates THREE budget dimensions:
  1. **`MaxCalls: 3`** — script 4 ReAct turns; the 4th's pre-call charge
     returns `ErrCallsExceeded`. The ReAct loop returns
     `(partialResult, err)` — partial result has the 3 completed
     thought/action/observation cycles. Print the cycle count + the
     sentinel.
  2. **`MaxTokens: 100`** — script 3 responses with 60 tokens each. After
     call 2 (cumulative 120) the post-call charge fires `ErrTokensExceeded`.
     The response from call 2 is still valid; the err is the sentinel.
     Print: "got valid response, but budget exhausted: <err>".
  3. **`MaxWall: 100ms`** — wrap the ScriptedLLM with a sleep of 200ms
     before responding; the underlying ctx-deadline fires; the chokepoint
     returns `context.DeadlineExceeded`. Print: "wall-clock exhausted:
     <err>".
- Each dimension is its own `main` subroutine for clarity:
  `demoMaxCalls()`, `demoMaxTokens()`, `demoMaxWall()`. `main()` invokes
  them in sequence and prints a final tracker `Snapshot()` after each
  block so the reader sees the accumulated state.
- **No CostMapper demo** — the v1.2 chokepoint does NOT charge cost
  (deferred to v1.3 per 35-02). The README mentions this honestly.
- The `examples/README.md` (the index page) — read it first, then add a
  one-line entry under the appropriate section. If the file does not
  already list 01-05 with one-liners, add the 06-budget entry alongside
  the existing layout (don't rewrite the whole file).
- Stdlib-only constraint applies to the core package, not the examples
  module. The example may use `context`, `errors`, `fmt`, `os`, `time`
  and the local `examples/scriptedllm` helper. No third-party deps.
- See `35-RESEARCH.md` §"Decision 1" (Budget shape), §"Decision 3"
  (chokepoint), §"Decision 4" (no new StreamEvent.Kind — wall-clock via
  ctx.Err()).
</context>

<tasks>
1. `ls examples/` to confirm the layout (sanity check against the
   pre-flight). Read `examples/README.md` end-to-end so the entry style
   in step 5 matches.
2. Read `examples/scriptedllm/` so the example uses its existing
   constructor (whatever the package exports — likely
   `scriptedllm.New([]llm.Response)` or similar). If the constructor
   does NOT support a per-response artificial sleep (needed for the
   `MaxWall` demo), define a small local wrapper struct inside
   `examples/06-budget/main.go` — a `type slowLLM struct { inner llm.ChatModel; pause time.Duration }`
   that wraps the canonical mock and sleeps in `Generate` honoring
   `ctx.Done()` (so the wall-clock test fires correctly). Document this
   inline.
3. Read `examples/01-simple-agent/main.go` end-to-end. Mirror its voice:
   short `package main`, `func main()` with comments above each block,
   `fmt.Println` to STDOUT for readers running the binary. No flags, no
   args — `go run .` is enough.
4. Write `examples/06-budget/main.go`:
   - `package main`. Imports: `context`, `errors`, `fmt`, `time`, the
     core `github.com/costa92/llm-agent` (alias `agents`), `github.com/costa92/llm-agent/budget`,
     `github.com/costa92/llm-agent/llm`, and the local
     `github.com/costa92/llm-agent/examples/scriptedllm`.
   - `func main()` — calls `demoMaxCalls()`, `demoMaxTokens()`,
     `demoMaxWall()` in sequence with `fmt.Println` headers separating
     them. After all three return, print "OK" so the test can assert
     the program ran to completion.
   - `func demoMaxCalls()`:
     - Build a `ScriptedLLM` with 4 canned responses, each
       `Usage.TotalTokens = 10`.
     - Build a ReActAgent (the simplest paradigm that takes ≥3 LLM
       calls — match `examples/02-tool-use/main.go`'s pattern for ReAct
       wiring). If wiring a multi-turn ReAct is heavy here, use the
       simpler SimpleAgent in a manual loop calling `agent.Run(ctx, ...)`
       4 times — clarity trumps paradigm-richness for the example. Pick
       the cleaner of the two; document the choice in a comment.
     - `ctx, _ := budget.WithBudget(context.Background(), budget.Budget{MaxCalls: 3})`.
     - Call the agent; expect `budget.ErrCallsExceeded` on the 4th call.
       Assert with `errors.Is(err, budget.ErrBudgetExceeded)` AND
       `errors.Is(err, budget.ErrCallsExceeded)`. Print: "MaxCalls: 3
       calls succeeded, 4th denied with %v (errors.Is(ErrBudgetExceeded) = true)".
     - Print the final tracker `Snapshot()` (extract via
       `t, _ := budget.From(ctx); fmt.Println(t.Snapshot())`).
   - `func demoMaxTokens()`:
     - Build a `ScriptedLLM` with 3 canned responses, each
       `Usage.TotalTokens = 60`.
     - `ctx, t := budget.WithBudget(context.Background(), budget.Budget{MaxTokens: 100})`.
     - Loop 3 times calling the SimpleAgent (or
       `generateFromPrompt` directly if SimpleAgent is too verbose for
       the example — but the public path is `SimpleAgent.Run`).
     - On iteration 1: success, `Snapshot().Tokens = 60`.
     - On iteration 2: post-call deny — the err is `ErrTokensExceeded`
       AND the response carried tokens (the network call succeeded).
       Print: "MaxTokens: call 2 returned a valid response but exhausted
       the budget: %v".
     - On iteration 3 (would-be): same again — post-call deny. Print to
       show the contract is consistent across cycles.
     - Print `t.Snapshot()`.
   - `func demoMaxWall()`:
     - Build the `slowLLM` wrapper (from step 2) with `pause: 200 *
       time.Millisecond` around a single-response `ScriptedLLM`.
     - `ctx, _ := budget.WithBudget(context.Background(), budget.Budget{MaxWall: 50 * time.Millisecond})`.
     - Call the agent; expect `errors.Is(err, context.DeadlineExceeded)`.
       Print: "MaxWall: deadline fired before response: %v".
5. Write `examples/06-budget/main_test.go`:
   - `package main` (so it can re-run `main()` directly in-process).
   - **`TestExample_RunsToCompletion`**:
     - Capture STDOUT via `os.Pipe`: re-assign `os.Stdout` to the write
       end of a pipe, call `main()`, restore `os.Stdout`, read all of
       the read end.
     - Assert the captured output contains: "MaxCalls", "4th denied",
       "ErrBudgetExceeded" or "budget: exceeded" (the sentinel surface),
       "MaxTokens", "valid response but exhausted", "MaxWall", "deadline
       fired", and a trailing "OK".
     - The test is **deterministic** — `ScriptedLLM` produces fixed
       responses; the wall-clock demo uses a 200ms sleep against a 50ms
       deadline which fires reliably on any machine (4× margin).
6. Write `examples/06-budget/README.md` (≤ 60 lines):
   - Title: `# 06 — Budget / cancellation context`.
   - 3-line intro: what the demo is, why it exists (CC-1, KC-4), how to
     run it (`cd examples && go run ./06-budget` — examples/ is a
     separate Go module).
   - **The Budget struct** section: a code block showing the four fields
     with a one-line comment per field.
   - **How enforcement happens** section: cite
     `agent_chatmodel.go::generateFromPrompt` (the chokepoint); note pre-
     call charge counts attempts (Q2); note post-call charge denies
     after a successful generate.
   - **Wall-clock + cancellation** section: a snippet showing that
     `MaxWall` plumbs `context.WithDeadline` automatically; callers do
     NOT need to wrap their own deadline. Show how the example's
     `slowLLM` wrapper honors `ctx.Done()`.
   - **What's deferred to v1.3** section: a bullet list — `budget.Wrap`
     decorator (for bench/rag/context-compress callers); `Estimator`
     interface (pre-call token denial); CostMapper / cost-table.
   - Stdlib-only note at the bottom: "the `budget` package depends only
     on `context`, `errors`, `fmt`, `sync`, `sync/atomic`, `time`."
7. Update `examples/README.md`:
   - Read the current contents.
   - Add a one-line entry for `06-budget` matching the existing entry
     style. If there's a table, add a row; if there's a bullet list, add
     a bullet. Do NOT rewrite the file.
8. Run `GOWORK=off go mod tidy` from `examples/` so `examples/go.mod`
   picks up the new `budget` import. If the tidy fails or pulls in any
   non-local module, **stop and surface** — do not amend.
9. Run every command in `<verify>`.

Out of scope:
- Updating the top-level `README.md` (the project root) — the prompt
  listed it but a grep of the repo root README shows examples are NOT
  listed there; the examples index lives at `examples/README.md`. If
  the top-level README does list examples, add one line there too;
  otherwise, skip. Document the choice in the slice's commit message.
- Demonstrating a CostMapper — deferred (v1.3 with the cost-table sister-
  repo work).
- Adding `main_test.go` to other examples — out of scope; this slice
  ships only the 06-budget example.
- Any change to `examples/scriptedllm/` itself unless the existing
  constructor cannot be reused — prefer the local-wrapper approach to
  isolate the example.
</tasks>

<verify>
- example directory exists with all four artifacts:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && test -f examples/06-budget/main.go && test -f examples/06-budget/main_test.go && test -f examples/06-budget/README.md && echo EXAMPLE-FILES-OK`
- example test passes (deterministic, network-free) — **examples/ is a separate Go module, must cd in**:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples && GOWORK=off GOCACHE=/tmp/go-build go test ./06-budget/... -count=1 && echo EXAMPLE-TEST-OK`
- example runs to completion (smoke) — same cd:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples && GOWORK=off GOCACHE=/tmp/go-build go run ./06-budget && echo EXAMPLE-RUN-OK`
- examples README mentions 06-budget:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q '06-budget' examples/README.md && echo INDEX-OK`
- core go.mod unchanged (the example's go.mod is a separate module —
  it may change; the CORE one must not):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- go.mod go.sum | wc -l` (0)
- example README cites the chokepoint + Q2 + deferred items:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q 'generateFromPrompt' examples/06-budget/README.md && grep -q -E '(attempts|Q2)' examples/06-budget/README.md && grep -q -E '(v1\.3|deferred)' examples/06-budget/README.md && echo README-OK`
- example imports the budget package:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q '"github.com/costa92/llm-agent/budget"' examples/06-budget/main.go && echo BUDGET-IMPORT-OK`
- example uses ScriptedLLM (canonical mock per CLAUDE.md):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q 'scriptedllm' examples/06-budget/main.go && echo SCRIPTED-OK`
- core repo-wide test still green (smoke for the integration from 35-02):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && echo CORE-TEST-OK`
</verify>

<acceptance>
- `examples/06-budget/main.go`, `examples/06-budget/main_test.go`, `examples/06-budget/README.md` all exist.
- The example demonstrates all three budget dimensions: `MaxCalls` (pre-call deny), `MaxTokens` (post-call deny — both `(resp, err)` returned), `MaxWall` (ctx-deadline cancellation).
- The example uses the canonical `examples/scriptedllm` mock per CLAUDE.md — no provider import, no network, no third-party dep.
- `go test ./examples/06-budget/... -count=1` passes and is in the standard test suite.
- `cd examples && go run ./06-budget` exits 0 with a deterministic transcript (examples is a separate Go module).
- `examples/README.md` lists `06-budget` alongside 01-05.
- The README walks through the Budget struct, chokepoint enforcement, wall-clock composition, and what's deferred to v1.3.
- Core `go.mod` and `go.sum` are unchanged.
- All `<verify>` commands pass.
</acceptance>
</content>
</invoke>