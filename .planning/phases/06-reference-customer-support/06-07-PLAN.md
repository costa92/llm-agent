---
phase: 06-reference-customer-support
plan: 07
type: execute
wave: 7
repo: llm-agent-customer-support
depends_on: [06-06]
files_modified:
  - internal/guardrails/
  - internal/supportflow/
  - internal/httpapi/
autonomous: true
requirements: [REFSVC-09]
must_haves:
  truths:
    - "prompt-injection defense is layered and auditable"
    - "tool allowlists never trust LLM-supplied user identity"
  artifacts:
    - path: "internal/guardrails/"
      provides: "prompt-injection defense layer"
      contains: "input filter + allowlist + untrusted-RAG marking"
---

<objective>
Ship the day-one prompt-injection protections promised by the roadmap.
</objective>

<tasks>
- Add input-filter heuristics and safe fallback response path.
- Add server-side tool allowlist and `user_id` enforcement.
- Mark retrieved knowledge content as untrusted in the system prompt path.
- Add tests for flagged input and blocked tool abuse.
</tasks>

<verify>
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./internal/guardrails ./internal/supportflow -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./... -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go build ./...`
</verify>
