---
phase: 06-reference-customer-support
plan: 01
type: execute
wave: 1
repo: llm-agent-customer-support
depends_on: []
files_modified:
  - cmd/server/
  - internal/config/
  - internal/app/
autonomous: true
requirements: [REFSVC-01]
must_haves:
  truths:
    - "service bootstrap is env-driven and provider-aware"
    - "OTel tracer provider and HTTP server both shut down cleanly"
  artifacts:
    - path: "cmd/server/main.go"
      provides: "signal-aware server entrypoint"
      contains: "config load + app bootstrap + graceful shutdown"
    - path: "internal/app/"
      provides: "runtime bootstrap seam"
      contains: "agent factory + telemetry lifecycle + server wiring"
---

<objective>
Land the minimal service bootstrap for the reference customer-support repo:
config parsing, real provider/model selection, OTel initialization, wrapped
agent construction, and graceful shutdown.
</objective>

<tasks>
- Add env parsing with provider-aware defaults and OTLP exporter knobs.
- Add a default agent/model factory that selects OpenAI, Anthropic, or Ollama.
- Wrap the chosen model/agent with `llm-agent-otel`.
- Build an `http.Server` bootstrap with context-driven shutdown.
- Add `cmd/server/main.go` with SIGINT/SIGTERM handling.
- Update README so it reflects the repo's new bootstrap state.
</tasks>

<verify>
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./internal/config ./internal/app -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./... -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go build ./...`
</verify>
