---
phase: 06-reference-customer-support
plan: 04
type: execute
wave: 4
repo: llm-agent-customer-support
depends_on: [06-03]
files_modified:
  - internal/supportflow/
  - internal/httpapi/
  - internal/app/
autonomous: true
requirements: [REFSVC-05]
must_haves:
  truths:
    - "reference flow composes RAG, StateGraph triage, and native tools"
    - "support flow extends the existing support_triage direction rather than replacing it with ad hoc prompts"
  artifacts:
    - path: "internal/supportflow/"
      provides: "customer-support orchestration"
      contains: "RAG lookup + triage graph + tool-enabled agent flow"
---

<objective>
Build the first real customer-support flow on top of the service transport and
provider seams.
</objective>

<tasks>
- Add typed support-flow state and graph routing.
- Add RAG lookup integration and tool surfaces.
- Connect the HTTP layer to the support flow instead of a simple echo agent.
- Add tests for triage branching, lookup fallback, and tool-call flow.
</tasks>

<verify>
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./internal/supportflow ./internal/httpapi -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./... -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go build ./...`
</verify>
