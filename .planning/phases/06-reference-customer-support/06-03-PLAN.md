---
phase: 06-reference-customer-support
plan: 03
type: execute
wave: 3
repo: llm-agent-customer-support
depends_on: [06-02]
files_modified:
  - internal/config/
  - internal/app/
  - internal/providers/
autonomous: true
requirements: [REFSVC-04]
must_haves:
  truths:
    - "chat and embedding provider selection are independent"
    - "Anthropic chat plus OpenAI or Ollama embeddings is supported explicitly"
  artifacts:
    - path: "internal/providers/"
      provides: "provider selection + embedding seams"
      contains: "LLM and embedding factories"
---

<objective>
Split chat-provider and embedding-provider selection so the service can run
cross-provider combinations without changing the binary.
</objective>

<tasks>
- Add explicit embedding-provider config and defaults.
- Add embedding factory with truthful Anthropic unsupported behavior.
- Thread both factories into app/service construction.
- Add tests covering supported mixed-provider combos and invalid config.
</tasks>

<verify>
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./internal/... -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go build ./...`
</verify>
