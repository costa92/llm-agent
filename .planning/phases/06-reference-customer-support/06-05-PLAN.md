---
phase: 06-reference-customer-support
plan: 05
type: execute
wave: 5
repo: llm-agent-customer-support
depends_on: [06-04]
files_modified:
  - internal/sessionstore/
  - internal/supportflow/
  - internal/httpapi/
autonomous: true
requirements: [REFSVC-06]
must_haves:
  truths:
    - "session state is persisted outside agent instances"
    - "SQLite dev and Postgres prod share one storage contract"
  artifacts:
    - path: "internal/sessionstore/"
      provides: "session persistence seam"
      contains: "SQLite + Postgres implementations"
---

<objective>
Add durable session state so requests stay stateless at the process layer while
conversation context survives across calls.
</objective>

<tasks>
- Add storage interface and schema.
- Add SQLite implementation for local/demo use.
- Add Postgres implementation for production parity.
- Thread session lookup/save into the support flow and HTTP layer.
</tasks>

<verify>
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./internal/sessionstore ./internal/supportflow -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./... -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go build ./...`
</verify>
