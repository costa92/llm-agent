---
phase: 06-reference-customer-support
plan: 02
type: execute
wave: 2
repo: llm-agent-customer-support
depends_on: [06-01]
files_modified:
  - internal/httpapi/
  - internal/app/
  - cmd/server/
autonomous: true
requirements: [REFSVC-02, REFSVC-03]
must_haves:
  truths:
    - "sync and streaming chat endpoints share one service runtime"
    - "every HTTP response exposes trace correlation"
  artifacts:
    - path: "internal/httpapi/"
      provides: "HTTP transport handlers"
      contains: "POST /chat + POST /chat/stream + GET /healthz + GET /readyz"
---

<objective>
Add the first transport surface: JSON chat, SSE chat streaming, health probes,
and `X-Trace-Id` response propagation.
</objective>

<tasks>
- Add request/response DTOs and JSON validation.
- Add `/chat` one-shot endpoint backed by the wrapped agent.
- Add `/chat/stream` SSE endpoint backed by `RunStream`.
- Add `/healthz` and `/readyz`.
- Add trace-id response header wiring shared by all handlers.
</tasks>

<verify>
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./internal/httpapi ./internal/app -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go test ./... -count=1`
- `GOWORK=/tmp/go.work GOCACHE=/tmp/go-build go build ./...`
</verify>
