---
phase: 06-reference-customer-support
plan: 08
type: execute
wave: 8
repo: llm-agent-customer-support
depends_on: [06-07]
files_modified:
  - compose/
  - dashboards/
  - README.md
autonomous: true
requirements: [REFSVC-10, REFSVC-11, REFSVC-12, REFSVC-13]
must_haves:
  truths:
    - "fresh clone demo stack becomes observable quickly"
    - "README states the demo-only boundary explicitly"
  artifacts:
    - path: "compose/"
      provides: "local demo stack"
      contains: "app + ollama + collector + grafana stack config"
    - path: "dashboards/"
      provides: "pre-provisioned Grafana panels"
      contains: "latency, tokens, cost, errors, tool success ratio"
---

<objective>
Package the service into a documented local demo stack with observability,
sampling policy, and explicit production caveats.
</objective>

<tasks>
- Add compose stack and collector tail-sampling config.
- Add Grafana dashboard JSON.
- Update README with startup steps, trace verification, and hardening banner.
- Verify end-to-end startup and basic trace visibility.
</tasks>

<verify>
- `docker compose up --build`
- `curl localhost:8080/readyz`
- `curl -X POST localhost:8080/chat -d '{"message":"hello"}'`
</verify>
