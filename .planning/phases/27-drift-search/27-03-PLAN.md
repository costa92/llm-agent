---
phase: 27-drift-search
plan: 03
type: execute
wave: 3
repo: llm-agent-rag
depends_on: [27-01, 27-02]
files_modified:
  - examples/graphrag_drift_example_test.go
  - docs/graphrag.md
requirements: [RAG-GRAPH4-04]
must_haves:
  truths:
    - "DRIFT search is demonstrated end-to-end by a deterministic worked example"
    - "a reader can learn DRIFT usage, the primer/local budget, and the round cap from the docs"
    - "the docs record the v1.0+ deferral list so the next milestone's scope is clear"
  artifacts:
    - path: "examples/graphrag_drift_example_test.go"
      provides: "deterministic DRIFT worked example"
      contains: |
        Example_graphRAGDrift with a stable Output
---

> Archived planning record only.
> Do not use as current development guide.
> Current development follows live code and `llm-agent-rag` current docs.


<objective>
Ship a deterministic DRIFT worked example and finalize `docs/graphrag.md`
for v0.9 — DRIFT usage, the budget, the round cap, and the v1.0+ deferral
list. Completes RAG-GRAPH4-04 and Phase 27 / the v0.9 milestone.
</objective>

<context>
- `rag.System.AskDrift` + `DriftOptions` + `Diagnostics.Drift` (27-01);
  `eval.DriftEvaluator` (27-02).
- `examples/graphrag_global_example_test.go` is the closest worked-example
  template — `DictionaryEntityExtractor` + `LouvainDetector` + a scripted
  `generate.Model` routed by `SystemPrompt` prefix, a stable `// Output:`.
- `docs/graphrag.md` is the GraphRAG doc — Tier-1, Tier-3, and (after
  Phase 26) the path-ranked-evidence section. v0.9 adds DRIFT and finalizes
  the deferral list.
- Read the Phase 26/27 SUMMARYs and the v0.9 keystones (KG4-1..KG4-7).
</context>

<tasks>
1. `examples/graphrag_drift_example_test.go` — `Example_graphRAGDrift`:
   a fully deterministic end-to-end wiring — `DictionaryEntityExtractor` +
   `LouvainDetector` in `Options`, a scripted `generate.Model` serving the
   primer map, the local-round generations, and the synthesis (routed by
   `SystemPrompt` prefix, as the global example does), import a small fixed
   corpus, call `AskDrift`, print the answer and the DRIFT diagnostics
   (primer community count, rounds run) — with a stable `// Output:`. No
   live model. If the `// Output:` does not match, run it and correct the
   block to the real deterministic output.
2. `docs/graphrag.md` — add a **DRIFT search** section: `System.AskDrift`,
   the primer → bounded local follow-up loop → synthesis flow, `DriftOptions`
   (and the round cap — default 2, hard cap 3), `Diagnostics.Drift`, a
   short wiring code example, and `eval.DriftEvaluator`. Position DRIFT as
   the hybrid between `AskGlobal` (pure global) and `Ask` + `GraphRetriever`
   (local). Cross-check every type/method name against the shipped code.
3. `docs/graphrag.md` — finalize the deferral section as **"Deferred to
   v1.0+"**: incremental community maintenance (with its profiling trigger
   condition — revisit only if `Detect` dominates re-ingest on a real
   corpus), claim/covariate extraction, a dedicated graph database. Keep
   the "Neo4j is a future `GraphStore` impl" note.
4. Run every command in `<verify>` — verify the build stays green; the only
   code change this slice makes is the example test.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./examples/... -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- confirm no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
- core facade smoke (run from the core repo `llm-agent`):
  `GOWORK=off go vet ./rag/... && go test ./rag/...`
</verify>

<acceptance>
- `Example_graphRAGDrift` runs deterministically with a stable `// Output:`.
- `docs/graphrag.md` documents DRIFT search — `AskDrift`, the budget, the
  round cap, `eval.DriftEvaluator` — and finalizes the v1.0+ deferral list.
- Every type/method name in the doc/example matches the shipped code.
- No new module dependency; all `<verify>` commands pass.
</acceptance>
