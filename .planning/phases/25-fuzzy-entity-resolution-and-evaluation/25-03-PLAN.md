---
phase: 25-fuzzy-entity-resolution-and-evaluation
plan: 03
type: execute
wave: 3
repo: llm-agent-rag
depends_on: [25-01, 25-02]
files_modified:
  - docs/graphrag.md
requirements: [RAG-GRAPH3-06]
must_haves:
  truths:
    - "a reader can learn how to use GraphRAG Tier-3 — communities, global search, fuzzy resolution — from the docs"
    - "the lazy-vs-eager tradeoff and the fuzzy-resolution false-positive risk are documented honestly"
    - "the v0.9 deferral list is recorded so the next milestone's scope is clear"
  artifacts:
    - path: "docs/graphrag.md"
      provides: "the GraphRAG doc, extended for v0.8 Tier-3"
      contains: |
        Tier-3 sections: community detection, community summaries,
        global search (AskGlobal), fuzzy entity resolution; v0.9 deferral list
---

<objective>
Update `docs/graphrag.md` for v0.8 GraphRAG Tier-3 — document community
detection, lazy community summaries, the `AskGlobal` global-search path,
and fuzzy entity resolution, with the honest tradeoffs and the v0.9
deferral list. Completes RAG-GRAPH3-06 and Phase 25 / the v0.8 milestone.
</objective>

<context>
- `docs/graphrag.md` is the v0.7 GraphRAG doc — three pieces
  (`EntityExtractor`, `GraphStore`, `GraphRetriever`), the `EnableGraph`
  toggle, a "Deferred to v0.8" section. v0.8 has now delivered that
  deferral list's first half; the doc must be extended, and its "Deferred"
  section rewritten for v0.9.
- v0.8 shipped (Phases 23-24-25): `graph.CommunityDetector`
  (`LouvainDetector`), `store.CommunityStore`, `graph.CommunitySummarizer`
  (lazy reports), `rag.System.AskGlobal` + `PrewarmCommunityReports`,
  `graph.EntityResolver` (`EmbeddingEntityResolver`), `eval.GlobalEvaluator`.
- Read the Phase 23/24/25 SUMMARY files and the v0.8 keystone decisions
  (KG3-1..KG3-8 in `.planning/milestones/`/`ROADMAP.md`) for accurate
  detail. Cross-check every type/method name against the actual code in
  `/tmp/llm-agent-rag`.
</context>

<tasks>
1. Update `docs/graphrag.md`:
   - keep the v0.7 Tier-1 content (the three pieces, `EnableGraph`).
   - add a **Tier-3** section covering:
     - **Community detection** — `graph.CommunityDetector` /
       `LouvainDetector` / `LabelPropagationDetector`; `Options.CommunityDetector`;
       communities persist behind `store.CommunityStore`; re-detection on
       re-ingest; deterministic, pure stdlib.
     - **Community summaries** — `CommunitySummarizer` /
       `LLMCommunitySummarizer`; the **lazy-by-default** model (reports
       generated at query time and cached, keyed by content hash) vs the
       opt-in eager `PrewarmCommunityReports`; the cost tradeoff.
     - **Global search** — `rag.System.AskGlobal`: map-reduce over
       community reports for whole-corpus "sense-making" questions; a
       separate answer path from `Ask` (no retrieve/rerank/pack); a short
       code example wiring it.
     - **Fuzzy entity resolution** — `graph.EntityResolver` /
       `EmbeddingEntityResolver`; the opt-in pre-pass before `Canonicalize`;
       the `NoopEntityResolver` default; the **false-positive caveat**
       (ship conservative, high threshold, same-type-only).
     - **Evaluating global search** — `eval.GlobalEvaluator` (Triad/Judge,
       not chunk recall).
   - rewrite the deferral section as **"Deferred to v0.9"**: DRIFT search,
     incremental community maintenance (v0.8 does full per-namespace
     re-detection), path-ranking / subgraph-as-evidence, fuzzy-resolution
     quality improvements. Keep the "Neo4j is a future `GraphStore` impl"
     note.
2. Run every command in `<verify>` — this slice is docs-only, so the build
   must stay green and unchanged; verify nothing regressed.

Out of scope:
- Any code change — this slice is `docs/graphrag.md` only.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- confirm no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
- confirm only the doc changed: `cd /tmp/llm-agent-rag && git diff --stat`
  shows `docs/graphrag.md` as the only file touched by this slice
- core facade smoke (run from the core repo `llm-agent`):
  `GOWORK=off go vet ./rag/... && go test ./rag/...`
</verify>

<acceptance>
- `docs/graphrag.md` documents GraphRAG Tier-3 — community detection, lazy
  community summaries, `AskGlobal` global search, fuzzy entity resolution,
  and global-search eval — with the lazy-vs-eager and false-positive
  tradeoffs called out honestly.
- The deferral section names the v0.9 list (DRIFT, incremental community
  maintenance, path-ranking).
- Every type/method name in the doc matches the shipped code.
- No code change, no new dependency; all `<verify>` commands pass.
</acceptance>
