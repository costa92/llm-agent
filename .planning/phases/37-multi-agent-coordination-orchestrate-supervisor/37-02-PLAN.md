---
phase: 37-multi-agent-coordination-orchestrate-supervisor
plan: 02
type: execute
wave: 2
repo: llm-agent (core)
depends_on: ["37-01"]
files_modified:
  - orchestrate/supervisor_budget_test.go
autonomous: true
requirements: [CC-3]
must_haves:
  truths:
    - "Supervisor inherits CC-1 budget enforcement WITH ZERO new code — ctx propagates unchanged from Supervisor.Run through worker.Run into agent_chatmodel.generateFromPrompt where budget.From(ctx).Charge fires"
    - "Supervisor inherits CC-2 policy enforcement per-worker — wrapping a worker's underlying llm.ChatModel with policy.Wrap fires gates without Supervisor knowing"
    - "Budget-beats-policy invariant holds at the Supervisor level — chokepoint Calls pre-charge fires BEFORE the policy decorator returns; the budget sentinel surfaces, NOT policy.ErrBlocked"
    - "Workers receive ctx UNCHANGED from Supervisor — verified by passing a budget.Tracker bound to ctx and observing tracker.Snapshot().Calls increment per worker invocation (Pitfall 4 hard rule)"
    - "go test -race ./orchestrate/... -count=1 stays green with the new tests"
    - "go.mod / go.sum unchanged; no third-party import added"
    - "KC-5 preserved: no edit to validated public types — only a NEW test file inside the existing orchestrate package"
  artifacts:
    - path: "orchestrate/supervisor_budget_test.go"
      provides: "TestSupervisor_BudgetPropagatesToWorker (per-call charges via chokepoint; MaxCalls=3 → 4th charge denies via budget.ErrCallsExceeded — proves CC-1 inheritance), TestSupervisor_BudgetMaxWall (MaxWall=50ms with a slow scripted model → context.DeadlineExceeded — wall-clock cap propagates via ctx), TestSupervisor_PolicyPerWorker (policy.Wrap on a worker's model fires on dispatch; second dispatch surfaces policy.ErrBlocked — proves CC-2 inheritance per-worker), TestSupervisor_PolicyPerPlanner (policy.Wrap on the planner's model fires; blocked planner surfaces policy.ErrBlocked from [plan] node), TestSupervisor_BudgetBeatsPolicy (model wrapped in both — chokepoint Calls pre-charge fires before policy decorator; assert errors.Is(err, budget.ErrCallsExceeded) AND NOT policy.ErrBlocked), TestSupervisor_NoDetachedCtx (sentinel test: pass a custom ctx with a recognizable Value; assert workers receive a ctx whose Value lookup returns the same value — Pitfall 4)"
  key_links:
    - from: "orchestrate.Supervisor.Run (37-01)"
      to: "agent_chatmodel.generateFromPrompt (Phase 35 chokepoint)"
      via: "ctx propagation through worker.Run + planner.Run — Supervisor adds ZERO new code"
      pattern: "ctx unchanged from Run to worker"
    - from: "Worker's underlying llm.ChatModel"
      to: "policy.Wrap decorator (Phase 36)"
      via: "caller-side composition BEFORE worker construction; Supervisor is policy-agnostic"
      pattern: "policy\\.Wrap\\("
    - from: "Budget cap"
      to: "Policy gate"
      via: "chokepoint pre-charge fires before policy decorator returns"
      pattern: "errors\\.Is\\(err,\\s*budget\\.ErrCallsExceeded\\)"
---

<objective>
Prove Phase 35 (CC-1 budget) and Phase 36 (CC-2 policy) compose
correctly with the just-shipped `orchestrate.Supervisor` — WITH ZERO
new code in Supervisor itself. The 6 integration tests in this slice
verify that Supervisor inherits budget enforcement via ctx propagation
(per-LLM-call charges at the `generateFromPrompt` chokepoint count for
both planner and worker invocations) and policy enforcement per-worker
(wrapping a worker's underlying model with `policy.Wrap` fires gates
without Supervisor knowing). Also verified: the budget-beats-policy
invariant holds at the Supervisor level (chokepoint pre-charge wins
over the policy decorator), and workers receive ctx UNCHANGED (Pitfall 4
hard rule — no detached children, no `context.WithoutCancel`). Pure
integration tests — no production-code changes. CC-3.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. Core stays stdlib-only (CLAUDE.md Rule 1; KC-5).
- This slice ships ONE new file: `orchestrate/supervisor_budget_test.go`
  (in-package `orchestrate` test file). NO production-code changes.
  Supervisor's behavior is fixed by 37-01; this slice proves it.
- **Why this slice exists separately from 37-01:** the 37-01 functional
  tests use stubAgents (not real LLM-driven paradigms), so the
  `generateFromPrompt` chokepoint is not exercised. To prove budget
  propagates through the chokepoint, this slice wires real `SimpleAgent`
  instances over `llm.NewScriptedLLM(...)` — the canonical mock per
  CLAUDE.md. Same for policy: the policy decorator wraps an
  `llm.ChatModel`, so we need a real LLM-driven Simple/ReAct paradigm
  to exercise the decorator.
- **The composition stack documented in Phase 36 (KC-3):**
  `policy.Wrap(otelmodel.Wrap(provider))` — outer-most policy denies
  before observed; middle observes; inner-most makes the call. Phase 37
  adds nothing to this stack at the Supervisor level: each WORKER's
  underlying model is independently wrapped by the caller before
  Supervisor construction. Supervisor itself is policy-agnostic.
- **The KC-4 chokepoint invariant:** budget enforces UNDERNEATH the
  policy decorator at `agent_chatmodel.generateFromPrompt`
  (`agent_chatmodel.go:11-54` — read for context, NOT edited). The
  pre-call `t.Charge(budget.Usage{Calls: 1})` fires BEFORE the policy
  decorator's `Generate` is invoked — so a budget-exhausted request
  surfaces `budget.ErrCallsExceeded` from the chokepoint, NEVER
  reaching the policy decorator. This invariant is the
  budget-beats-policy property documented in
  `policy/integration_test.go::TestCompose_BudgetBeatsPolicyAtChokepoint`
  (lines 425-469); this slice asserts the same invariant holds at the
  Supervisor level.
- **The per-worker policy pattern (KC-3):** workers expose an
  `agents.Agent` interface; each worker's CONSTRUCTOR takes an
  `llm.ChatModel`. The caller wraps that model with `policy.Wrap(...)`
  BEFORE passing it to the worker constructor:
  ```
  base := llm.NewScriptedLLM(...)
  wrapped := policy.Wrap(base, blockingGate)
  worker := agents.NewSimpleAgent(wrapped, agents.SimpleOptions{Name: "w"})
  sup := orchestrate.NewSupervisor("x", orchestrate.SupervisorOptions{
      ..., Workers: map[string]agents.Agent{"w": worker}, ...,
  })
  ```
  Supervisor's `Workers` map sees only `agents.Agent`; the policy
  decoration is invisible at that boundary.
- **Pitfall 4 — workers receive ctx UNCHANGED.** No
  `context.WithoutCancel`, no detached child. This slice has a
  sentinel test (`TestSupervisor_NoDetachedCtx`) that passes a custom
  ctx value and asserts workers can read it via `ctx.Value(...)` — a
  detached child would fail this lookup.
- **Test imports allowed in `supervisor_budget_test.go`:** `context`,
  `errors`, `strings`, `sync/atomic`, `testing`, `time`, plus the
  in-repo packages `github.com/costa92/llm-agent` (root agents),
  `github.com/costa92/llm-agent/budget`, `github.com/costa92/llm-agent/llm`,
  `github.com/costa92/llm-agent/policy`. ScriptedLLM lives in `llm`
  (verified — `llm/scriptedllm.go` exists).
- See `37-RESEARCH.md` §"Decision E" (no per-round budget charge — the
  chokepoint suffices), §"Common Pitfalls" 4 (ctx unbroken), §"Slice
  Breakdown" row 37-02 (5+1 tests), §"Phase Requirements → Test Map"
  rows 11-12 (CC-3 budget/policy integration coverage).
- See `37-PATTERNS.md` §"orchestrate/supervisor_budget_test.go" (the
  test-shape templates lifted from `agent_chatmodel_test.go` chokepoint
  patterns + `policy/integration_test.go` budget-beats-policy invariant).
</context>

<interfaces>
Key contracts the executor needs. Already verified by direct file read.

From `budget/budget.go` (the Phase 35 surface):
```
type Budget struct {
    MaxTokens int
    MaxCalls  int
    MaxWall   time.Duration
    MaxCost   float64
}
type Usage struct { Tokens int; Calls int; Wall time.Duration; Cost float64 }
type Tracker interface { Charge(Usage) error; Snapshot() Usage; Remaining(Budget) Usage }
func WithBudget(ctx context.Context, b Budget) (context.Context, Tracker)
func From(ctx context.Context) (Tracker, bool)
func NewStrict(b Budget) Tracker
func NewSoft(b Budget) Tracker
var (
    ErrBudgetExceeded   = errors.New("budget: exceeded")
    ErrTokensExceeded   = fmt.Errorf("%w: tokens", ErrBudgetExceeded)
    ErrCallsExceeded    = fmt.Errorf("%w: calls", ErrBudgetExceeded)
    ErrWallExceeded     = fmt.Errorf("%w: wall", ErrBudgetExceeded)
    ErrCostExceeded     = fmt.Errorf("%w: cost", ErrBudgetExceeded)
)
```

From `policy/policy.go` + `policy/gate.go` (the Phase 36 surface):
```
func Wrap(inner llm.ChatModel, gates ...Gate) llm.ChatModel
type Gate interface {
    Inspect(ctx context.Context, ev Event) Decision
    Name() string
}
type Event struct { Kind EventKind; Req *llm.Request; Resp *llm.Response; Delta *llm.StreamEvent }
type EventKind uint8
const ( PreGenerate EventKind = iota; PostGenerate; PreStream; StreamDelta; PostStream )
type Decision struct { Action DecisionAction; Reason string; Replacement string; Gate string }
type DecisionAction uint8
const ( Allow DecisionAction = iota; Block; Redact; Replace )
var ErrBlocked = errors.New("policy: blocked")
type BlockedError struct { Gate, Reason string; Decision Decision; Wrapped error }
// (*BlockedError).Is(target) returns target == ErrBlocked
```

From `llm/scriptedllm.go` (the canonical mock):
```
func NewScriptedLLM(opts ...ScriptedOption) ChatModel
// Options include WithResponses(...string), WithProvider(...), WithModel(...), etc.
```

From `simple.go` (the SimpleAgent — the canonical paradigm for these tests):
```
type SimpleOptions struct {
    Name         string
    SystemPrompt string
    // ... (more fields)
}
func NewSimpleAgent(model llm.ChatModel, opts SimpleOptions) *SimpleAgent
// SimpleAgent satisfies agents.Agent (Name/Run/RunStream)
```

From `agent_chatmodel.go` (the chokepoint — read for context, NOT edited):
```
func generateFromPrompt(ctx context.Context, model llm.ChatModel, systemPrompt, prompt string) (llm.Response, error) {
    t, hasBudget := budget.From(ctx)
    if hasBudget {
        if err := t.Charge(budget.Usage{Calls: 1}); err != nil {
            return llm.Response{}, err  // PRE-CALL DENY: budget wins before policy
        }
    }
    resp, err := model.Generate(ctx, req)  // model HERE is the policy.Wrap'd one
    // ... post-call Charge for Tokens ...
}
```
This is the precedent location for "budget beats policy" — budget
charges BEFORE `model.Generate` (which is the policy wrapper); a denied
budget short-circuits without the policy gate ever firing.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write orchestrate/supervisor_budget_test.go — 6 integration tests for budget + policy compose</name>
  <files>orchestrate/supervisor_budget_test.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/supervisor.go` (the just-written file from 37-01 — the surface under test)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/supervisor_test.go` (the 37-01 sibling — for `parseDemoDispatch` + `joinWorkerResults` helpers that this slice REUSES via in-package access)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/agent_chatmodel.go` (entire 71 LOC — the chokepoint; proves Supervisor adds zero budget code)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/agent_chatmodel_test.go` (lines 101-129 `TestGenerateFromPrompt_MaxCalls_PreCallDeny` — the chokepoint pre-call deny pattern; lines 145-197 `TestGenerateFromPrompt_MaxTokens_PostCallDeny_ReturnsBoth` — the post-call deny pattern)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/budget/budget.go` (entire ~250 LOC — the Phase 35 surface; specifically `WithBudget`, `From`, `Budget{MaxCalls, MaxWall}`, `ErrCallsExceeded`, `ErrBudgetExceeded`, `Tracker.Snapshot`)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/budget/budget_test.go` (snapshot + Charge patterns)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/policy/policy.go` (the Phase 36 surface; `Wrap`, `Config`)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/policy/gate.go` (the `Gate` interface; `Decision{Action, Reason, ...}`; `ErrBlocked`)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/policy/integration_test.go` (lines 41-145 — the in-test `observerModel` pattern for "mimic a sister-repo decorator without importing it"; lines 425-469 `TestCompose_BudgetBeatsPolicyAtChokepoint` — the budget-beats-policy invariant test shape this slice mirrors at the Supervisor level)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/simple.go` (SimpleAgent + SimpleOptions — the paradigm used to wire LLM-driven planner/worker)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/llm/scriptedllm.go` (the canonical mock; ScriptedOption list)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-RESEARCH.md` §"Decision E" + §"Common Pitfalls" 4 + §"Slice Breakdown" row 37-02
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-PATTERNS.md` §"orchestrate/supervisor_budget_test.go"
  </read_first>
  <behavior>
    - `TestSupervisor_BudgetPropagatesToWorker`: ctx with `Budget{MaxCalls: 3}`. SimpleAgent planner over ScriptedLLM emitting 3 lines ("dispatch to w: a", "dispatch to w: b", "FINISH"); SimpleAgent worker "w" over ScriptedLLM emitting 2 lines. Supervisor runs; chokepoint charges 1 Call per planner.Run AND 1 Call per worker.Run; the 4th charge fails. Assert `errors.Is(err, budget.ErrCallsExceeded) == true` AND `errors.Is(err, budget.ErrBudgetExceeded) == true` AND `tracker.Snapshot().Calls == 3` (cap-pinned).
    - `TestSupervisor_BudgetMaxWall`: ctx with `Budget{MaxWall: 50 * time.Millisecond}`. Worker's ScriptedLLM is wrapped in a `slowLLM` decorator (in-test, ~15 LOC) that sleeps 200ms before responding. Run returns within ~50ms; assert `errors.Is(err, context.DeadlineExceeded) == true`. (NO new budget sentinel — wall enforcement is via the deadline derived in `budget.WithBudget`.)
    - `TestSupervisor_PolicyPerWorker`: worker's underlying scripted model wrapped with `policy.Wrap(model, gate)` where `gate` blocks on the SECOND `PreGenerate` event. Supervisor dispatches worker twice. Assert `errors.Is(err, policy.ErrBlocked) == true` on the second dispatch's surfacing through Supervisor.Run. Confirm Supervisor doesn't swallow the error — `err != nil` AND the supervisor name + worker name + round number appear in `err.Error()`.
    - `TestSupervisor_PolicyPerPlanner`: planner's underlying scripted model wrapped with `policy.Wrap`. Gate blocks on FIRST `PreGenerate`. Assert `errors.Is(err, policy.ErrBlocked) == true` AND the err surfaces from the [plan] node (not [dispatch]) — wrapped message contains "planner round 1".
    - `TestSupervisor_BudgetBeatsPolicy`: same worker model wrapped in BOTH a blocking policy gate AND a budget cap of `MaxCalls: 0` on ctx. Run. Assert `errors.Is(err, budget.ErrCallsExceeded) == true` AND `errors.Is(err, policy.ErrBlocked) == false`. Budget's pre-call charge wins because the chokepoint sits underneath the policy decorator (verified by `agent_chatmodel.go:23-29` — `t.Charge(...)` fires before `model.Generate(ctx, req)` which is the policy-wrapped model).
    - `TestSupervisor_NoDetachedCtx`: pass a ctx with `context.WithValue(ctx, supervisorCtxKey, "sentinel")` (using a package-private key type defined in-test); workers' scripted models are wrapped in an in-test `ctxCheckingModel` that asserts `ctx.Value(supervisorCtxKey) == "sentinel"` inside `Generate(ctx, req)`. Run. Assert the model's assertion fired (counter > 0) AND no panic/failure (the value was preserved end-to-end — Pitfall 4 hard rule).
  </behavior>
  <action>
    Create `orchestrate/supervisor_budget_test.go` in
    `package orchestrate` (in-package so `parseDemoDispatch` and
    `joinWorkerResults` from 37-01's `supervisor_test.go` are reachable
    without redeclaration). Note: this is a `_test.go` file — its
    contents are only compiled during `go test`.

    Imports: `context`, `errors`, `strings`, `sync/atomic`, `testing`,
    `time`, plus `github.com/costa92/llm-agent` (root, identifier
    `agents`), `github.com/costa92/llm-agent/budget`,
    `github.com/costa92/llm-agent/llm`,
    `github.com/costa92/llm-agent/policy`.

    Define at file top, before tests:

    - **`type ctxKey int; const supervisorCtxKey ctxKey = 1`** — typed
      key for the Pitfall 4 sentinel test.

    - **`type slowLLM struct { inner llm.ChatModel; delay time.Duration }`**
      with `Generate(ctx, req)` body:
      ```
      select {
      case <-time.After(s.delay):
          return s.inner.Generate(ctx, req)
      case <-ctx.Done():
          return llm.Response{}, ctx.Err()
      }
      ```
      and `Stream`/`Info` delegating to inner. ~15 LOC. Used in
      `TestSupervisor_BudgetMaxWall`.

    - **`type ctxCheckingModel struct { inner llm.ChatModel; hits *int64; expectedVal any }`**
      with `Generate(ctx, req)` body:
      ```
      v := ctx.Value(supervisorCtxKey)
      if v == c.expectedVal {
          atomic.AddInt64(c.hits, 1)
      }
      return c.inner.Generate(ctx, req)
      ```
      Stream/Info delegate. ~15 LOC. Used in
      `TestSupervisor_NoDetachedCtx`.

    - **`type policyGate struct { name string; blockOn int; counter *int64 }`**
      with `Name() string { return g.name }` and:
      ```
      func (g *policyGate) Inspect(_ context.Context, ev policy.Event) policy.Decision {
          if ev.Kind != policy.PreGenerate { return policy.Decision{} }
          n := atomic.AddInt64(g.counter, 1)
          if int(n) == g.blockOn {
              return policy.Decision{Action: policy.Block, Reason: "test", Gate: g.name}
          }
          return policy.Decision{}
      }
      ```
      ~15 LOC. Used in policy tests. `blockOn=1` means block on the
      first PreGenerate; `blockOn=2` means block on the second.

    Tests in this order:

    **Test 1 — `TestSupervisor_BudgetPropagatesToWorker`** (~50 LOC):
    Wire a SimpleAgent planner over `llm.NewScriptedLLM(...)` with
    3 responses (`"dispatch to w: a"`, `"dispatch to w: b"`, `"FINISH"`)
    and a SimpleAgent worker `w` over ScriptedLLM with 2 responses
    (`"answer-a"`, `"answer-b"`). Construct Supervisor with MaxRounds=5,
    Workers `{"w": worker}`, `parseDemoDispatch`, `joinWorkerResults`.
    Build ctx via `ctx, tracker := budget.WithBudget(context.Background(), budget.Budget{MaxCalls: 3})`.
    Call `sup.Run(ctx, "investigate")`. Assert:
    - `err != nil`
    - `errors.Is(err, budget.ErrCallsExceeded)` — the dimension-specific sentinel
    - `errors.Is(err, budget.ErrBudgetExceeded)` — the umbrella sentinel
    - `tracker.Snapshot().Calls == 3` — cap-pinned (the 4th attempt was denied pre-call, so the counter capped at 3 successful Charges)
    - The error message contains either "planner" or worker name "w" (the chokepoint denial propagates with the agent's name in the wrapping per `simple.go`'s error path).

    **Test 2 — `TestSupervisor_BudgetMaxWall`** (~40 LOC): wire planner
    (ScriptedLLM emitting "dispatch to w: a", "FINISH") and worker over
    a `slowLLM` wrapping a ScriptedLLM with one response. `slowLLM.delay = 200 * time.Millisecond`. Build
    `ctx, _ := budget.WithBudget(context.Background(), budget.Budget{MaxWall: 50 * time.Millisecond})`.
    Call Run. Assert `err != nil` AND `errors.Is(err, context.DeadlineExceeded) == true`. (Phase 35
    derives a `context.WithDeadline` from `MaxWall` inside `WithBudget`;
    the deadline fires the ctx into Done state at 50ms; the slow
    worker's `slowLLM.Generate` honors ctx and returns `ctx.Err()`;
    Supervisor surfaces it.)

    **Test 3 — `TestSupervisor_PolicyPerWorker`** (~50 LOC): planner
    (ScriptedLLM with "dispatch to w: a", "dispatch to w: b", "FINISH").
    Worker's UNDERLYING model is wrapped via
    `wrapped := policy.Wrap(base, &policyGate{name: "test", blockOn: 2, counter: new(int64)})`;
    then the worker is built via `agents.NewSimpleAgent(wrapped, agents.SimpleOptions{Name: "w"})`.
    Construct Supervisor with this worker. Call Run. Assert `err != nil`
    AND `errors.Is(err, policy.ErrBlocked) == true`. Assert
    `strings.Contains(err.Error(), "w")` (worker name in supervisor wrap).

    **Test 4 — `TestSupervisor_PolicyPerPlanner`** (~40 LOC): planner's
    UNDERLYING model wrapped via
    `policy.Wrap(base, &policyGate{name: "test", blockOn: 1, counter: new(int64)})`.
    Worker is unwrapped. Call Run. Assert `errors.Is(err, policy.ErrBlocked) == true`.
    Assert `strings.Contains(err.Error(), "planner") || strings.Contains(err.Error(), "round 1")`
    (the supervisor wrap names "planner round N" per 37-01's planNode error pattern).

    **Test 5 — `TestSupervisor_BudgetBeatsPolicy`** (~45 LOC): wire
    planner over a model wrapped with `policy.Wrap(base, blockingGate)`.
    Build `ctx, _ := budget.WithBudget(context.Background(), budget.Budget{MaxCalls: 0})`
    — zero allows; the very first pre-call charge fails. Call Run.
    Assert `errors.Is(err, budget.ErrCallsExceeded) == true` AND
    `errors.Is(err, policy.ErrBlocked) == false`. Confirm
    `tracker.Snapshot().Calls == 0` (the chokepoint denied pre-call;
    the counter never incremented; the policy decorator's `Generate`
    was never invoked).

    **Test 6 — `TestSupervisor_NoDetachedCtx`** (~50 LOC): build planner
    and worker over `ctxCheckingModel` wrappers; `hits := new(int64)`;
    the wrappers assert `ctx.Value(supervisorCtxKey) == "sentinel"`.
    Build base ctx via `ctx := context.WithValue(context.Background(), supervisorCtxKey, any("sentinel"))`.
    Wire happy-path Supervisor (planner emits dispatch+finish; worker
    runs once). Call Run. Assert `err == nil`. Assert
    `atomic.LoadInt64(hits) == 2` (planner called once + worker called
    once — both saw the ctx value). If any `context.WithoutCancel`
    sneaks into Supervisor's implementation, this counter goes to 0 (a
    detached ctx loses the WithValue chain — Pitfall 4 enforcement).

    Helper at file bottom: `func buildPlannerWorker(t *testing.T, plannerResponses []string, workerResponses []string) (agents.Agent, agents.Agent)`
    — convenience constructor returning `(planner, worker)`. Calls
    `t.Helper()` first. Wires both via `agents.NewSimpleAgent(...)`. Avoids ~10 LOC of repetition across tests.

    DO NOT redeclare `parseDemoDispatch`, `joinWorkerResults`,
    `stubAgent`, `validOpts`, or any helper from the 37-01 sibling
    test file — they are in-package and reachable directly.

    DO NOT import `llm-agent-otel` or `llm-agent-rag`. The Decision G
    pattern from Phase 36 (in-test mimic instead of import) holds for
    Phase 37 — no sister-repo dependency.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./orchestrate/... && GOWORK=off GOCACHE=/tmp/go-build go test ./orchestrate/... -run 'TestSupervisor_(BudgetPropagatesToWorker|BudgetMaxWall|PolicyPerWorker|PolicyPerPlanner|BudgetBeatsPolicy|NoDetachedCtx)' -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `orchestrate/supervisor_budget_test.go` exists and `go vet ./orchestrate/...` exits 0.
    - All 6 named integration tests pass: `go test ./orchestrate/... -run 'TestSupervisor_(BudgetPropagatesToWorker|BudgetMaxWall|PolicyPerWorker|PolicyPerPlanner|BudgetBeatsPolicy|NoDetachedCtx)' -count=1` exits 0.
    - `go test -race ./orchestrate/... -count=1` exits 0 (full orchestrate test suite race-clean after this slice's additions).
    - `grep -c 'errors.Is(err, budget.ErrCallsExceeded)' orchestrate/supervisor_budget_test.go` returns at least `2` (Test 1 + Test 5).
    - `grep -c 'errors.Is(err, policy.ErrBlocked)' orchestrate/supervisor_budget_test.go` returns at least `3` (Test 3 + Test 4 + Test 5 negative assertion).
    - `grep -c 'errors.Is(err, context.DeadlineExceeded)' orchestrate/supervisor_budget_test.go` returns at least `1` (Test 2).
    - `grep -c 'policy.Wrap(' orchestrate/supervisor_budget_test.go` returns at least `2` (Tests 3, 4, 5 each wrap a model).
    - `grep -c 'budget.WithBudget(' orchestrate/supervisor_budget_test.go` returns at least `3` (Tests 1, 2, 5 each install a budget).
    - `grep -c 'tracker.Snapshot()' orchestrate/supervisor_budget_test.go` returns at least `2` (Tests 1, 5 inspect snapshot).
    - `grep -c 'supervisorCtxKey' orchestrate/supervisor_budget_test.go` returns at least `3` (typed key + the test uses it).
    - No import of `llm-agent-rag` or `llm-agent-otel`: `grep -E '"github.com/costa92/llm-agent-(rag|otel)' orchestrate/supervisor_budget_test.go | wc -l` returns `0`.
    - KC-5: no edit to validated public types — `git diff main -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go orchestrate/supervisor.go orchestrate/supervisor_test.go orchestrate/doc.go agent_chatmodel.go budget/ policy/ | wc -l` returns `0` (this slice modifies only `orchestrate/supervisor_budget_test.go` — a brand-new file).
    - `go.mod` / `go.sum` unchanged: `git status --short -- go.mod go.sum | wc -l` returns `0`.
  </acceptance_criteria>
  <done>
    `orchestrate/supervisor_budget_test.go` ships 6 integration tests proving Supervisor inherits CC-1 (budget) + CC-2 (policy) with ZERO new code in production: budget propagates via ctx through the Phase 35 chokepoint; policy fires per-worker via the Phase 36 decorator; budget-beats-policy invariant holds at the Supervisor level; workers receive ctx UNCHANGED. All 6 tests pass; race-clean.
  </done>
</task>

</tasks>

<verify>
- vet + build clean repo-wide:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go build ./... && echo VET-OK`
- orchestrate tests green (incl. the new integration tests):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./orchestrate/... -count=1 && echo TEST-OK`
- race-clean on orchestrate:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1 && echo RACE-OK`
- repo-wide tests still green (no regression):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && echo REPO-TEST-OK`
- composition proofs:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -c 'errors.Is(err, budget.ErrCallsExceeded)' orchestrate/supervisor_budget_test.go` >= 2
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -c 'errors.Is(err, policy.ErrBlocked)' orchestrate/supervisor_budget_test.go` >= 3
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -c 'errors.Is(err, context.DeadlineExceeded)' orchestrate/supervisor_budget_test.go` >= 1
- no sister-repo import:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -E '"github.com/costa92/llm-agent-(rag|otel|providers|customer-support)' orchestrate/supervisor_budget_test.go | wc -l` returns 0
- KC-5 unchanged validated public types:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go agent_chatmodel.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go orchestrate/supervisor.go orchestrate/supervisor_test.go orchestrate/doc.go budget/ policy/ | wc -l` returns `0`
- go.mod / go.sum unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- go.mod go.sum | wc -l` returns `0`
</verify>

<acceptance>
- `orchestrate/supervisor_budget_test.go` exists with 6 integration tests proving:
  1. Budget propagates: ctx-bound `budget.Tracker` charges on each planner + worker LLM call via the chokepoint; `MaxCalls=3` → 4th call denies with `budget.ErrCallsExceeded`.
  2. Budget wall: `MaxWall=50ms` ctx deadline fires `context.DeadlineExceeded` when a worker's model is slow.
  3. Policy per-worker: `policy.Wrap(workerModel, gate)` fires PreGenerate gate on the worker's call; blocked dispatch surfaces `policy.ErrBlocked` from Supervisor.Run.
  4. Policy per-planner: `policy.Wrap(plannerModel, gate)` fires on the planner's call; blocked planner surfaces `policy.ErrBlocked` from the [plan] node.
  5. Budget-beats-policy: at the chokepoint, pre-call budget Charge fires BEFORE the policy decorator's Generate; budget sentinel wins.
  6. Ctx propagation: a custom ctx value passed to Supervisor.Run is reachable inside the worker's model.Generate — workers receive ctx UNCHANGED (Pitfall 4 hard rule).
- `go test -race ./orchestrate/...` is green.
- KC-5: no edit to validated public types, paradigm files, agent_chatmodel.go, budget/, policy/, or any orchestrate/* file other than the new `supervisor_budget_test.go`.
- `go.mod` unchanged; no `go.sum` created.
- No sister-repo (`llm-agent-rag`, `llm-agent-otel`, `llm-agent-providers`, `llm-agent-customer-support`) import.
- All `<verify>` commands pass.
</acceptance>

<success_criteria>
The compose stack `policy.Wrap(provider)` (and by extension
`policy.Wrap(otelmodel.Wrap(provider))` per KC-3) Just Works at the
Supervisor level — without Supervisor knowing or caring. CC-1 inheritance
is proved via the chokepoint chain; CC-2 inheritance is proved via the
per-worker decorator. The budget-beats-policy invariant from Phase 36
holds at the Supervisor level too (chokepoint architecture is the
single seam). The Pitfall 4 hard rule (workers receive ctx unchanged)
is enforced by an automated sentinel test — if a future contributor
adds `context.WithoutCancel` or detaches the ctx, the test goes red.
This slice's tests are the foundation for the compose-with-StateGraph
tests + example (37-03) and the exit gate (37-04). The Supervisor
surface from 37-01 is now proven correct under realistic LLM-driven
workloads.
</success_criteria>

<output>
Create `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-02-SUMMARY.md`
when done. Summary records: the 6 integration tests + outcomes;
proof-of-inheritance for CC-1 budget + CC-2 policy; budget-beats-policy
invariant verified at Supervisor level; Pitfall 4 ctx-propagation
sentinel test green; race-clean status; KC-5 + stdlib-only audit pass.
</output>
</content>
</invoke>