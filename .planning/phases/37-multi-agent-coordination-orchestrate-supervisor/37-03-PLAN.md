---
phase: 37-multi-agent-coordination-orchestrate-supervisor
plan: 03
type: execute
wave: 3
repo: llm-agent (core)
depends_on: ["37-01", "37-02"]
files_modified:
  - orchestrate/supervisor_compose_test.go
  - examples/08-supervisor/main.go
  - examples/08-supervisor/main_test.go
  - examples/08-supervisor/README.md
autonomous: true
requirements: [CC-3]
must_haves:
  truths:
    - "KC-1's 'facade works both directions' is proved by 3 compose tests — Supervisor inside StateGraph, StateGraph (via in-test graphAsAgent adapter) inside Supervisor, Supervisor of Supervisor"
    - "examples/08-supervisor exists as a stdlib-only deterministic program — `go run ./examples/08-supervisor` exits 0"
    - "Example smoke test pipes stdout and asserts the deterministic markers (`--- Basic:`, `OK`, budget.ErrCallsExceeded marker, compose-section header) appear — mirror of examples/06-budget/main_test.go pattern"
    - "README documents MaxRounds vs Budget.MaxCalls distinction (Decision E) AND the composition stack `policy.Wrap(otelmodel.Wrap(provider))` on a worker's model (KC-3 carry-forward, README-only)"
    - "Supervisor-of-Supervisor composition test demonstrates KC-1's composability claim — `var _ agents.Agent = (*Supervisor)(nil)` is proven not just by compile-time but by runtime composition"
    - "No sister-repo import (`llm-agent-otel`, `llm-agent-providers`, `llm-agent-customer-support`) in the example or its tests — stdlib + canonical scriptedllm only"
    - "go.mod / go.sum unchanged; KC-5 preserved"
  artifacts:
    - path: "orchestrate/supervisor_compose_test.go"
      provides: "TestSupervisor_InsideStateGraph (Supervisor as a NodeFunc inside outer StateGraph[outerState]; preprocess → supervise → postprocess), TestStateGraph_InsideSupervisor (small CompiledGraph wrapped via ~20-LOC in-test graphAsAgent adapter, registered as a Supervisor worker, dispatched), TestSupervisor_OfSupervisor (outer Supervisor with inner Supervisor as worker; each Supervisor's MaxRounds independent; documented in test comment that a shared Budget.MaxCalls would cap across both — verified separately by 37-02's TestSupervisor_BudgetPropagatesToWorker)"
    - path: "examples/08-supervisor/main.go"
      provides: "deterministic 3-demo program — demoBasic (1 planner + 2 specialists × 2 rounds → aggregate), demoBudget (Supervisor under Budget{MaxCalls: 3} → ErrCallsExceeded marker printed), demoComposeWithStateGraph (Supervisor as node in outer StateGraph[outerState]); package-level docstring naming Phase 37 + CC-3 + KC-1; uses examples/scriptedllm or llm.NewScriptedLLM (whichever is canonical per repo pattern); prints 'OK' on success"
    - path: "examples/08-supervisor/main_test.go"
      provides: "TestExample_RunsToCompletion — captures stdout via os.Pipe, calls main(), asserts deterministic markers (--- Basic:, --- Budget, errors.Is(err, budget.ErrCallsExceeded) = true, --- Compose, OK) present in output; mirror of examples/06-budget/main_test.go verbatim shape"
    - path: "examples/08-supervisor/README.md"
      provides: "≤80-line canonical readme — runtime invocation (`go run .`), one section per demo (Basic / Budget / Compose), explicit MaxRounds-vs-Budget.MaxCalls distinction (Decision E), composition stack note `policy.Wrap(otelmodel.Wrap(provider))` (KC-3 — README-only no import), graceful MaxRounds-hit behavior note (Q1/A4)"
  key_links:
    - from: "orchestrate.Supervisor"
      to: "orchestrate.NewStateGraph[outerState] as a NodeFunc"
      via: "TestSupervisor_InsideStateGraph proves Supervisor can be wrapped by another StateGraph"
      pattern: "g\\.AddNode\\(\"supervise\""
    - from: "orchestrate.CompiledGraph[wState]"
      to: "agents.Agent (via in-test graphAsAgent adapter)"
      via: "TestStateGraph_InsideSupervisor proves a StateGraph can be a Supervisor worker via a tiny adapter"
      pattern: "graphAsAgent"
    - from: "outer Supervisor"
      to: "inner Supervisor (as worker)"
      via: "TestSupervisor_OfSupervisor proves KC-1's composition claim at runtime"
      pattern: "Supervisor.*Supervisor"
    - from: "examples/08-supervisor/main.go::demoComposeWithStateGraph"
      to: "deterministic stdout assertion in main_test.go"
      via: "scripted LLM responses produce a fixed string sequence"
      pattern: "OK"
---

<objective>
Ship the KC-1 "facade works both directions" verification AND the
user-facing demo. Three compose tests in
`orchestrate/supervisor_compose_test.go` prove the Supervisor composes
with `StateGraph[S]` in both directions (Supervisor inside StateGraph;
StateGraph as a worker inside Supervisor via a tiny in-test
`graphAsAgent` adapter) AND that Supervisor-of-Supervisor composition
works (KC-1's "a Supervisor may be a worker of another Supervisor" is
runtime-verified, not just compile-time-asserted). A deterministic
example in `examples/08-supervisor/` — three demos (Basic, Budget,
Compose) — gives downstream readers a canonical setup; the smoke test
asserts stdout markers; the README documents the composition stack +
the MaxRounds-vs-Budget.MaxCalls distinction (Decision E). Pure stdlib;
no sister-repo import. CC-3.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. Core stays stdlib-only (CLAUDE.md Rule 1; KC-5). The
  example program lives under `examples/` — already established
  precedent at `examples/06-budget/` (Phase 35) and `examples/07-policy/`
  (Phase 36). Both are the closest siblings.
- This slice ships ONE new test file in `orchestrate/` and THREE new
  files under `examples/08-supervisor/`. NO production-code changes;
  Supervisor's behavior is fixed by 37-01.
- **The compose-direction tests are NOVEL.** No prior orchestrator
  shipped a runtime "Supervisor inside StateGraph" or "StateGraph inside
  Supervisor" test. But the test scaffolding (StateGraph builder,
  stubAgent fixtures, ctx-propagation idioms) is all established by
  37-01's predecessors. The reverse direction needs a ~20-LOC in-test
  `graphAsAgent` adapter that wraps a `*CompiledGraph[S]` as an
  `agents.Agent` (the adapter is test-only — not exported).
- **Example shape (Decision H — operator-default ratified):** 1 planner
  + 2 specialist workers × 2 rounds → aggregate. Plus a budget-exhaust
  demo and a compose-with-StateGraph demo. Mirrors
  `examples/06-budget/main.go` (215 LOC, 3 demos) and
  `examples/07-policy/main.go` (199 LOC, 3 demos). Target ~200-250 LOC.
- **Determinism rule (CLAUDE.md):** every demo MUST use ScriptedLLM
  (no network). The smoke test pipes stdout and asserts deterministic
  markers — same shape as `examples/06-budget/main_test.go` (67 LOC).
- **Composition stack documentation (KC-3 carry-forward).** The README
  documents that the canonical worker stack is
  `policy.Wrap(otelmodel.Wrap(provider))` — the example does NOT import
  `otelmodel` (KC-3 + Decision G from Phase 36 — README-only mention,
  no code dependency). The README is the place this is restated for
  v0.6.2 readers.
- **Decision E distinction (`MaxRounds` vs `Budget.MaxCalls`):** the
  README explains that these are SEPARATE caps that COEXIST. `MaxRounds`
  is the supervisor-loop's iteration cap (number of planner-emits-dispatch
  cycles); `Budget.MaxCalls` is the cross-agent spend cap (total LLM
  call attempts across planner + all workers). This distinction is
  load-bearing for users — a common misunderstanding is "MaxCalls=10
  means 10 rounds" (NO — 10 LLM calls total).
- **Q1/A4 graceful MaxRounds behavior** is also documented in the
  README. Hitting `MaxRounds` is NOT an error — Supervisor calls
  `BuildAggregate(results-so-far)` and returns a partial answer.
- **Naming convention for the example directory:** Phase 35 shipped
  `examples/06-budget/`, Phase 36 shipped `examples/07-policy/`. Phase
  37 ships `examples/08-supervisor/`. The numeric prefix matches the
  v0.6.x ordering (`06-budget` for v0.6.0, `07-policy` for v0.6.1,
  `08-supervisor` for v0.6.2). Verify by `ls examples/` — see the
  existing examples to confirm the numeric prefix progression.
- See `37-RESEARCH.md` §"Decision H" (example shape: 1 planner + 2
  workers + 2 rounds), §"Code Examples" Example D (the compose-direction
  test sketch), §"Slice Breakdown" row 37-03 (the compose + example
  scope).
- See `37-PATTERNS.md` §"orchestrate/supervisor_compose_test.go" + §
  "examples/08-supervisor/main.go" + §"examples/08-supervisor/main_test.go"
  + §"examples/08-supervisor/README.md" — every file has a verbatim
  template referenced.
</context>

<interfaces>
Key contracts the executor needs. Already verified by direct file read.

From `orchestrate/graph.go` (the substrate used by the compose tests):
```
type NodeFunc[S any] func(ctx context.Context, state S) (S, error)
func NewStateGraph[S any]() *StateGraph[S]
func (g *StateGraph[S]) AddNode(name string, fn NodeFunc[S]) *StateGraph[S]
func (g *StateGraph[S]) AddEdge(from, to string) *StateGraph[S]
func (g *StateGraph[S]) SetEntry(name string) *StateGraph[S]
func (g *StateGraph[S]) Compile() (*CompiledGraph[S], error)
func (c *CompiledGraph[S]) Run(ctx context.Context, initial S, opts ...RunOption) (S, error)
const NodeEnd = "__end__"
```

From `orchestrate/supervisor.go` (the just-shipped 37-01 surface):
```
func NewSupervisor(name string, opts SupervisorOptions) *Supervisor
type SupervisorOptions struct {
    Planner agents.Agent
    Workers map[string]agents.Agent
    MaxRounds int
    ParseDispatch DispatchParser
    BuildAggregate Aggregator
}
type Dispatch struct { WorkerName string; Input string; Metadata map[string]any }
type WorkerResult struct { Dispatch Dispatch; Result agents.Result }
func (s *Supervisor) Name() string
func (s *Supervisor) Run(ctx context.Context, input string) (agents.Result, error)
func (s *Supervisor) RunStream(ctx context.Context, input string) (<-chan agents.StepEvent, error)
var _ agents.Agent = (*Supervisor)(nil)
```

From `orchestrate/supervisor_test.go` (37-01 helpers — reachable in-package by `supervisor_compose_test.go`):
```
func parseDemoDispatch(plannerAnswer string) (*Dispatch, error)
func joinWorkerResults(results []WorkerResult) (string, error)
func validOpts() SupervisorOptions
```

From `examples/06-budget/main.go` + `examples/07-policy/main.go` (the
template — verified by `ls examples/` and `wc -l`):
- Package-level docstring with `Demo NN: <title>` header.
- `func main() { demoBasic(); fmt.Println(); demoBudget(); fmt.Println(); demoComposeX(); fmt.Println("OK") }`.
- Per-demo function ~30-60 LOC; uses `scriptedllm.New(...)` or
  `llm.NewScriptedLLM(...)` (verify which is canonical in this repo by
  checking `examples/06-budget/main.go`'s actual imports — `examples/scriptedllm/` exists per `ls`).
- `errors.Is` introspection for the budget demo's error printing.

From `examples/06-budget/main_test.go` (the smoke test template, 67 LOC):
```
package main

import (
    "io"
    "os"
    "strings"
    "testing"
)

func TestExample_RunsToCompletion(t *testing.T) {
    origStdout := os.Stdout
    r, w, _ := os.Pipe()
    os.Stdout = w
    done := make(chan string, 1)
    go func() { b, _ := io.ReadAll(r); done <- string(b) }()
    main()
    _ = w.Close()
    os.Stdout = origStdout
    out := <-done
    mustContain(t, out, "fragment1", "fragment2", "OK")
}
func mustContain(t *testing.T, out string, fragments ...string) {
    t.Helper()
    for _, f := range fragments {
        if !strings.Contains(out, f) {
            t.Errorf("missing fragment %q.\nFull output:\n%s", f, out)
        }
    }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write orchestrate/supervisor_compose_test.go — 3 compose tests + graphAsAgent adapter</name>
  <files>orchestrate/supervisor_compose_test.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/supervisor.go` (the 37-01 surface under test)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/supervisor_test.go` (37-01 sibling — reuse `parseDemoDispatch`, `joinWorkerResults`, `validOpts` in-package)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/graph.go` (entire 208 LOC — the substrate; builder pattern + Compile + Run)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/graph_test.go` (lines 19-42 LinearPath + 44-79 ConditionalBranching — chain builder shape; lines 266-305 ExampleStateGraph_loop)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/pipeline_test.go` (lines 13-45 — `stubAgent` shape; the in-test adapter mirrors this idiom)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-RESEARCH.md` §"Code Examples" Example D (the worked sketch for both compose directions; ~70 LOC of pseudo-code)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-PATTERNS.md` §"orchestrate/supervisor_compose_test.go" (the compose-outer test sketch + the compose-inner adapter sketch)
  </read_first>
  <behavior>
    - `TestSupervisor_InsideStateGraph`: outer StateGraph with `preprocess` → `supervise` → `postprocess` nodes; `supervise` calls `sup.Run(ctx, state.input)` and stores `res.Answer` on `state.supRes`; `postprocess` formats `state.final = "[" + state.supRes + "]"`. Final state has `state.final == "[final: <expected supervisor output>]"`. `err == nil`. Proves Supervisor can be a NodeFunc inside any StateGraph[S].
    - `TestStateGraph_InsideSupervisor`: a small CompiledGraph[wState] (single `transform` node that produces `out = "transformed: " + in`) wrapped via `&graphAsAgent{cg: cg, name: "graph-worker"}`. Register this adapter as a Supervisor worker; planner emits "dispatch to graph-worker: hi" once then "FINISH"; Supervisor calls `worker.Run(ctx, "hi")` which delegates to the CompiledGraph; the graph's output appears in `BuildAggregate`. `err == nil`. Proves a StateGraph can be a Supervisor worker via a tiny adapter.
    - `TestSupervisor_OfSupervisor`: outer Supervisor has inner Supervisor (which has its own planner + worker) registered as a worker named `"sub"`. Outer planner emits "dispatch to sub: nested" once then "FINISH". Outer Supervisor calls `sub.Run(ctx, "nested")`; inner Supervisor runs its own loop; returns; outer Supervisor's BuildAggregate sees the inner's final answer. `err == nil`. Assert the answer contains both the inner Supervisor's worker output AND wraps it via the outer's BuildAggregate. This is the runtime proof of KC-1's "composition" claim.
    - The `graphAsAgent` adapter is defined IN THIS TEST FILE (test-only, not exported). It is ~20 LOC, satisfies `agents.Agent` (Name/Run/RunStream), wraps a `*CompiledGraph[wState]` (with wState declared locally as a small `struct { in, out string }`), and forwards `Run(ctx, input)` by setting `state.in = input`, running the graph, and returning `agents.Result{Answer: state.out, Usage: agents.Usage{LLMCalls: 1}}`.
  </behavior>
  <action>
    Create `orchestrate/supervisor_compose_test.go` in
    `package orchestrate`. Imports: `context`, `errors`, `strings`,
    `testing`, plus `github.com/costa92/llm-agent` (identifier `agents`).

    Define at file top, before the tests:

    - **`type composeWState struct { in, out string }`** — the local
      state type for the CompiledGraph used inside the Supervisor.

    - **`type graphAsAgent struct { name string; cg *CompiledGraph[composeWState] }`**
      with these methods (the ~20-LOC adapter):
      ```
      func (g *graphAsAgent) Name() string { return g.name }
      func (g *graphAsAgent) Run(ctx context.Context, input string) (agents.Result, error) {
          final, err := g.cg.Run(ctx, composeWState{in: input})
          if err != nil { return agents.Result{}, err }
          return agents.Result{Answer: final.out, Usage: agents.Usage{LLMCalls: 1, Tokens: len(final.out)}}, nil
      }
      func (g *graphAsAgent) RunStream(_ context.Context, _ string) (<-chan agents.StepEvent, error) {
          return nil, errors.New("graphAsAgent: stream not implemented")
      }
      ```
      Compile-time sanity: `var _ agents.Agent = (*graphAsAgent)(nil)`
      inside the test file (proves the adapter satisfies the interface
      even though it's test-only).

    Tests in this order:

    **Test 1 — `TestSupervisor_InsideStateGraph`** (~60 LOC):
    - Build a happy-path Supervisor `sup` using stubAgents + `parseDemoDispatch` + `joinWorkerResults` (mirror the 37-01 happy-path setup). The planner emits one dispatch then FINISH; 1 worker; produces a known answer like `"final: <worker-output>"`.
    - Declare a local state type: `type outerState struct { input, supRes, final string }`.
    - Build the outer graph:
      ```
      g := NewStateGraph[outerState]()
      g.AddNode("preprocess", func(_ context.Context, s outerState) (outerState, error) {
          s.input = "preprocessed: " + s.input
          return s, nil
      })
      g.AddNode("supervise", func(ctx context.Context, s outerState) (outerState, error) {
          res, err := sup.Run(ctx, s.input)
          if err != nil { return s, err }
          s.supRes = res.Answer
          return s, nil
      })
      g.AddNode("postprocess", func(_ context.Context, s outerState) (outerState, error) {
          s.final = "[" + s.supRes + "]"
          return s, nil
      })
      g.SetEntry("preprocess").
          AddEdge("preprocess", "supervise").
          AddEdge("supervise", "postprocess").
          AddEdge("postprocess", NodeEnd)
      cg, err := g.Compile()
      // assert err == nil
      final, err := cg.Run(context.Background(), outerState{input: "hi"})
      // assert err == nil
      // assert strings.HasPrefix(final.final, "[final:")
      // assert strings.HasSuffix(final.final, "]")
      // assert strings.Contains(final.final, "preprocessed: hi") — the supervisor saw the preprocessed input
      ```

    **Test 2 — `TestStateGraph_InsideSupervisor`** (~60 LOC):
    - Build a small CompiledGraph[composeWState]:
      ```
      wg := NewStateGraph[composeWState]()
      wg.AddNode("transform", func(_ context.Context, s composeWState) (composeWState, error) {
          s.out = "transformed: " + s.in
          return s, nil
      })
      wg.SetEntry("transform").AddEdge("transform", NodeEnd)
      cg, err := wg.Compile()
      // assert err == nil
      ```
    - Wrap via the adapter: `graphWorker := &graphAsAgent{name: "graph-worker", cg: cg}`.
    - Build a stubAgent planner that emits "dispatch to graph-worker: hi" then "FINISH".
    - Build the Supervisor:
      ```
      sup := NewSupervisor("outer", SupervisorOptions{
          Planner: planner,
          Workers: map[string]agents.Agent{"graph-worker": graphWorker},
          MaxRounds: 5,
          ParseDispatch: parseDemoDispatch,
          BuildAggregate: joinWorkerResults,
      })
      res, err := sup.Run(context.Background(), "go")
      // assert err == nil
      // assert strings.Contains(res.Answer, "transformed: hi") — the graph's output came through
      // assert res.Usage.LLMCalls >= 2 — planner (2x) + graph worker (1x via adapter; the adapter reports LLMCalls: 1)
      ```

    **Test 3 — `TestSupervisor_OfSupervisor`** (~70 LOC):
    - Build the INNER Supervisor first:
      - Inner planner stubAgent emits "dispatch to inner-w: <input>" then "FINISH".
      - Inner worker stubAgent has `transform: func(s string) string { return "inner-w: " + s }`.
      - `inner := NewSupervisor("inner", SupervisorOptions{Planner: innerPlanner, Workers: map[string]agents.Agent{"inner-w": innerWorker}, MaxRounds: 3, ParseDispatch: parseDemoDispatch, BuildAggregate: joinWorkerResults})`.
    - Build the OUTER Supervisor with `inner` as a worker named "sub":
      - Outer planner stubAgent emits "dispatch to sub: nested-task" then "FINISH".
      - `outer := NewSupervisor("outer", SupervisorOptions{Planner: outerPlanner, Workers: map[string]agents.Agent{"sub": inner}, MaxRounds: 3, ParseDispatch: parseDemoDispatch, BuildAggregate: joinWorkerResults})`.
    - Call `outer.Run(context.Background(), "top-level")`.
    - Assert `err == nil`.
    - Assert `strings.Contains(res.Answer, "inner-w: ")` — the deepest worker's output reached the outer aggregator.
    - Assert `strings.HasPrefix(res.Answer, "final: ")` — wrapped by the outer `joinWorkerResults`.
    - Add an inline comment: `// Runtime proof of KC-1's "Supervisor satisfies agents.Agent so a Supervisor may be a worker of another Supervisor" composition claim.`

    DO NOT export `graphAsAgent` or `composeWState`. They are
    test-only. DO NOT redeclare `parseDemoDispatch`, `joinWorkerResults`,
    or `stubAgent` (in-package access from 37-01 + pipeline_test.go).
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./orchestrate/... && GOWORK=off GOCACHE=/tmp/go-build go test ./orchestrate/... -run 'TestSupervisor_InsideStateGraph|TestStateGraph_InsideSupervisor|TestSupervisor_OfSupervisor' -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `orchestrate/supervisor_compose_test.go` exists; `go vet` exits 0.
    - The 3 compose tests pass: `go test ./orchestrate/... -run 'TestSupervisor_InsideStateGraph|TestStateGraph_InsideSupervisor|TestSupervisor_OfSupervisor' -count=1` exits 0.
    - `go test -race ./orchestrate/... -count=1` exits 0 (race-clean across the full orchestrate suite including 37-01 + 37-02 + this slice).
    - `grep -c 'type graphAsAgent struct' orchestrate/supervisor_compose_test.go` returns `1`.
    - `grep -c 'var _ agents.Agent = (\*graphAsAgent)(nil)' orchestrate/supervisor_compose_test.go` returns `1` (adapter satisfies interface).
    - `grep -c 'NewSupervisor(' orchestrate/supervisor_compose_test.go` returns at least `4` (Tests 1, 2, 3: outer in Test 3, inner in Test 3, sup in Test 1, sup in Test 2).
    - `grep -c 'NewStateGraph\[' orchestrate/supervisor_compose_test.go` returns at least `2` (outer graph in Test 1, worker graph in Test 2).
    - No edits to validated public types: `git status --short -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go orchestrate/supervisor.go orchestrate/supervisor_test.go orchestrate/doc.go orchestrate/supervisor_budget_test.go agent_chatmodel.go budget/ policy/ | wc -l` returns `0`.
    - `go.mod` / `go.sum` unchanged: `git status --short -- go.mod go.sum | wc -l` returns `0`.
  </acceptance_criteria>
  <done>
    `orchestrate/supervisor_compose_test.go` ships the 3 compose tests + the ~20-LOC `graphAsAgent` adapter, proving KC-1's "facade works both directions" claim at runtime. The Supervisor-of-Supervisor test specifically proves the composition claim (a Supervisor can be a worker of another Supervisor). Race-clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write examples/08-supervisor/main.go — 3 deterministic demos</name>
  <files>examples/08-supervisor/main.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/06-budget/main.go` (entire 215 LOC — closest template; 3-demo structure + ScriptedLLM canonical mock + countingLLM helper)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/07-policy/main.go` (entire 199 LOC — sibling pattern; errors.As BlockedError introspection)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/supervisor.go` (37-01 surface — what the demo invokes)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/supervisor_test.go` (the `parseDemoDispatch` + `joinWorkerResults` shapes — the example needs equivalents; can either copy verbatim or define local equivalents — copy verbatim is simpler since the test funcs are unexported)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/simple.go` (NewSimpleAgent + SimpleOptions)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/llm/scriptedllm.go` (the mock; verify the canonical constructor name)
    - List the existing examples to confirm the directory layout convention: `ls /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/`
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-PATTERNS.md` §"examples/08-supervisor/main.go" (docstring header + imports + demo function shapes)
  </read_first>
  <action>
    Verify the directory does NOT already exist:
    `ls /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/08-supervisor 2>/dev/null || echo "ok-not-exists"` should show "ok-not-exists".

    Create `examples/08-supervisor/main.go` (this will create the
    parent dir if missing). Use `package main`.

    File structure (mirror `examples/06-budget/main.go`'s layout):

    1. **Package-level docstring** above `package main`:
       ```
       // Demo 08: Multi-agent coordination via orchestrate.Supervisor.
       //
       // Wires a planner-emits-Dispatch → worker-runs → planner-observes →
       // repeat loop on top of orchestrate.Supervisor (Phase 37, CC-3). The
       // loop substrate is StateGraph[supervisorState] per KC-1; budget +
       // policy compose underneath via the agent_chatmodel.generateFromPrompt
       // chokepoint (Phases 35/36).
       //
       //   - demoBasic       : 1 planner + 2 specialists × 2 rounds → aggregate.
       //   - demoBudget      : Supervisor under Budget{MaxCalls: 3} → the 4th
       //                       chokepoint call surfaces ErrCallsExceeded mid-run.
       //   - demoComposeWithStateGraph : Supervisor as a node in an outer
       //                       StateGraph[outerState] — proves KC-1's "facade
       //                       works both directions".
       //
       // The whole demo is deterministic — the canonical ScriptedLLM mock
       // (per CLAUDE.md) returns pre-recorded responses, no network is touched.
       //
       // Run:
       //
       //	cd examples && go run ./08-supervisor
       package main
       ```

    2. **Imports** (verify against `examples/06-budget/main.go:24-35` —
       use the same `scriptedllm` adapter package OR `llm.NewScriptedLLM`,
       whichever the sibling uses; the existing pattern is authoritative):
       Likely shape:
       ```
       import (
           "context"
           "errors"
           "fmt"

           agents "github.com/costa92/llm-agent"
           "github.com/costa92/llm-agent/budget"
           "github.com/costa92/llm-agent/llm"
           "github.com/costa92/llm-agent/orchestrate"
       )
       ```
       If `examples/06-budget/main.go` uses `examples/scriptedllm` package
       instead of `llm.NewScriptedLLM`, follow that pattern; if it uses
       `llm.NewScriptedLLM` directly, follow that. Inspect the sibling
       and match exactly.

    3. **`func main()`**:
       ```
       func main() {
           demoBasic()
           fmt.Println()
           demoBudget()
           fmt.Println()
           demoComposeWithStateGraph()
           fmt.Println("OK")
       }
       ```

    4. **`demoBasic()`** (~50 LOC): prints `--- Basic: 1 planner + 2 specialists × 2 rounds ---`.
       - Build 3 ScriptedLLMs: planner with responses `["dispatch to researcher: find facts about X", "dispatch to summarizer: condense the facts", "FINISH"]`; researcher with `["Facts: A, B, C."]`; summarizer with `["Summary: 3 facts."]`.
       - Build 3 SimpleAgents (`NewSimpleAgent`) with names "planner", "researcher", "summarizer".
       - Build Supervisor with `parseDispatch` + `joinWorkerResults` (local equivalents — define at file bottom or inline; copy the parsing/joining logic verbatim from 37-01's `supervisor_test.go` helpers).
       - Run with `context.Background(); fmt.Printf("answer: %s\n", res.Answer); fmt.Printf("usage : %+v\n", res.Usage)`.

    5. **`demoBudget()`** (~40 LOC): prints `--- Budget: Supervisor under Budget{MaxCalls: 3} ---`.
       - Wire a planner that emits 3+ dispatches (won't reach FINISH within the budget).
       - 1 worker with multiple responses.
       - Build `ctx, tracker := budget.WithBudget(context.Background(), budget.Budget{MaxCalls: 3})`.
       - Run Supervisor. On error, print introspection:
         `fmt.Printf("errors.Is(err, budget.ErrCallsExceeded) = %v\n", errors.Is(err, budget.ErrCallsExceeded))`
         `fmt.Printf("tracker snapshot: %+v\n", tracker.Snapshot())`
       - The first print MUST emit literally `errors.Is(err, budget.ErrCallsExceeded) = true` — the smoke test asserts on this exact fragment.

    6. **`demoComposeWithStateGraph()`** (~50 LOC): prints `--- Compose with StateGraph: Supervisor as a node ---`.
       - Wire a simple Supervisor (1 planner + 1 worker + 1 round).
       - Build an outer `orchestrate.NewStateGraph[outerState]()` with `preprocess` → `supervise` → `postprocess`; `supervise` calls `sup.Run(ctx, state.input)`.
       - Run. Print `final.final` and a one-line summary.

    7. **Helper at file bottom** — local equivalents of
       `parseDemoDispatch` and `joinWorkerResults` (copy verbatim from
       37-01's `supervisor_test.go`, name them `parseDispatch` and
       `aggregate`):
       ```
       func parseDispatch(plannerAnswer string) (*orchestrate.Dispatch, error) {
           // ... same as 37-01's parseDemoDispatch
       }
       func aggregate(results []orchestrate.WorkerResult) (string, error) {
           // ... same as 37-01's joinWorkerResults
       }
       ```
       (The test-helper file in `orchestrate/` is `*_test.go`, NOT
       importable. So this is verbatim duplication — acceptable per
       CLAUDE.md "trust existing patterns".)

    Target length: ~220-250 LOC (matches 06-budget at 215 LOC plus
    one extra demo for compose).

    DO NOT import `llm-agent-rag`, `llm-agent-otel`,
    `llm-agent-providers`, `llm-agent-customer-support`, or `policy`
    (the policy composition is documented in the README only, NOT in
    the example code — KC-3 carry-forward from Phase 36's Decision G;
    Phase 37 inherits that policy).

    DO NOT use real LLM provider adapters. DO NOT use `time.Sleep`. DO
    NOT touch the network.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./examples/08-supervisor/... && GOWORK=off GOCACHE=/tmp/go-build go run ./examples/08-supervisor
    </automated>
  </verify>
  <acceptance_criteria>
    - `examples/08-supervisor/main.go` exists; `go vet ./examples/08-supervisor/...` exits 0.
    - `go run ./examples/08-supervisor` exits 0 deterministically (no flakes; run twice and observe identical output).
    - `grep -c 'func main()' examples/08-supervisor/main.go` returns `1`.
    - `grep -c 'func demo' examples/08-supervisor/main.go` returns at least `3` (`demoBasic`, `demoBudget`, `demoComposeWithStateGraph`).
    - `grep -c 'orchestrate.NewSupervisor(' examples/08-supervisor/main.go` returns at least `3` (one per demo).
    - `grep -c 'budget.WithBudget(' examples/08-supervisor/main.go` returns at least `1` (the Budget demo).
    - `grep -c 'orchestrate.NewStateGraph\[' examples/08-supervisor/main.go` returns at least `1` (the Compose demo).
    - `grep -c 'fmt.Println("OK")' examples/08-supervisor/main.go` returns at least `1` (the smoke test asserts on this).
    - `grep -c '--- Basic:' examples/08-supervisor/main.go` returns at least `1` (smoke marker).
    - `grep -c '--- Budget' examples/08-supervisor/main.go` returns at least `1` (smoke marker).
    - `grep -c '--- Compose' examples/08-supervisor/main.go` returns at least `1` (smoke marker).
    - `grep -c 'errors.Is(err, budget.ErrCallsExceeded)' examples/08-supervisor/main.go` returns at least `1` (the introspection print).
    - No sister-repo / policy / otelmodel import: `grep -E '"github.com/costa92/llm-agent-(rag|otel|providers|customer-support)|github.com/costa92/llm-agent/policy' examples/08-supervisor/main.go | wc -l` returns `0`.
    - `go.mod` / `go.sum` unchanged: `git status --short -- go.mod go.sum | wc -l` returns `0`.
  </acceptance_criteria>
  <done>
    `examples/08-supervisor/main.go` is a deterministic stdlib-only example exercising the Supervisor surface — 3 demos covering Basic, Budget, and Compose. `go run ./examples/08-supervisor` exits 0 with reproducible output. Smoke markers (`--- Basic:`, `--- Budget`, `--- Compose`, `OK`, `errors.Is(err, budget.ErrCallsExceeded) = true`) appear in stdout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write examples/08-supervisor/main_test.go — stdout smoke test</name>
  <files>examples/08-supervisor/main_test.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/06-budget/main_test.go` (entire ~67 LOC — copy verbatim shape; sub markers)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/08-supervisor/main.go` (just-written from Task 2 — for the exact markers to assert)
  </read_first>
  <action>
    Create `examples/08-supervisor/main_test.go` (`package main`).

    Imports: `io`, `os`, `strings`, `testing`.

    Verbatim shape from `examples/06-budget/main_test.go` (the markers
    differ; the structure is identical):

    ```
    package main

    import (
        "io"
        "os"
        "strings"
        "testing"
    )

    func TestExample_RunsToCompletion(t *testing.T) {
        origStdout := os.Stdout
        r, w, err := os.Pipe()
        if err != nil { t.Fatalf("os.Pipe: %v", err) }
        os.Stdout = w
        done := make(chan string, 1)
        go func() { b, _ := io.ReadAll(r); done <- string(b) }()
        main()
        _ = w.Close()
        os.Stdout = origStdout
        out := <-done
        mustContain(t, out,
            "--- Basic:",
            "answer:",
            "--- Budget",
            "errors.Is(err, budget.ErrCallsExceeded) = true",
            "--- Compose",
            "OK",
        )
    }

    func mustContain(t *testing.T, out string, fragments ...string) {
        t.Helper()
        for _, f := range fragments {
            if !strings.Contains(out, f) {
                t.Errorf("missing fragment %q.\nFull output:\n%s", f, out)
            }
        }
    }
    ```

    The 6 asserted fragments are the deterministic markers Task 2's
    `main.go` emits. If Task 2's marker strings differ from the
    placeholders above, sync the fragments here to match (e.g., if Task
    2 prints `--- Budget: Supervisor under Budget{MaxCalls: 3} ---` the
    fragment `--- Budget` is a substring and matches).
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./examples/08-supervisor/... -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `examples/08-supervisor/main_test.go` exists; `go vet` exits 0.
    - `go test ./examples/08-supervisor/... -count=1` exits 0 — smoke test passes.
    - `grep -c 'os.Pipe()' examples/08-supervisor/main_test.go` returns `1` (the stdout capture idiom).
    - `grep -c 'mustContain(' examples/08-supervisor/main_test.go` returns at least `1` (the helper is called).
    - `grep -c '"OK"' examples/08-supervisor/main_test.go` returns at least `1` (terminal marker asserted).
    - `grep -c '"--- Basic:"' examples/08-supervisor/main_test.go` returns at least `1`.
    - `grep -c '"--- Budget"' examples/08-supervisor/main_test.go` returns at least `1`.
    - `grep -c '"--- Compose"' examples/08-supervisor/main_test.go` returns at least `1`.
    - `grep -c '"errors.Is(err, budget.ErrCallsExceeded) = true"' examples/08-supervisor/main_test.go` returns at least `1`.
  </acceptance_criteria>
  <done>
    `examples/08-supervisor/main_test.go` is a stdout-capturing smoke test that asserts 6 deterministic markers — proves the example runs to completion and produces the documented output without any non-determinism.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write examples/08-supervisor/README.md — ≤80-line canonical readme</name>
  <files>examples/08-supervisor/README.md</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/07-policy/README.md` (the just-shipped v0.6.1 sibling — voice + length target)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples/06-budget/README.md` (the v0.6.0 sibling)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-RESEARCH.md` §"Decision E" + §"Decision F" + §"Common Pitfalls" 4 (the user-facing distinctions this README must document)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-PATTERNS.md` §"examples/08-supervisor/README.md"
  </read_first>
  <action>
    Create `examples/08-supervisor/README.md`. Target ≤ 80 lines.
    Mirror the structure of `examples/07-policy/README.md`. Use plain
    English; no emoji.

    Sections (in order):

    1. **`# Supervisor — multi-agent coordination`** (H1).

    2. **Short intro paragraph (3-4 lines):** Names the Supervisor as a
       thin facade over `orchestrate.StateGraph[supervisorState]` per
       KC-1. Notes that workers are `agents.Agent` (so a Supervisor can
       supervise another Supervisor — composition). The example is
       deterministic (ScriptedLLM, no network).

    3. **`## Run`** section with the canonical invocation:
       ```
       cd examples
       go run ./08-supervisor
       ```

    4. **`## Demos`** section listing the 3 demos:
       - **demoBasic** — 1 planner + 2 specialists (researcher,
         summarizer) × 2 rounds → aggregate. The minimal shape.
       - **demoBudget** — Supervisor under `Budget{MaxCalls: 3}` on ctx.
         The 4th LLM call (planner OR worker — both charge the same
         budget via the Phase 35 chokepoint) surfaces
         `budget.ErrCallsExceeded`. Demonstrates that **rounds count
         against `Budget.Calls` INDIRECTLY** — each planner round + each
         worker call charges once.
       - **demoComposeWithStateGraph** — Supervisor as a NodeFunc inside
         an outer StateGraph[outerState] (preprocess → supervise →
         postprocess). Proves KC-1's "facade works both directions"
         claim.

    5. **`## MaxRounds vs Budget.MaxCalls`** section (Decision E
       distinction — load-bearing for users):
       - `MaxRounds`: the supervisor loop's iteration cap (number of
         planner-emits-dispatch cycles before graceful aggregation).
         Counted by Supervisor itself in the [plan] node's conditional
         edge.
       - `Budget.MaxCalls`: the cross-agent cap on total LLM call
         attempts (planner + all workers). Counted at the
         `generateFromPrompt` chokepoint (Phase 35) — Supervisor adds
         zero code; per-call charging is automatic.
       - The two **coexist**. Hitting `MaxRounds` is **graceful**
         (BuildAggregate over results-so-far per Q1/A4); hitting
         `Budget.MaxCalls` surfaces `budget.ErrCallsExceeded`. Common
         user pitfall: `MaxCalls=10` does NOT mean "at most 10 rounds"
         — it means "at most 10 LLM calls total across planner + all
         workers".

    6. **`## Composition stack (KC-3)`** section (~6 lines): The
       canonical worker stack is
       ```
       policy.Wrap(otelmodel.Wrap(provider))
       ```
       Outer-most `policy` denies; middle `otelmodel` observes;
       inner-most provider makes the call. Budget enforces UNDERNEATH
       this stack at the chokepoint (Phase 35). The example does NOT
       import `otelmodel` (the README documents the stack; the example
       itself is stdlib-only with ScriptedLLM — KC-3 Decision G
       carry-forward from Phase 36).

    7. **`## Graceful MaxRounds behavior`** section (~4 lines): When
       Supervisor hits its `MaxRounds` cap, the next planner round is
       **skipped** and the `[final]` node runs `BuildAggregate` over the
       results-so-far. The user gets a **partial answer** (best-effort
       aggregation), not an error. Mirrors `RoundRobinChat`'s
       `Stopped="max_turns"` graceful behavior.

    8. **`## See also`** section with links/references:
       - `orchestrate/supervisor.go` — the type.
       - `orchestrate/supervisor_test.go` — the 12 functional tests.
       - `orchestrate/supervisor_budget_test.go` — the budget+policy
         integration tests.
       - `orchestrate/supervisor_compose_test.go` — the compose-direction
         tests.
       - `examples/06-budget/` and `examples/07-policy/` — the v0.6.0
         and v0.6.1 sibling examples.
       - `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-RESEARCH.md`
         — the full design rationale (KC-1 facade, Decisions A-I, Q1-Q5).

    Total length target: 60-80 lines including blank lines and headings.

    DO NOT add emoji. DO NOT add badges. DO NOT add a license section
    (LICENSE is at repo root).
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && [ -f examples/08-supervisor/README.md ] && [ "$(wc -l < examples/08-supervisor/README.md)" -le 100 ] && grep -q '^# Supervisor' examples/08-supervisor/README.md && grep -q 'MaxRounds' examples/08-supervisor/README.md && grep -q 'Budget.MaxCalls' examples/08-supervisor/README.md && grep -q 'KC-1' examples/08-supervisor/README.md && grep -q 'KC-3' examples/08-supervisor/README.md && grep -q 'policy.Wrap' examples/08-supervisor/README.md && echo README-OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `examples/08-supervisor/README.md` exists.
    - `wc -l examples/08-supervisor/README.md` is at most `100` (target is ≤ 80; tolerance to 100 for line-wrap variance).
    - `grep -c '^# Supervisor' examples/08-supervisor/README.md` returns at least `1`.
    - `grep -c 'MaxRounds' examples/08-supervisor/README.md` returns at least `3` (the Decision E section names it multiple times).
    - `grep -c 'Budget.MaxCalls' examples/08-supervisor/README.md` returns at least `2` (Decision E section).
    - `grep -c 'KC-1' examples/08-supervisor/README.md` returns at least `1` (facade claim).
    - `grep -c 'KC-3' examples/08-supervisor/README.md` returns at least `1` (composition stack section).
    - `grep -c 'policy.Wrap' examples/08-supervisor/README.md` returns at least `1` (composition stack — README-only mention).
    - `grep -c 'otelmodel' examples/08-supervisor/README.md` returns at least `1` (composition stack mentions the sister repo by name).
    - `grep -c 'graceful' examples/08-supervisor/README.md` returns at least `1` (Q1/A4 graceful MaxRounds behavior section).
    - `grep -c 'stdlib' examples/08-supervisor/README.md` returns at least `1` (the example is stdlib-only).
  </acceptance_criteria>
  <done>
    `examples/08-supervisor/README.md` is the canonical reference for the demo — runtime invocation, per-demo description, the load-bearing MaxRounds-vs-Budget.MaxCalls distinction, the composition stack with the sister-repo `otelmodel`, the graceful MaxRounds-hit behavior. ≤80 lines. No emoji.
  </done>
</task>

</tasks>

<verify>
- vet + build clean repo-wide:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go build ./... && echo VET-OK`
- orchestrate tests green (incl. the new compose tests):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./orchestrate/... -count=1 && echo TEST-OK`
- race-clean on orchestrate:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1 && echo RACE-OK`
- repo-wide tests green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && echo REPO-TEST-OK`
- example runs deterministically:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go run ./examples/08-supervisor >/dev/null && echo EXAMPLE-RUN-OK`
- example smoke test:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./examples/08-supervisor/... -count=1 && echo SMOKE-OK`
- 3 compose tests pass:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./orchestrate/... -run 'TestSupervisor_InsideStateGraph|TestStateGraph_InsideSupervisor|TestSupervisor_OfSupervisor' -count=1 && echo COMPOSE-OK`
- README documents Decision E + KC-3:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q 'MaxRounds' examples/08-supervisor/README.md && grep -q 'Budget.MaxCalls' examples/08-supervisor/README.md && grep -q 'KC-3' examples/08-supervisor/README.md && grep -q 'policy.Wrap' examples/08-supervisor/README.md && echo README-OK`
- no sister-repo import in main.go or compose tests:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -E '"github.com/costa92/llm-agent-(rag|otel|providers|customer-support)' examples/08-supervisor/main.go orchestrate/supervisor_compose_test.go | wc -l` returns `0`
- KC-5 preserved (broad sweep):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go agent_chatmodel.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go orchestrate/supervisor.go orchestrate/supervisor_test.go orchestrate/supervisor_budget_test.go orchestrate/doc.go budget/ policy/ | wc -l` returns `0`
- go.mod / go.sum unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- go.mod go.sum | wc -l` returns `0`
</verify>

<acceptance>
- `orchestrate/supervisor_compose_test.go` exists with the 3 compose tests + in-test `graphAsAgent` adapter; race-clean.
- `examples/08-supervisor/main.go`, `examples/08-supervisor/main_test.go`, `examples/08-supervisor/README.md` all exist; `go run ./examples/08-supervisor` exits 0; `go test ./examples/08-supervisor/...` passes.
- KC-1's "facade works both directions" claim is runtime-verified — Supervisor as a NodeFunc inside StateGraph (Test 1); StateGraph as a Supervisor worker via the in-test adapter (Test 2); Supervisor-of-Supervisor composition (Test 3).
- The deterministic stdout markers (`--- Basic:`, `--- Budget`, `--- Compose`, `OK`, `errors.Is(err, budget.ErrCallsExceeded) = true`) are emitted and asserted.
- README documents the load-bearing distinctions: MaxRounds vs Budget.MaxCalls (Decision E), composition stack `policy.Wrap(otelmodel.Wrap(provider))` (KC-3 carry-forward), graceful MaxRounds-hit behavior (Q1/A4).
- No sister-repo import in the example code or compose tests.
- KC-5: no edit to validated public types; `go.mod` / `go.sum` unchanged.
- All `<verify>` commands pass.
</acceptance>

<success_criteria>
A downstream reader who clones this repo at `v0.6.2` can run
`cd examples && go run ./08-supervisor` and see a deterministic 3-demo
output. The README answers the two most common user questions ("what's
the difference between MaxRounds and Budget.MaxCalls?" and "how do I
add policy/observability around a worker?") definitively. The 3 compose
tests prove KC-1's facade claim at runtime — a Supervisor can be a
worker of another Supervisor (composition), a Supervisor can be a node
of any `StateGraph[S]` (facade-outward), and a `StateGraph[S]` can be
a Supervisor worker via a tiny adapter (facade-inward). With 37-03
shipped, only the exit gate (37-04) remains before `v0.6.2` is taggable.
</success_criteria>

<output>
Create `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-03-SUMMARY.md`
when done. Summary records: the 3 compose tests + outcomes; the
~20-LOC `graphAsAgent` adapter shape; the example's 3 demos + smoke
markers; the README's documented distinctions (Decision E, KC-3, Q1/A4);
race-clean status; KC-5 + stdlib-only audit pass.
</output>
</content>
</invoke>