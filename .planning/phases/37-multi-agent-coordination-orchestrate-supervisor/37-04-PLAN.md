---
phase: 37-multi-agent-coordination-orchestrate-supervisor
plan: 04
type: execute
wave: 4
repo: llm-agent (core)
depends_on: ["37-01", "37-02", "37-03"]
files_modified: []
autonomous: false
requirements: [CC-3]
must_haves:
  truths:
    - "phase 37 exit gate passes: go vet + go test + go test -race all green repo-wide"
    - "go list -deps ./orchestrate/... shows only stdlib + the existing in-repo dependencies — no third-party module added by Supervisor"
    - "go.mod and go.sum unchanged from pre-37 state — no new require, no go.sum file created"
    - "KC-5 holds: no edit to llm.ChatModel / agents.Agent / memory.Memory / orchestrate.NodeFunc[S] / agent_chatmodel.go's generateFromPrompt body or signature / orchestrate/graph.go / orchestrate/fanout.go / orchestrate/roundrobin.go / orchestrate/pipeline.go / orchestrate/roleplay.go / orchestrate/termination.go"
    - "no edit to budget/ or policy/ — Phase 37 inherits Phases 35/36 with zero code changes"
    - "no edit to any v0.6.1 paradigm file (simple.go / react.go / reflection.go / plan_solve.go / function_call.go)"
    - "examples/08-supervisor runs deterministically (no network, exits 0)"
    - "CHANGELOG entry recorded for v0.6.2 (the additive Supervisor type)"
    - "tag v0.6.2 from main is achievable (operator-gated push)"
  artifacts: []
  key_links: []
---

<objective>
Phase 37 exit gate. Verifies that the `orchestrate.Supervisor` surface
shipped by slices 37-01 through 37-03 is production-ready: stdlib-only,
race-clean, no edit to validated public types, no `go.mod`/`go.sum`
churn, every CC-3 requirement met. Records a CHANGELOG entry for
v0.6.2 (the third v1.2 patch tag). After the automated checks pass,
the operator tags `v0.6.2` from `main` — the third v1.2 patch tag
(`v0.6.1 → v0.6.2` per the 2026-05-21 retargeted versioning plan;
Phase 38 will cut the milestone-cap `v0.7.0`). CC-3.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. This slice does NOT modify Go source — it verifies the
  state produced by 37-01 / 37-02 / 37-03 and gates the tag. The
  exception: ONE additive line in `CHANGELOG.md` (the v0.6.2 entry).
- **Target tag: `v0.6.2`** (patch — additive `orchestrate.Supervisor`
  type; KC-5 preserved). Per the 2026-05-21 ROADMAP retarget, the v1.2
  milestone cap tag is `v0.7.0` (Phase 38), so Phase 37 lands on
  `v0.6.2` (patch). The tag is strict-additive: existing `v0.6.1`
  callers compile unchanged against `v0.6.2`.
- **CHANGELOG handling — Phase 37 SHIPS a CHANGELOG entry.** This is a
  departure from Phase 35's and Phase 36's convention (those did NOT
  touch CHANGELOG, deferring all entries to the milestone close at
  Phase 38). Rationale: by Phase 37 there are three additive surfaces
  (`budget`, `policy`, `orchestrate.Supervisor`) and the operator
  benefits from a single per-tag entry to make the tag self-documenting.
  EXCEPTION: if reading `CHANGELOG.md` reveals that v0.6.0 and v0.6.1
  do NOT have their own entries either (i.e., Phase 35/36 confirmed
  the "defer to milestone close" pattern), then this slice ALSO defers
  — and Task 2's CHANGELOG step is skipped. The operator-decision
  branch is recorded in Task 1's verdict table.
- **Operator-gated push.** Following the Phase 35/36 close convention,
  the actual `git tag` + `git push` commands are operator-run (this
  slice records the commands but does NOT execute the push). The
  human-verify checkpoint at the end of this slice is the gate.
- **Exit-gate checks** (lifted from 37-RESEARCH.md §"Slice Breakdown"
  row 37-04 and §"Validation Architecture" — mirror Phase 36's
  17-check audit, scoped to Phase 37's surface):
  1. `go vet ./...` — repo-wide, no warnings.
  2. `go test ./... -count=1` — repo-wide, all green.
  3. `go test -race ./orchestrate/... -count=1` — race-clean.
  4. `go list -deps ./orchestrate/...` — zero NEW third-party modules
     (the orchestrate package may already import the in-repo
     `pkg/fanout` or other internals — verify the set is unchanged
     from v0.6.1 by direct comparison via git).
  5. `git diff main -- go.mod go.sum` — empty (no churn).
  6. `git diff main -- llm/` — empty (no edits to validated public
     types).
  7. `git diff main -- agent.go agents.go simple.go react.go
     reflection.go plan_solve.go function_call.go memory/` — empty
     (no edits to agent-package files or paradigm files per KC-5).
  8. `git diff main -- agent_chatmodel.go` — empty (Phase 35 wired
     the budget chokepoint; Phase 37 does NOT re-touch it).
  9. `git diff main -- budget/ policy/` — empty (Phases 35/36 shipped
     their surfaces; Phase 37 inherits with zero changes).
  10. `git diff main -- orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go` — empty (existing orchestrate paradigms unchanged).
  11. `go run ./examples/08-supervisor` — deterministic 0-exit.
  12. `go list -m all` — module set unchanged (only
      `github.com/costa92/llm-agent-rag v1.0.1`).
- **Files touched across the phase** (verification target list — every
  file from 37-01 through 37-03):
  - `orchestrate/supervisor.go` (37-01)
  - `orchestrate/supervisor_test.go` (37-01)
  - `orchestrate/doc.go` (37-01 — additive paragraph only)
  - `orchestrate/supervisor_budget_test.go` (37-02)
  - `orchestrate/supervisor_compose_test.go` (37-03)
  - `examples/08-supervisor/main.go` (37-03)
  - `examples/08-supervisor/main_test.go` (37-03)
  - `examples/08-supervisor/README.md` (37-03)
  Plus phase-planning artifacts under
  `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/`
  (4 PLAN.md + 4 SUMMARY.md). Plus optionally `CHANGELOG.md` (if v0.6.0
  / v0.6.1 also have entries — see Task 1 verdict).
- **Tag protocol** (mirrors Phase 35/36 close):
  - Verify `main` is at the tip the operator wants to tag.
  - Verify all 4 slice SUMMARYs exist:
    `37-01-SUMMARY.md` ... `37-04-SUMMARY.md`.
  - Verify `git status` is clean (no uncommitted changes).
  - Operator runs: `git tag -a v0.6.2 -m "v0.6.2: orchestrate.Supervisor (CC-3)"`
    then `git push origin v0.6.2`.
- **STATE.md update is OUT of this slice's scope** — that's the
  operator's `/gsd-transition` slice. This slice's `<output>` produces
  an audit summary; STATE.md flipping happens elsewhere.
- See `37-RESEARCH.md` §"Slice Breakdown" row 37-04 (the exit-gate
  command list) + §"Project Constraints (from CLAUDE.md)" (the KC-5 /
  stdlib-only / no-replace constraint list to verify) + §"Phase
  Requirements → Test Map" (the 17-row matrix of CC-3 → test coverage).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated exit-gate checks</name>
  <files></files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-RESEARCH.md` §"Slice Breakdown" row 37-04 (the canonical command list)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-05-PLAN.md` (the Phase 36 exit-gate slice — same shape this slice mirrors)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/35-budget-and-cancellation-context/35-04-PLAN.md` (the Phase 35 exit-gate slice — the precedent)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/CLAUDE.md` (hard rules — verify each holds)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/CHANGELOG.md` (read once to determine if Phases 35/36 added entries — informs the CHANGELOG decision in Task 2)
  </read_first>
  <behavior>
    - Every command in the exit-gate list runs successfully and emits the expected zero/empty output.
    - The compile-time interface assertion `var _ agents.Agent = (*Supervisor)(nil)` from 37-01 still holds (verified implicitly by `go vet`).
    - `go test -race ./orchestrate/...` reveals no data races.
    - `go list -deps ./orchestrate/...` enumerates only the stdlib + the existing in-repo dependencies (no new sister-repo import).
    - `examples/08-supervisor` is the only NEW example dir (verify by `ls examples/` showing 06-budget, 07-policy, 08-supervisor as the v0.6.x triplet).
  </behavior>
  <action>
    Run each of the 12 exit-gate commands sequentially. Capture stdout
    + stderr. For each, record PASS / FAIL with the actual output line
    that justifies the verdict. Use the following exact commands:

    1. **Repo-wide vet:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
       PASS if exit code 0 and no warnings emitted.

    2. **Repo-wide test:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
       PASS if exit code 0 and per-package `ok` for every package.

    3. **Race-clean on orchestrate:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1`
       PASS if exit code 0 and no `DATA RACE` line in the output.

    4. **Stdlib-only on orchestrate's Supervisor surface — no new third-party module:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -f '{{join .Imports "\n"}}' ./orchestrate/ | sort -u | grep -v '^$' | grep -vE '^(context|errors|fmt|sort|strings|sync|sync/atomic|testing|time|github.com/costa92/llm-agent(/(llm|pkg/fanout|budget|policy))?)$'`
       PASS if the command produces zero lines of output. (The shipped
       `orchestrate/` package may import `github.com/costa92/llm-agent/pkg/fanout` per `orchestrate/fanout.go:8`; that's an existing internal import. No NEW external module added by Supervisor.)

    5. **go.mod / go.sum unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- go.mod go.sum | wc -l`
       PASS if output is `0`.

    6. **KC-5 — llm/ unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- llm/ | wc -l`
       PASS if output is `0`.

    7. **KC-5 — agent-package + paradigm files + memory/ unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ | wc -l`
       PASS if output is `0`.

    8. **KC-5 — Phase 35 chokepoint unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- agent_chatmodel.go | wc -l`
       PASS if output is `0`.

    9. **KC-5 — Phases 35/36 surfaces unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- budget/ policy/ | wc -l`
       PASS if output is `0`.

    10. **KC-5 — existing orchestrate paradigms unchanged:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go orchestrate/graph_test.go orchestrate/fanout_test.go orchestrate/roundrobin_test.go orchestrate/pipeline_test.go orchestrate/roleplay_test.go orchestrate/termination_test.go | wc -l`
        PASS if output is `0`.

    11. **Example runs deterministically:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go run ./examples/08-supervisor`
        PASS if exit code 0.

    12. **Module set unchanged:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -m all`
        PASS if output contains exactly the expected set: `github.com/costa92/llm-agent` (self) and `github.com/costa92/llm-agent-rag v1.0.1` (the rag back-edge). No new modules.

    Additionally run **shape checks** verifying the Phase 37 surface
    shipped:

    13. **Public surface check — every CC-3 required symbol exists in `orchestrate/supervisor.go`:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for s in 'func NewSupervisor(' 'type Supervisor struct' 'type SupervisorOptions struct' 'type Dispatch struct' 'type WorkerResult struct' 'type DispatchParser' 'type Aggregator' 'ErrSupervisorNilPlanner' 'ErrSupervisorNoWorkers' 'ErrSupervisorNilParseDispatch' 'ErrSupervisorNilBuildAggregate' 'ErrSupervisorUnknownWorker' 'ErrSupervisorMaxRounds' 'ErrSupervisorParseDispatch' 'var _ agents.Agent = (*Supervisor)(nil)'; do grep -q "$s" orchestrate/supervisor.go || { echo "MISSING $s"; exit 1; }; done && echo SURFACE-OK`
        PASS if echoes `SURFACE-OK`.

    14. **3-node state machine present in supervisor.go:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -cE 'AddNode\("(plan|dispatch|final)"' orchestrate/supervisor.go`
        PASS if output is exactly `3`.

    15. **doc.go cites Supervisor + KC-1:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q 'Supervisor' orchestrate/doc.go && grep -q 'KC-1' orchestrate/doc.go && echo DOC-OK`
        PASS if echoes `DOC-OK`.

    16. **All 3 SUMMARY files for the prior slices exist:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for f in 37-01-SUMMARY.md 37-02-SUMMARY.md 37-03-SUMMARY.md; do [ -f ".planning/phases/37-multi-agent-coordination-orchestrate-supervisor/$f" ] || { echo "MISSING $f"; exit 1; }; done && echo SUMMARIES-OK`
        PASS if echoes `SUMMARIES-OK`.

    17. **No sister-repo import in any Phase 37 file:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -rE '"github.com/costa92/llm-agent-(rag|otel|providers|customer-support)' orchestrate/supervisor.go orchestrate/supervisor_test.go orchestrate/supervisor_budget_test.go orchestrate/supervisor_compose_test.go examples/08-supervisor/main.go examples/08-supervisor/main_test.go | wc -l`
        PASS if output is `0`.

    18. **CHANGELOG decision branch:** Check whether v0.6.0 and v0.6.1
        have CHANGELOG entries. Run:
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && [ -f CHANGELOG.md ] && grep -E '^##.*v0\.6\.[01]' CHANGELOG.md | wc -l`
        - If output is `0`: Phases 35/36 deferred CHANGELOG to the
          milestone close. Phase 37 ALSO defers. Task 2 SKIPS the
          CHANGELOG step. Record `CHANGELOG-DEFERRED` in the verdict.
        - If output is `>= 1`: Phase 37 ALSO ships an entry. Task 2
          adds the v0.6.2 stanza. Record `CHANGELOG-INLINE` in the
          verdict.
        - If `CHANGELOG.md` does not exist: defer (no CHANGELOG to
          update). Record `CHANGELOG-NOFILE`.

    Record the verdict for each check in a structured table that the
    summary will commit. If ANY check (1-17) fails, do NOT proceed to
    Task 2 (CHANGELOG) or Task 3 (operator checkpoint); instead,
    summarize the failure and stop — the gate is non-passing, and the
    operator must run a `--gaps` slice to close the gap before the tag.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1 && GOWORK=off GOCACHE=/tmp/go-build go run ./examples/08-supervisor >/dev/null && [ "$(GOWORK=off go list -f '{{join .Imports "\n"}}' ./orchestrate/ | sort -u | grep -v '^$' | grep -vE '^(context|errors|fmt|sort|strings|sync|sync/atomic|testing|time|github.com/costa92/llm-agent(/(llm|pkg/fanout|budget|policy))?)$' | wc -l)" = "0" ] && [ "$(git diff main -- go.mod go.sum llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ agent_chatmodel.go budget/ policy/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go | wc -l)" = "0" ] && echo PHASE-37-EXIT-GATE-PASS
    </automated>
  </verify>
  <acceptance_criteria>
    - All 17 mandatory exit-gate checks pass (12 automated + 5 shape).
    - Check 18 (CHANGELOG decision) records one of `CHANGELOG-DEFERRED` / `CHANGELOG-INLINE` / `CHANGELOG-NOFILE`.
    - The combined automated command exits 0 and emits `PHASE-37-EXIT-GATE-PASS`.
    - The verdict table includes a row per check with PASS / FAIL + actual output (or the CHANGELOG decision branch).
    - If any check 1-17 fails, this task records the failure with reproducer commands and STOPS — Task 2 and Task 3 are not entered until the gate is green.
  </acceptance_criteria>
  <done>
    Every exit-gate constraint is verified green. The combined command emits `PHASE-37-EXIT-GATE-PASS`. The CHANGELOG decision branch is recorded. The verdict table is ready for the slice summary commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG entry for v0.6.2 (only if CHANGELOG-INLINE branch from Task 1)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/CHANGELOG.md` (the file to potentially edit)
    - Verdict from Task 1 — specifically the CHANGELOG decision branch (CHANGELOG-DEFERRED / CHANGELOG-INLINE / CHANGELOG-NOFILE)
  </read_first>
  <action>
    Conditional execution:

    - **If Task 1's verdict was `CHANGELOG-DEFERRED` or `CHANGELOG-NOFILE`:** This task is a no-op. Skip CHANGELOG editing entirely; Phase 38 (milestone close) will add a single rollup entry. Record "Skipped per Task 1 verdict" in this task's done note. Do NOT create a CHANGELOG.md file if one does not already exist.

    - **If Task 1's verdict was `CHANGELOG-INLINE`:** Add a v0.6.2 entry to `CHANGELOG.md`. Insert AFTER the existing v0.6.1 stanza (so the file stays in reverse chronological order with newest at top). Use this exact entry shape (matching the style of the existing v0.6.0 / v0.6.1 stanzas — adjust slightly if they use a different format):

      ```
      ## [v0.6.2] - 2026-MM-DD

      ### Added
      - `orchestrate.Supervisor` — iterative planner ↔ worker coordinator
        shipped as a thin facade over `StateGraph[supervisorState]` per KC-1.
        Workers are `agents.Agent`; the Supervisor itself satisfies `agents.Agent`
        so a Supervisor may be a worker of another Supervisor (composition).
        Inherits CC-1 budget via ctx propagation through the `generateFromPrompt`
        chokepoint (Phase 35) and CC-2 policy per-worker via `policy.Wrap`
        decoration on the worker's underlying model (Phase 36). Stdlib-only;
        no edit to validated public types (KC-5).
      - `examples/08-supervisor/` — deterministic 3-demo example covering
        the basic shape, budget exhaustion, and StateGraph composition.

      Phase 37 of v1.2 milestone (CC-3). See
      `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/`
      for full design rationale.
      ```

      Use today's date in the `2026-MM-DD` slot (operator's local date
      at the time of the slice).

      DO NOT modify any prior version's stanza. DO NOT change CHANGELOG
      formatting conventions if they differ from the above template —
      match the existing v0.6.1 stanza style exactly.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && ( [ ! -f CHANGELOG.md ] || grep -E '^##.*v0\.6\.2|orchestrate.Supervisor' CHANGELOG.md >/dev/null ) && echo CHANGELOG-TASK-OK
    </automated>
  </verify>
  <acceptance_criteria>
    - If Task 1 branch was `CHANGELOG-DEFERRED` or `CHANGELOG-NOFILE`: `CHANGELOG.md` is UNCHANGED from `main`. Verify via `git diff main -- CHANGELOG.md | wc -l == 0`.
    - If Task 1 branch was `CHANGELOG-INLINE`: `CHANGELOG.md` contains the new v0.6.2 stanza — verified by `grep -c 'v0.6.2' CHANGELOG.md >= 1` AND `grep -c 'orchestrate.Supervisor' CHANGELOG.md >= 1` AND `grep -c 'KC-1' CHANGELOG.md >= 1`. Earlier stanzas (v0.6.0, v0.6.1) are untouched — `git diff main -- CHANGELOG.md` shows ONLY additive lines (no deletions).
  </acceptance_criteria>
  <done>
    CHANGELOG handled per Task 1's decision branch. Either: (a) the file is unchanged (deferred to Phase 38 milestone-close rollup), or (b) the v0.6.2 stanza is added without modifying any prior version's entries.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator gate — tag and push v0.6.2 from main</name>
  <what-built>
    Phase 37 shipped the `orchestrate.Supervisor` surface across slices
    37-01 through 37-03:
    - 37-01 — Supervisor skeleton + types + 3-node StateGraph[supervisorState]
      facade: `NewSupervisor`, `SupervisorOptions{Planner, Workers, MaxRounds,
      ParseDispatch, BuildAggregate}`, `Dispatch{WorkerName, Input, Metadata}`,
      `WorkerResult{Dispatch, Result}`, `DispatchParser`, `Aggregator`,
      7 sentinels, `var _ agents.Agent = (*Supervisor)(nil)` compile-time
      assertion (KC-1 composability proof), locally-reimplemented runStream
      helper (Decision G option b). 12 functional tests. Q1-Q5 ratified
      inline. Race-clean.
    - 37-02 — Budget + policy compose integration tests: 6 tests proving
      Supervisor inherits CC-1 (budget propagation via the Phase 35 chokepoint
      — workers receive ctx UNCHANGED per Pitfall 4) and CC-2 (per-worker
      policy.Wrap decoration — Supervisor is policy-agnostic). Budget-beats-
      policy invariant holds at the Supervisor level. ZERO new code in
      production — Supervisor inherits both layers.
    - 37-03 — Compose tests + example: 3 compose tests (Supervisor inside
      StateGraph; StateGraph as worker via ~20-LOC graphAsAgent adapter;
      Supervisor-of-Supervisor composition). Deterministic example at
      `examples/08-supervisor/` with 3 demos (Basic, Budget, Compose);
      smoke test asserts stdout markers; README documents MaxRounds-vs-
      Budget.MaxCalls (Decision E), composition stack `policy.Wrap(otelmodel.Wrap(provider))`
      (KC-3 carry-forward), and graceful MaxRounds behavior (Q1/A4).

    Task 1 of this slice confirmed the exit gate is green:
    - `go vet ./... && go test ./... && go test -race ./orchestrate/...`
      all PASS.
    - `go.mod` and `go.sum` unchanged (no new third-party module).
    - KC-5 holds: no edit to `llm/`, agent-package files, paradigm
      files, `agent_chatmodel.go`, `memory/`, `budget/`, `policy/`, or
      any existing `orchestrate/` paradigm file. Only the additive
      Supervisor surface + the additive doc.go paragraph + the test
      files + the example dir.
    - `go list -deps ./orchestrate/...` shows zero NEW third-party modules.
    - `go run ./examples/08-supervisor` exits 0 deterministically.

    Task 2 handled CHANGELOG per the recorded decision branch (either
    added the v0.6.2 stanza inline OR deferred to Phase 38 per the
    Phase 35/36 precedent).

    The next concrete action is operator-only: tag `v0.6.2` from `main`
    and push.
  </what-built>
  <how-to-verify>
    1. Verify the local `main` branch is at the expected commit:
       ```sh
       cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent
       git status
       git log --oneline -5
       ```
       Expect: clean working tree; the most recent commit is the 37-04
       summary commit; no uncommitted changes.

    2. Verify the tag does NOT already exist:
       ```sh
       git tag --list 'v0.6.2'
       ```
       Expect: empty output. If non-empty, the tag was already cut —
       skip the push and just confirm visibility.

    3. (Optional final smoke) Re-run the combined exit-gate command:
       ```sh
       cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && \
         GOWORK=off GOCACHE=/tmp/go-build go vet ./... && \
         GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && \
         GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1 && \
         GOWORK=off GOCACHE=/tmp/go-build go run ./examples/08-supervisor >/dev/null
       ```
       Expect: every command exits 0.

    4. Create the annotated tag from `main`:
       ```sh
       git tag -a v0.6.2 -m "v0.6.2: orchestrate.Supervisor (CC-3)

       Iterative planner ↔ worker coordinator shipped as a thin facade over
       StateGraph[supervisorState] per KC-1. Workers are agents.Agent; the
       Supervisor itself satisfies agents.Agent (composition). Inherits CC-1
       budget via the Phase 35 chokepoint and CC-2 policy per-worker via
       the Phase 36 decorator. Stdlib-only. No edit to validated public types
       (KC-5).

       Phase 37 (v1.2). See .planning/v1.2-ROADMAP.md."
       ```
       (HEREDOC commit message acceptable; the wording is illustrative —
       operator picks the final tone.)

    5. Push the tag:
       ```sh
       git push origin v0.6.2
       ```

    6. Verify the tag is visible on the remote:
       ```sh
       git ls-remote --tags origin | grep 'v0.6.2'
       ```
       Expect: one line showing the SHA + `refs/tags/v0.6.2`.

    7. Confirm the post-tag invariants hold:
       - `go.mod` not edited (no `require` for v0.6.2 anywhere — this is
         the project's own tag, not a consumed dep).
       - The umbrella dep-currency CI gate (KE-6) will NOT fire yet
         against sister repos — they still pin v0.5.1; the gate fires
         only when sisters bump core. That's a future v1.3 ecosystem-
         alignment task per STATE.md, NOT v1.2's work.

    If anything is wrong (working tree dirty, exit gate not green, tag
    already exists with mismatched SHA), describe the issue instead
    of typing "approved" so the planner can produce a `--gaps` slice
    to remediate.
  </how-to-verify>
  <resume-signal>
    Type `approved` (or `approved-but-skipped-push` if the tag was
    already pushed by a prior operator session) to mark Phase 37
    complete. Or describe the issue blocking the tag — that will
    feed a `--gaps` slice to the planner.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the audit summary + the next-step pointer for /gsd-transition</name>
  <files></files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/STATE.md` (the current STATE.md — to know what the next phase is and what STATE will say post-37)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/v1.2-ROADMAP.md` (Phase 38 — the next phase: milestone close)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md` (the Phase 36 exit-gate slice's SUMMARY — the template this slice's SUMMARY follows)
  </read_first>
  <action>
    Write `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md`
    capturing:
    1. Phase 37 outcome: PASS / FAIL (PASS expected per Task 1's gate).
    2. The verdict table from Task 1 (18 rows: 12 automated + 5 shape + 1 CHANGELOG decision).
    3. Surface summary: the public types and functions that
       `v0.6.1 → v0.6.2` adds (the `orchestrate.Supervisor` surface).
    4. Stack of decisions ratified (Q1-Q5 — operator-default values
       chosen in 37-01-PLAN):
       - Q1: MaxRounds hit is GRACEFUL (BuildAggregate over results-so-far)
       - Q2: runStream helper is REIMPLEMENTED LOCALLY in supervisor.go
       - Q3: NO OnStep field on SupervisorOptions (caller uses RunStream)
       - Q4: Dispatch.Metadata IS included as map[string]any
       - Q5: NO SupervisorAgent alias (the compile-time assertion is the proof)
    5. Known carry-forwards intentionally deferred (per
       37-RESEARCH.md §"Deferred Ideas"):
       - StructuredPlanner interface (typed-return planner) — v1.3 candidate.
       - Parallel worker dispatch (planner emits N dispatches per round) — v1.3 candidate.
       - Streaming workers (worker.RunStream from inside Supervisor) — v1.3 candidate.
       - Memory scoping per worker (KC-2 deferred to v1.3).
       - Refund-on-policy-block budget semantics (carry-forward from Phase 36).
       - Cross-Supervisor distributed coordination (`comm/` integration) — out of v1.2 scope.
       - OTel emission of supervisor rounds — sister-repo concern; v1.3 ecosystem alignment.
    6. KC-5 audit: list of files NOT edited (`llm/`, paradigm files,
       `agent_chatmodel.go`, `memory/`, `budget/`, `policy/`, `orchestrate/graph.go`,
       `orchestrate/fanout.go`, `orchestrate/roundrobin.go`, `orchestrate/pipeline.go`,
       `orchestrate/roleplay.go`, `orchestrate/termination.go`, `go.mod`, `go.sum`).
    7. Tag status: `v0.6.2` cut from `main` (operator-confirmed in Task
       3) — OR — `pending push` if the operator did not push during
       the checkpoint.
    8. CHANGELOG status: per the verdict branch from Task 1 (deferred
       to Phase 38 OR inline with the v0.6.2 stanza).
    9. v1.2 progress: 3/4 phases shipped (Phase 35 in v0.6.0; Phase 36
       in v0.6.1; Phase 37 in v0.6.2). Phase 38 (milestone close, cut
       v0.7.0) is the final phase.
    10. Next step pointer: operator runs `/gsd-transition` to flip
        STATE.md from "Phase 36 shipped, Phase 37 next" to "Phase 37
        shipped (v0.6.2), Phase 38 next" — then `/gsd-plan-phase 38`
        to plan the v1.2 milestone close (CC-4, cap tag v0.7.0).
    11. Lessons learned (specific to Phase 37):
        - KC-1's "thin facade over StateGraph[S]" hypothesis held in
          practice — the 3-node state machine ([plan] / [dispatch] /
          [final]) with a conditional edge on [plan] is the minimal
          correct shape. The substrate provides cancellation +
          MaxSteps for free.
        - The "zero new code for budget integration" claim from
          37-RESEARCH Decision E held — 37-02's TestSupervisor_BudgetPropagatesToWorker
          passes WITHOUT Supervisor charging anything; the chokepoint
          handles all per-call charging via ctx propagation.
        - The compose-direction tests (37-03) proved KC-1's facade
          claim at runtime. The `graphAsAgent` ~20-LOC adapter is the
          ONE bit of glue needed to wrap a CompiledGraph[S] as an
          agents.Agent — small enough to define in-test, not worth
          exporting in v1.2 (v1.3 may export if a second use case emerges).
        - Q2 (reimplement runStream locally) was the right call — the
          27-line helper duplicate keeps the agents.* exported surface
          narrow. KC-5-friendlier than exporting `agents.RunStreamFromBlocking`.
        - The MaxRounds*3+4 slack formula (Pitfall 2) was exercised by
          TestSupervisor_MaxRoundsExceeded — no premature ErrGraphMaxSteps
          surfacing. The err-translator from ErrGraphMaxSteps to
          ErrSupervisorMaxRounds is a defense-in-depth path that
          shouldn't fire in practice with the +4 slack.

    Commit message: `docs(37): record phase 37 audit + v0.6.2 close`.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && [ -f .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md ] && grep -q 'PASS' .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md && grep -q 'v0.6.2' .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md && grep -q 'gsd-transition' .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md && grep -q '/gsd-plan-phase 38' .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md && grep -q 'Q1' .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md && grep -q 'Q5' .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md && echo SUMMARY-OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md` exists.
    - File contains the verdict table from Task 1 (18 rows).
    - File names the public surface added in `v0.6.2`.
    - File records Q1-Q5 ratifications + the carry-forward list.
    - File records the tag status (cut OR pending push).
    - File records the CHANGELOG status (deferred OR inline).
    - File records v1.2 progress as 3/4 phases shipped.
    - File names the next concrete action (`/gsd-transition` then `/gsd-plan-phase 38`).
    - Commit message follows the project convention (e.g., `docs(37): record phase 37 audit + v0.6.2 close`).
  </acceptance_criteria>
  <done>
    The phase audit summary is committed and ready for the operator's `/gsd-transition` slice — which will flip STATE.md to "Phase 37 shipped, Phase 38 next" and update the v1.2 progress counter from 2/4 → 3/4.
  </done>
</task>

</tasks>

<verify>
- exit gate green (one-shot combined command):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1 && echo PHASE-37-GATE-OK`
- example runs deterministically:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go run ./examples/08-supervisor >/dev/null && echo EXAMPLE-RUN-OK`
- core stdlib-only (no new third-party from Supervisor):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -f '{{join .Imports "\n"}}' ./orchestrate/ | sort -u | grep -v '^$' | grep -vE '^(context|errors|fmt|sort|strings|sync|sync/atomic|testing|time|github.com/costa92/llm-agent(/(llm|pkg/fanout|budget|policy))?)$' | wc -l` returns `0`
- KC-5 unchanged (the broad sweep):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go agent_chatmodel.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go orchestrate/graph_test.go orchestrate/fanout_test.go orchestrate/roundrobin_test.go orchestrate/pipeline_test.go orchestrate/roleplay_test.go orchestrate/termination_test.go budget/ policy/ go.mod go.sum | wc -l` returns `0`
- module set unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -m all | wc -l` returns `2` (self + rag)
- 3 prior slice summaries exist (37-01..37-03):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && ls .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-0{1,2,3}-SUMMARY.md | wc -l` returns `3`
- 37-04 SUMMARY records the audit:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && [ -f .planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md ] && echo SUMMARY-EXISTS`
- no sister-repo import anywhere in Phase 37 code:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -rE '"github.com/costa92/llm-agent-(rag|otel|providers|customer-support)' orchestrate/supervisor.go orchestrate/supervisor_test.go orchestrate/supervisor_budget_test.go orchestrate/supervisor_compose_test.go examples/08-supervisor/main.go examples/08-supervisor/main_test.go | wc -l` returns `0`
- tag visibility (operator-gated; verify post-push):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git tag --list 'v0.6.2' || true`
  Expect `v0.6.2` after operator runs Task 3.
</verify>

<acceptance>
- Task 1's 17 mandatory exit-gate checks all PASS; check 18 records the CHANGELOG decision branch.
- Task 2 handles CHANGELOG per the recorded branch (no-op if deferred; add v0.6.2 stanza if inline).
- Task 3's operator checkpoint receives `approved` (or `approved-but-skipped-push` if the tag was already cut in a prior session).
- Task 4's `37-04-SUMMARY.md` is committed and records the verdict table, Q1-Q5 ratifications, KC-5 audit, tag status, CHANGELOG status, v1.2 progress (3/4 shipped), and the next concrete action pointer.
- `v0.6.2` is the post-phase tag visible on the remote (operator-confirmed).
- Phase 37 closes the third v1.2 phase (CC-3 satisfied); the v1.2 progress counter is ready to advance from 2/4 → 3/4 on the operator's next `/gsd-transition`.
- All `<verify>` commands pass.
</acceptance>

<success_criteria>
The phase-37 audit gate confirms:
- CC-3 fully satisfied — `orchestrate.Supervisor` exists as a typed
  `StateGraph[supervisorState]` facade with the locked `SupervisorOptions`
  field set; workers are `agents.Agent`; Supervisor satisfies
  `agents.Agent` (KC-1 composition); compose-with-StateGraph tests
  prove the facade works both directions; budget + policy compose
  via Phases 35/36 chokepoint + decorator with ZERO new code in
  Supervisor; stdlib-only preserved.
- The core repo's `go.mod` / `go.sum` are byte-identical to their
  pre-Phase-37 state — KC-5 honored verbatim.
- The 5 v1.2 agent paradigms (Simple/ReAct/Reflection/PlanSolve/
  FunctionCall) compose with Supervisor uniformly — any of them can
  be a planner OR a worker (verified via the SimpleAgent-driven 37-02
  integration tests).
- `v0.6.2` is taggable from `main`. After the operator pushes,
  downstreams can pin `github.com/costa92/llm-agent v0.6.2` and
  consume the `orchestrate.Supervisor` type additively (no breaking
  changes from v0.6.1).
- Phase 38 (CC-4, v1.2 milestone close → `v0.7.0` cap tag) is
  unblocked — all three feature phases (35/36/37) shipped; the
  milestone audit can begin.
</success_criteria>

<output>
Create `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-04-SUMMARY.md`
recording the audit verdict + tag status + CHANGELOG status + next-step
pointer. This is authored in Task 4 of this slice.
</output>
</content>
</invoke>