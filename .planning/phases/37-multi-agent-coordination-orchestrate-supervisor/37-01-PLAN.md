---
phase: 37-multi-agent-coordination-orchestrate-supervisor
plan: 01
type: execute
wave: 1
repo: llm-agent (core)
depends_on: []
files_modified:
  - orchestrate/supervisor.go
  - orchestrate/supervisor_test.go
  - orchestrate/doc.go
autonomous: true
requirements: [CC-3]
must_haves:
  truths:
    - "orchestrate.Supervisor satisfies agents.Agent — `var _ agents.Agent = (*Supervisor)(nil)` compiles (KC-1 composability proof)"
    - "Supervisor wraps a 3-node StateGraph[supervisorState] facade ([plan] → [dispatch] → [plan] loop with conditional edge; [final] terminal) — KC-1 verified"
    - "Open Questions Q1-Q5 from 37-RESEARCH.md ratified inline in supervisor.go package doc + reflected in test names"
    - "go vet ./orchestrate/... and go test -race ./orchestrate/... -count=1 exit 0 — no race in concurrent Run on independent Supervisors"
    - "Stdlib-only: go list -f '{{join .Imports \"\\n\"}}' ./orchestrate/ shows no third-party module added by Supervisor; only context/errors/fmt/strings + existing github.com/costa92/llm-agent + (test) sync/atomic"
    - "KC-5 preserved: git diff main -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go agent_chatmodel.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go shows zero edits (doc.go gets one additive paragraph only)"
    - "go.mod and go.sum unchanged"
  artifacts:
    - path: "orchestrate/supervisor.go"
      provides: "Supervisor struct, NewSupervisor, SupervisorOptions{Planner, Workers, MaxRounds, ParseDispatch, BuildAggregate}, Dispatch{WorkerName, Input, Metadata}, WorkerResult{Dispatch, Result}, DispatchParser + Aggregator type aliases, 7 sentinels (ErrSupervisorNilPlanner / ErrSupervisorNoWorkers / ErrSupervisorNilParseDispatch / ErrSupervisorNilBuildAggregate / ErrSupervisorUnknownWorker / ErrSupervisorMaxRounds / ErrSupervisorParseDispatch), unexported supervisorState struct, compileGraph() 3-node builder, Name/Run/RunStream methods, `var _ agents.Agent = (*Supervisor)(nil)` compile-time assertion, locally-reimplemented runStream helper (Decision G option b)"
    - path: "orchestrate/supervisor_test.go"
      provides: "TestSupervisor_HappyPath (2 rounds × 2 workers → aggregate), TestSupervisor_Validation (table — 5 error cases including zero MaxRounds), TestSupervisor_MaxRoundsExceeded (graceful BuildAggregate-over-results-so-far per Decision F + A4), TestSupervisor_UnknownWorker, TestSupervisor_ParseDispatchError, TestSupervisor_ParseDispatchFinish (nil, nil terminates), TestSupervisor_CtxCancel (pre-cancelled ctx), TestSupervisor_RunStreamEmitsRoundEvents (StepAction/StepObservation pairs per round + terminal StepFinal), TestSupervisor_UsageRollup (planner + workers summed), TestSupervisor_NameDefault, TestSupervisor_SatisfiesAgentInterface (documentary; compile-time assertion is the real check), TestSupervisor_ConcurrentRunIsRaceClean (2 goroutines × 10 Runs on independent Supervisors, sync/atomic counters)"
    - path: "orchestrate/doc.go"
      provides: "additive paragraph entry for Supervisor in the EN + 中文 paradigm list and one new line in the 'Choosing a paradigm' section — doc-comment-only edit (KC-5: no Go symbol added or edited in doc.go)"
  key_links:
    - from: "orchestrate/supervisor.go::compileGraph"
      to: "orchestrate.NewStateGraph[supervisorState] + AddNode + AddConditionalEdge + AddEdge + Compile"
      via: "3-node typed StateGraph facade per KC-1"
      pattern: "g.AddNode\\(\"plan\".*g.AddNode\\(\"dispatch\".*g.AddNode\\(\"final\""
    - from: "orchestrate/supervisor.go::Supervisor.Run"
      to: "CompiledGraph[supervisorState].Run(ctx, initial, WithMaxSteps(MaxRounds*3+4))"
      via: "MaxSteps slack derived per Pitfall 2 + A3 of 37-RESEARCH"
      pattern: "WithMaxSteps\\(.*MaxRounds.*\\*\\s*3.*\\+"
    - from: "orchestrate/supervisor.go::Supervisor.Run"
      to: "agents.Result{Answer, Trace, Usage} returned to caller"
      via: "final.finalAnswer + buildTrace(final) + aggregateUsage(final)"
      pattern: "agents\\.Result\\{"
    - from: "orchestrate/supervisor.go::*Supervisor (type)"
      to: "agents.Agent interface (Name/Run/RunStream)"
      via: "compile-time assertion proves composability"
      pattern: "var _ agents\\.Agent = \\(\\*Supervisor\\)\\(nil\\)"
---

<objective>
Ship the `orchestrate.Supervisor` skeleton — the typed `StateGraph[supervisorState]`
facade per KC-1, with the full public surface verbatim from CC-3
(`NewSupervisor`, `SupervisorOptions{Planner, Workers, MaxRounds,
ParseDispatch, BuildAggregate}`, `Dispatch`, `WorkerResult`,
`DispatchParser`, `Aggregator`, the 7-sentinel family), the unexported
3-node state machine (`[plan]`/`[dispatch]`/`[final]` with a conditional
edge on `[plan]`), the compile-time `agents.Agent` assertion (KC-1
composability proof), a locally-reimplemented `RunStream` helper per
Decision G option (b), and the 12-test functional sweep covering happy
path, validation, MaxRounds graceful terminate, unknown worker,
ParseDispatch finish/error, ctx cancel, RunStream events, usage rollup,
and race-clean concurrent Run. Ratify Open Questions Q1-Q5 inline in the
package doc. Pure stdlib. CC-3.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. Core stays stdlib-only (CLAUDE.md Rule 1; KC-5). `go.mod`
  today has exactly one require — `github.com/costa92/llm-agent-rag v1.0.1`
  for the RAG facade — and Supervisor adds zero new requires. No `go.sum`
  by design.
- Repo layout is **flat** — files at the repo root declare `package agents`
  (see `agent.go:5`). The `orchestrate/` subdirectory is the StateGraph
  substrate; `supervisor.go` is a new file inside that existing package.
- This slice ships the **skeleton + types + state machine + functional
  tests only**. Budget/policy compose integration tests land in 37-02
  (need this skeleton). Compose-with-StateGraph tests + example land in
  37-03 (need this skeleton). Exit gate is 37-04.
- **The Supervisor's shape mirrors `orchestrate/roundrobin.go` in
  vocabulary (Name/Run shape, sentinel family, error wrapping idioms)
  and `orchestrate/graph.go` in substrate (StateGraph builder + Compile
  + Run + WithMaxSteps).** Read both files in full before writing
  `supervisor.go`. The 8-wrapper analog `otelmodel.go` from Phase 36
  does NOT apply — Supervisor wraps `agents.Agent`, not `llm.ChatModel`.
- **The 5 SupervisorOptions field names are LOCKED by KC-1 / CC-3
  verbatim:** `Planner agents.Agent`, `Workers map[string]agents.Agent`,
  `MaxRounds int`, `ParseDispatch DispatchParser`, `BuildAggregate Aggregator`.
  Do NOT rename. Do NOT add additional required fields. (`Metadata` is
  optional on `Dispatch` — kept per Q4 / Decision B.)
- **Q1-Q5 ratification (operator-default values for the 5 open
  questions from 37-RESEARCH.md §"Open Questions"):**
  - **Q1 — MaxRounds hit is GRACEFUL.** The conditional edge on `[plan]`
    routes to `[final]` when `st.round >= opts.MaxRounds` AFTER the
    just-completed dispatch is appended to `state.results`. `BuildAggregate`
    is called with whatever results were accumulated. NOT an error.
    Mirrors `RoundRobinChat.Stopped="max_turns"` (`roundrobin.go:90`).
    The sentinel `ErrSupervisorMaxRounds` is reserved for the
    err-translator that wraps `ErrGraphMaxSteps` (the inner
    StateGraph cap fired due to off-by-one or unexpected node count —
    that path SHOULD be unreachable when `WithMaxSteps(MaxRounds*3+4)`
    is set correctly).
  - **Q2 — `runStreamFromBlocking` is REIMPLEMENTED LOCALLY** in
    `supervisor.go` (Decision G option b). The 27-line helper from
    `agent.go:88-125` is NOT exported. Local copy avoids widening the
    `agents.` exported surface (KC-5 friendlier).
  - **Q3 — `OnStep` field is OMITTED** from `SupervisorOptions` in this
    slice. Callers observe via `RunStream`. If 37-02 integration tests
    surface a real need, plan ratifies the addition then (strict
    additive, KC-5-friendly).
  - **Q4 — `Dispatch.Metadata` IS included** as `map[string]any`.
    Zero-LOC cost; future seam for planner-to-worker hints. Documented
    as "transparent to Supervisor; planner-defined contract".
  - **Q5 — NO `SupervisorAgent` alias.** Stick with `Supervisor`. The
    compile-time `var _ agents.Agent = (*Supervisor)(nil)` is the
    definitive answer.
- **Locked surface (CC-3 verbatim + 37-RESEARCH Standard Stack):**
  - `func NewSupervisor(name string, opts SupervisorOptions) *Supervisor`
  - `(*Supervisor).Name() string` — defaults to `"supervisor"` when
    `name == ""`
  - `(*Supervisor).Run(ctx context.Context, input string) (agents.Result, error)`
  - `(*Supervisor).RunStream(ctx context.Context, input string) (<-chan agents.StepEvent, error)`
  - `type SupervisorOptions struct { Planner agents.Agent; Workers map[string]agents.Agent; MaxRounds int; ParseDispatch DispatchParser; BuildAggregate Aggregator }`
  - `type Dispatch struct { WorkerName string; Input string; Metadata map[string]any }`
  - `type WorkerResult struct { Dispatch Dispatch; Result agents.Result }`
  - `type DispatchParser func(plannerAnswer string) (*Dispatch, error)`
    — `(*Dispatch, nil)` continue; `(nil, nil)` terminate cleanly;
    `(nil, err)` surface as `ErrSupervisorParseDispatch` wrapping err
    (Pitfall 3).
  - `type Aggregator func(results []WorkerResult) (string, error)`
  - Sentinels: `ErrSupervisorNilPlanner`, `ErrSupervisorNoWorkers`,
    `ErrSupervisorNilParseDispatch`, `ErrSupervisorNilBuildAggregate`,
    `ErrSupervisorUnknownWorker`, `ErrSupervisorMaxRounds`,
    `ErrSupervisorParseDispatch`. Mirror `orchestrate/fanout.go:253-260`.
- **MaxSteps slack formula (Pitfall 2 + A3):** `WithMaxSteps(opts.MaxRounds * 3 + 4)`.
  Each round consumes 2 graph steps (`[plan]` + `[dispatch]`); the
  terminating round adds `[plan]` + `[final]`; +1 slack. The substrate's
  `defaultMaxSteps = 100` (`graph.go:148`) is too low for `MaxRounds > 32`.
- **Imports allowed in `orchestrate/supervisor.go`:** `context`,
  `errors`, `fmt`, `strings` (for trace assembly + planner prompt
  building) — plus the root `github.com/costa92/llm-agent` package
  (imported as `agents` per the project convention — see
  `orchestrate/roundrobin.go:9`). NO `sync` in this file (Supervisor is
  per-Run, no shared state). Tests may import `sync/atomic` for race
  counters.
- See `37-RESEARCH.md` §"Standard Stack", §"Architecture Patterns"
  (Pattern 1: facade; Pattern 2: 3 nodes; Pattern 3: trace assembly),
  §"Decision A" (supervisorState shape), §"Decision B" (Dispatch
  shape), §"Decision C" (WorkerResult shape), §"Decision D" (Planner =
  agents.Agent), §"Decision E" (no per-round budget charge),
  §"Decision F" (BuildAggregate once at FINAL), §"Decision G" (RunStream
  via local helper), §"Common Pitfalls" 1-5, §"Slice Breakdown" row 37-01.
- See `37-PATTERNS.md` rows for `orchestrate/supervisor.go`,
  `orchestrate/supervisor_test.go`, `orchestrate/doc.go` — every imports
  pattern, sentinel block, builder chain, and test fixture has a
  verbatim source listed.
</context>

<interfaces>
Key contracts the executor needs. Already verified by direct file read.

From `orchestrate/graph.go` (the substrate Supervisor's facade builds on):
```
const (
    NodeStart = "__start__"
    NodeEnd   = "__end__"
)
type NodeFunc[S any] func(ctx context.Context, state S) (S, error)
type ConditionFunc[S any] func(state S) string
func NewStateGraph[S any]() *StateGraph[S]
func (g *StateGraph[S]) AddNode(name string, fn NodeFunc[S]) *StateGraph[S]
func (g *StateGraph[S]) AddEdge(from, to string) *StateGraph[S]
func (g *StateGraph[S]) AddConditionalEdge(from string, fn ConditionFunc[S]) *StateGraph[S]
func (g *StateGraph[S]) SetEntry(name string) *StateGraph[S]
func (g *StateGraph[S]) Compile() (*CompiledGraph[S], error)
type RunOption func(*runOptions)
func WithMaxSteps(n int) RunOption
func (c *CompiledGraph[S]) Run(ctx context.Context, initial S, opts ...RunOption) (S, error)
var ErrGraphMaxSteps = errors.New("...")
const defaultMaxSteps = 100  // (unexported; the basis for the slack calculation)
```

From `agent.go` (the interface Supervisor satisfies + the helper to mirror locally):
```
type Agent interface {
    Name() string
    Run(ctx context.Context, input string) (Result, error)
    RunStream(ctx context.Context, input string) (<-chan StepEvent, error)
}
type Result struct { Answer string; Trace []Step; Usage Usage }
type Usage struct { LLMCalls int; Tokens int }
type Step struct { Kind StepKind; Content string; Tool string; Args string; Result string }
type StepKind string
const (
    StepThought StepKind = "thought"
    StepAction StepKind = "action"
    StepObservation StepKind = "observation"
    StepReflection StepKind = "reflection"
    StepPlan StepKind = "plan"
    StepFinal StepKind = "final"
)
type StepEvent struct { Step Step; Done bool; Final *Result; Err error }
// runStreamFromBlocking is package-private — reimplement locally (Decision G option b):
// func runStreamFromBlocking(ctx, runFn func(ctx, onStep func(Step)) (Result, error)) (<-chan StepEvent, error)
// Helper spawns a goroutine; pipes Step→StepEvent into a 16-buffer channel; emits {Done, Final|Err} on exit.
```

From `orchestrate/fanout.go` (sentinel + Usage rollup precedent):
```
var (
    ErrPlannerNil = errors.New("orchestrate: fanout: planner is required")
    ErrWorkersEmpty = errors.New("orchestrate: fanout: workers are required")
    // ... 7 more
)
func addUsage(a, b agents.Usage) agents.Usage {
    return agents.Usage{LLMCalls: a.LLMCalls + b.LLMCalls, Tokens: a.Tokens + b.Tokens}
}
```
`addUsage` is already exported within `orchestrate/` (in-package) and
Supervisor reuses it directly (no redeclaration).

From `orchestrate/pipeline_test.go:13-45` (in-package test fixture
Supervisor's tests reuse directly):
```
type stubAgent struct {
    name      string
    transform func(input string) string
    err       error
    llmCalls  int
}
func (a *stubAgent) Name() string { return a.name }
func (a *stubAgent) Run(_ context.Context, input string) (agents.Result, error) { /* ... */ }
func (a *stubAgent) RunStream(_ context.Context, _ string) (<-chan agents.StepEvent, error) {
    return nil, errors.New("stubAgent: stream not implemented")
}
```

From `orchestrate/roundrobin.go:1-10` (the imports template):
```
package orchestrate
import (
    "context"
    "errors"
    "fmt"
    "strings"

    "github.com/costa92/llm-agent"
)
```
Note: the root module imports as identifier `agents` (the package
declaration at `agent.go:5` is `package agents`); the module path is
`github.com/costa92/llm-agent`. Code references the interface as
`agents.Agent`.

From `agent_chatmodel.go` (the chokepoint Supervisor inherits — read
for context, NOT edited):
```
func generateFromPrompt(ctx context.Context, model llm.ChatModel, systemPrompt, prompt string) (llm.Response, error) {
    // pre-charge budget.Calls; if denied, return early without calling provider
    // call provider
    // post-charge budget.Tokens; if denied, return both resp AND error
}
```
Supervisor passes ctx UNCHANGED to workers; each worker's underlying
paradigm calls `generateFromPrompt(ctx, ...)`, charging the budget
tracker automatically. Supervisor adds zero budget code.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write orchestrate/supervisor.go — types + state machine + RunStream helper</name>
  <files>orchestrate/supervisor.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/roundrobin.go` (entire 110 LOC — closest iterative-coordinator analog; imports + Name + Run shape)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/graph.go` (entire 208 LOC — the substrate; especially lines 36-43 builder, 67-87 AddConditionalEdge, 140-208 Run + MaxSteps)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/fanout.go` (lines 14-44 vocabulary; 67-77 Name; 184-191 worker map lookup; 246-260 sentinel family + addUsage)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/agent.go` (lines 12-21 agents.Agent interface; 28-33 StepEvent; 58-76 Step + StepKind; 88-125 runStreamFromBlocking — copy this 27-line helper into supervisor.go per Q2/Decision G option b)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/agent_chatmodel.go` (entire 71 LOC — proves ctx → budget propagation; Supervisor adds zero budget code, just passes ctx unchanged)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-RESEARCH.md` §"Standard Stack" + §"Architecture Patterns" (Pattern 1, Pattern 2, Pattern 3) + §"Decision A" (supervisorState) + §"Decision B" (Dispatch) + §"Decision C" (WorkerResult) + §"Decision D" (Planner = agents.Agent) + §"Decision E" (no per-round budget) + §"Decision F" (BuildAggregate once) + §"Decision G" (RunStream local) + §"Common Pitfalls" 1-5
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-PATTERNS.md` §"orchestrate/supervisor.go" (the imports / constructor / sentinel / builder / lookup / usage / error-wrap / MaxSteps / err-translation / RunStream excerpts)
  </read_first>
  <behavior>
    - `(*Supervisor).Name()` returns the configured name; returns `"supervisor"` when `name == ""`.
    - `(*Supervisor).Run(ctx, input)` validates opts FIRST: nil Planner → `ErrSupervisorNilPlanner`; empty Workers map → `ErrSupervisorNoWorkers`; nil ParseDispatch → `ErrSupervisorNilParseDispatch`; nil BuildAggregate → `ErrSupervisorNilBuildAggregate`; non-positive MaxRounds → `fmt.Errorf("orchestrate: supervisor: MaxRounds must be positive (got %d)", opts.MaxRounds)`.
    - On valid opts: builds the 3-node CompiledGraph[supervisorState], runs it with `WithMaxSteps(opts.MaxRounds*3 + 4)`, translates `ErrGraphMaxSteps` → `ErrSupervisorMaxRounds`, returns `agents.Result{Answer: final.finalAnswer, Trace: buildTrace(final), Usage: aggregateUsage(final)}` on success.
    - Planner is invoked at the `[plan]` node with a prompt assembled from the user input + the accumulated `WorkerResult`s so far (a deterministic stringification — see `buildPlannerPrompt` in 37-PATTERNS).
    - `ParseDispatch(plannerAnswer)` returns `(*Dispatch, nil)` → store on state, route to `[dispatch]`; `(nil, nil)` → route to `[final]` (clean finish before MaxRounds); `(nil, err)` → return `fmt.Errorf("supervisor %q: parse dispatch round %d: %w", s.name, st.round, ErrSupervisorParseDispatch)` wrapping `err`.
    - `[dispatch]` looks up `opts.Workers[d.WorkerName]`; on missing/nil → `fmt.Errorf("%w: %q (round %d)", ErrSupervisorUnknownWorker, d.WorkerName, st.round)`.
    - `[dispatch]` calls `worker.Run(ctx, d.Input)` with ctx UNCHANGED (Pitfall 4 hard rule); on error → wraps with supervisor name + worker name + round; on success → appends `WorkerResult{Dispatch: *d, Result: res}` to `state.results`.
    - Conditional edge from `[plan]`: returns `"final"` if `st.dispatch == nil` (clean finish) OR `st.round >= s.opts.MaxRounds` (graceful cap-hit per Q1/A4); else `"dispatch"`.
    - `[final]` calls `s.opts.BuildAggregate(state.results)` ONCE (Decision F); on err → wraps; on success → `state.finalAnswer = ans`.
    - `RunStream` emits one `StepAction` + one `StepObservation` per completed round, then a terminal `StepFinal` with `state.finalAnswer`. On error, terminal event is `{Done: true, Err: err}`. Channel is closed after the terminal event. Buffer size = 16 (matches `agent.go:102`).
    - `var _ agents.Agent = (*Supervisor)(nil)` compiles — KC-1 composability proof.
    - No mutation of any field on `*Supervisor` after construction — `Run` builds the graph fresh per call; safe for concurrent invocation on the same instance.
    - Workers receive ctx UNCHANGED — no `context.WithoutCancel`, no `context.Background()`, no detached children (Pitfall 4 hard rule).
  </behavior>
  <action>
    Create `orchestrate/supervisor.go` with `package orchestrate`.
    Imports: `context`, `errors`, `fmt`, `strings`, plus
    `github.com/costa92/llm-agent` (identifier `agents`) — verbatim
    shape from `orchestrate/roundrobin.go:1-10`. No `sync` import. No
    `time` import.

    Define in this exact order (the order is load-bearing for godoc
    readability — types before functions; constructors before methods;
    state machine helpers before public surface):

    (1) **Package-doc comment block** above `package orchestrate`
    (godoc convention). The doc comment is NOT in this file (the
    package's primary doc.go gets the additive edit in Task 3); this
    file leads with a *type-doc* comment on `Supervisor` instead.

    (2) **`type Supervisor struct { name string; opts SupervisorOptions }`**
    with a doc comment naming KC-1 ("Supervisor is a thin facade over
    StateGraph[supervisorState] per KC-1"), Q1 (MaxRounds graceful),
    Q5 (no agent suffix in name), and the composition note ("satisfies
    agents.Agent so a Supervisor may be a worker of another
    Supervisor").

    (3) **`type SupervisorOptions struct`** with the 5 KC-1-locked
    fields: `Planner agents.Agent`, `Workers map[string]agents.Agent`,
    `MaxRounds int`, `ParseDispatch DispatchParser`, `BuildAggregate Aggregator`.
    Per-field doc comments. The `Workers` doc comment names Pitfall 1:
    "Lookup is case-sensitive; convention is lowercase names. ParseDispatch
    is the normalization seam if needed."

    (4) **`type Dispatch struct { WorkerName string; Input string; Metadata map[string]any }`**
    — Q4 ratified Metadata included. Doc comment: "Metadata is opaque
    to Supervisor; planner-to-worker hints. Zero-LOC cost; documented
    as a future extension seam."

    (5) **`type WorkerResult struct { Dispatch Dispatch; Result agents.Result }`**
    — Decision C verbatim.

    (6) **`type DispatchParser func(plannerAnswer string) (*Dispatch, error)`**
    — Pitfall 3 ratified contract in doc comment: "(*Dispatch, nil)
    continue; (nil, nil) terminate cleanly; (nil, err) fail with
    ErrSupervisorParseDispatch wrapping err."

    (7) **`type Aggregator func(results []WorkerResult) (string, error)`**
    — Decision F verbatim: "Called ONCE at the [final] node, NOT per
    round."

    (8) **Sentinel block** mirroring `orchestrate/fanout.go:253-260`:
    `var ( ErrSupervisorNilPlanner = errors.New("orchestrate: supervisor requires non-nil planner"); ErrSupervisorNoWorkers = errors.New("orchestrate: supervisor requires at least one worker"); ErrSupervisorNilParseDispatch = errors.New("orchestrate: supervisor requires non-nil ParseDispatch"); ErrSupervisorNilBuildAggregate = errors.New("orchestrate: supervisor requires non-nil BuildAggregate"); ErrSupervisorUnknownWorker = errors.New("orchestrate: supervisor dispatch references unknown worker"); ErrSupervisorMaxRounds = errors.New("orchestrate: supervisor max rounds exceeded"); ErrSupervisorParseDispatch = errors.New("orchestrate: supervisor parse dispatch failed") )`. All 7 sentinels. The `ErrSupervisorMaxRounds` doc names that it is reserved for the `ErrGraphMaxSteps` translation path (should be unreachable in practice with the `+4` slack).

    (9) **`type supervisorState struct`** (unexported) with the 7 fields
    from 37-RESEARCH Decision A: `input string`, `round int`,
    `lastPlannerAnswer string`, `dispatch *Dispatch`, `results []WorkerResult`,
    `plannerUsage agents.Usage`, `finalAnswer string`. Per-field doc.

    (10) **`func NewSupervisor(name string, opts SupervisorOptions) *Supervisor { return &Supervisor{name: name, opts: opts} }`**
    — verbatim shape from `orchestrate/fanout.go:67-77`. No validation
    here; validation happens in `Run` (per `fanout.go` precedent).

    (11) **`func (s *Supervisor) Name() string { if s.name == "" { return "supervisor" } ; return s.name }`**
    — `roundrobin.go:52-57` shape.

    (12) **`func (s *Supervisor) validate() error`** — internal; returns
    one of the 5 validation sentinels (or `fmt.Errorf` for MaxRounds <= 0)
    in the order listed in <behavior> above.

    (13) **`func (s *Supervisor) compileGraph() (*CompiledGraph[supervisorState], error)`** —
    builds the 3-node graph per 37-PATTERNS:
    ```
    g := NewStateGraph[supervisorState]()
    g.AddNode("plan", s.planNode)
    g.AddNode("dispatch", s.dispatchNode)
    g.AddNode("final", s.finalNode)
    g.SetEntry("plan").
        AddConditionalEdge("plan", s.routeFromPlan).
        AddEdge("dispatch", "plan").
        AddEdge("final", NodeEnd)
    return g.Compile()
    ```
    Use `NodeEnd` (already declared in `graph.go:12`). Method values
    bind `s` automatically.

    (14) **`func (s *Supervisor) planNode(ctx context.Context, st supervisorState) (supervisorState, error)`** —
    increments `st.round`; calls `s.opts.Planner.Run(ctx, s.buildPlannerPrompt(st))`;
    on error → `fmt.Errorf("supervisor %q: planner round %d: %w", s.name, st.round, err)`;
    on success → set `st.lastPlannerAnswer = res.Answer`, accumulate
    `st.plannerUsage` via `addUsage` (already in package); call
    `s.opts.ParseDispatch(res.Answer)`; on parser err →
    `fmt.Errorf("supervisor %q: parse dispatch round %d: %w", s.name, st.round, fmt.Errorf("%w: %v", ErrSupervisorParseDispatch, perr))`
    (wraps both the sentinel AND the underlying err so callers can
    `errors.Is(err, ErrSupervisorParseDispatch)`); on parser success
    (including `(nil, nil)` finish) → set `st.dispatch = d` (which may
    be nil — that is the finish signal), return st.

    (15) **`func (s *Supervisor) dispatchNode(ctx context.Context, st supervisorState) (supervisorState, error)`** —
    look up `s.opts.Workers[st.dispatch.WorkerName]` (use the cached
    `st.dispatch` set by planNode; the conditional edge guarantees this
    is non-nil when dispatchNode runs); if not found or nil →
    `fmt.Errorf("%w: %q (round %d)", ErrSupervisorUnknownWorker, st.dispatch.WorkerName, st.round)`;
    call `worker.Run(ctx, st.dispatch.Input)` with ctx UNCHANGED; on
    error → `fmt.Errorf("supervisor %q: worker %q round %d: %w", s.name, st.dispatch.WorkerName, st.round, err)`;
    on success → append `WorkerResult{Dispatch: *st.dispatch, Result: res}`
    to `st.results`. Return st.

    (16) **`func (s *Supervisor) finalNode(_ context.Context, st supervisorState) (supervisorState, error)`** —
    calls `ans, err := s.opts.BuildAggregate(st.results)`; on err →
    `fmt.Errorf("supervisor %q: aggregate: %w", s.name, err)`; on success →
    `st.finalAnswer = ans`. Return st.

    (17) **`func (s *Supervisor) routeFromPlan(st supervisorState) string`** —
    `if st.dispatch == nil { return "final" }; if st.round >= s.opts.MaxRounds { return "final" }; return "dispatch"`.
    Order matters: nil-dispatch (clean finish) wins over cap-hit so a
    final planner round that emits `(nil, nil)` terminates cleanly
    without spuriously triggering the cap-hit path.

    (18) **`func (s *Supervisor) buildPlannerPrompt(st supervisorState) string`** —
    deterministic stringification using `strings.Builder`. Format
    (this exact shape is documented and asserted by tests):
    ```
    Task: <st.input>

    Round <st.round> of up to <s.opts.MaxRounds>.
    [Prior results — N items:]
      [1] dispatched <wr.Dispatch.WorkerName>(<wr.Dispatch.Input>) → <wr.Result.Answer>
      ...
    Decide the next dispatch or finish.
    ```
    For round 1 (no prior results) omit the "[Prior results ...]" block.

    (19) **`func (s *Supervisor) translateErr(err error) error`** —
    `if errors.Is(err, ErrGraphMaxSteps) { return fmt.Errorf("%w: rounds=%d", ErrSupervisorMaxRounds, s.opts.MaxRounds) } ; return err`.
    Pitfall 2.

    (20) **`func (s *Supervisor) buildTrace(st supervisorState) []agents.Step`** —
    Pattern 3 of 37-RESEARCH: emit `StepAction{Tool: wr.Dispatch.WorkerName, Args: wr.Dispatch.Input}` then `StepObservation{Result: wr.Result.Answer}` for each WorkerResult; if `st.lastPlannerAnswer != ""`, emit one trailing `StepThought{Content: "planner: " + st.lastPlannerAnswer}`; finally `StepFinal{Content: st.finalAnswer}`. Preallocate `make([]agents.Step, 0, len(st.results)*2+2)`.

    (21) **`func (s *Supervisor) aggregateUsage(st supervisorState) agents.Usage`** —
    starts from `st.plannerUsage`; iterates `st.results`; uses
    `addUsage` from `orchestrate/fanout.go:246-251` (already in
    package — DO NOT redeclare). Returns the rolled-up `agents.Usage`.

    (22) **`func (s *Supervisor) Run(ctx context.Context, input string) (agents.Result, error)`** —
    public entry. Validates via `s.validate()`. If valid, compiles the
    graph via `s.compileGraph()`. Calls `cg.Run(ctx, supervisorState{input: input}, WithMaxSteps(s.opts.MaxRounds*3+4))`.
    Translates error via `s.translateErr(err)`. Returns
    `agents.Result{Answer: final.finalAnswer, Trace: s.buildTrace(final), Usage: s.aggregateUsage(final)}`
    on success.

    (23) **`func (s *Supervisor) runInternal(ctx context.Context, input string, onStep func(agents.Step)) (agents.Result, error)`** —
    the variant `RunStream` calls. Identical to `Run` except: after
    each `WorkerResult` is appended to `st.results` in `dispatchNode`,
    `onStep(StepAction{...})` + `onStep(StepObservation{...})` are
    invoked. Implementation: wrap `dispatchNode` in a closure that
    captures `onStep` and emits the two steps after the worker call
    succeeds; emit `onStep(StepFinal{Content: final.finalAnswer})`
    before returning. Build a SEPARATE graph for this method (or pass
    onStep via the state — pick whichever is cleaner; the closure-over-
    onStep approach is simpler and matches `agent.go:88-125` shape).

    (24) **`func (s *Supervisor) RunStream(ctx context.Context, input string) (<-chan agents.StepEvent, error)`** —
    Decision G option (b) local reimplementation of
    `agent.go:88-125 runStreamFromBlocking`. Verbatim shape:
    `ch := make(chan agents.StepEvent, 16); go func() { defer close(ch); cb := func(st agents.Step) { select { case ch <- agents.StepEvent{Step: st}: case <-ctx.Done(): } }; res, err := s.runInternal(ctx, input, cb); if err != nil { select { case ch <- agents.StepEvent{Done: true, Err: err}: case <-ctx.Done(): }; return }; select { case ch <- agents.StepEvent{Done: true, Final: &res}: case <-ctx.Done(): } }(); return ch, nil`.
    The 27-line helper from `agent.go:88-125` is the template — same
    shape, supervisor-scoped.

    (25) **Compile-time assertion** at end of file:
    `var _ agents.Agent = (*Supervisor)(nil)` — KC-1 composability
    proof. Add a one-line comment naming the property.

    File length target: ~400-500 LOC including doc comments, mirroring
    `fanout.go`'s 260 LOC plus the locally-reimplemented `runStream`
    helper + 3 node methods + buildPlannerPrompt + buildTrace + translateErr.

    DO NOT add `Validate()`/`Verify()` methods. DO NOT add an `OnStep`
    field to `SupervisorOptions` (Q3 ratified omit). DO NOT export any
    additional helpers (`supervisorState`, `planNode`, etc. all stay
    unexported).
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./orchestrate/... && GOWORK=off GOCACHE=/tmp/go-build go build ./orchestrate/...
    </automated>
  </verify>
  <acceptance_criteria>
    - `orchestrate/supervisor.go` exists and `go vet ./orchestrate/...` exits 0.
    - `grep -c "^func NewSupervisor(name string, opts SupervisorOptions) \*Supervisor" orchestrate/supervisor.go` returns `1`.
    - `grep -c "^type SupervisorOptions struct" orchestrate/supervisor.go` returns `1`.
    - `grep -c "^type Dispatch struct" orchestrate/supervisor.go` returns `1`.
    - `grep -c "^type WorkerResult struct" orchestrate/supervisor.go` returns `1`.
    - `grep -c "^type DispatchParser func" orchestrate/supervisor.go` returns `1`.
    - `grep -c "^type Aggregator func" orchestrate/supervisor.go` returns `1`.
    - `grep -cE 'ErrSupervisor(NilPlanner|NoWorkers|NilParseDispatch|NilBuildAggregate|UnknownWorker|MaxRounds|ParseDispatch)' orchestrate/supervisor.go` returns at least `7` (one declaration line each).
    - `grep -c 'var _ agents.Agent = (\*Supervisor)(nil)' orchestrate/supervisor.go` returns `1` (KC-1 composability proof).
    - `grep -c 'AddNode("plan"' orchestrate/supervisor.go` returns `1` AND `grep -c 'AddNode("dispatch"' orchestrate/supervisor.go` returns `1` AND `grep -c 'AddNode("final"' orchestrate/supervisor.go` returns `1` (3-node state machine present).
    - `grep -c 'AddConditionalEdge("plan"' orchestrate/supervisor.go` returns `1` (conditional routing on [plan]).
    - `grep -cE 'WithMaxSteps\(.*MaxRounds.*\*\s*3.*\+' orchestrate/supervisor.go` returns at least `1` (Pitfall 2 slack formula).
    - `grep -cE 'errors\.Is\(err,\s*ErrGraphMaxSteps\)' orchestrate/supervisor.go` returns at least `1` (err translation present).
    - No edit to validated public types: `git diff main -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go agent_chatmodel.go | wc -l` returns `0`.
    - Imports are stdlib + agents: `grep -E '^\s+"' orchestrate/supervisor.go | sort -u | grep -vE '^\s+"(context|errors|fmt|strings|github\.com/costa92/llm-agent)"$' | wc -l` returns `0`.
    - `go.mod` / `go.sum` unchanged: `git status --short -- go.mod go.sum | wc -l` returns `0`.
  </acceptance_criteria>
  <done>
    `orchestrate/supervisor.go` ships the full Supervisor surface (constructor, options struct, Dispatch, WorkerResult, parser + aggregator function types, 7 sentinels, the 3-node state machine, Name/Run/RunStream public methods, the locally-reimplemented runStream helper, and the compile-time agents.Agent assertion). `go vet` and `go build` both pass. Q1-Q5 ratifications are reflected in doc comments inside the file (the package-level paragraph lands in Task 3's doc.go edit).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write orchestrate/supervisor_test.go — 12-test functional sweep + race-clean check</name>
  <files>orchestrate/supervisor_test.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/supervisor.go` (the just-written file from Task 1 — needed for the surface under test)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/roundrobin_test.go` (entire ~137 LOC — closest test-shape analog; countingAgent + table-driven + ctx-cancel + max-turns)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/pipeline_test.go` (lines 13-45 — the in-package `stubAgent` struct already exists; reuse directly via package access, NO redeclare)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/fanout_test.go` (lines 14-32 parser fixture; lines 215-237 validation table — copy table pattern verbatim)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/graph_test.go` (lines 44-79 conditional-edge test; lines 162-172 MaxSteps test; lines 252-264 ctx-cancel test)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/agent.go` (lines 28-33 StepEvent; lines 58-76 Step + StepKind — needed for asserting RunStream events)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-PATTERNS.md` §"orchestrate/supervisor_test.go" (stubAgent reuse + happy-path + validation-table + ctx-cancel + MaxRounds patterns)
  </read_first>
  <behavior>
    - `TestSupervisor_HappyPath`: planner emits dispatch round-1, dispatch round-2, then "FINISH" round-3; ParseDispatch parses "dispatch to <worker>: <input>" → Dispatch, parses "FINISH" → (nil, nil). 2 workers (researcher, summarizer). Run returns answer that joins both worker outputs (via the test's `joinWorkerResults` Aggregator). `res.Usage.LLMCalls == 5` (3 planner + 2 worker — stubAgent returns LLMCalls: 1 per Run).
    - `TestSupervisor_Validation`: table-driven; 5 cases (nil Planner, empty Workers map, nil ParseDispatch, nil BuildAggregate, MaxRounds=0). Each case asserts `errors.Is(err, tc.want)` where `tc.want` is the matching sentinel OR (for MaxRounds=0) asserts `err != nil` AND `strings.Contains(err.Error(), "MaxRounds")`.
    - `TestSupervisor_MaxRoundsExceeded`: planner emits 3 dispatches (round 1, 2, 3); MaxRounds=2 → after round 2 the conditional edge routes to `[final]` (graceful per Q1/A4); `BuildAggregate` is called with 2 results; `res.Answer` contains both worker outputs; `err == nil` (graceful, not error); confirm `errors.Is(err, ErrSupervisorMaxRounds) == false` AND `errors.Is(err, ErrGraphMaxSteps) == false`.
    - `TestSupervisor_UnknownWorker`: planner emits `dispatch to nonexistent: <input>`; Workers map contains only `{"researcher": ...}`; Run returns error with `errors.Is(err, ErrSupervisorUnknownWorker)`.
    - `TestSupervisor_ParseDispatchError`: ParseDispatch always returns `(nil, errors.New("gibberish"))`; Run returns error with `errors.Is(err, ErrSupervisorParseDispatch)`.
    - `TestSupervisor_ParseDispatchFinish`: ParseDispatch returns `(nil, nil)` on round 1; Run terminates cleanly BEFORE MaxRounds; `res.Answer` is `BuildAggregate([])` (empty results); `err == nil`.
    - `TestSupervisor_CtxCancel`: pre-cancelled ctx (`ctx, cancel := context.WithCancel(...); cancel()`); Run returns `errors.Is(err, context.Canceled) == true`.
    - `TestSupervisor_RunStreamEmitsRoundEvents`: drain channel from `RunStream(ctx, input)`; assert: 2 rounds × (StepAction + StepObservation) = 4 mid-events + 1 terminal `{Done: true, Final: &res}`; assert each StepAction has `Tool` populated and StepObservation has `Result` populated; assert final event has `Final.Answer` non-empty.
    - `TestSupervisor_UsageRollup`: assert `res.Usage.LLMCalls == sum(planner_calls + worker_calls)`; assert `res.Usage.Tokens > 0` (stubAgent returns `Tokens: len(out)`).
    - `TestSupervisor_NameDefault`: `NewSupervisor("", ...)` then `sup.Name() == "supervisor"`; `NewSupervisor("research", ...)` then `sup.Name() == "research"`.
    - `TestSupervisor_SatisfiesAgentInterface`: assigns `*Supervisor` to `agents.Agent` variable; documentary test (the compile-time `var _ agents.Agent = (*Supervisor)(nil)` in supervisor.go IS the real check).
    - `TestSupervisor_ConcurrentRunIsRaceClean`: 2 goroutines, each calling `sup1.Run` and `sup2.Run` (independent Supervisors with independent stubAgents) 10 times; sync/atomic counter records total worker invocations; assert counter matches expected (20 × workers_per_run); `go test -race` must NOT flag.
  </behavior>
  <action>
    Create `orchestrate/supervisor_test.go` in `package orchestrate`
    (in-package so unexported helpers are testable AND the existing
    `stubAgent` from `pipeline_test.go:13-45` is reachable directly —
    DO NOT redeclare it).

    Imports: `context`, `errors`, `strings`, `sync`, `sync/atomic`,
    `testing`, `github.com/costa92/llm-agent` (identifier `agents`).

    Define at file top, before the tests:
    - **`parseDemoDispatch(plannerAnswer string) (*Dispatch, error)`**
      — a deterministic test parser. Recognizes:
      `"dispatch to <name>: <input>"` (case-sensitive — Pitfall 1
      enforcement) → `&Dispatch{WorkerName: name, Input: input}, nil`;
      `"FINISH"` → `nil, nil`; anything else →
      `nil, errors.New("parseDemoDispatch: unrecognized")`. Uses
      `strings.HasPrefix` + `strings.Index` for the parse — stdlib-only,
      no regex.
    - **`joinWorkerResults(results []WorkerResult) (string, error)`**
      — deterministic aggregator. Returns
      `"final: " + strings.Join(answers, " | ")` where `answers` is
      the slice of `wr.Result.Answer` values. Empty results returns
      `"final: (no results)"`.

    Tests in this order:

    **Test 1 — `TestSupervisor_HappyPath`** (~30 LOC): create three
    stubAgents driven by a per-call counter — planner returns "dispatch
    to researcher: find facts", "dispatch to summarizer: summarize", "FINISH"
    via its `transform` closure with an `int` counter; researcher and
    summarizer have simple `transform` that returns `"Facts: "+input`
    and `"Summary: "+input` respectively. Wire SupervisorOptions with
    `MaxRounds: 5`, the two workers, `parseDemoDispatch`,
    `joinWorkerResults`. Call `sup.Run(context.Background(), "investigate X")`.
    Assert `err == nil`. Assert `strings.Contains(res.Answer, "Facts")` AND
    `strings.Contains(res.Answer, "Summary")` AND `strings.HasPrefix(res.Answer, "final: ")`.
    Assert `res.Usage.LLMCalls == 5` (3 planner + 2 worker — stubAgent's
    default LLMCalls = 1 per Run).

    **Test 2 — `TestSupervisor_Validation`** (~50 LOC): use the table
    pattern from `fanout_test.go:215-237`. 5 sub-cases via `t.Run`. For
    `nil Planner`, `empty Workers`, `nil ParseDispatch`, `nil BuildAggregate`:
    construct opts missing the named field; build sup; run; assert
    `errors.Is(err, tc.want)` where `tc.want` is the matching `ErrSupervisor*Nil*`
    or `ErrSupervisorNoWorkers` sentinel. For `MaxRounds=0`: construct
    valid opts with `MaxRounds: 0`; assert `err != nil` AND
    `strings.Contains(err.Error(), "MaxRounds")`.

    **Test 3 — `TestSupervisor_MaxRoundsExceeded`** (~25 LOC): planner
    emits 3 dispatches via counter (never says FINISH); MaxRounds=2. Call
    Run. Assert `err == nil` (graceful per Q1/A4). Assert
    `strings.Contains(res.Answer, "final:")`. Assert NEITHER
    `errors.Is(err, ErrSupervisorMaxRounds)` NOR
    `errors.Is(err, ErrGraphMaxSteps)` is true (err is nil).
    Sanity: the StateGraph cap (`WithMaxSteps(MaxRounds*3+4) = 10`) is
    well above the actual node count (2 rounds × 3 nodes = 6 steps in
    worst case), so `ErrGraphMaxSteps` should not fire.

    **Test 4 — `TestSupervisor_UnknownWorker`** (~15 LOC): planner emits
    "dispatch to nonexistent: hi"; Workers map contains only `{"r": ...}`.
    Assert `errors.Is(err, ErrSupervisorUnknownWorker) == true`. Assert
    `strings.Contains(err.Error(), "nonexistent")` (the worker name is
    in the wrapped message per Pattern S3).

    **Test 5 — `TestSupervisor_ParseDispatchError`** (~15 LOC): a
    "broken" ParseDispatch that always returns
    `(nil, errors.New("gibberish"))`. Assert
    `errors.Is(err, ErrSupervisorParseDispatch) == true`.

    **Test 6 — `TestSupervisor_ParseDispatchFinish`** (~15 LOC): a
    ParseDispatch that returns `(nil, nil)` on EVERY call. Assert
    `err == nil`. Assert `res.Answer == "final: (no results)"` (no
    workers ran). Assert `res.Usage.LLMCalls == 1` (only the planner
    ran once before signaling finish).

    **Test 7 — `TestSupervisor_CtxCancel`** (~12 LOC): `ctx, cancel := context.WithCancel(context.Background()); cancel()`.
    Run with the pre-cancelled ctx. Assert `errors.Is(err, context.Canceled) == true`.

    **Test 8 — `TestSupervisor_RunStreamEmitsRoundEvents`** (~40 LOC):
    use the same happy-path opts (2 rounds × 2 workers). Call
    `events, err := sup.RunStream(ctx, "x")`; iterate via `for ev := range events`
    accumulating into a slice. Assert: exactly 4 mid-events (2 StepAction
    + 2 StepObservation, interleaved) followed by 1 terminal
    `{Done: true, Final: &res}`. Assert StepAction events have non-empty
    `Tool` and `Args` fields; StepObservation events have non-empty
    `Result` field. Assert `events.Final.Answer == res.Answer` from a
    parallel blocking `sup.Run` call (after re-constructing fresh
    stubAgents — counters reset).

    **Test 9 — `TestSupervisor_UsageRollup`** (~15 LOC): build stubAgents
    with custom `llmCalls` field values (planner: 2 per call, worker: 3 per call);
    happy path 2 rounds → planner runs 3× (3 × 2 = 6 LLMCalls) + workers
    run 2× (2 × 3 = 6 LLMCalls). Assert `res.Usage.LLMCalls == 12`.
    Tokens are `len(out)` per stubAgent default — assert `> 0`.

    **Test 10 — `TestSupervisor_NameDefault`** (~10 LOC): empty name →
    `Name() == "supervisor"`; non-empty name → `Name() == "research"`.

    **Test 11 — `TestSupervisor_SatisfiesAgentInterface`** (~5 LOC):
    `var a agents.Agent = NewSupervisor("x", validOpts())` — assigns;
    `t.Logf("Supervisor satisfies agents.Agent: %T", a)`. Documentary.

    **Test 12 — `TestSupervisor_ConcurrentRunIsRaceClean`** (~40 LOC):
    create 2 independent Supervisors (each with its own fresh stubAgents);
    a shared `sync/atomic.Int64` counter; helper function returns a
    stubAgent whose `transform` increments the counter via
    `atomic.AddInt64(&counter, 1)`. Spawn 2 goroutines via
    `go func() { for i := 0; i < 10; i++ { sup1.Run(ctx, "x") } }()`
    and similar for sup2; `sync.WaitGroup` to join. Assert
    `atomic.LoadInt64(&counter) == 60` (20 runs × 3 calls per run —
    1 planner increment + 2 worker increments, where the stubAgents
    record on each invocation). With `go test -race ./orchestrate/...`
    the race detector must NOT flag.

    Helper at file bottom: `func validOpts() SupervisorOptions { return SupervisorOptions{Planner: &stubAgent{name: "p"}, Workers: map[string]agents.Agent{"w": &stubAgent{name: "w"}}, MaxRounds: 3, ParseDispatch: parseDemoDispatch, BuildAggregate: joinWorkerResults} }`
    — convenience for the validation table and the misc tests.

    DO NOT use `time.Sleep`. DO NOT use file/network. DO NOT redeclare
    `stubAgent` (it's already in `pipeline_test.go:13-45` and reachable
    in-package).
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./orchestrate/... -run 'TestSupervisor_' -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -run 'TestSupervisor_' -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `orchestrate/supervisor_test.go` exists and `go vet ./orchestrate/...` exits 0.
    - `go test ./orchestrate/... -run 'TestSupervisor_HappyPath|TestSupervisor_Validation|TestSupervisor_MaxRoundsExceeded|TestSupervisor_UnknownWorker|TestSupervisor_ParseDispatchError|TestSupervisor_ParseDispatchFinish|TestSupervisor_CtxCancel|TestSupervisor_RunStreamEmitsRoundEvents|TestSupervisor_UsageRollup|TestSupervisor_NameDefault|TestSupervisor_SatisfiesAgentInterface|TestSupervisor_ConcurrentRunIsRaceClean' -count=1` — all 12 pass.
    - `go test -race ./orchestrate/... -run 'TestSupervisor_' -count=1` exits 0 — race-clean.
    - `grep -v '^#' orchestrate/supervisor_test.go | grep -c '^func TestSupervisor_'` returns at least `12` (one Test function per case; subtests under TestSupervisor_Validation count as one).
    - `grep -c 'errors.Is(err, ErrSupervisor' orchestrate/supervisor_test.go` returns at least `5` (sentinel checks across the 5 sentinel-driven tests).
    - `grep -c 'errors.Is(err, context.Canceled)' orchestrate/supervisor_test.go` returns at least `1` (ctx-cancel test).
    - `grep -c 'sync/atomic' orchestrate/supervisor_test.go` returns at least `1` (concurrent test uses atomic counter).
    - `grep -c 'type stubAgent struct' orchestrate/supervisor_test.go` returns `0` (no redeclare; the in-package fixture from `pipeline_test.go` is reused).
    - `git diff main -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go agent_chatmodel.go | wc -l` returns `0` (KC-5).
  </acceptance_criteria>
  <done>
    `orchestrate/supervisor_test.go` covers the 12 functional cases (happy path, 5-case validation table, MaxRounds graceful, unknown worker, parser error, parser finish, ctx cancel, RunStream events, usage rollup, name default, agents.Agent satisfaction, race-clean concurrent run). `go test -race` is green across the whole `./orchestrate/...` set.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add additive Supervisor paragraph to orchestrate/doc.go</name>
  <files>orchestrate/doc.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/orchestrate/doc.go` (entire file, ~82 LOC — the file to edit; lists 5 paradigms in EN + 中文)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/policy/doc.go` (the Phase 36 sibling — shows the "decisions ratified" voice for inline Q-list)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-PATTERNS.md` §"orchestrate/doc.go" (the additive edit pattern)
  </read_first>
  <action>
    Edit `orchestrate/doc.go`. The edit is STRICTLY ADDITIVE — only
    doc-comment lines are added; no Go symbol added or changed (KC-5
    preserves package doc-only edits).

    1. In the EN list (currently 5 entries: Pipeline / FanOutFanIn /
       RoundRobinChat / RolePlay / StateGraph[S]), add a 6th bullet
       AFTER the `StateGraph[S]` bullet:
       `//   - Supervisor     — iterative planner ↔ worker loop with re-planning (KC-1 v1.2 — orchestrate.Supervisor satisfies agents.Agent so a Supervisor may be a worker of another Supervisor)`

    2. In the 中文 list (the matching 5-entry list), add the matching
       6th bullet AFTER the `StateGraph[S]` 中文 bullet:
       `//   - Supervisor     — planner ↔ worker 迭代循环，支持重新规划（KC-1 v1.2 — Supervisor 实现 agents.Agent，可作为另一个 Supervisor 的 worker）`

    3. In the "Choosing a paradigm" section (the section at the bottom
       of `orchestrate/doc.go` listing when-to-use guidance), add two
       new lines:
       `//   - planner re-plans based on worker output (loop with conditional dispatch) → Supervisor`
       `//   - planner 根据 worker 输出重新规划（带条件分发的循环）→ Supervisor`

    DO NOT add any new Go symbol. DO NOT change the existing 5
    paradigm entries. DO NOT add an `import` statement. The file
    remains `package orchestrate` with doc-comment-only content.

    File length grows by ~4 lines (2 list entries + 2 "choosing"
    entries) — total ~86 LOC.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./orchestrate/... && grep -q 'Supervisor.*iterative planner.*worker loop' orchestrate/doc.go && grep -q 'Supervisor.*planner.*worker 迭代循环' orchestrate/doc.go && grep -q 'planner re-plans based on worker output' orchestrate/doc.go && echo DOC-OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `orchestrate/doc.go` mentions `Supervisor` in BOTH the EN bullet list AND the 中文 bullet list.
    - `grep -c '\- Supervisor' orchestrate/doc.go` returns at least `2` (EN + 中文 bullets).
    - `grep -c 'planner re-plans based on worker output' orchestrate/doc.go` returns at least `1` (EN "choosing" entry).
    - `grep -c 'planner 根据 worker 输出重新规划' orchestrate/doc.go` returns at least `1` (中文 "choosing" entry).
    - `go vet ./orchestrate/...` exits 0.
    - The diff against `main` for `orchestrate/doc.go` is doc-comment-only — verified by `git diff main -- orchestrate/doc.go | grep -E '^[+-]' | grep -v '^[+-]//' | grep -v '^[+-]$' | grep -v '^[+-]\{3\}' | wc -l` returns `0` (no non-comment changes).
    - `grep -c '^package orchestrate' orchestrate/doc.go` returns `1` (single package declaration unchanged).
  </acceptance_criteria>
  <done>
    `orchestrate/doc.go` lists `Supervisor` alongside the 5 existing paradigms in both EN and 中文, and includes a "Choosing a paradigm" guidance entry for the loop-with-routing use case. No Go symbol is added or changed.
  </done>
</task>

</tasks>

<verify>
- vet + build clean repo-wide:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go build ./... && echo VET-OK`
- orchestrate tests green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./orchestrate/... -count=1 && echo TEST-OK`
- race-clean on orchestrate:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race ./orchestrate/... -count=1 && echo RACE-OK`
- repo-wide tests still green (no regression):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && echo REPO-TEST-OK`
- stdlib-only — orchestrate imports:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -f '{{join .Imports "\n"}}' ./orchestrate/ | sort -u | grep -v '^$' | grep -vE '^(context|errors|fmt|sort|strings|sync|sync/atomic|testing|github.com/costa92/llm-agent(/(llm|pkg/fanout))?)$' | wc -l` returns `0`
- KC-1 composability proof present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -c 'var _ agents.Agent = (\*Supervisor)(nil)' orchestrate/supervisor.go` returns `1`
- 3-node state machine present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -cE 'AddNode\("(plan|dispatch|final)"' orchestrate/supervisor.go` returns at least `3`
- 7 sentinels declared:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -cE '^\s*ErrSupervisor(NilPlanner|NoWorkers|NilParseDispatch|NilBuildAggregate|UnknownWorker|MaxRounds|ParseDispatch)\s*=' orchestrate/supervisor.go` returns at least `7`
- KC-5 unchanged validated public types (broad sweep — the only allowed orchestrate edit is doc.go):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go agent_chatmodel.go memory/ orchestrate/graph.go orchestrate/fanout.go orchestrate/roundrobin.go orchestrate/pipeline.go orchestrate/roleplay.go orchestrate/termination.go | wc -l` returns `0`
- go.mod / go.sum unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- go.mod go.sum | wc -l` returns `0`
- public surface present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for s in 'func NewSupervisor(' 'type Supervisor struct' 'type SupervisorOptions struct' 'type Dispatch struct' 'type WorkerResult struct' 'type DispatchParser func' 'type Aggregator func' 'ErrSupervisorNilPlanner' 'ErrSupervisorNoWorkers' 'ErrSupervisorMaxRounds' 'ErrSupervisorUnknownWorker' 'ErrSupervisorParseDispatch'; do grep -q "$s" orchestrate/supervisor.go || { echo "MISSING $s"; exit 1; }; done && echo SURFACE-OK`
- doc.go cites Supervisor + KC-1:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -q 'Supervisor' orchestrate/doc.go && grep -q 'KC-1' orchestrate/doc.go && echo DOC-OK`
</verify>

<acceptance>
- `orchestrate/supervisor.go`, `orchestrate/supervisor_test.go` exist; `orchestrate/doc.go` has the additive Supervisor entry.
- Public surface present and doc-commented: `Supervisor`, `NewSupervisor`, `SupervisorOptions{Planner, Workers, MaxRounds, ParseDispatch, BuildAggregate}`, `Dispatch{WorkerName, Input, Metadata}`, `WorkerResult{Dispatch, Result}`, `DispatchParser`, `Aggregator`, 7 sentinels.
- `var _ agents.Agent = (*Supervisor)(nil)` compiles — KC-1 composability proof.
- 3-node state machine present: `[plan]`, `[dispatch]`, `[final]` with conditional edge from `[plan]`.
- 12 functional tests pass (`TestSupervisor_*`); `go test -race ./orchestrate/...` is green.
- Q1-Q5 ratifications reflected: Q1 (MaxRounds graceful) verified by `TestSupervisor_MaxRoundsExceeded`; Q2 (local runStream helper) verified by inspection (no `runStreamFromBlocking` export); Q3 (no OnStep field) — `grep -c OnStep orchestrate/supervisor.go` returns `0`; Q4 (Metadata included) — `grep -c 'Metadata map\[string\]any' orchestrate/supervisor.go` returns at least `1`; Q5 (no agent suffix) — `grep -c 'SupervisorAgent' orchestrate/supervisor.go` returns `0`.
- `go.mod` unchanged; no `go.sum` created.
- No edit to validated public types (KC-5).
- All `<verify>` commands pass.
</acceptance>

<success_criteria>
A v0.6.1 caller compiles unchanged when they import
`github.com/costa92/llm-agent/orchestrate` and add a Supervisor (KC-5
additive). The Supervisor composes with the existing 5 agent paradigms
(any of Simple/ReAct/Reflection/PlanSolve/FunctionCall can be a worker
OR the planner). The 3-node StateGraph[supervisorState] facade is the
load-bearing proof for KC-1's "facade design" hypothesis. This slice's
surface is the foundation the budget/policy integration tests (37-02),
the compose-with-StateGraph tests + example (37-03), and the exit gate
(37-04) all build on.
</success_criteria>

<output>
Create `.planning/phases/37-multi-agent-coordination-orchestrate-supervisor/37-01-SUMMARY.md`
when done. Summary records: the public surface shipped, Q1-Q5
ratifications, the 12 functional test names + outcomes, race-clean
status, KC-5 + stdlib-only audit pass.
</output>
</content>
</invoke>