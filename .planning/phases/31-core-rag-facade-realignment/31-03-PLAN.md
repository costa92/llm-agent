---
phase: 31-core-rag-facade-realignment
plan: 03
type: execute
wave: 2
repo: llm-agent
depends_on: ["31-02"]
files_modified:
  - rag/doc.go
requirements: [ECO-01]
must_haves:
  truths:
    - "the core builds and tests green against llm-agent-rag v1.0.0 and is proven stdlib-only"
    - "the cross-repo contract gate still compiles"
    - "the facade docs no longer name a stale SDK version"
  artifacts:
    - path: "rag/doc.go"
      provides: "facade docs current with the v1.0.0 SDK"
      contains: "no reference to a superseded llm-agent-rag version"
---

<objective>
The Phase 31 exit gate: prove the core `llm-agent` builds and tests green
against `llm-agent-rag v1.0.0`, stays stdlib-only, keeps the cross-repo
contract gate compiling, and carries no stale SDK-version references in
its facade docs. ECO-01.
</objective>

<context>
- 31-01 bumped `go.mod` to `v1.0.0`; 31-02 repaired the facade. This
  slice is the verification gate + a small doc-currency pass — minimal
  code change.
- KE-3 exit gate: build + test green AND `go list -deps ./rag` lists zero
  third-party modules AND `go.sum` holds only the `llm-agent-rag` lines.
- `rag/contract_test.go` is the core side of the cross-repo contract gate
  — it must still compile (the facade's public surface did not change in
  31-02, so it should).
- `rag/doc.go` references `docs/2026-05-13-standalone-rag-sdk-design.md`
  and `docs/2026-05-13-rag-sdk-migration-status.md` — check whether any
  facade doc/comment names a now-superseded `llm-agent-rag` version
  (e.g. `v0.1.x`) and correct it to v1.0.0.
- See `31-RESEARCH.md` Decision 2.
</context>

<tasks>
1. Full verification — run every `<verify>` command and confirm green.
2. Prove stdlib-only: `GOWORK=off go list -deps ./rag` must list no
   third-party module; `go.sum` must contain only `llm-agent-rag` lines
   (no `pgx`/`pgvector`/other). Record both in `31-03-SUMMARY.md`.
3. Confirm the cross-repo contract gate compiles:
   `GOWORK=off go test ./rag/ -run Contract -count=1` (or
   `go vet ./rag/` — `contract_test.go` is compile-checked by any
   `rag` test build).
4. Doc currency: scan the facade for stale SDK-version references —
   `grep -rn 'v0\.1\.\|v0\.[0-9]' rag/*.go docs/*rag* 2>/dev/null`. Update
   `rag/doc.go` (and any other facade comment) so it states the facade
   delegates to `llm-agent-rag v1.0.0` and does not name a superseded
   version. If the referenced `docs/2026-05-13-*` migration files contain
   a stale version claim, correct that line only — do not rewrite the
   docs.
5. Run every command in `<verify>`.

Out of scope:
- Re-tagging the core (`v0.5.0`) — Phase 33.
- The core `CHANGELOG.md` entry — Phase 33 cuts the tag and owns the
  changelog for it.
- Any sister-repo work — Phases 32-33.
</tasks>

<verify>
- `cd <core repo> && GOWORK=off go vet ./...`
- `cd <core repo> && GOWORK=off go build ./...`
- `cd <core repo> && GOWORK=off go test ./... -count=1` (all green)
- stdlib-only proof:
  `GOWORK=off go list -deps ./rag | grep -E '^github\.com/(jackc|pgvector)|golang\.org/x' && echo LEAK || echo STDLIB-ONLY-OK`
- `go.sum` minimal:
  `! grep -E 'pgx|pgvector' go.sum && echo GOSUM-CLEAN`
- contract gate compiles:
  `GOWORK=off go test ./rag/ -run Contract -count=1`
- no stale SDK version named in facade docs:
  `! grep -rn 'llm-agent-rag v0\.' rag/*.go && echo DOCS-CURRENT`
- `gofmt -l rag/` (no output)
</verify>

<acceptance>
- `go vet`/`go build`/`go test ./...` all green against
  `llm-agent-rag v1.0.0`.
- `go list -deps ./rag` proves zero third-party modules; `go.sum` carries
  only the `llm-agent-rag` lines — the core is provably stdlib-only.
- `rag/contract_test.go` compiles — the cross-repo contract gate holds.
- No facade doc names a superseded `llm-agent-rag` version.
- All `<verify>` commands pass.
</acceptance>
