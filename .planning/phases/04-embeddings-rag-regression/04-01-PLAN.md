---
phase: 04-embeddings-rag-regression
plan: 01
type: execute
wave: 1
repo: llm-agent-providers
depends_on: []
files_modified:
  - openai/
autonomous: true
requirements: [OAI-04, OAI-08]
must_haves:
  truths:
    - "OpenAI adapter implements llm.Embedder on the bound model"
    - "Embedding usage and vector dimensions are surfaced through the shared llm types"
  artifacts:
    - path: "openai/"
      provides: "OpenAI Embedder"
      contains: "Embed"
---

<objective>
Add `llm.Embedder` to the OpenAI adapter using the existing pinned SDK and
bound-model capability surface.
</objective>

<tasks>
- Implement `Embed(ctx, []string)` and `EmbedDimensions()` on the OpenAI
  adapter.
- Bind truthful `ProviderInfo.Capabilities.Embeddings=true` only for
  embedding-capable models.
- Add unit tests for batch order, usage propagation, and dimension reporting.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./openai/... -count=1`
- `GOCACHE=/tmp/go-build go build ./openai/...`
</verify>
