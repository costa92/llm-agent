---
phase: 04-embeddings-rag-regression
plan: 02
type: execute
wave: 1
repo: llm-agent-providers
depends_on: []
files_modified:
  - ollama/
autonomous: true
requirements: [OLL-04, OLL-07]
must_haves:
  truths:
    - "Ollama adapter implements llm.Embedder on embedding-capable models"
    - "Nightly/live strategy remains separate from PR fixture CI"
  artifacts:
    - path: "ollama/"
      provides: "Ollama Embedder"
      contains: "Embed"
---

<objective>
Add `llm.Embedder` to the Ollama adapter using the native embed endpoint and
truthful per-model capability reporting.
</objective>

<tasks>
- Implement batch embeddings against the Ollama embed API.
- Extend the model strategy/capability surface so embedding-capable models are
  reported honestly.
- Add fixture-driven tests for vector count, dimension stability, and usage.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./ollama/... -count=1`
- `GOCACHE=/tmp/go-build go build ./ollama/...`
</verify>
