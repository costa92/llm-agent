---
phase: 04-embeddings-rag-regression
plan: 05
type: execute
wave: 2
repo: llm-agent
depends_on: [04]
files_modified:
  - rag/
  - PROVIDER_AUTHORING.md
autonomous: true
requirements: [CONF-06, CORE-11]
must_haves:
  truths:
    - "rag.RAGSystem works with llm.Embedder without breaking v0.2 API shape"
    - "Provider authoring guidance documents the Embedder capability and documented-gap pattern"
  artifacts:
    - path: "rag/"
      provides: "embedding-backed regression coverage"
      contains: "RAGSystem tests"
---

> Archived planning record only.
> Do not use as current development guide.
> Current development follows live code and `llm-agent-rag` current docs.


<objective>
Prove the core RAG abstraction can consume the Phase 4 embedding capability and
document the provider contract clearly for future adapters.
</objective>

<tasks>
- Add or adapt RAG regression tests to use a real `llm.Embedder`.
- Preserve the existing public RAG API while wiring the new capability surface.
- Update `PROVIDER_AUTHORING.md` with Embedder support and documented-gap rules.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./rag/... -count=1`
- `GOCACHE=/tmp/go-build go build ./rag/...`
</verify>
