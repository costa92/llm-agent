---
phase: 04-embeddings-rag-regression
plan: 03
type: execute
wave: 1
repo: llm-agent-providers
depends_on: []
files_modified:
  - anthropic/
  - README.md
autonomous: true
requirements: [ANT-04, ANT-07]
must_haves:
  truths:
    - "Anthropic remains non-Embedder and reports Capabilities.Embeddings=false"
    - "Unsupported embedding use is documented, typed, and testable"
  artifacts:
    - path: "anthropic/"
      provides: "documented embedding gap"
      contains: "ErrCapabilityNotSupported path"
---

<objective>
Document and test Anthropic's deliberate embedding gap so callers can detect it
cleanly instead of discovering it by accident.
</objective>

<tasks>
- Ensure Anthropic bound models report `Capabilities.Embeddings=false`.
- Add tests and docs proving embedding requests resolve to
  `llm.ErrCapabilityNotSupported` semantics.
- Update provider-facing docs if needed to make the gap explicit.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./anthropic/... -count=1`
- `GOCACHE=/tmp/go-build go build ./anthropic/...`
</verify>
