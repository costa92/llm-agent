---
phase: 11-structure-aware-retrieval-and-explainability
plan: 03
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [11-01, 11-02]
files_modified:
  - tree/
  - README.md
autonomous: true
requirements: [RAG-STRUCT-01]
must_haves:
  truths:
    - "hierarchical document structure is available through an explicit tree model rather than only implicit metadata"
    - "tree navigation helpers can locate sections and chunk leaves by stable identifiers"
  artifacts:
    - path: "tree/"
      provides: "document tree primitives and traversal helpers"
      contains: "Build, Find, Sections, Leaves"
---

<objective>
Add an explicit document tree package so hierarchical section structure can be
reused by future structure-aware retrieval and explainability features.
</objective>

<tasks>
- Create a `tree/` package for document hierarchy nodes.
- Build trees from markdown-aware ingestion chunks.
- Add helpers for section traversal and node lookup.
- Add tests proving stable section/chunk navigation.
- Update standalone README to reflect tree support.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./tree ./ingest -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
