---
phase: 11-structure-aware-retrieval-and-explainability
plan: 01
type: execute
wave: 1
repo: llm-agent-rag
depends_on: [10-04]
files_modified:
  - store/
  - rag/
  - ingest/
autonomous: true
requirements: [RAG-STRUCT-01]
must_haves:
  truths:
    - "stored chunks carry first-class section lineage fields suitable for structure-aware retrieval"
    - "existing heading-aware ingestion metadata is normalized into retrieval-ready chunk fields"
  artifacts:
    - path: "store/"
      provides: "chunk-level structure fields"
      contains: "section ids and section paths"
---

<objective>
Promote section hierarchy from loose metadata into first-class stored chunk
fields so structure-aware retrieval has a stable substrate.
</objective>

<tasks>
- Extend stored chunk types with section-aware lineage fields.
- Populate these fields during import from markdown-aware chunk metadata.
- Keep behavior additive for non-structured documents.
- Add tests proving structure lineage is preserved after import.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./rag ./store -count=1`
</verify>
