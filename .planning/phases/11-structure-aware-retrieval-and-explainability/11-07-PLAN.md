---
phase: 11-structure-aware-retrieval-and-explainability
plan: 07
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05, 11-06]
files_modified:
  - retrieve/
  - rag/
autonomous: true
requirements: [RAG-STRUCT-01, RAG-STRUCT-02]
must_haves:
  truths:
    - "automatic routing should surface multiple candidate section routes, not only one selected route"
    - "query variants should contribute route candidates into a merged trace"
    - "ask diagnostics should expose route candidates for planner/debug use"
  artifacts:
    - path: "retrieve/retrieve.go"
      provides: "multi-candidate route proposal and variant aggregation"
      contains: "RouteCandidate, AutoRouteCandidates, candidate merge logic"
---

<objective>
Upgrade automatic routing from single-path selection to multi-candidate route
proposal so later planner work can reason over several section hypotheses.
</objective>

<tasks>
- add a route-candidate structure with path, score, and contributing queries
- let auto-route proposal emit several ranked candidates
- merge route candidates across query variants in `VariantRetriever`
- expose candidates through `Ask` trace and diagnostics
- add regression tests for candidate ranking and variant aggregation
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
