---
phase: 11-structure-aware-retrieval-and-explainability
plan: 05
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - retrieve/
  - rag/
  - README.md
autonomous: true
requirements: [RAG-STRUCT-01, RAG-STRUCT-02]
must_haves:
  truths:
    - "callers can explicitly route retrieval into a section subtree"
    - "dense, lexical, and structure-aware retrieval respect the same route constraint"
    - "ask trace exposes the adopted route path"
  artifacts:
    - path: "rag/options.go"
      provides: "public subtree routing option"
      contains: "RoutePath"
    - path: "retrieve/retrieve.go"
      provides: "route-constrained retrieval execution"
      contains: "subtree filtering across retrieval modes"
---

<objective>
Add an explicit route-path control so callers can constrain retrieval to a
known section subtree and observe that route in answer trace output.
</objective>

<tasks>
- add `RoutePath` to search and retrieval request contracts
- enforce route-path subtree filtering in dense, lexical, and structure retrieval
- propagate route-path through ask trace output
- add regression tests for subtree-constrained retrieval
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
