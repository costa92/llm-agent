---
phase: 11-structure-aware-retrieval-and-explainability
plan: 02
type: execute
wave: 1
repo: llm-agent-rag
depends_on: [11-01]
files_modified:
  - retrieve/
  - prompt/
  - rag/
autonomous: true
requirements: [RAG-STRUCT-01, RAG-STRUCT-02]
must_haves:
  truths:
    - "retrieval can score section/path cues in addition to dense and lexical signals"
    - "ask-time traces, citations, and prompts expose section lineage and search trajectory"
  artifacts:
    - path: "retrieve/"
      provides: "structure-aware retrieval path"
      contains: "StructureRetriever + hybrid fusion integration"
    - path: "prompt/"
      provides: "structure-aware rendering"
      contains: "section path references in prompt context"
---

<objective>
Add a structure-aware retrieval path and explainability outputs so retrieval and
ask flows can expose section lineage and search trajectory.
</objective>

<tasks>
- Add a structure-aware retriever over section/path/heading fields.
- Fuse structure-aware hits into the default hybrid retrieval path.
- Extend ask traces, diagnostics, and citations with matched sections and
  search path data.
- Render section lineage in the default QA prompt when available.
- Add tests covering structure-aware retrieve and ask flows.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./prompt ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
