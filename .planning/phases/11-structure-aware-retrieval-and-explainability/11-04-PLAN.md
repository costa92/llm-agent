---
phase: 11-structure-aware-retrieval-and-explainability
plan: 04
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - retrieve/
  - rag/
  - tree/
autonomous: true
requirements: [RAG-STRUCT-01, RAG-STRUCT-02]
must_haves:
  truths:
    - "structure-aware retrieval uses the explicit document tree rather than only flat chunk metadata"
    - "matched section nodes expand into descendant leaf chunks with explainable trace output"
    - "ask/diagnostics surface expanded sections and chunk ids from the real retrieval trace"
  artifacts:
    - path: "retrieve/retrieve.go"
      provides: "tree-aware structure retrieval and trace propagation"
      contains: "section-node matching, descendant chunk expansion, expanded trace fields"
    - path: "rag/ask.go"
      provides: "real retrieval trace propagation into ask diagnostics"
      contains: "expanded sections/chunks and search path"
---

<objective>
Convert the explicit document tree into live retrieval behavior so structure
matching can expand through descendant leaves and produce richer search
trajectory output.
</objective>

<tasks>
- match query tokens against section nodes built from the explicit document tree
- expand matched sections into descendant chunk leaves with bounded depth
- propagate structure expansion trace through `VariantRetriever`, `Retrieve`, and `Ask`
- add regression tests for nested section expansion and ask diagnostics
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
