---
phase: 11-structure-aware-retrieval-and-explainability
plan: 06
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05]
files_modified:
  - retrieve/
  - rag/
  - README.md
autonomous: true
requirements: [RAG-STRUCT-01, RAG-STRUCT-02]
must_haves:
  truths:
    - "the system can automatically propose a section subtree route from the query"
    - "auto-routed retrieval reuses the same route-path execution path as explicit routing"
    - "answer trace exposes both effective route path and auto-selected route path"
  artifacts:
    - path: "retrieve/retrieve.go"
      provides: "automatic route proposal and reuse of route-constrained retrieval"
      contains: "EnableAutoRoute, AutoRouteMinScore, AutoRoutePath"
---

<objective>
Add lightweight automatic section route selection so structured corpora can
constrain retrieval to the most relevant subtree without requiring the caller
to specify `RoutePath` manually.
</objective>

<tasks>
- add auto-route controls to search and retrieval contracts
- propose a section route from query-to-section-path overlap
- reuse the existing route-constrained retrieval path once a route is selected
- expose selected auto-route information in retrieval and ask traces
- add regression tests for automatic route selection
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
