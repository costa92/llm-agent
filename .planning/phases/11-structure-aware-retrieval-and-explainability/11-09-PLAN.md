---
phase: 11-structure-aware-retrieval-and-explainability
plan: 09
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05, 11-06, 11-07, 11-08]
files_modified:
  - retrieve/
  - rag/
autonomous: true
requirements: [RAG-STRUCT-01, RAG-STRUCT-02]
must_haves:
  truths:
    - "route-candidate metadata must influence execution, not only trace output"
    - "the system should support confidence-driven selection and optional multi-route fanout"
    - "fanout policy should remain additive and preserve explainable trace output"
  artifacts:
    - path: "retrieve/retrieve.go"
      provides: "route execution policy"
      contains: "confidence thresholding and top-N route fanout"
---

<objective>
Turn route-candidate metadata into executable retrieval policy so the system can
either trust the top route or fan out across several strong routes.
</objective>

<tasks>
- add fanout and confidence-threshold controls to retrieval requests
- implement top-N route execution over auto-selected route candidates
- merge route-constrained retrieval results back into a single ranked hit set
- preserve route candidate / section trace information after fanout
- add regression tests for multi-route execution behavior
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
