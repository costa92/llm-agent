---
phase: 11-structure-aware-retrieval-and-explainability
plan: 08
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05, 11-06, 11-07]
files_modified:
  - retrieve/
  - rag/
autonomous: true
requirements: [RAG-STRUCT-01, RAG-STRUCT-02]
must_haves:
  truths:
    - "route candidates should carry richer planner-facing metadata than only path and score"
    - "candidate ranking should expose relative confidence for later planner policy work"
    - "candidate traces should preserve query contributions across route merges"
  artifacts:
    - path: "retrieve/retrieve.go"
      provides: "route-confidence groundwork"
      contains: "confidence normalization and candidate signal fields"
---

<objective>
Add richer route-candidate metadata so the route-planning layer can evolve from
raw score sorting toward explicit planner policy.
</objective>

<tasks>
- extend route candidates with confidence and signal fields
- normalize candidate confidence relative to the top route
- preserve signal/query metadata through candidate cloning and merging
- add regression checks that candidate metadata survives retrieval flows
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./retrieve ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
