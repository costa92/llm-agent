---
phase: 29-documentation-completeness-and-compatibility-policy
plan: 01
type: execute
wave: 1
repo: llm-agent-rag
depends_on: []
files_modified:
  - advanced/*.go
  - embed/*.go
  - generate/*.go
  - ingest/*.go
  - pack/*.go
  - prompt/*.go
  - rag/system.go
  - rerank/*.go
  - retrieve/*.go
  - store/store.go
  - tree/*.go
  - adapter/llmagent/model.go
  - contract/contract_test.go
  - examples/basic_import_and_ask_test.go
requirements: [RAG-API-03]
must_haves:
  truths:
    - "every package of llm-agent-rag has a package doc comment — none render blank on pkg.go.dev"
    - "each package comment starts with `// Package <name>` and states the package's role"
  artifacts:
    - path: "rag/system.go"
      provides: "the rag front-door package comment"
      contains: "// Package rag — orchestration-layer overview"
---

<objective>
Add a package doc comment to every `llm-agent-rag` package that lacks one —
11 importable packages, `adapter/llmagent`, and the two test-only packages
`contract` + `examples`. KS-7: a v1.0 with blank `pkg.go.dev` pages does
not ship. RAG-API-03.

No Go API surface change — package clauses gain comments, nothing else.
</objective>

<context>
- Packages MISSING a comment (verified via `go doc ./pkg`): `advanced`,
  `embed`, `generate`, `ingest`, `pack`, `prompt`, `rag`, `rerank`,
  `retrieve`, `store`, `tree` (11 importable) + `adapter/llmagent`
  (build-tagged) + `contract` + `examples` (test-only). 14 total.
- Packages that already HAVE one (do not touch): `agentic`, `eval`,
  `graph`, `guard`, `obs`, `feedback`, `postgres`, `store/storetest`, root
  `ragkit`.
- **Placement convention** (observed): the package comment lives on the
  file named `<pkg>.go` (`eval/eval.go`, `graph/graph.go`, …) or, when
  there is none, on the package's primary file (`guard/redact.go`,
  `agentic/correct.go`). Follow it — do **not** create new `doc.go` files.
  - `store` → `store/store.go`.
  - `rag` has no `rag.go`; its central type `System` lives in
    `rag/system.go` → put the comment there.
  - `adapter/llmagent` → above `package llmagent` in `model.go` (the doc
    comment goes after the `//go:build llmagent` line + its blank line).
  - `contract` / `examples` → a one-line `// Package …` above the
    `package` clause in an existing `_test.go` file.
- `docs/api-audit-v1.0.md` (Phase 28) describes every package's role —
  source the comment content from it.
- See `29-RESEARCH.md` Decision 1.
</context>

<tasks>
1. For each of the 11 importable packages (`advanced`, `embed`, `generate`,
   `ingest`, `pack`, `prompt`, `rag`, `rerank`, `retrieve`, `store`,
   `tree`), add a package doc comment on the conventional file (see
   <context>). Each comment:
   - starts with `// Package <name> ` (Go convention — required for
     `go doc`/`pkg.go.dev` rendering);
   - states the package's role in the SDK in 1-4 lines;
   - for seam packages, names the central interface(s) (e.g. `embed` →
     `Embedder`; `store` → `Store` + the capability interfaces; `retrieve`
     → `Retriever`; `generate` → `Model`; `prompt` → the template seam;
     `rerank` → the reranker seam).
   - `rag`'s comment is the most important — it is the front door; describe
     `System`, `New`, and the three answer paths `Ask`/`AskGlobal`/`AskDrift`.
2. `adapter/llmagent/model.go` — add a `// Package llmagent` comment
   describing the build-tagged `llm-agent` adapter (placed after the
   `//go:build llmagent` line).
3. `contract` + `examples` — add a one-line `// Package contract …` /
   `// Package examples …` comment above the `package` clause in
   `contract/contract_test.go` and one `examples/*_test.go` file
   respectively.
4. Run every command in `<verify>`.

Out of scope:
- Exported-symbol doc comments — slice 29-02.
- `docs/compatibility.md` and the README status line — slice 29-03.
- Any rename, signature change, or symbol add/remove — the surface is
  frozen (Phase 28).
</tasks>

<verify>
- every package now has a doc comment — line 3 of `go doc` is prose, not a
  declaration:
  `cd /tmp/llm-agent-rag && for p in $(GOWORK=off GOCACHE=/tmp/go-build go list ./... | sed 's#github.com/costa92/llm-agent-rag/\?##;s#^/##'); do l3=$(GOWORK=off GOCACHE=/tmp/go-build go doc "./${p:-.}" 2>/dev/null | sed -n '3p'); case "$l3" in CONSTANTS|VARIABLES|FUNCTIONS|TYPES|func\ *|type\ *|var\ *|const\ *|"") echo "STILL-MISSING: ${p:-root}";; esac; done`
  (no STILL-MISSING lines)
- `adapter/llmagent` has a comment:
  `cd /tmp/llm-agent-rag && head -6 adapter/llmagent/model.go | grep -q '// Package llmagent' && echo ADAPTER-OK`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- `cd /tmp/llm-agent-rag && gofmt -l advanced embed generate ingest pack prompt rag rerank retrieve store tree adapter contract examples` (no output)
- no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
</verify>

<acceptance>
- All 11 importable packages, `adapter/llmagent`, `contract`, and
  `examples` have a package doc comment starting with `// Package <name>`.
- The `rag` package comment describes the front door (`System`, `New`, the
  three answer paths).
- No Go API symbol changed; `gofmt` clean; build/vet/test green.
- No new module dependency; all `<verify>` commands pass.
</acceptance>
