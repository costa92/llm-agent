---
phase: 00-keystone-interfaces
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - docs/migration-v0.2-to-v0.3.md
  - DEPRECATIONS.md
  - CHANGELOG.md
autonomous: true
requirements:
  - CORE-09
  - INFRA-07
tags:
  - migration
  - deprecation
  - changelog
  - documentation

must_haves:
  truths:
    - "docs/migration-v0.2-to-v0.3.md exists with at least one ## Worked example section for the Simple paradigm"
    - "docs/migration-v0.2-to-v0.3.md contains a Quick reference type-rename table covering Client → ChatModel + every renamed type and the alias-preserves-source-compatibility row"
    - "DEPRECATIONS.md exists at repo root listing every // Deprecated: symbol with target removal version v0.4.0 and a migration link"
    - "CHANGELOG.md has an `## [Unreleased]` section with `### Added` (new llm/ types) and `### Deprecated` (LegacyClient + companion types) subsections"
    - "CHANGELOG.md `### Added` section explicitly lists ChatModel, ToolCaller, Embedder, StructuredOutputs, StreamReader, StreamEvent, ProviderInfo, Capabilities, ScriptedLLM, ChatOnlyMock"
    - "Versioning policy is documented (or pointed to) in CHANGELOG.md so the v0.3.x track + sister-repo v0.1.x track is unambiguous (INFRA-07)"
  artifacts:
    - path: "docs/migration-v0.2-to-v0.3.md"
      provides: "Migration guide for v0.2 → v0.3 callers — one worked Simple-paradigm example + generic mapping table"
      contains: "## Worked example: Simple paradigm"
    - path: "DEPRECATIONS.md"
      provides: "Single-source-of-truth table: every Deprecated symbol → target removal version → migration link (Pitfall 15)"
      contains: "| Symbol | Deprecated In | Removed In | Migration |"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] section with Added (new types), Deprecated (LegacyClient + companions), and a Versioning subsection capturing INFRA-07 multi-repo policy"
      contains: "## [Unreleased]"
  key_links:
    - from: "llm/legacy.go (// Deprecated: comments)"
      to: "docs/migration-v0.2-to-v0.3.md"
      via: "comment text mentions the doc path"
      pattern: "docs/migration-v0\\.2-to-v0\\.3\\.md"
    - from: "DEPRECATIONS.md table rows"
      to: "docs/migration-v0.2-to-v0.3.md"
      via: "Migration column links to the guide"
      pattern: "migration-v0\\.2-to-v0\\.3\\.md"
    - from: "CHANGELOG.md ### Deprecated"
      to: "docs/migration-v0.2-to-v0.3.md"
      via: "bullet text references the guide"
      pattern: "migration-v0\\.2-to-v0\\.3"
---

<objective>
Produce the human-facing documentation that closes the deprecation loop for the v0.2 → v0.3 reboot done in plan `00-01`. The migration guide gives one worked example (Simple paradigm) plus a generic type-rename mapping table covering every public symbol. `DEPRECATIONS.md` becomes the single source of truth for "what is going away when" — the structural defence against Pitfall 15 (deprecation kept forever). `CHANGELOG.md` gets the `[Unreleased]` block with `### Added` (new types) + `### Deprecated` (LegacyClient + 5 companion types) plus a Versioning subsection capturing the INFRA-07 multi-repo policy.

Purpose: With the alias `type Client = LegacyClient` preserving source compatibility (plan 01), Phase 0 has zero functional pressure on existing callers — but we need to communicate "you don't HAVE to migrate now, but you DO need to before v0.4.0." Migration guide + DEPRECATIONS.md + CHANGELOG entry together form the contract: every Deprecated comment in code points at the migration guide; every IDE hover shows the v0.4.0 deadline; every release-bump grep against DEPRECATIONS.md surfaces the symbols that have hit their removal phase.

Output: 3 markdown files (1 new at `docs/migration-v0.2-to-v0.3.md`, 1 new at `DEPRECATIONS.md`, 1 modified at `CHANGELOG.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-keystone-interfaces/00-CONTEXT.md
@.planning/phases/00-keystone-interfaces/00-RESEARCH.md
@.planning/phases/00-keystone-interfaces/00-PATTERNS.md

# Source of truth for type renames + alias decisions (plan 01 output)
@llm/legacy.go
@llm/types.go
@llm/chatmodel.go
@llm/capabilities.go
@llm/stream.go
@llm/info.go
@llm/scripted.go
@llm/chat_only_mock.go

# Existing markdown style template
@CHANGELOG.md
@README.md
@example_simple_test.go

<interfaces>
<!-- These are the symbols Plan 02 documents. Pulled directly from plan 01's output. -->

Renamed / deprecated v0.2 symbols (from llm/legacy.go after plan 01):
- llm.Client       → llm.ChatModel    (LegacyClient alias retained; Client = LegacyClient)
- llm.LegacyClient → llm.ChatModel    (renamed; deprecated; removal v0.4.0)
- llm.GenerateRequest  → llm.Request
- llm.GenerateResponse → llm.Response
- llm.StreamChunk      → llm.StreamEvent (typed union, NOT a single value type)
- llm.StreamUsage      → llm.Usage      (with new UsageSource field)

NEW v0.3 symbols (from llm/*.go after plan 01):
- llm.ChatModel, llm.ToolCaller, llm.Embedder, llm.StructuredOutputs (capability interfaces)
- llm.StreamReader, llm.StreamEvent (+ Kind enum), llm.ToolCallDelta (typed streaming union)
- llm.ProviderInfo, llm.Capabilities (per-(provider × model) identity + feature struct)
- llm.Request, llm.Response (chat-layer request/response)
- llm.Vector, llm.Usage, llm.UsageSource (embeddings + token accounting)
- llm.ScriptedLLM (full-capability mock; promoted from _test.go), llm.ChatOnlyMock (capability-degrade mock)
- llm.ErrCapabilityNotSupported, llm.ErrScriptExhausted (sentinels)

UNCHANGED shape (shared between LegacyClient and ChatModel):
- llm.Tool, llm.Message, llm.FinishReason (alias to legacyFinishReason — same constants)
- llm.ToolCall (added optional ID field; v0.2 callers don't construct, only read)

Plan 01 acceptance: Client = LegacyClient alias means EVERY existing caller compiles unchanged through Phase 0. Migration is INFORMATIONAL in v0.3.0 and BECOMES MANDATORY at v0.4.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/migration-v0.2-to-v0.3.md (worked Simple example + generic type-rename mapping table)</name>

  <files>docs/migration-v0.2-to-v0.3.md (NEW)</files>

  <read_first>
    - example_simple_test.go (current — the canonical Simple-paradigm example whose v0.2 → v0.3 diff goes into the worked example)
    - simple.go (current — to confirm the v0.2 SimpleAgent constructor signature)
    - llm/legacy.go (after plan 01 — confirms which symbols are deprecated)
    - llm/scripted.go (after plan 01 — confirms NewScriptedLLM + WithProvider/WithModel/WithResponses + TextResponse)
    - .planning/phases/00-keystone-interfaces/00-RESEARCH.md §"Migration Playbook" lines 1129-1217 — exact outline + worked example body + internal user inventory
    - .planning/phases/00-keystone-interfaces/00-PATTERNS.md §"`docs/migration-v0.2-to-v0.3.md`" lines 511-547 — markdown style: ` ```diff `, ` ```go `, table style `|---|`, backticks around symbols
    - CHANGELOG.md lines 71-83 — existing v0.2.0 Migration block as the diff-style template
    - .planning/phases/00-keystone-interfaces/00-CONTEXT.md §"Claude's Discretion" — depth target: 1 worked example (Simple) + generic mapping table covering all type renames
  </read_first>

  <action>
Create the file at `docs/migration-v0.2-to-v0.3.md` with the EXACT body below. The directory `docs/` does not exist yet — create it (`mkdir -p docs`) before writing the file.

The structure is: H1 + intro paragraph → Quick reference table → Worked example (Simple paradigm, with three views: v0.2 baseline, v0.3 transitional with alias, v0.3 idiomatic post-Phase-3) → When to migrate → Capability-detection idiom (as a forward-looking section since agent paradigms migrate in Phase 3) → Notes on companion types (Tool/Message/FinishReason unchanged, ToolCall adds optional ID).

```markdown
# Migrating from v0.2 to v0.3

The v0.3 release adds capability-aware interfaces (`llm.ChatModel`, `llm.ToolCaller`,
`llm.Embedder`, `llm.StructuredOutputs`) alongside the existing `llm.Client` (now
`llm.LegacyClient`). The old surface remains callable through v0.3.x; **it will be
removed in v0.4.0**.

> **You do not have to migrate immediately.** `type Client = LegacyClient` is an
> alias — every existing caller compiles unchanged in v0.3.0. Migration becomes
> mandatory only at the v0.4.0 cut (one minor cycle later, per the project's
> dual-track BC policy).

## Quick reference: type renames

| v0.2 | v0.3 (new code) | v0.3 (legacy callers — alias) |
|---|---|---|
| `llm.Client` | `llm.ChatModel` | `llm.LegacyClient` (or `llm.Client`, alias) |
| `llm.Client.Generate(ctx, GenerateRequest) (GenerateResponse, error)` | `llm.ChatModel.Generate(ctx, Request) (Response, error)` | unchanged via alias |
| `llm.Client.GenerateStream(ctx, GenerateRequest) (<-chan StreamChunk, error)` | `llm.ChatModel.Stream(ctx, Request) (StreamReader, error)` | unchanged via alias |
| `llm.GenerateRequest` | `llm.Request` | unchanged via alias |
| `llm.GenerateResponse` | `llm.Response` | unchanged via alias |
| `llm.StreamChunk` (single channel value) | `llm.StreamEvent` (typed union with `Kind`) | unchanged via alias |
| `llm.StreamUsage` | `llm.Usage` (+ new `Source` field) | unchanged via alias |
| `llm.Tool` | `llm.Tool` (unchanged shape) | unchanged |
| `llm.ToolCall` (Name + Arguments) | `llm.ToolCall` (now: ID + Name + Arguments) | unchanged shape; `ID` is `omitempty` (back-compat) |
| `llm.Message` | `llm.Message` (unchanged shape) | unchanged |
| `llm.FinishReason` + 6 const | unchanged (alias to underlying type) | unchanged |
| (no equivalent) | `llm.ProviderInfo` returned by `Info()` | n/a — new in v0.3 |
| (no equivalent) | `llm.Capabilities` (struct on `ProviderInfo`) | n/a — new in v0.3 |
| (no equivalent) | `llm.ToolCaller`, `llm.Embedder`, `llm.StructuredOutputs` | n/a — new in v0.3 |
| (no equivalent) | `llm.ErrCapabilityNotSupported` | n/a — new in v0.3 |
| `scriptedllm_test.go` `scriptedLLM` (test helper) | `llm.ScriptedLLM` (production code; full-capability mock) | shim retained until Phase 3 |

## Worked example: Simple paradigm

The Simple paradigm is the smallest agent — it just forwards every prompt to the
LLM and returns the text. Both versions below are runnable.

### v0.2 (current — still works in v0.3.0 via alias)

\`\`\`go
import (
    "context"

    "github.com/costa92/llm-agent"
    "github.com/costa92/llm-agent/llm"
)

func main() {
    // scriptedllm.New here returns the v0.2 llm.Client contract.
    client := scriptedllm.New(scriptedllm.Text("hello, world"))

    agent := agents.NewSimpleAgent(client, agents.SimpleOptions{})
    res, err := agent.Run(context.Background(), "hi")
    _ = err
    println(res)
}
\`\`\`

### v0.3 transitional — no source change required

\`\`\`go
// SAME CODE WORKS. llm.Client is now an alias for llm.LegacyClient,
// and scriptedllm.New still returns the legacy contract for now.
// Compiles unchanged in v0.3.0.
import (
    "context"

    "github.com/costa92/llm-agent"
    "github.com/costa92/llm-agent/llm"
)

func main() {
    client := scriptedllm.New(scriptedllm.Text("hello, world"))
    agent := agents.NewSimpleAgent(client, agents.SimpleOptions{})
    res, _ := agent.Run(context.Background(), "hi")
    println(res)
}
\`\`\`

### v0.3 idiomatic — uses the new ChatModel directly

This shape becomes available after Phase 3 lands `agents.NewSimpleAgent(llm.ChatModel)`
(CORE-10 in the roadmap). Until then, stay on the legacy path above.

\`\`\`go
import (
    "context"

    "github.com/costa92/llm-agent"
    "github.com/costa92/llm-agent/llm"
)

func main() {
    model := llm.NewScriptedLLM(
        llm.WithProvider("scripted"),
        llm.WithModel("test-1"),
        llm.WithResponses(llm.TextResponse("hello, world")),
    )

    agent := agents.NewSimpleAgent(model, agents.SimpleOptions{}) // accepts ChatModel post-Phase 3
    res, _ := agent.Run(context.Background(), "hi")
    println(res)
}
\`\`\`

## Capability detection (forward-looking)

In v0.3 the canonical idiom for capability-dependent code paths is type assertion
PLUS a `Capabilities` runtime check — both are required because the Go type may
implement an interface (e.g., Ollama always implements `ToolCaller`) while the
bound model does not actually support the feature (`llama2` returns
`Capabilities.Tools == false`). This is the K2 keystone of v0.3.

\`\`\`go
if tc, ok := model.(llm.ToolCaller); ok && model.Info().Capabilities.Tools {
    bound, err := tc.WithTools(tools)
    if err != nil { return err }
    return bound.Generate(ctx, req)
}
// Fall back: scratchpad templating, or return ErrCapabilityNotSupported.
return model.Generate(ctx, scratchpadReq(req))
\`\`\`

Phase 3 (CORE-10) refactors `agents.NewReActAgent`, `agents.NewFunctionCallAgent`,
etc., to use this idiom. v0.2 callers don't need to do anything.

## Streaming

The v0.3 streaming contract uses `llm.StreamReader` (iterator-style: `Next + Close`)
and emits a typed `llm.StreamEvent` union with a `Kind` enum (`EventTextDelta` /
`EventToolCallStart` / `EventToolCallArgsDelta` / `EventToolCallEnd` /
`EventThinkingDelta` / `EventDone`). Adapters (Phase 2) emit their NATIVE
granularity; consumers that want a flat `Response` can call
`llm.AccumulateStream(sr)`.

The v0.2 `<-chan StreamChunk` shape remains via `LegacyClient.GenerateStream`.

## When to migrate

- **v0.3.0 (now):** new provider adapter authors target `llm.ChatModel` directly.
  Existing callers do nothing — alias preserves compilation.
- **v0.3.x (Phase 3 / CORE-10):** internal agents migrate to `llm.ChatModel`.
  Examples + tests follow.
- **v0.4.0 (one minor cycle after v0.3.0 ships):** `llm.Client`,
  `llm.LegacyClient`, `llm.GenerateRequest`, `llm.GenerateResponse`,
  `llm.StreamChunk`, `llm.StreamUsage` removed. All remaining callers MUST
  migrate before this tag.

The full timeline + every Deprecated symbol → target version mapping lives in
[`DEPRECATIONS.md`](../DEPRECATIONS.md) at the repo root.

## Notes on shared / unchanged types

These types KEEP THEIR v0.2 SHAPE in v0.3 — both the legacy and new surfaces use
the same declarations from `llm/types.go`:

- `llm.Tool` — unchanged. Same `Name` / `Description` / `Parameters` fields.
- `llm.Message` — unchanged. Same `Role` / `Content` fields.
- `llm.FinishReason` + 6 constants — unchanged. The new `FinishReason` is a Go
  type alias to the legacy underlying type, so `llm.FinishReasonStop` is the same
  value in both surfaces.
- `llm.ToolCall` — adds an optional `ID string` field (used by Phase 3's tool
  dedupe layer keyed by `(message_id, tool_use_id)`). The field is `omitempty`,
  so v0.2 callers continue to work; only the LLM-side populates `ID`.

Sharing reduces churn and avoids two parallel type systems. If a future v0.4
release evolves any of these shapes, that's the migration window — it doesn't
affect v0.3.x.

---

Last updated: 2026-05-10 (Phase 0 of the v0.3 milestone).
```

Important details:
- The fenced code blocks for the worked example use ` ```go ` (NOT ` ```diff `) — Per RESEARCH.md the worked example shows three full files rather than line-level diffs, because each example is small and self-contained. The PATTERNS.md ` ```diff ` style is for the future migration of the agent paradigm internals (which is out of Phase 0 scope).
- The two relative links ([`DEPRECATIONS.md`](../DEPRECATIONS.md)) point UP from `docs/` to the repo root — the migration guide lives in `docs/`, DEPRECATIONS.md lives at the repo root.
- The Quick reference table uses three columns matching the format shown in RESEARCH.md §"Migration Playbook".
  </action>

  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent && \
      test -f docs/migration-v0.2-to-v0.3.md && \
      grep -q '^## Worked example: Simple paradigm$' docs/migration-v0.2-to-v0.3.md && \
      grep -q '^## Quick reference: type renames$' docs/migration-v0.2-to-v0.3.md && \
      grep -q '`llm.Client`' docs/migration-v0.2-to-v0.3.md && \
      grep -q '`llm.ChatModel`' docs/migration-v0.2-to-v0.3.md && \
      grep -q '`llm.ToolCaller`' docs/migration-v0.2-to-v0.3.md && \
      grep -q '`llm.Embedder`' docs/migration-v0.2-to-v0.3.md && \
      grep -q '`llm.StructuredOutputs`' docs/migration-v0.2-to-v0.3.md && \
      grep -q 'v0.4.0' docs/migration-v0.2-to-v0.3.md && \
      grep -q 'NewScriptedLLM' docs/migration-v0.2-to-v0.3.md && \
      grep -q 'DEPRECATIONS.md' docs/migration-v0.2-to-v0.3.md
    </automated>
  </verify>

  <done>
    - `docs/migration-v0.2-to-v0.3.md` exists with the bilingual-free, en-only structure described above (~110 lines).
    - At least one `## Worked example` section exists (Simple paradigm) showing v0.2 baseline + v0.3 transitional + v0.3 idiomatic.
    - The Quick reference table covers every renamed type AND the unchanged-shape companion types (Tool, Message, FinishReason, ToolCall).
    - Forward-looking sections (capability detection, streaming) document patterns that land fully in Phase 3 / Phase 2 — Phase 0 is informational.
    - Cross-link to `DEPRECATIONS.md` (relative path `../DEPRECATIONS.md` since the guide lives in `docs/`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DEPRECATIONS.md at repo root (single source of truth for symbol → removal version)</name>

  <files>DEPRECATIONS.md (NEW)</files>

  <read_first>
    - llm/legacy.go (after plan 01 — confirms every // Deprecated: symbol)
    - .planning/phases/00-keystone-interfaces/00-PATTERNS.md §"`DEPRECATIONS.md`" lines 549-566 — recommended table shape (Symbol | Deprecated In | Removed In | Migration)
    - .planning/phases/00-keystone-interfaces/00-RESEARCH.md §"File-by-file breakdown" — DEPRECATIONS.md is NEW (Pitfall 15 enforcement)
    - README.md lines 60-77 — existing markdown table style (`|---|`, no inner padding)
  </read_first>

  <action>
Create `DEPRECATIONS.md` at the REPO ROOT (not under `docs/`). The file is the single source of truth for "what is deprecated and when does it disappear" — Phase 7 (`/gsd-execute-phase 7`) will grep this file to know which symbols to remove.

```markdown
# Deprecations

This file is the single source of truth for **what is deprecated and when it disappears**.
Every symbol with a `// Deprecated:` godoc comment in this repo MUST appear here with a
target removal version. Phase 7 of the v0.3 → v0.4 cycle audits this list and removes
each row whose `Removed In` matches the in-flight tag.

> Why this file exists: Pitfall 15 of the v0.3 research bundle (`deprecation kept forever`).
> The `// Deprecated:` comment surfaces in IDE hover, but is invisible at release-tagging
> time — a separate file forces a sweep before every minor cut.

## Active deprecations

| Symbol | Deprecated In | Removed In | Migration |
|---|---|---|---|
| `llm.Client` (interface) | v0.3.0 | v0.4.0 | Use `llm.ChatModel`. See [migration guide](docs/migration-v0.2-to-v0.3.md). The `type Client = LegacyClient` alias preserves source compatibility through v0.3.x. |
| `llm.LegacyClient` (interface) | v0.3.0 | v0.4.0 | Use `llm.ChatModel`. See [migration guide](docs/migration-v0.2-to-v0.3.md). |
| `llm.GenerateRequest` (struct) | v0.3.0 | v0.4.0 | Use `llm.Request`. See [migration guide](docs/migration-v0.2-to-v0.3.md). |
| `llm.GenerateResponse` (struct) | v0.3.0 | v0.4.0 | Use `llm.Response`. See [migration guide](docs/migration-v0.2-to-v0.3.md). |
| `llm.StreamChunk` (struct) | v0.3.0 | v0.4.0 | Use `llm.StreamEvent` (typed union with `Kind` enum). See [migration guide](docs/migration-v0.2-to-v0.3.md). |
| `llm.StreamUsage` (struct) | v0.3.0 | v0.4.0 | Use `llm.Usage` (now includes `Source` field for K4 cost record). See [migration guide](docs/migration-v0.2-to-v0.3.md). |
| `agents.scriptedLLM` (root-package test helper) | v0.3.0 | Phase 3 (~v0.3.3) | Use `llm.NewScriptedLLM(...)` directly. The test-helper shim in `scriptedllm_test.go` exists only because agent paradigms still consume the v0.2 `llm.Client` contract; Phase 3 (CORE-10) migrates them to `llm.ChatModel` and the shim goes away. |

## Removed (historical)

*(none yet — first removal scheduled for v0.4.0)*

## Adding new deprecations

When you add a `// Deprecated:` comment to any public symbol:

1. Add a row to the **Active deprecations** table above with the exact symbol path,
   the version that introduced the deprecation, the target removal version, and a
   migration link.
2. Use this exact godoc format on the symbol so `gopls` and `staticcheck` can warn:
   ```
   // Deprecated: <use what instead>. Will be removed in vX.Y.Z. See <doc link>.
   ```
3. The `Removed In` column MUST point at a real planned release. Vague targets
   ("future", "TBD") are forbidden — they encode Pitfall 15.

## Removal procedure

When a tag matches an `Removed In` value:

1. Confirm `git grep -n '<symbol>' -- ':!DEPRECATIONS.md' ':!CHANGELOG.md' ':!docs/'`
   returns zero internal users.
2. Delete the symbol declaration AND its godoc comment.
3. Move the row from **Active deprecations** to **Removed (historical)**, adding
   the actual removal commit SHA.
4. Add a `### Breaking` entry to `CHANGELOG.md` for the release.
5. Bump sister-repo `require github.com/costa92/llm-agent` lines in coordinated tags.

---

Last updated: 2026-05-10 (Phase 0 of the v0.3 milestone — first entry).
```

The file:
- Uses 4-column markdown tables with `|---|---|---|---|` separator (no inner padding) per repo style.
- Symbol column carries backticks for IDE link resolution.
- Migration column links to `docs/migration-v0.2-to-v0.3.md` (relative from the repo root) — note this path differs from the migration guide's relative-back link `../DEPRECATIONS.md`.
- Includes a "Removal procedure" section so the file is self-documenting for Phase 7.
- Lists the `agents.scriptedLLM` test-helper shim (`scriptedllm_test.go`) in addition to the public `llm.*` symbols, because that shim is also `// Deprecated:` per plan 01 Task 7 — and the removal target is Phase 3, not v0.4.0.
  </action>

  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent && \
      test -f DEPRECATIONS.md && \
      grep -q '^| Symbol | Deprecated In | Removed In | Migration |$' DEPRECATIONS.md && \
      grep -q '`llm.Client`' DEPRECATIONS.md && \
      grep -q '`llm.LegacyClient`' DEPRECATIONS.md && \
      grep -q '`llm.GenerateRequest`' DEPRECATIONS.md && \
      grep -q '`llm.GenerateResponse`' DEPRECATIONS.md && \
      grep -q '`llm.StreamChunk`' DEPRECATIONS.md && \
      grep -q '`llm.StreamUsage`' DEPRECATIONS.md && \
      grep -q 'v0.4.0' DEPRECATIONS.md && \
      grep -q 'docs/migration-v0.2-to-v0.3.md' DEPRECATIONS.md && \
      grep -q '## Removal procedure' DEPRECATIONS.md
    </automated>
  </verify>

  <done>
    - `DEPRECATIONS.md` exists at repo root (NOT under `docs/`).
    - All 6 deprecated `llm.*` public symbols are listed (Client + LegacyClient + GenerateRequest + GenerateResponse + StreamChunk + StreamUsage), each pointing at v0.4.0 removal target and the migration guide.
    - `agents.scriptedLLM` shim is also listed with target Phase 3 removal — different from v0.4.0 because plan 01 Task 7 made it a thin shim that goes away with the agent-paradigm refactor.
    - "Removal procedure" section is self-documenting so Phase 7 doesn't need to re-derive the workflow.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add ## [Unreleased] section to CHANGELOG.md (Added + Deprecated + Versioning subsections; INFRA-07)</name>

  <files>CHANGELOG.md (MODIFIED)</files>

  <read_first>
    - CHANGELOG.md (current — entire file; the new section is INSERTED above the existing v0.2.0 entry)
    - .planning/phases/00-keystone-interfaces/00-PATTERNS.md §"`CHANGELOG.md` (modified)" lines 568-602 — `## [vX.Y.Z] — YYYY-MM-DD` heading style; `### Added | Changed | Deprecated | Removed | Fixed | Security | Breaking` keep-a-changelog conventions
    - llm/*.go (after plan 01 — full inventory of new types to enumerate in `### Added`)
    - DEPRECATIONS.md (after Task 2 — same symbols enumerated in `### Deprecated`)
    - .planning/REQUIREMENTS.md INFRA-07 — versioning policy across 4 repos
  </read_first>

  <action>
Modify `CHANGELOG.md` by INSERTING a new `## [Unreleased]` section between the file header (the `<!--` HTML comments) and the existing `## [v0.2.0]` heading. The existing v0.1.0 and v0.2.0 entries are PRESERVED VERBATIM.

The location to insert: after line 11 (the last HTML comment about Breaking changes) and before the existing `## [v0.1.0] — 2026-04-28` heading. The current file actually has v0.2.0 BEFORE v0.1.0 in display order? Re-check by reading the file — but the existing pattern is that the most recent release comes first. The new `## [Unreleased]` section becomes the topmost release block.

After inserting, the new section to add reads (use Edit tool, insert before the most recent existing release heading):

```markdown
## [Unreleased]

Phase 0 of the v0.3 milestone — multi-repo infra + capability-aware `llm/` interfaces.
The new `llm/` types coexist with the v0.2 `llm.Client` surface (now renamed to
`llm.LegacyClient` with a `type Client = LegacyClient` alias for source compatibility).
Existing callers continue to compile unchanged.

### Added

- New capability-aware interfaces in `llm/`:
  - `llm.ChatModel` — base contract (`Generate` + `Stream` + `Info`)
  - `llm.ToolCaller` — capability for native function-calling (`WithTools`, immutable)
  - `llm.Embedder` — capability for vector embeddings (NOT embedding `ChatModel`)
  - `llm.StructuredOutputs` — capability for JSON-schema-constrained generation
- Typed streaming union in `llm/stream.go`:
  - `llm.StreamReader` — iterator-style streaming (`Next` + `Close`)
  - `llm.StreamEvent` + `llm.StreamEventKind` — typed union with `EventTextDelta`,
    `EventToolCallStart`, `EventToolCallArgsDelta`, `EventToolCallEnd`,
    `EventThinkingDelta`, `EventDone`
  - `llm.ToolCallDelta` — per-tool-call streaming state with stable `Index` field
  - `llm.AccumulateStream` — convenience for consumers that want a flat `Response`
- Per-(provider × model) identity:
  - `llm.ProviderInfo` — bound provider+model identity returned by `Info()`
  - `llm.Capabilities` — JSON-serializable feature struct (`Tools`, `Embeddings`,
    `StructuredOutputs`, `PromptCaching` bool fields)
- New chat-layer request/response types:
  - `llm.Request` (replaces `GenerateRequest`)
  - `llm.Response` (replaces `GenerateResponse`)
  - `llm.Vector` (`[]float32`)
  - `llm.Usage` + `llm.UsageSource` (`Reported` / `Estimated` / `Unknown`) for
    K4 three-state cost tracking
- New mocks (promoted from `_test.go`):
  - `llm.ScriptedLLM` — full-capability deterministic mock with functional options
    (`WithProvider`, `WithModel`, `WithCapabilities`, `WithResponses`,
    `WithEmbedDimensions`); helpers `TextResponse`, `ToolCallResponse`
  - `llm.ChatOnlyMock` — `ChatModel`-only mock for capability-degradation testing
- Sentinel errors:
  - `llm.ErrCapabilityNotSupported` — wrap with `fmt.Errorf("...: %w", ...)`
  - `llm.ErrScriptExhausted` — emitted by `ScriptedLLM` when the script runs out
- Migration guide at `docs/migration-v0.2-to-v0.3.md` with one worked Simple-paradigm
  example + a generic type-rename mapping table.
- `DEPRECATIONS.md` at repo root — single source of truth for symbol → target
  removal version; Phase 7 audits this file before the v0.4 cut (Pitfall 15).

### Deprecated

The following symbols are retained for v0.3.x source compatibility but will be
**removed in v0.4.0**. See [`docs/migration-v0.2-to-v0.3.md`](docs/migration-v0.2-to-v0.3.md)
for migration steps; full table in [`DEPRECATIONS.md`](DEPRECATIONS.md).

- `llm.Client` (interface) — now an alias for `llm.LegacyClient`. Use `llm.ChatModel`.
- `llm.LegacyClient` (interface) — renamed from `llm.Client`. Use `llm.ChatModel`.
- `llm.GenerateRequest` (struct) — use `llm.Request`.
- `llm.GenerateResponse` (struct) — use `llm.Response`.
- `llm.StreamChunk` (struct) — use `llm.StreamEvent` (typed union).
- `llm.StreamUsage` (struct) — use `llm.Usage` (with `Source` field).
- `agents.scriptedLLM` (root-package test helper) — use `llm.NewScriptedLLM`.
  Removed in Phase 3 (~v0.3.3) once agent paradigms migrate to `llm.ChatModel`.

### Versioning policy (INFRA-07)

The v0.3 milestone covers a 4-repo umbrella:

| Repo | v0.3 track | Notes |
|---|---|---|
| `github.com/costa92/llm-agent` (this repo) | `v0.3.x` | Stdlib-only core. Pre-release tag `v0.3.0-pre.1` cut at end of Phase 0; final `v0.3.0` after Phase 6. |
| `github.com/costa92/llm-agent-providers` | `v0.1.x` | Sister repo created in Phase 0; first content lands in Phase 1. |
| `github.com/costa92/llm-agent-otel` | `v0.1.x` | Sister repo created in Phase 0; first content lands in Phase 5. |
| `github.com/costa92/llm-agent-customer-support` | `v0.1.x` | Sister repo created in Phase 0; first content lands in Phase 6. |

- 0.x BC policy applies per repo: minor/patch within a 0.x line are BC-compatible;
  0.x→0.y (y>x) may break. Each repo declares breaking changes with a `### Breaking`
  section in its CHANGELOG.
- Sister repos pin `require github.com/costa92/llm-agent vX.Y.Z` per
  Phase / sister-repo release; coordinated tags during the v0.4 cut (Phase 7).
- `replace` directives are FORBIDDEN on any branch matching `release/**` —
  enforced by the `release-precheck` CI workflow in every repo.
- `go.work` is `.gitignore`d in every repo; CI runs with `GOWORK=off`.

```

After inserting, save the file. The existing `## [v0.2.0]` and `## [v0.1.0]` blocks remain UNCHANGED below the new section.

Verify:
- The new section is now the FIRST `## [...]` heading after the file's leading HTML comments.
- The entire CHANGELOG remains valid markdown (no broken tables, no orphan code fences).
- The relative links (`docs/migration-v0.2-to-v0.3.md`, `DEPRECATIONS.md`) work from the repo root.
  </action>

  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent && \
      test -f CHANGELOG.md && \
      grep -q '^## \[Unreleased\]$' CHANGELOG.md && \
      grep -q '^### Added$' CHANGELOG.md && \
      grep -q '^### Deprecated$' CHANGELOG.md && \
      grep -q '^### Versioning policy' CHANGELOG.md && \
      grep -q '`llm.ChatModel`' CHANGELOG.md && \
      grep -q '`llm.ToolCaller`' CHANGELOG.md && \
      grep -q '`llm.Embedder`' CHANGELOG.md && \
      grep -q '`llm.StructuredOutputs`' CHANGELOG.md && \
      grep -q '`llm.StreamReader`' CHANGELOG.md && \
      grep -q '`llm.StreamEvent`' CHANGELOG.md && \
      grep -q '`llm.ProviderInfo`' CHANGELOG.md && \
      grep -q '`llm.Capabilities`' CHANGELOG.md && \
      grep -q '`llm.ScriptedLLM`' CHANGELOG.md && \
      grep -q '`llm.ChatOnlyMock`' CHANGELOG.md && \
      grep -q 'v0.3.0-pre.1' CHANGELOG.md && \
      grep -q 'v0.4.0' CHANGELOG.md && \
      grep -q 'replace' CHANGELOG.md && \
      grep -q 'GOWORK=off' CHANGELOG.md && \
      grep -q 'docs/migration-v0.2-to-v0.3.md' CHANGELOG.md && \
      grep -q 'DEPRECATIONS.md' CHANGELOG.md && \
      grep -q '## \[v0.2.0\]' CHANGELOG.md && \
      grep -q '## \[v0.1.0\]' CHANGELOG.md
    </automated>
  </verify>

  <done>
    - `CHANGELOG.md` has a new `## [Unreleased]` section with three subsections (`### Added`, `### Deprecated`, `### Versioning policy`).
    - `### Added` enumerates all new public types from plan 01 (10+ exported symbols).
    - `### Deprecated` lists the 6 deprecated `llm.*` symbols + the `agents.scriptedLLM` shim.
    - `### Versioning policy` ratifies INFRA-07 (4-repo version tracks, BC policy per repo, replace ban + GOWORK=off invariants).
    - Existing v0.1.0 and v0.2.0 entries are preserved verbatim below the new section.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public documentation surface (`docs/migration-v0.2-to-v0.3.md`, `DEPRECATIONS.md`, `CHANGELOG.md`) | These files are the contract callers READ to plan their own migration. A factually wrong claim (e.g., "v0.2 X is now Y" when actually the rename was Z) silently breaks downstream code. |
| Cross-link integrity | Each file links to the others via relative paths; broken links break IDE/web navigation but not CI. The grep verifications catch path typos at plan-execution time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | Information Disclosure | `docs/migration-v0.2-to-v0.3.md` | accept | The migration guide is intentionally public and complete; all symbols documented are already exported (visible in godoc). No PII, no secrets. Public-by-design. |
| T-00-02-02 | Repudiation | `DEPRECATIONS.md` table drift | mitigate | The "Removal procedure" section requires every new `// Deprecated:` comment to add a row here BEFORE the comment lands. Phase 7 audits this file as the source of truth — anything not listed is not removed. The grep verify in Task 2 asserts that all 6 expected `llm.*` symbols + the shim are present (will fail if a row is omitted). |
| T-00-02-03 | Tampering | `CHANGELOG.md` accidentally drops historical entries | mitigate | Task 3 INSERTS the new `[Unreleased]` block above the existing release headings; the verify step asserts both `## [v0.2.0]` and `## [v0.1.0]` headings still exist post-edit. A well-meaning rewrite that drops history fails CI. |
| T-00-02-04 | Architectural | Missing forward-link from `// Deprecated:` godoc to migration guide | mitigate | Plan 01 Task 1 already enforces the exact comment format `// Deprecated: ... See docs/migration-v0.2-to-v0.3.md.` This plan creates the linked document. The two plans together close the loop: comment exists in plan 01, linked target exists in plan 02. |
| T-00-02-05 | Information Disclosure | Versioning policy section reveals release process | accept | INFRA-07 requires this be public; it's the contract sister-repo authors and downstream users rely on. Public-by-design; nothing sensitive (no secrets, no internal infra detail). |
</threat_model>

<verification>
- All three files exist and pass their automated grep checks.
- `git diff --stat CHANGELOG.md` shows additions only (no deletions of historical content).
- `markdown-it`/`mdformat` (if available locally) parses each file without error — `pandoc -f gfm -o /dev/null docs/migration-v0.2-to-v0.3.md DEPRECATIONS.md CHANGELOG.md` succeeds (pandoc may not be available in CI; manual gate).
- The relative links resolve correctly: `docs/migration-v0.2-to-v0.3.md` references `../DEPRECATIONS.md`; `DEPRECATIONS.md` references `docs/migration-v0.2-to-v0.3.md`; `CHANGELOG.md` references both via root-relative paths.
- `go vet ./... && go build ./... && go test ./... -count=1` remain green (this plan touches only markdown — Go tests are unaffected).
</verification>

<success_criteria>
1. `docs/migration-v0.2-to-v0.3.md` exists with 1 worked example (Simple paradigm) + a Quick reference type-rename table covering ALL renamed and unchanged-shape types from plan 01.
2. `DEPRECATIONS.md` at repo root has the 4-column table with all 6 `llm.*` deprecated symbols + the `agents.scriptedLLM` shim, each pointing at a target removal version (v0.4.0 or Phase 3) and the migration guide.
3. `CHANGELOG.md` has a `## [Unreleased]` section with `### Added`, `### Deprecated`, and `### Versioning policy` subsections. The Added subsection enumerates every new public type from plan 01 (≥ 10 symbols). Existing v0.1.0 + v0.2.0 entries preserved verbatim.
4. Cross-links between the three files resolve correctly (relative paths verified by the grep checks in each task).
5. INFRA-07 (versioning policy across 4 repos) is captured in CHANGELOG.md `### Versioning policy`.
6. CORE-09 (migration guide with worked example) is satisfied by `docs/migration-v0.2-to-v0.3.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/00-keystone-interfaces/00-02-SUMMARY.md` capturing:
- The exact file paths created/modified and their LOC counts
- Confirmation that `go test ./... -count=1` remains green (this plan touches only markdown)
- Cross-link validation result (each forward link resolves)
- Note that any future addition of a `// Deprecated:` comment elsewhere in the repo MUST add a corresponding DEPRECATIONS.md row (this is now process, not just a one-time deliverable)
</output>
