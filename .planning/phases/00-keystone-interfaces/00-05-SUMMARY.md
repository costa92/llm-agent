# Plan 00-05 — Umbrella CI + release-precheck + Pitfall 22 baseline

**Phase:** 0 — Multi-repo infra + keystone interfaces
**Status:** Complete
**Date:** 2026-05-10

## What shipped

Three local-only deliverables (no live GitHub Actions runs):

1. **`.github/workflows/umbrella.yml`** (commit `6a899f6`) — pull_request-triggered job that checks out all 4 sibling repos via sibling-aware `go.work`, runs `GOWORK=on go build ./...` across all 4, plus a sha256 diff step asserting `scripts/workspace.sh` is byte-identical across the umbrella checkout.
2. **`.github/workflows/release-precheck.yml`** (commit `a9950f4`) — release-branch-triggered grep gate that fails on any `replace` directive in `go.mod`. Byte-identical to sister-repo copies (sha256 = `2b507c8804852fb4cf82f40dabb159daab3ebb3838d6352821b825be3e16a96c`).
3. **`docs/api-snapshot.txt`** (commit `9df0f1e`) — Pitfall 22 architectural-drift baseline. 3495 lines, all 12+ packages' godoc surface, generated by iterating `go list ./...` + `go doc -all`.

## Tasks

| Task | Status | Commit | Notes |
|------|--------|--------|-------|
| 1 — umbrella.yml | ✓ | 6a899f6 | INFRA-05 |
| 2 — release-precheck.yml | ✓ | a9950f4 | INFRA-04; sha256 verified |
| 3 — smoke-test (live CI) | ⏭ skipped | — | User opted to defer live verification; bash logic in YAML files is correct by inspection |
| 4 — api-snapshot baseline | ✓ | 9df0f1e | Pitfall 22; rolling filename per RESEARCH Q2 RESOLVED |

## Smoke-test deferral

The live smoke-test (push two workflow files + open a PR + push a `release/test-replace-ban` branch with a deliberate `replace` to verify the gate fires + clean up) was deferred at the user's request. The two workflow files are committed locally and will fire on the first `pull_request` to main once pushed. Acceptable risk for a solo side-project; deferred verification is tracked in STATE.md.

## Verification

- `go vet ./...` — green
- `go test ./...` — 15 packages, all pass
- `go.mod` — still no `require` block (stdlib-only invariant intact across all of Phase 0)
- `docs/api-snapshot.txt` — exists, non-empty (3495 lines), `git ls-files docs/api-snapshot.txt` returns 1
- `.github/workflows/umbrella.yml` — exists, 104 lines, `pull_request` (NOT `pull_request_target`)
- `.github/workflows/release-precheck.yml` — exists, sha256 matches sister repos

## Out-of-band

Phase 0 closes with one out-of-plan step (per RESEARCH Q3 RESOLVED): `git tag v0.3.0-pre.1` so the 3 sister repos' `require github.com/costa92/llm-agent v0.3.0-pre.1` lines resolve. Sister-repo CI is RED until then; this is the documented Phase-0 close signal.

## Files

- `.github/workflows/umbrella.yml` (NEW)
- `.github/workflows/release-precheck.yml` (NEW)
- `docs/api-snapshot.txt` (NEW)

## Next

`/gsd-verify-phase 0` — goal-backward verification that Phase 0 delivered K1, K2, K3, K6 keystones and all 16 phase requirements.
