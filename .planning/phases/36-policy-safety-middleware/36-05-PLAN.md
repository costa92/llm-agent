---
phase: 36-policy-safety-middleware
plan: 05
type: execute
wave: 5
repo: llm-agent (core)
depends_on: ["36-01", "36-02", "36-03", "36-04"]
files_modified: []
autonomous: false
requirements: [CC-2]
must_haves:
  truths:
    - "phase exit gate passes: go vet + go test + go test -race all green repo-wide"
    - "go list -deps ./policy/ shows only stdlib + github.com/costa92/llm-agent/llm — no third-party module added"
    - "go.mod and go.sum unchanged from pre-36 state — no new require, no go.sum file created"
    - "KC-5 holds: no edit to llm.ChatModel / agents.Agent / memory.Memory / orchestrate.NodeFunc[S] / agent_chatmodel.go's generateFromPrompt body or signature"
    - "no edit to any v0.6.0 paradigm file (simple.go / react.go / reflection.go / plan_solve.go / function_call.go)"
    - "examples/07-policy runs deterministically (no network, exits 0)"
    - "tag v0.6.1 from main is achievable (operator-gated push)"
  artifacts: []
  key_links: []
---

<objective>
Phase 36 exit gate. Verifies that the `policy` package shipped by
slices 36-01 through 36-04 is production-ready: stdlib-only,
race-clean, no edit to validated public types, no `go.mod`/`go.sum`
churn, every CC-2 requirement met. After the automated checks pass,
the operator tags `v0.6.1` from `main` — the second v1.2 patch tag
(`v0.6.0 → v0.6.1` per the 2026-05-21 retargeted versioning plan).
CC-2.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. This slice does NOT modify Go source — it verifies the
  state produced by 36-01 / 36-02 / 36-03 / 36-04 and gates the tag.
- **Target tag: `v0.6.1`** (patch — additive `policy` sub-package; KC-5
  preserved). Per the 2026-05-21 ROADMAP retarget, the v1.2 milestone
  cap tag is now `v0.7.0` (Phase 38), so Phase 36 lands on `v0.6.1`
  (patch). The tag is strict-additive: existing `v0.6.0` callers
  compile unchanged against `v0.6.1`.
- **Operator-gated push.** Following the Phase 35 close convention,
  the actual `git tag` + `git push` commands are operator-run (this
  slice records the commands but does NOT execute the push). The
  human-verify checkpoint at the end of this slice is the gate.
- **Exit-gate checks** (lifted from 36-RESEARCH.md §"Slice Breakdown"
  row 36-05 and §"Validation Architecture" §"Sampling Rate"):
  1. `go vet ./...` — repo-wide, no warnings.
  2. `go test ./... -count=1` — repo-wide, all green.
  3. `go test -race ./policy/... -count=1` — race-clean.
  4. `go list -deps ./policy/...` — zero third-party modules.
  5. `git diff main -- go.mod go.sum` — empty (no churn).
  6. `git diff main -- llm/` — empty (no edits to validated public
     types).
  7. `git diff main -- agent.go agents.go simple.go react.go
     reflection.go plan_solve.go function_call.go memory/
     orchestrate/` — empty (no edits to paradigm files / memory /
     orchestrate per KC-5).
  8. `git diff main -- agent_chatmodel.go` — empty (Phase 35 wired
     the budget chokepoint; Phase 36 does NOT re-touch it).
  9. `go run ./examples/07-policy` — deterministic 0-exit.
  10. `go list -m all` — module set unchanged (only
      `github.com/costa92/llm-agent-rag v1.0.1`).
- **Files touched across the phase** (verification target list — every
  file from 36-01 through 36-04):
  - `policy/doc.go` (36-01)
  - `policy/gate.go` + `policy/gate_test.go` (36-01)
  - `policy/policy.go` + `policy/policy_test.go` (36-01)
  - `policy/patterns.go` (36-02)
  - `policy/pii.go` + `policy/pii_test.go` (36-02)
  - `policy/injection.go` + `policy/injection_test.go` (36-02)
  - `policy/length.go` + `policy/length_test.go` (36-02)
  - `policy/integration_test.go` (36-03)
  - `examples/07-policy/main.go` + `examples/07-policy/README.md` (36-04)
  Plus phase-planning artifacts under
  `.planning/phases/36-policy-safety-middleware/` (5 PLAN.md + 5
  SUMMARY.md). Nothing else.
- **Tag protocol** (mirrors Phase 35 close — verified via
  `git tag --list 'v*'` showing the v0.6.0 cut):
  - Verify `main` is at the tip the operator wants to tag.
  - Verify all 4 slice SUMMARYs exist:
    `36-01-SUMMARY.md ... 36-04-SUMMARY.md`.
  - Verify `git status` is clean (no uncommitted changes).
  - Operator runs: `git tag -a v0.6.1 -m "v0.6.1: policy package (CC-2)"`
    then `git push origin v0.6.1`.
  - DO NOT update CHANGELOG.md in this slice — CHANGELOG is touched
    only at v1.2's milestone close (Phase 38), per the Phase 35
    convention (the `v0.6.0` cut also did NOT touch CHANGELOG; STATE.md
    confirms).
- **STATE.md update is OUT of this slice's scope** — that's the
  operator's `/gsd-transition` slice or a separate post-phase
  housekeeping commit. This slice's `<output>` produces an audit
  summary; STATE.md flipping happens elsewhere.
- See `36-RESEARCH.md` §"Slice Breakdown" row 36-05 (the exit-gate
  command list) + §"Project Constraints (from CLAUDE.md)" (the KC-5 /
  stdlib-only / no-replace constraint list to verify).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated exit-gate checks</name>
  <files></files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-RESEARCH.md` §"Slice Breakdown" row 36-05 (the canonical command list)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/35-budget-and-cancellation-context/35-04-PLAN.md` (the Phase 35 exit-gate slice — same shape this slice mirrors)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/CLAUDE.md` (hard rules — verify each holds)
  </read_first>
  <behavior>
    - Every command in the exit-gate list runs successfully and emits the expected zero/empty output.
    - The 21 compile-time interface assertions from 36-01 still hold (verified implicitly by `go vet`).
    - `go test -race ./policy/...` reveals no data races.
    - `go list -deps ./policy/` enumerates only the stdlib + `github.com/costa92/llm-agent/llm` (the in-repo dependency).
  </behavior>
  <action>
    Run each of the 10 exit-gate commands sequentially. Capture stdout
    + stderr. For each, record PASS / FAIL with the actual output line
    that justifies the verdict. Use the following exact commands:

    1. **Repo-wide vet:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
       PASS if exit code 0 and no warnings emitted.

    2. **Repo-wide test:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
       PASS if exit code 0 and the final line is `ok` (or per-package `ok` for every package).

    3. **Race-clean on policy:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race ./policy/... -count=1`
       PASS if exit code 0 and no `DATA RACE` line in the output.

    4. **Stdlib-only (zero non-stdlib imports outside the in-repo `llm` and `budget` packages):**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -f '{{join .Imports "\n"}}' ./policy/ | sort -u | grep -v '^$' | grep -vE '^(context|errors|fmt|io|regexp|strings|sync|sync/atomic|testing|time|unicode/utf8|github.com/costa92/llm-agent(/(llm|budget))?)$'`
       PASS if the command produces zero lines of output.

    5. **go.mod / go.sum unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- go.mod go.sum | wc -l`
       PASS if output is `0`.

    6. **KC-5 — llm/ unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- llm/ | wc -l`
       PASS if output is `0`.

    7. **KC-5 — paradigm files unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/ | wc -l`
       PASS if output is `0`.

    8. **KC-5 — Phase 35 chokepoint unchanged:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- agent_chatmodel.go | wc -l`
       PASS if output is `0`.

    9. **Example runs deterministically:**
       `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples && GOWORK=off GOCACHE=/tmp/go-build go run ./07-policy`
       PASS if exit code 0.

    10. **Module set unchanged:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -m all`
        PASS if output contains exactly the expected set: `github.com/costa92/llm-agent` (self) and `github.com/costa92/llm-agent-rag v1.0.1` (the rag back-edge). No new modules.

    Additionally run **shape checks** verifying the public surface
    shipped:

    11. **Public surface check** — every CC-2 required symbol exists:
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for s in 'func Wrap(' 'func WrapConfig(' 'type Config struct' 'type Gate interface' 'type Event struct' 'type EventKind' 'type Decision struct' 'type DecisionAction' 'ErrBlocked' 'type BlockedError struct' 'func NewPIIRedactor(' 'func NewInjectionScanner(' 'func NewMaxInputLen('; do grep -rq "$s" policy/ || { echo "MISSING $s"; exit 1; }; done && echo SURFACE-OK`
        PASS if echoes `SURFACE-OK`.

    12. **21 compile-time interface assertions in policy.go:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -cE '^\s*_ llm\.(ChatModel|ToolCaller|Embedder|StructuredOutputs)\s+=' policy/policy.go`
        PASS if output is `>= 21`.

    13. **8 wrapper struct definitions in policy.go:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -cE '^type (wrapper|toolWrapper|embedWrapper|schemaWrapper|toolEmbedWrapper|toolSchemaWrapper|embedSchemaWrapper|toolEmbedSchemaWrapper) struct' policy/policy.go`
        PASS if output is exactly `8`.

    14. **No `llm-agent-rag` import anywhere in policy/** (KS-5 rag freeze):
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -r 'llm-agent-rag' policy/ | wc -l`
        PASS if output is `0`.

    15. **No `llm-agent-otel` import anywhere in policy/** (Decision G — in-test mimic only):
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -r 'llm-agent-otel' policy/ | wc -l`
        PASS if output is `0`.

    16. **Q1-Q5 cited in policy/doc.go**:
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for t in Q1 Q2 Q3 Q4 Q5 CC-2 KC-3 KC-5; do grep -q "$t" policy/doc.go || { echo "MISSING $t in doc.go"; exit 1; }; done && echo DOC-OK`
        PASS if echoes `DOC-OK`.

    17. **All 4 slice SUMMARYs exist:**
        `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for f in 36-01-SUMMARY.md 36-02-SUMMARY.md 36-03-SUMMARY.md 36-04-SUMMARY.md; do [ -f ".planning/phases/36-policy-safety-middleware/$f" ] || { echo "MISSING $f"; exit 1; }; done && echo SUMMARIES-OK`
        PASS if echoes `SUMMARIES-OK`.

    Record the verdict for each check in a structured table that the
    summary will commit. If ANY check fails, do NOT proceed to Task 2;
    instead, summarize the failure and stop — the gate is non-passing,
    and the operator must run a `--gaps` slice to close the gap before
    the tag.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./policy/... -count=1 && cd examples && GOWORK=off GOCACHE=/tmp/go-build go run ./07-policy >/dev/null && cd .. && [ "$(GOWORK=off go list -f '{{join .Imports "\n"}}' ./policy/ | sort -u | grep -v '^$' | grep -vE '^(context|errors|fmt|io|regexp|strings|sync|sync/atomic|testing|time|unicode/utf8|github.com/costa92/llm-agent(/(llm|budget))?)$' | wc -l)" = "0" ] && [ "$(git diff main -- go.mod go.sum llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/ agent_chatmodel.go | wc -l)" = "0" ] && echo EXIT-GATE-PASS
    </automated>
  </verify>
  <acceptance_criteria>
    - All 17 exit-gate checks pass (10 automated + 7 shape).
    - The combined automated command exits 0 and emits `EXIT-GATE-PASS`.
    - The verdict table includes a row per check with PASS / FAIL + actual output.
    - If any check fails, this task records the failure with reproducer commands and STOPS — Task 2 (the tag-push checkpoint) is not entered until the gate is green.
  </acceptance_criteria>
  <done>
    Every exit-gate constraint is verified green. The combined command emits `EXIT-GATE-PASS`. The verdict table is ready for the slice summary commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator gate — tag and push v0.6.1 from main</name>
  <what-built>
    Phase 36 shipped the `policy` package across slices 36-01 through
    36-04:
    - 36-01 — decorator + types skeleton: `Wrap`, `WrapConfig`, `Config`,
      `Gate`, `Event` (5 kinds), `Decision` (4 actions), `ErrBlocked` +
      `BlockedError`, 8 wrapper structs, 21 compile-time interface
      assertions, `streamReader`. Q1-Q5 ratified in `policy/doc.go`.
    - 36-02 — 3 built-in gates: `NewPIIRedactor` (email/phone/IPv4 per
      Q2 — ssn/credit_card dropped), `NewInjectionScanner` (4 patterns
      from rag), `NewMaxInputLen` (bytes per Q3). `WithStreamRedaction`
      opt-in for PIIRedactor (Q4 default OFF).
    - 36-03 — compose integration: 5 tests (capability preservation,
      blocked-by-policy short-circuit, streaming through both layers,
      budget-beats-policy at chokepoint, per-paradigm smoke for all 5
      agent paradigms). In-test `observerModel` mimics otelmodel.Wrap
      WITHOUT importing it (Decision G).
    - 36-04 — example: `examples/07-policy/main.go` with three deterministic
      demos + README documenting the canonical setup and the composition
      stack with `otelmodel.Wrap` (README-only, no import).

    Task 1 of this slice confirmed the exit gate is green:
    - `go vet ./... && go test ./... && go test -race ./policy/...`
      all PASS.
    - `go.mod` and `go.sum` unchanged (no new third-party module).
    - KC-5 holds: no edit to `llm/`, paradigm files, or
      `agent_chatmodel.go`.
    - `go list -deps ./policy/` shows zero third-party modules.
    - `go run ./examples/07-policy` exits 0 deterministically.

    The next concrete action is operator-only: tag `v0.6.1` from `main`
    and push.
  </what-built>
  <how-to-verify>
    1. Verify the local `main` branch is at the expected commit:
       ```sh
       cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent
       git status
       git log --oneline -5
       ```
       Expect: clean working tree; the most recent commit is the 36-05
       summary commit; no uncommitted changes.

    2. Verify the tag does NOT already exist:
       ```sh
       git tag --list 'v0.6.1'
       ```
       Expect: empty output. If non-empty, the tag was already cut —
       skip the push and just confirm visibility.

    3. (Optional final smoke) Re-run the combined exit-gate command:
       ```sh
       cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && \
         GOWORK=off GOCACHE=/tmp/go-build go vet ./... && \
         GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && \
         GOWORK=off GOCACHE=/tmp/go-build go test -race ./policy/... -count=1
       ```
       Expect: every command exits 0.

    4. Create the annotated tag from `main`:
       ```sh
       git tag -a v0.6.1 -m "v0.6.1: policy package (CC-2)

       Capability-preserving llm.ChatModel decorator with 3 built-in gates
       (PIIRedactor / InjectionScanner / MaxInputLen). Mirrors otelmodel.Wrap
       shape (KC-3). Stdlib-only. No edit to validated public types (KC-5).
       Composes cleanly with otelmodel.Wrap and budget chokepoint.

       Phase 36 (v1.2). See .planning/v1.2-ROADMAP.md."
       ```
       (HEREDOC commit message acceptable; the wording is illustrative —
       operator picks the final tone.)

    5. Push the tag:
       ```sh
       git push origin v0.6.1
       ```

    6. Verify the tag is visible on the remote:
       ```sh
       git ls-remote --tags origin | grep 'v0.6.1'
       ```
       Expect: one line showing the SHA + `refs/tags/v0.6.1`.

    7. Confirm the post-tag invariants hold:
       - `go.mod` not edited (no `require` for v0.6.1 anywhere — this is
         the project's own tag, not a consumed dep).
       - The umbrella dep-currency CI gate (KE-6) will NOT fire yet
         against sister repos — they still pin v0.5.1; the gate fires
         only when sisters bump core. That's a future v1.3 ecosystem-
         alignment task per STATE.md, NOT v1.2's work.

    If anything is wrong (working tree dirty, exit gate not green, tag
    already exists with mismatched SHA), describe the issue instead
    of typing "approved" so the planner can produce a `--gaps` slice
    to remediate.
  </how-to-verify>
  <resume-signal>
    Type `approved` (or `approved-but-skipped-push` if the tag was
    already pushed by a prior operator session) to mark Phase 36
    complete. Or describe the issue blocking the tag — that will
    feed a `--gaps` slice to the planner.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the audit summary + the next-step pointer for /gsd-transition</name>
  <files></files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/STATE.md` (the current STATE.md — to know what the next phase is and what STATE will say post-36)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/v1.2-ROADMAP.md` (Phase 37 — the next phase)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/35-budget-and-cancellation-context/35-04-SUMMARY.md` (the Phase 35 exit-gate slice's SUMMARY — the template this slice's SUMMARY follows)
  </read_first>
  <action>
    Write `.planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md`
    capturing:
    1. Phase 36 outcome: PASS / FAIL (PASS expected per Task 1's
       gate).
    2. The verdict table from Task 1 (17 rows: 10 automated + 7 shape).
    3. Surface summary: the public types and functions that
       `v0.6.0 → v0.6.1` adds (the `policy` package's complete exported
       surface).
    4. Stack of decisions ratified (Q1-Q5 — operator-default values
       chosen in 36-01-PLAN).
    5. Known carry-forwards intentionally deferred (cross-delta PII
       buffering; SSN/credit_card via `NewUSLocalePIIRedactor`;
       `MaxInputLenRunes`; provider-side schema validation; OWASP/NIST
       full taxonomy; ML-classifier gates).
    6. KC-5 audit: list of files NOT edited (`llm/`, paradigm files,
       `agent_chatmodel.go`, `memory/`, `orchestrate/`, `go.mod`,
       `go.sum`).
    7. Tag status: `v0.6.1` cut from `main` (operator-confirmed in Task
       2) — OR — `pending push` if the operator did not push during
       the checkpoint.
    8. Next step pointer: operator runs `/gsd-transition` to flip
       STATE.md from "Phase 35 shipped, Phase 36 next" to "Phase 36
       shipped (v0.6.1), Phase 37 next" — then `/gsd-plan-phase 37`
       to plan the `orchestrate.Supervisor` slice (CC-3).
    9. Lessons learned (specific to Phase 36):
       - Decision G (in-test mimicked observer) works — the policy
         package proves capability preservation across both layers
         without importing the sister-repo decorator. This pattern
         is reusable for any future "compose-with-cross-repo-decorator"
         test.
       - The 8-wrapper type-switch tree from `otelmodel.go` is the
         load-bearing reference for any future model-boundary
         decorator in v1.x; mirror it line-for-line. Phase 37's
         `Supervisor` does NOT need this tree (it wraps
         `agents.Agent`, not `llm.ChatModel`), but the next future
         decorator slice will.
       - Lifting regex patterns by COPY (not import) from
         `llm-agent-rag/guard` preserves the rag freeze (KS-5) and
         lets each repo own its evolution path. Documented as the
         KC-3 + KS-5 pattern.
       - Q4 (StreamDelta opt-in) is the right default — the per-delta
         cost matters more than the cross-delta-leak limitation, and
         users who need cross-delta detection register a PostGenerate
         redactor.
       - Compile-time `var _ Interface = (*Struct)(nil)` assertions
         caught at least one type-switch miss during 36-01 development
         (predicted in 36-RESEARCH.md Pitfall 1; held in practice).

    Commit message: `docs(36): record phase 36 audit + v0.6.1 close`.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && [ -f .planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md ] && grep -q 'PASS' .planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md && grep -q 'v0.6.1' .planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md && grep -q 'gsd-transition' .planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md && grep -q '/gsd-plan-phase 37' .planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md && echo SUMMARY-OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md` exists.
    - File contains the verdict table from Task 1 (17 rows).
    - File names the public surface added in `v0.6.1`.
    - File records Q1-Q5 ratifications + the carry-forward list.
    - File records the tag status (cut OR pending push).
    - File names the next concrete action (`/gsd-transition` then `/gsd-plan-phase 37`).
    - Commit message follows the project convention (e.g., `docs(36): record phase 36 audit + v0.6.1 close`).
  </acceptance_criteria>
  <done>
    The phase audit summary is committed and ready for the operator's `/gsd-transition` slice — which will flip STATE.md to "Phase 36 shipped, Phase 37 next" and update the v1.2 progress counter from 1/4 → 2/4.
  </done>
</task>

</tasks>

<verify>
- exit gate green (one-shot combined command):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./policy/... -count=1 && echo PHASE-36-GATE-OK`
- example runs:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/examples && GOWORK=off GOCACHE=/tmp/go-build go run ./07-policy >/dev/null && echo EXAMPLE-RUN-OK`
- core stdlib-only:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -f '{{join .Imports "\n"}}' ./policy/ | sort -u | grep -v '^$' | grep -vE '^(context|errors|fmt|io|regexp|strings|sync|sync/atomic|testing|time|unicode/utf8|github.com/costa92/llm-agent(/(llm|budget))?)$' | wc -l` returns 0
- KC-5 unchanged (the broad sweep):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git diff main -- llm/ agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/ agent_chatmodel.go go.mod go.sum | wc -l` returns 0
- module set unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -m all | wc -l` returns 2 (self + rag)
- 4 slice summaries exist (36-01..36-04):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && ls .planning/phases/36-policy-safety-middleware/36-0{1,2,3,4}-SUMMARY.md | wc -l` returns 4
- 36-05 SUMMARY records the audit:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && [ -f .planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md ] && echo SUMMARY-EXISTS`
- tag visibility (operator-gated; verify post-push):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git tag --list 'v0.6.1' || true`
  Expect `v0.6.1` after operator runs Task 2. (This is informational
  here — the checkpoint records the actual push state.)
</verify>

<acceptance>
- Task 1's 17 exit-gate checks all PASS.
- Task 2's operator checkpoint receives `approved` (or `approved-but-skipped-push` if the tag was already cut in a prior session).
- Task 3's `36-05-SUMMARY.md` is committed and records the verdict table, Q1-Q5 ratifications, KC-5 audit, tag status, and the next concrete action pointer.
- `v0.6.1` is the post-phase tag visible on the remote (operator-confirmed).
- Phase 36 closes the second v1.2 phase (CC-2 satisfied); the v1.2 progress counter is ready to advance from 1/4 → 2/4 on the operator's next `/gsd-transition`.
- All `<verify>` commands pass.
</acceptance>

<success_criteria>
The phase-36 audit gate confirms:
- CC-2 fully satisfied — `policy` package exists with all named
  surfaces, all 3 built-in gates, the documented composition stack
  verified by an integration test, and stdlib-only preserved.
- The core repo's `go.mod` / `go.sum` are byte-identical to their
  pre-Phase-36 state — KC-5 honored verbatim.
- The 5 v1.2 agent paradigms (Simple/ReAct/Reflection/PlanSolve/
  FunctionCall) compose with `policy.Wrap` uniformly through the
  Phase 35 chokepoint, with `BlockedError` propagating correctly.
- `v0.6.1` is taggable from `main`. After the operator pushes,
  downstreams can pin `github.com/costa92/llm-agent v0.6.1` and
  consume the `policy` package additively (no breaking changes
  from v0.6.0).
- Phase 37 (CC-3, `orchestrate.Supervisor`) is unblocked — it can
  rely on `policy.Wrap` to provide safety middleware around worker
  agents (the carry-forward note in 36-04 forward-references this).
</success_criteria>

<output>
Create `.planning/phases/36-policy-safety-middleware/36-05-SUMMARY.md`
recording the audit verdict + tag status + next-step pointer. This is
authored in Task 3 of this slice.
</output>
