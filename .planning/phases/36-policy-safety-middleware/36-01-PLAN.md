---
phase: 36-policy-safety-middleware
plan: 01
type: execute
wave: 1
repo: llm-agent (core)
depends_on: []
files_modified:
  - policy/doc.go
  - policy/gate.go
  - policy/gate_test.go
  - policy/policy.go
  - policy/policy_test.go
autonomous: true
requirements: [CC-2]
must_haves:
  truths:
    - "policy package exists with Wrap, WrapConfig, Config, Gate, Event, EventKind, Decision, DecisionAction, ErrBlocked, BlockedError public surface"
    - "policy.Wrap is capability-preserving — all 8 wrapper types ship, all 21 compile-time interface assertions hold (mirror otelmodel.go:300-321)"
    - "go test -race ./policy/... is green — concurrent Generate / Stream under gates produce no race"
    - "go list -deps ./policy/... shows only stdlib + github.com/costa92/llm-agent/llm — no third-party module added"
    - "go.mod unchanged after this slice; no go.sum created"
    - "no edit to llm.ChatModel / agents.Agent / memory.Memory / orchestrate.NodeFunc[S] (KC-5)"
    - "Allow is the zero-value DecisionAction (Q1 — forgetful gate is non-interfering by default)"
    - "Open-Question Q1-Q5 from 36-RESEARCH.md ratified with operator-default values and cited in policy/doc.go"
  artifacts:
    - path: "policy/policy.go"
      provides: "wrapper base + 7 nested wrappers (toolWrapper / embedWrapper / schemaWrapper / toolEmbedWrapper / toolSchemaWrapper / embedSchemaWrapper / toolEmbedSchemaWrapper), Wrap factory + WrapConfig with type-switch tree, Config{Gates, OnDecision}, (*wrapper).wrap re-wrap helper, streamReader decorator, runGates internal helper, 21 compile-time assertions"
    - path: "policy/gate.go"
      provides: "Gate interface, Event{Kind,Req,Resp,Delta}, EventKind enum (5 kinds, PreGenerate=0), Decision{Action,Reason,Replacement,Gate}, DecisionAction enum (Allow=0,Block,Redact,Replace), ErrBlocked sentinel, BlockedError rich error with Is/Unwrap"
    - path: "policy/policy_test.go"
      provides: "TestWrap_PreservesCapabilities (all 8 capability combinations), TestWithTools_PreservesGates, TestWithSchema_PreservesGates, TestBlock_ShortCircuits, TestRedact_RewritesResponse, TestReplace_RewritesRequest, TestStream_BlockedOnPreStream, TestStream_RedactDelta, TestStream_PostStreamFires, TestOnDecision_Sync, TestGenerate_AllowsByDefault, race-safe"
    - path: "policy/gate_test.go"
      provides: "TestErrBlocked (errors.Is sentinel + errors.As rich BlockedError), TestDecision_ZeroValueIsAllow (Q1 ratification test), TestBlockedError_UnwrapWrapped"
    - path: "policy/doc.go"
      provides: "package doc citing CC-2, KC-3, KC-5, and ratifying Q1-Q5 with operator-default values"
  key_links:
    - from: "policy/policy.go::WrapConfig"
      to: "policy/policy.go::wrapper, toolWrapper, ..., toolEmbedSchemaWrapper"
      via: "8-wrapper type-switch tree mirroring otelmodel.go:20-49"
      pattern: "return &<capability-named>Wrapper{wrapper: base, ...}"
    - from: "policy/policy.go::(*wrapper).wrap"
      to: "policy/policy.go::WrapConfig"
      via: "re-wrap helper closes over gates+onDecision"
      pattern: "return WrapConfig(next, Config{Gates: w.gates, OnDecision: w.onDecision})"
    - from: "policy/policy.go::wrapper.Generate"
      to: "policy/gate.go::Event{PreGenerate}+Event{PostGenerate}"
      via: "runGates fires pre/post; Block returns BlockedError; Replace rewrites req; Redact rewrites resp.Text"
      pattern: "runGates(w.gates, ev, w.onDecision)"
    - from: "policy/policy.go::streamReader.Next"
      to: "policy/gate.go::Event{PreStream,StreamDelta,PostStream}"
      via: "stream decorator fires PreStream once, StreamDelta per event (opt-in), PostStream on EventDone or io.EOF"
      pattern: "ev.Kind == llm.EventDone"
---

<objective>
Ship the `policy` package decorator skeleton — surface only, no built-in
gates. Deliver the capability-preserving `Wrap` factory + `WrapConfig`
entry point, the 8-wrapper type-switch tree mirroring
`llm-agent-otel/otelmodel/otelmodel.go:20-49` line-for-line in shape,
the typed `Gate` event union (5 kinds), the `Decision{Action, Reason,
Replacement, Gate}` shape with 4 actions (Allow / Block / Redact /
Replace), the `ErrBlocked` sentinel + `BlockedError` rich error pair
(mirroring `llm/errors.go` + `budget/budget.go` combined shape), and
the `streamReader` decorator covering PreStream / StreamDelta /
PostStream lifecycle. All 21 compile-time interface assertions ship.
Exhaustive capability-preservation + gate-action tests. Pure stdlib.
CC-2.
</objective>

<context>
- Repo: `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent`,
  branch `main`. Core stays stdlib-only (CLAUDE.md Rule 1; KC-5). `go.mod`
  today has exactly one require — `github.com/costa92/llm-agent-rag
  v1.0.1` — and the policy package adds zero. No `go.sum` by design.
- Repo layout is **flat** — files at the repo root declare `package agents`.
  The new `policy/` subdirectory is created in this slice; it sits next to
  the existing `budget/` Phase-35 sibling.
- This slice ships the **decorator + types only**. Built-in gates
  (`PIIRedactor`, `InjectionScanner`, `MaxInputLen`) land in slice 36-02
  and need the `Gate` interface from this slice. Integration test
  (compose-with-mimicked-otel + budget short-circuit) lands in slice
  36-03 and needs 36-02's gates. Slices 36-04 (example) and 36-05 (exit
  gate) depend on all above.
- **The decorator is a verbatim-shape mirror of `otelmodel.Wrap`.** Read
  `llm-agent-otel/otelmodel/otelmodel.go:14-49` (Wrap factory + 8-wrapper
  tree) and `:300-321` (compile-time interface assertions) — those are
  the load-bearing references. Swap `tp / tracer` for
  `gates / onDecision` and `Config{TracerProvider: ...}` for
  `Config{Gates: ..., OnDecision: ...}`. Every wrapper name, every field
  layout, every `WithTools`/`WithSchema` re-wrap incantation matches
  otelmodel exactly. Mismatch breaks the capability-preservation
  contract (Pitfall 1 in 36-RESEARCH.md §"Common Pitfalls").
- **Q1-Q5 ratification (operator-default values for the 5 open questions
  from 36-RESEARCH.md §"Open Questions"):**
  - **Q1 — `OnDecision` returns no error.** Symmetric with `otelmodel`'s
    tracer callback (observation never blocks the request path). User-side
    failures inside `OnDecision` are the user's to handle. Document on
    `Config.OnDecision` field comment.
  - **Q2 — Drop `ssn` + `credit_card` from core PII set.** US-locale-
    specific, high false-positive rate. The `policy.NewPIIRedactor`
    built-in (slice 36-02) ships `email` + `phone` + `ipv4` only. The
    v1.3-add candidate `NewUSLocalePIIRedactor` is KC-5-friendly additive.
    Recorded in `policy/doc.go`.
  - **Q3 — `MaxInputLen` measures bytes.** Provider HTTP byte budgets are
    the operative cap; `len(string)` is O(1); language-agnostic in the
    network-cost sense. A future `MaxInputLenRunes(n int)` can be added
    additively. Recorded in `policy/doc.go`.
  - **Q4 — `StreamDelta` is opt-in (default OFF) for all 3 built-in
    gates.** Per-delta regex is expensive; cross-delta PII can leak
    by design (best-effort, matches rag's known limit). Document at
    `EventKind`'s `StreamDelta` constant comment + each built-in gate's
    doc (slice 36-02 enforces).
  - **Q5 — `BlockedError.Decision` field IS shipped** (a struct copy of
    the deciding `Decision`). Lets callers introspect the full
    decision rather than just `Gate` + `Reason`. Modest cost; high
    value for audit / debugging. Recorded in `policy/doc.go`.
  Beyond ratifying defaults: surface a sixth implicit contract (research
  Decision F clarification) — **Block on `StreamDelta` surfaces
  immediately** (return `(StreamEvent{}, &BlockedError{...})` from the
  same `Next()` call, discard the inner event). The "next-Next()"
  alternative is rejected for race reasons.
- **Gate ordering contract.** Gates run sequentially in registration
  order; first non-Allow wins; on `Replace` the gates continue evaluation
  against the rewritten request (so a downstream gate may still `Block`);
  on `Block` the chain short-circuits. Document in `policy/doc.go` and at
  the `runGates` internal helper comment.
- **Locked surface (verbatim from 36-RESEARCH.md §"Standard Stack"
  + Decisions A/B/D + Q5):**
  - `func Wrap(inner llm.ChatModel, gates ...Gate) llm.ChatModel`
    — convenience variadic shortcut over `WrapConfig`.
  - `func WrapConfig(inner llm.ChatModel, cfg Config) llm.ChatModel`
    — structured-option entry point; runs the 8-way type-switch.
  - `type Config struct { Gates []Gate; OnDecision func(Decision) }`
    — option struct mirroring `otelmodel.Config`. Nil `OnDecision`
    is a no-op.
  - `type Gate interface { Inspect(ctx context.Context, ev Event) Decision; Name() string }`
    — single-method seam plus `Name()` so `BlockedError.Gate` and
    `OnDecision`'s `Decision.Gate` get populated automatically.
  - `type EventKind uint8` with constants
    `PreGenerate (=0) / PostGenerate / PreStream / StreamDelta / PostStream`.
  - `type Event struct { Kind EventKind; Req *llm.Request; Resp *llm.Response; Delta *llm.StreamEvent }`
    — typed union; pointer fields populated per kind (mirrors
    `llm.StreamEvent`'s typed-union pattern from `llm/stream.go:41-47`).
  - `type DecisionAction uint8` with constants
    `Allow (=0) / Block / Redact / Replace`.
  - `type Decision struct { Action DecisionAction; Reason string; Replacement string; Gate string }`
    — gate verdict. `Gate` is populated by the decorator from
    `Gate.Name()`; read-only to callers (gates returning a non-empty
    `Gate` field have it overwritten).
  - `var ErrBlocked = errors.New("policy: blocked")` — umbrella sentinel.
  - `type BlockedError struct { Gate, Reason string; Decision Decision; Wrapped error }`
    with `Error() string`, `Is(target error) bool` (returns
    `target == ErrBlocked`), `Unwrap() error`. The `Decision` field is
    Q5's ratified contract.
- Stdlib imports allowed in `policy/`: `context`, `errors`, `fmt`, `io`,
  `regexp`, `strings`, `sync`, `unicode/utf8` (optional). Plus the
  in-repo `github.com/costa92/llm-agent/llm` for the ChatModel /
  capabilities / Request / Response / StreamReader / StreamEvent types.
  No other imports — verified by exit gate.
- See `36-RESEARCH.md` §"Architecture Patterns" (Patterns 1-2),
  §"Decision A" (Decision shape), §"Decision B" (Event shape),
  §"Decision C" (OnDecision sync), §"Decision D" (sentinel + rich
  error), §"Decision F" (streaming semantics — surface-immediately
  variant), §"Common Pitfalls" 1-3, §"Slice Breakdown" row 36-01.
- See `36-PATTERNS.md` rows for `policy/policy.go`,
  `policy/policy.go (Config)`, `policy/gate.go (Gate/Event/Decision)`,
  `policy/gate.go (ErrBlocked+BlockedError)`, `policy/policy_test.go`.
</context>

<interfaces>
Key contracts the executor needs. Already verified by direct file read.

From `llm/chatmodel.go` (the type policy wraps):
```
type ChatModel interface {
    Generate(ctx context.Context, req Request) (Response, error)
    Stream(ctx context.Context, req Request) (StreamReader, error)
    Info() ProviderInfo
}
```

From `llm/capabilities.go` (the 3 optional interfaces wrap must preserve):
```
type ToolCaller interface {
    ChatModel
    WithTools(tools []Tool) (ToolCaller, error)
}
type Embedder interface {
    ChatModel
    Embed(ctx context.Context, input []string) ([][]float32, error)
}
type StructuredOutputs interface {
    ChatModel
    WithSchema(schema []byte) (StructuredOutputs, error)
}
```

From `llm/stream.go` (the typed-union pattern policy mirrors for Event):
```
type StreamReader interface {
    Next() (StreamEvent, error)
    Close() error
}
type StreamEvent struct {
    Kind     EventKind
    Text     string
    ToolCall *ToolCallDelta
    Usage    *Usage
    // ...
}
const (
    EventTextDelta EventKind = iota
    EventToolCallStart
    EventToolCallArgsDelta
    EventToolCallEnd
    EventDone
)
```

From `llm/errors.go` (sentinel + rich-error shape policy mirrors):
```
var ErrCapabilityNotSupported = errors.New("llm: capability not supported by bound model")
type AuthError struct{ Provider string; Wrapped error }
func (e *AuthError) Error() string { ... }
func (e *AuthError) Unwrap() error { return e.Wrapped }
```

From `budget/budget.go` (sentinel-family pattern policy mirrors, plus
"single-method interface + value-type union" v1.2 sibling shape):
```
var ErrBudgetExceeded = errors.New("budget: exceeded")
var ErrTokensExceeded = fmt.Errorf("%w: tokens", ErrBudgetExceeded)
// ...
type Tracker interface { Charge(Usage) error; Snapshot() Usage; Remaining(Budget) Usage }
```

From `llm/scriptedllm.go` (the canonical mock the tests use):
```
func NewScriptedLLM(opts ...ScriptedOption) ChatModel
// WithProvider / WithModel / WithCapabilities{Tools/Embeddings/StructuredOutputs} / WithResponses
// WithStreamEvents (for streaming tests)
```

From `llm-agent-otel/otelmodel/otelmodel.go` (THE analog — the planner's
read_first list points executor at this file lines 14-49 and 300-321):
- 8 wrapper struct names: `wrapper`, `toolWrapper`, `embedWrapper`,
  `schemaWrapper`, `toolEmbedWrapper`, `toolSchemaWrapper`,
  `embedSchemaWrapper`, `toolEmbedSchemaWrapper`.
- Each non-base wrapper embeds `*wrapper` and adds the capability
  fields (`toolCaller llm.ToolCaller`, `embedder llm.Embedder`,
  `structured llm.StructuredOutputs`).
- `(*wrapper).wrap(next)` closes over the config and re-calls Wrap.
- `WithTools` and `WithSchema` on each wrapper variant: bind on inner,
  re-wrap via `w.wrap(next)`, type-assert and return.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write policy/gate.go and policy/gate_test.go — types + sentinel/rich-error pair</name>
  <files>policy/gate.go, policy/gate_test.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/llm/errors.go` (the canonical sentinel + rich-error pattern — lines 1-45)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/budget/budget.go` (lines 1-115 — the v1.2 sibling's sentinel family + single-method-interface shape; lines 12-42 are the sentinel-family wrap pattern)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/llm/stream.go` (lines 1-50 — the typed-union pattern that `Event` mirrors)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-RESEARCH.md` §"Decision A" (Decision shape, lines 552-596) and §"Decision B" (Event shape, lines 598-652)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-PATTERNS.md` §"policy/gate.go — Gate/Event/Decision" and §"policy/gate.go — ErrBlocked + BlockedError"
  </read_first>
  <behavior>
    - `errors.Is(&BlockedError{...}, ErrBlocked)` returns true.
    - `errors.As(err, &be)` populates `*BlockedError` from a wrapped error.
    - `Decision{}` zero-value has `Action == Allow` (Q1 ratification).
    - `(*BlockedError).Unwrap()` returns the `.Wrapped` field unchanged.
    - `EventKind(0) == PreGenerate` so a forgotten Kind defaults to the most-common case.
  </behavior>
  <action>
    Create `policy/gate.go`. Imports: `context`, `errors`, `fmt`,
    plus `github.com/costa92/llm-agent/llm` for `llm.Request`,
    `llm.Response`, `llm.StreamEvent` pointer fields. Define in this
    order: (1) `type EventKind uint8` with constants `PreGenerate`,
    `PostGenerate`, `PreStream`, `StreamDelta`, `PostStream` —
    `PreGenerate` MUST be `iota = 0`. Each constant gets a doc comment
    naming which `Event` fields are populated (per 36-RESEARCH.md
    Decision B). (2) `type Event struct { Kind EventKind; Req *llm.Request; Resp *llm.Response; Delta *llm.StreamEvent }`.
    (3) `type DecisionAction uint8` with constants `Allow` (`iota = 0`,
    explicit doc comment "zero-value — non-interfering default; Q1
    ratification"), `Block`, `Redact`, `Replace`. (4) `type Decision struct { Action DecisionAction; Reason string; Replacement string; Gate string }` —
    doc comments per field (Reason: gate-defined string;
    Replacement: populated when Action ∈ {Redact, Replace}; Gate:
    populated by the decorator from `Gate.Name()`, gate-returned values
    are overwritten). (5) `type Gate interface { Inspect(ctx context.Context, ev Event) Decision; Name() string }`
    — `Name()` returns the gate's stable identity string used to populate
    `Decision.Gate` and `BlockedError.Gate`. (6) `var ErrBlocked = errors.New("policy: blocked")`
    — umbrella sentinel, doc comment per the 36-PATTERNS.md template
    citing "callers detect with errors.Is(err, policy.ErrBlocked)". (7)
    `type BlockedError struct { Gate string; Reason string; Decision Decision; Wrapped error }`
    with the three methods `Error() string` (formats
    `"policy: blocked by %s: %s"` using `fmt.Sprintf`), `Is(target error) bool` (returns
    `target == ErrBlocked`), and `Unwrap() error` (returns `e.Wrapped`).
    Q5: include the `Decision Decision` field (a struct copy of the
    deciding decision). NEVER use a pointer for `Decision` — value
    semantics avoid post-block mutation of the audit record.
    Create `policy/gate_test.go` in `package policy`. Tests:
    (a) `TestDecision_ZeroValueIsAllow` — `var d Decision; if d.Action != Allow { t.Fatal(...) }`
    (Q1 ratification gate). (b) `TestErrBlocked_SentinelDetection` —
    construct `err := &BlockedError{Gate: "g", Reason: "r"}`; assert
    `errors.Is(err, ErrBlocked) == true`, and that
    `errors.Is(err, io.EOF) == false`. (c) `TestErrBlocked_AsRich` —
    use `errors.As(err, &be)` to extract a `*BlockedError` from a
    wrapped error (`fmt.Errorf("upstream: %w", &BlockedError{...})`);
    assert the extracted struct's `Gate` / `Reason` match. (d)
    `TestBlockedError_UnwrapsWrapped` — construct with
    `Wrapped: io.ErrUnexpectedEOF`; assert
    `errors.Is(err, io.ErrUnexpectedEOF) == true` via `Unwrap` chain.
    (e) `TestBlockedError_ErrorString` — assert
    `(&BlockedError{Gate: "InjectionScanner", Reason: "instruction_override"}).Error() == "policy: blocked by InjectionScanner: instruction_override"`.
    Use `errors`, `fmt`, `io`, `testing` stdlib only.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./policy/... && GOWORK=off GOCACHE=/tmp/go-build go test ./policy/... -run 'TestDecision_ZeroValueIsAllow|TestErrBlocked|TestBlockedError' -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `policy/gate.go` contains `var ErrBlocked = errors.New("policy: blocked")`, `type BlockedError struct`, `type Gate interface { Inspect(...) Decision; Name() string }`, `type Event struct`, `type EventKind uint8`, `type Decision struct`, `type DecisionAction uint8`.
    - `go vet ./policy/...` exits 0.
    - `go test ./policy/... -run TestDecision_ZeroValueIsAllow` passes.
    - `go test ./policy/... -run TestErrBlocked_SentinelDetection` passes.
    - `go test ./policy/... -run TestErrBlocked_AsRich` passes.
    - `go test ./policy/... -run TestBlockedError_UnwrapsWrapped` passes.
    - `go test ./policy/... -run TestBlockedError_ErrorString` passes.
    - `grep -E "^const \(|EventKind|DecisionAction" policy/gate.go` shows both enum const blocks present.
    - `grep -E "PreGenerate.*EventKind = iota|Allow.*DecisionAction = iota" policy/gate.go` shows BOTH zero-values are `iota`-anchored (PreGenerate=0, Allow=0).
    - No third-party import in `policy/gate.go` — `go list -f '{{join .Imports "\n"}}' ./policy/ | grep -vE '^(context|errors|fmt|github.com/costa92/llm-agent/llm)$' | grep -v '^$' | wc -l` returns 0.
  </acceptance_criteria>
  <done>
    `policy/gate.go` and `policy/gate_test.go` exist and pass the gate-types unit tests; Q1 ratification (Allow == 0) is enforced by an automated test; Q5 ratification (`BlockedError.Decision` field) is present in the struct definition.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write policy/policy.go — 8-wrapper decorator + streamReader, mirroring otelmodel.go</name>
  <files>policy/policy.go, policy/policy_test.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel/otelmodel/otelmodel.go` (full file, ~329 lines — THE analog; Wrap factory at 14-49, streamReader at 102-148, WithTools re-wrap at 154-162, compile-time assertions at 300-321)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel/otelmodel/config.go` (15 lines — full file; Config option struct shape)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent-otel/otelmodel/otelmodel_test.go` (lines 1-180 — the canonical capability-preservation + WithTools re-wrap test shape policy_test.go MUST mirror)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/llm/chatmodel.go` (lines 1-40 — the ChatModel interface contract)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/llm/capabilities.go` (full file — ToolCaller / Embedder / StructuredOutputs interfaces)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/llm/stream.go` (full file — StreamReader interface + StreamEvent typed union; io.EOF semantics)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/policy/gate.go` (the just-written file from Task 1 — needed for Gate, Event, Decision, etc.)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-RESEARCH.md` §"Pattern 1" + §"Pattern 2" + §"Decision F" (streaming surface-immediately variant) + §"Common Pitfalls" 1-4
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-PATTERNS.md` §"policy/policy.go" (the verbatim mirror sections)
  </read_first>
  <behavior>
    - `policy.Wrap(model).(llm.ToolCaller)` returns `(_, true)` iff `model.(llm.ToolCaller)` returns `(_, true)` — for all 2³ capability combinations.
    - Same identity for `(llm.Embedder)` and `(llm.StructuredOutputs)`.
    - `tc, _ := wrapped.(llm.ToolCaller); bound, _ := tc.WithTools([]llm.Tool{{Name:"t", Parameters:[]byte("{}")}}); _, ok := any(bound).(llm.ToolCaller)` — `ok == true` (re-wrap preserves capability).
    - A gate returning `Block` on `PreGenerate` causes `wrapped.Generate(ctx, req)` to return `(llm.Response{}, err)` where `errors.Is(err, ErrBlocked) == true` AND `inner.Generate` was NEVER called.
    - A gate returning `Redact` on `PostGenerate` causes `wrapped.Generate(ctx, req)` to return `(resp, nil)` where `resp.Text == Decision.Replacement`.
    - A gate returning `Replace` on `PreGenerate` causes `inner.Generate` to be invoked with a request whose user-message content equals `Decision.Replacement` (NOT the caller's original).
    - Streaming: a gate returning `Block` on `PreStream` causes the FIRST `Next()` call on the returned `StreamReader` to return `(StreamEvent{}, &BlockedError{...})`, and `inner.Stream(ctx, req)` was NEVER called (or was called and `Close()`'d before the first read — implementation may pre-call inner.Stream then Close on Block; assert via inner-call counter that AT MOST one Stream call happened and zero `Next()` calls reached the inner).
    - Streaming: a gate returning `Redact` on `StreamDelta` causes the caller's `Next()` to return a `StreamEvent` whose `Text` field equals `Decision.Replacement` (not the inner's raw text).
    - Streaming: `PostStream` event fires once on `EventDone` AND once on `io.EOF` — verify with an `OnDecision` counter inside the test.
    - `OnDecision` callback fires exactly once per non-Allow `Decision` returned; receives a `Decision` value-copy.
    - `OnDecision` panics are recovered by the decorator — request still completes (the recovery is logged-via-no-op; we don't surface a "panic in OnDecision" error to the caller).
    - All 8 wrapper structs satisfy `llm.ChatModel`. `toolWrapper` satisfies `llm.ToolCaller`. `embedWrapper` satisfies `llm.Embedder`. `schemaWrapper` satisfies `llm.StructuredOutputs`. The 4 combinatorial wrappers (`toolEmbedWrapper` / `toolSchemaWrapper` / `embedSchemaWrapper` / `toolEmbedSchemaWrapper`) satisfy their respective combinations. 21 `var _ <iface> = (*<wrapper>)(nil)` assertions block the file from compiling if any is dropped.
    - `go test -race ./policy/...` is green — no data race when two goroutines call `Generate` concurrently on the same wrapped model.
  </behavior>
  <action>
    Create `policy/policy.go`. Imports: `context`, `errors`, `fmt`,
    `io`, `sync`, `github.com/costa92/llm-agent/llm`. (No `regexp` /
    `strings` / `unicode/utf8` in this slice — those land in 36-02.)
    Implement, in order:
    (1) `type Config struct { Gates []Gate; OnDecision func(Decision) }`
    mirroring `otelmodel.Config` shape exactly — single struct, no
    builder methods. Doc-comment `OnDecision` field: "Called
    synchronously in the request goroutine for every non-Allow decision
    returned by a gate. Nil-safe (treated as no-op). Panics inside
    OnDecision are recovered by the decorator; the request completes as
    if the callback were absent." (Q1 ratification: no error return.)
    (2) `type wrapper struct { inner llm.ChatModel; gates []Gate; onDecision func(Decision) }`
    — the base wrapper. Field layout MUST mirror
    `otelmodel.wrapper` exactly except for the swapped fields. Implement
    `(w *wrapper) Generate(ctx, req) (llm.Response, error)` running gates
    on `Event{Kind: PreGenerate, Req: &req}` (apply Block/Replace per
    Decision A semantics), invoking `w.inner.Generate(ctx, req)`, then
    running gates on `Event{Kind: PostGenerate, Req: &req, Resp: &resp}`
    (apply Block/Redact). The `req` passed to `inner.Generate` MUST be
    the **post-Replace** value (gates can rewrite the request via
    `Decision.Replacement` — apply to the SystemPrompt if `req.SystemPrompt`
    is the only content, else to the last `req.Messages[i].Content` of
    role "user"; document this rule at the top of the file with a
    comment "Replace target: last user-role message Content, else
    SystemPrompt; documented in 36-RESEARCH.md Decision A"). On `Redact`
    in PostGenerate, replace `resp.Text` with `Decision.Replacement`.
    Implement `(w *wrapper) Stream(ctx, req) (llm.StreamReader, error)`
    returning `&streamReader{inner: ..., gates: w.gates, onDecision: w.onDecision, req: req}`
    — the inner.Stream is INVOKED here, but PreStream is fired on the
    first Next() (matching otelmodel's lazy-start shape). Implement
    `(w *wrapper) Info() llm.ProviderInfo { return w.inner.Info() }`
    (the wrapper is transparent at the Info layer; capabilities are
    Go-type-assertion-driven per CLAUDE.md Rule 5).
    Implement `(w *wrapper) wrap(next llm.ChatModel) llm.ChatModel { return WrapConfig(next, Config{Gates: w.gates, OnDecision: w.onDecision}) }`
    — the re-wrap helper. CRITICAL: this is the Pitfall 2 fix; every
    `WithTools` / `WithSchema` MUST call `w.wrap(next)` else gates drop.
    (3) The 7 nested wrappers — `toolWrapper`, `embedWrapper`,
    `schemaWrapper`, `toolEmbedWrapper`, `toolSchemaWrapper`,
    `embedSchemaWrapper`, `toolEmbedSchemaWrapper`. Each embeds
    `*wrapper` and adds the capability fields (e.g. `toolCaller llm.ToolCaller`).
    Implement `WithTools` on every wrapper that carries `toolCaller`,
    and `WithSchema` on every wrapper that carries `structured`. Body
    of each method is the verbatim 5-line shape from
    `otelmodel.go:154-162`: bind on inner, on err return, re-wrap via
    `w.wrap(next)`, type-assert to the target interface, return.
    Implement `Embed(ctx, input)` on every wrapper that carries
    `embedder` — body forwards to `w.embedder.Embed(ctx, input)` (no
    gates fire on Embed in v1.2; embedding is not a Generate-shape
    request — KC-3 keeps gates at the ChatModel boundary). Comment this
    decision at the embedder forward.
    (4) `func Wrap(model llm.ChatModel, gates ...Gate) llm.ChatModel { return WrapConfig(model, Config{Gates: gates}) }`
    — convenience entry point.
    (5) `func WrapConfig(model llm.ChatModel, cfg Config) llm.ChatModel`
    — the 8-way type-switch tree. Body MUST mirror
    `otelmodel.go:20-49` line-for-line in shape. Return the most-
    capability-rich wrapper that the inner satisfies.
    (6) `type streamReader struct { inner llm.StreamReader; gates []Gate; onDecision func(Decision); req llm.Request; started bool; closed bool; mu sync.Mutex }`
    — per-stream state. Implement `Next() (llm.StreamEvent, error)` per
    36-RESEARCH.md §"Pattern 2": on first call, fire PreStream gates,
    apply Block (close inner, return BlockedError immediately — Decision
    F surface-immediately variant); for each inner.Next() result, on
    `err == io.EOF` fire PostStream gates (best-effort, observe only —
    Block on PostStream is functionally a no-op; document); on
    `ev.Kind == llm.EventDone` fire PostStream with `Delta: &ev`; for
    text/tool-call deltas fire StreamDelta (gates can return Block
    (close inner, return BlockedError immediately) or Redact/Replace
    (rewrite `ev.Text` and return the modified event)). Implement
    `Close() error` that idempotently closes the inner and marks
    `r.closed`. The `mu sync.Mutex` serializes Next/Close so the
    `-race` test stays green.
    (7) `func runGates(gates []Gate, ev Event, onDecision func(Decision)) (Decision, bool)`
    — internal helper. Iterate `gates` in order; for each gate call
    `g.Inspect(ctx, ev)` (note: gate's ctx is the request's ctx);
    populate `d.Gate = g.Name()` on the returned Decision; if
    `d.Action != Allow`, invoke `onDecision(d)` via a panic-recovered
    helper `safeOnDecision(onDecision, d)`; then check action:
    `Block` → return `(d, true)` (the bool `blocked = true`); `Replace`
    or `Redact` → update the ev fields for downstream gates (mutate
    `*ev.Req` / `*ev.Resp` / `*ev.Delta` via the pointer), continue;
    `Allow` → continue. After all gates: return `(Decision{}, false)`.
    First non-Allow wins for Block (short-circuit); Replace/Redact let
    subsequent gates see the rewrite (chain continues). Document the
    contract at the `runGates` comment.
    (8) `func safeOnDecision(cb func(Decision), d Decision) { defer func() { _ = recover() }(); if cb != nil { cb(d) } }`
    — panic-recovered callback dispatch.
    (9) Compile-time assertion block at the end of the file. 21
    assertions verbatim per 36-PATTERNS.md "Capability-preservation
    compile-time assertion" — every wrapper × every interface it
    claims.
    Create `policy/policy_test.go` in `package policy`. Tests (using
    `llm.NewScriptedLLM` per CLAUDE.md):
    (a) `TestWrap_PreservesCapabilities` — table-driven over the 8
    capability combinations: for each `(tools, embeds, structured)` ∈
    `{0,1}^3`, build a `ScriptedLLM` with the matching capabilities,
    wrap with `Wrap(model)`, assert the wrapped value's type-assertions
    match the expected combo. Mirror
    `otelmodel_test.go:22-41 TestWrap_PreservesCapabilities`.
    (b) `TestWithTools_PreservesGates` — wrap a Tools-capable scripted
    LLM with a counting gate; call `wrapped.(llm.ToolCaller).WithTools(...)`;
    on the rebound result fire a request that the gate blocks; assert
    `errors.Is(err, ErrBlocked) == true` (the rebind preserved the
    gate). Mirror `otelmodel_test.go:124-144 TestWithTools_RewrapsBoundModel`
    in shape.
    (c) `TestWithSchema_PreservesGates` — analogous for `WithSchema` on
    a StructuredOutputs-capable model.
    (d) `TestBlock_ShortCircuits` — install a "always block on
    PreGenerate" gate; wrap a counting ScriptedLLM (use an in-test
    `countingChatModel` wrapper struct with an atomic int counter for
    `Generate` invocations); call `wrapped.Generate(ctx, req)`; assert
    `errors.Is(err, ErrBlocked) == true` AND `counter == 0` (proves
    `inner.Generate` was never invoked).
    (e) `TestRedact_RewritesResponse` — gate returns
    `Decision{Action: Redact, Replacement: "[REDACTED]", Reason: "test"}`
    on `PostGenerate`; ScriptedLLM returns `"raw response"`; assert
    `wrapped.Generate(ctx, req)` returns `(resp, nil)` with
    `resp.Text == "[REDACTED]"`.
    (f) `TestReplace_RewritesRequest` — gate returns
    `Decision{Action: Replace, Replacement: "safe input", Reason: "test"}`
    on `PreGenerate`; the in-test counting ChatModel records the
    `req` it received; assert it saw `"safe input"` not the caller's
    original.
    (g) `TestStream_BlockedOnPreStream` — gate returns Block on
    PreStream; call `wrapped.Stream(ctx, req)` then `sr.Next()`;
    assert the FIRST Next() returns `(StreamEvent{}, err)` with
    `errors.Is(err, ErrBlocked)`. Verify the inner stream's events
    were never seen (the counting inner saw zero `Next()` calls).
    (h) `TestStream_RedactDelta` — gate returns Redact with
    `Replacement: "[X]"` on StreamDelta; ScriptedLLM emits a
    `EventTextDelta` with `Text: "secret"`; drain the wrapped stream;
    assert the consumed event's `Text` is `"[X]"`.
    (i) `TestStream_PostStreamFires` — gate counts PostStream events;
    ScriptedLLM emits text deltas then `EventDone`; drain; assert
    PostStream fired exactly once. Separately, with ScriptedLLM
    emitting deltas then `io.EOF` (no EventDone), drain; assert
    PostStream fired exactly once. (Two sub-tests, table-driven.)
    (j) `TestOnDecision_Sync` — set `OnDecision: func(d) { atomic.AddInt64(&n, 1) }`;
    fire a request that triggers two non-Allow gates and one Allow;
    assert `n == 2`. Then in a second sub-test, set `OnDecision: func(d) { panic("boom") }`;
    fire a request that triggers a Block gate; assert
    `errors.Is(err, ErrBlocked) == true` (the request DOES complete its
    error path — panic in OnDecision was recovered).
    (k) `TestGenerate_AllowsByDefault` — wrap a ScriptedLLM with zero
    gates; assert `wrapped.Generate(ctx, req)` returns the raw inner
    response, no rewrite, no error, no OnDecision invocations.
    (l) `TestConcurrent_NoRace` — wrap with one gate that records
    invocations to a `sync.Mutex`-guarded slice; spawn 20 goroutines,
    each calling `wrapped.Generate(...)` 10 times; assert no
    `-race` violation and the gate was invoked exactly 200 times.
    Mark as run-with-race in the test file by NOT depending on
    `-short` semantics (run unconditionally).
    Define helper test types: `countingChatModel struct { inner llm.ChatModel; generateCalls, streamCalls int64; lastReq llm.Request; mu sync.Mutex }`
    forwarding to inner with atomic counters; `testGate struct { name string; pre, post, preStream, streamDelta, postStream Decision }`
    returning the configured Decision for each event kind (Allow if zero).
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./policy/... && GOWORK=off GOCACHE=/tmp/go-build go test ./policy/... -count=1 && GOWORK=off GOCACHE=/tmp/go-build go test -race ./policy/... -count=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `policy/policy.go` contains `func Wrap(model llm.ChatModel, gates ...Gate) llm.ChatModel`.
    - `policy/policy.go` contains `func WrapConfig(model llm.ChatModel, cfg Config) llm.ChatModel`.
    - `policy/policy.go` contains all 8 wrapper struct definitions: `wrapper`, `toolWrapper`, `embedWrapper`, `schemaWrapper`, `toolEmbedWrapper`, `toolSchemaWrapper`, `embedSchemaWrapper`, `toolEmbedSchemaWrapper` — verified by `grep -cE '^type (wrapper|toolWrapper|embedWrapper|schemaWrapper|toolEmbedWrapper|toolSchemaWrapper|embedSchemaWrapper|toolEmbedSchemaWrapper) struct' policy/policy.go == 8`.
    - 21 compile-time assertions present — verified by `grep -cE '^\s*_ llm\.(ChatModel|ToolCaller|Embedder|StructuredOutputs) +=' policy/policy.go >= 21`.
    - `policy/policy.go` contains `func (w *wrapper) wrap(next llm.ChatModel) llm.ChatModel` (the re-wrap helper — Pitfall 2 fix).
    - `policy/policy.go` contains `type streamReader struct` and `func (r *streamReader) Next() (llm.StreamEvent, error)`.
    - `go vet ./policy/...` exits 0.
    - `go test ./policy/... -run 'TestWrap_PreservesCapabilities|TestWithTools_PreservesGates|TestWithSchema_PreservesGates|TestBlock_ShortCircuits|TestRedact_RewritesResponse|TestReplace_RewritesRequest|TestStream_BlockedOnPreStream|TestStream_RedactDelta|TestStream_PostStreamFires|TestOnDecision_Sync|TestGenerate_AllowsByDefault|TestConcurrent_NoRace' -count=1` — all pass.
    - `go test -race ./policy/... -count=1` exits 0.
    - `go list -f '{{join .Imports "\n"}}' ./policy/ | grep -v '^$' | grep -vE '^(context|errors|fmt|io|sync|github.com/costa92/llm-agent/llm)$' | wc -l` returns 0 (this slice does NOT use regexp/strings/utf8 yet — those land in 36-02).
    - `git diff main -- llm/ agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/` shows no changes (KC-5).
    - `git status --short -- go.mod go.sum | wc -l` is 0 (no go.mod / go.sum change).
  </acceptance_criteria>
  <done>
    `policy/policy.go` ships the 8-wrapper decorator + streamReader + 21 compile-time assertions. `policy/policy_test.go` covers capability preservation, WithTools/WithSchema re-wrap, all 4 Decision actions on Generate, all 3 stream gates (PreStream/StreamDelta/PostStream), OnDecision sync + panic-recovery, the no-gates Allow path, and concurrent race-safety. `go test -race ./policy/...` is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write policy/doc.go — package doc citing CC-2, KC-3, KC-5, and ratifying Q1-Q5</name>
  <files>policy/doc.go</files>
  <read_first>
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/budget/doc.go` (full file, 46 lines — the v1.2 sibling's package-doc template policy/doc.go mirrors)
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-RESEARCH.md` §"Open Questions" 1-5 + §"Decision A" through §"Decision H"
    - `/home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent/.planning/phases/36-policy-safety-middleware/36-PATTERNS.md` §"policy/doc.go"
  </read_first>
  <action>
    Create `policy/doc.go` with `package policy` declaration preceded by
    a package-level doc comment. First sentence per Go convention:
    `// Package policy provides a capability-preserving llm.ChatModel decorator that runs typed Gate events at request, response, and stream boundaries.`
    Paragraph 1: one-line each summary of (a) `Wrap` / `WrapConfig` + the
    8-wrapper type-switch (capability preservation), (b) the typed
    `Gate` event union (5 EventKinds), (c) `Decision` (4 actions —
    Allow/Block/Redact/Replace), (d) `ErrBlocked` + `BlockedError`
    sentinel+rich error pair, (e) the documented composition stack
    `policy.Wrap(otelmodel.Wrap(provider))` — outer denies before
    observed; middle observes; inner makes the call.
    Subsection `// # Decisions ratified by this package (do not re-litigate)`:
    one paragraph per Q1-Q5 from 36-RESEARCH.md §"Open Questions",
    stating the operator-default ratified value:
    - Q1: `OnDecision` returns no error; sync in request goroutine;
      panic-recovered.
    - Q2: PII set ships `email + phone + ipv4` only; `ssn` +
      `credit_card` deferred to a v1.3 `NewUSLocalePIIRedactor` additive.
    - Q3: `MaxInputLen` measures bytes; `MaxInputLenRunes` is a v1.3
      additive candidate.
    - Q4: `StreamDelta` is opt-in (default OFF) for all 3 built-in
      gates; the per-delta best-effort semantics + cross-delta-leak
      limitation documented; users wanting cross-delta detection
      register a `PostGenerate` redactor that fires once on the assembled
      response.
    - Q5: `BlockedError.Decision` field IS shipped (struct copy of
      deciding decision); callers introspect via `errors.As`.
    Subsection `// # Gate ordering`: gates run in registration order,
    sequentially; first `Block` wins (chain short-circuits); `Replace`
    and `Redact` let subsequent gates see the rewrite.
    Subsection `// # Streaming semantics`: PreStream fires once on first
    Next(); StreamDelta fires per inner event (gates opt in); PostStream
    fires on EventDone OR io.EOF; Block on StreamDelta surfaces
    immediately (current Next() returns BlockedError, inner closed).
    Cross-delta PII can leak — documented limitation matching rag's
    `guard`.
    Subsection `// # Composition with budget`: the v1.2 stack is
    `policy.Wrap(otelmodel.Wrap(provider))`; budget enforces at the
    `generateFromPrompt` chokepoint UNDERNEATH the wrapped model — a
    budget-exhausted request short-circuits BEFORE policy is reached.
    `BlockedError.Wrapped` may be a budget error in cascading scenarios.
    Final subsection `// # References`: cite `CC-2`, `KC-3`, `KC-5`,
    `K1`, and the package-internal Decisions A-H from 36-RESEARCH.md.
    File ends with `package policy`. No imports. Total length ≤ 80
    lines.
  </action>
  <verify>
    <automated>
      cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./policy/... && grep -q '^// Package policy provides' policy/doc.go && grep -q 'Q1' policy/doc.go && grep -q 'Q2' policy/doc.go && grep -q 'Q3' policy/doc.go && grep -q 'Q4' policy/doc.go && grep -q 'Q5' policy/doc.go && grep -q 'CC-2' policy/doc.go && grep -q 'KC-3' policy/doc.go && grep -q 'KC-5' policy/doc.go && echo DOC-OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `policy/doc.go` exists, contains `package policy` declaration, and `go vet ./policy/...` exits 0.
    - First non-blank line is `// Package policy provides ...` (godoc convention).
    - Q1, Q2, Q3, Q4, Q5 all cited verbatim by name in `policy/doc.go`.
    - `CC-2`, `KC-3`, `KC-5` all cited in the References subsection.
    - `wc -l policy/doc.go` ≤ 80.
    - `grep -E '^import|^\timport' policy/doc.go | wc -l` returns 0 (no imports in doc.go).
  </acceptance_criteria>
  <done>
    `policy/doc.go` is the package-doc canonical reference for future readers; Q1-Q5 ratifications are recorded so future plan phases or sister-repo contributors do not re-litigate the design.
  </done>
</task>

</tasks>

<verify>
- vet clean repo-wide:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && echo VET-OK`
- policy package tests green:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./policy/... -count=1 && echo POLICY-TEST-OK`
- race-clean on policy:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test -race ./policy/... -count=1 && echo POLICY-RACE-OK`
- repo-wide build still green (smoke):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go build ./... && echo BUILD-OK`
- repo-wide tests still green (no regression on agents/budget/llm):
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1 && echo REPO-TEST-OK`
- stdlib-only — zero non-stdlib imports outside `github.com/costa92/llm-agent/llm`:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && GOWORK=off go list -f '{{join .Imports "\n"}}' ./policy/ | grep -v '^$' | grep -vE '^(context|errors|fmt|io|sync|github.com/costa92/llm-agent/llm)$' | wc -l` returns 0
- public surface present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for s in 'func Wrap(' 'func WrapConfig(' 'type Config struct' 'type Gate interface' 'type Event struct' 'type EventKind uint8' 'type Decision struct' 'type DecisionAction uint8' 'ErrBlocked' 'type BlockedError struct' 'PreGenerate' 'PostGenerate' 'PreStream' 'StreamDelta' 'PostStream' 'Allow' 'Block' 'Redact' 'Replace'; do grep -rq "$s" policy/ || { echo "MISSING $s"; exit 1; }; done && echo SURFACE-OK`
- 21 compile-time assertions present:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -E '^\s*_ llm\.(ChatModel|ToolCaller|Embedder|StructuredOutputs)\s+=' policy/policy.go | wc -l` >= 21
- 8 wrapper structs defined:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && grep -cE '^type (wrapper|toolWrapper|embedWrapper|schemaWrapper|toolEmbedWrapper|toolSchemaWrapper|embedSchemaWrapper|toolEmbedSchemaWrapper) struct' policy/policy.go` == 8
- KC-5 unchanged validated public types:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- llm/chatmodel.go llm/capabilities.go llm/types.go llm/stream.go agent.go agents.go simple.go react.go reflection.go plan_solve.go function_call.go memory/ orchestrate/ | wc -l` (0)
- go.mod / go.sum unchanged:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && git status --short -- go.mod go.sum | wc -l` (0)
- doc.go cites Q1-Q5 + CC-2 + KC-3 + KC-5:
  `cd /home/hellotalk/code/go/src/github.com/costa92/llm-agent-ecosystem/llm-agent && for t in Q1 Q2 Q3 Q4 Q5 CC-2 KC-3 KC-5; do grep -q "$t" policy/doc.go || { echo "MISSING $t in doc.go"; exit 1; }; done && echo DOC-OK`
</verify>

<acceptance>
- `policy/` directory exists with `doc.go`, `gate.go`, `gate_test.go`, `policy.go`, `policy_test.go` — all 5 files compile and vet clean.
- Public surface present and doc-commented: `Wrap`, `WrapConfig`, `Config`, `Gate`, `Event`, `EventKind` (5 constants), `Decision`, `DecisionAction` (4 constants — `Allow` is zero), `ErrBlocked`, `BlockedError` (with `Is`, `Unwrap`, `Decision` field per Q5).
- All 8 wrapper struct types ship: `wrapper`, `toolWrapper`, `embedWrapper`, `schemaWrapper`, `toolEmbedWrapper`, `toolSchemaWrapper`, `embedSchemaWrapper`, `toolEmbedSchemaWrapper`.
- 21 compile-time `var _ llm.X = (*Y)(nil)` assertions ship — `go vet` catches any drop.
- `policy.Wrap(model)` is capability-preserving across all 2³ combinations (`TestWrap_PreservesCapabilities`).
- `WithTools` / `WithSchema` rebind via `(*wrapper).wrap` — gates survive (`TestWithTools_PreservesGates`, `TestWithSchema_PreservesGates`).
- Block / Redact / Replace decisions each have a passing test on the Generate path.
- Stream decorator fires PreStream / StreamDelta / PostStream correctly; Block on PreStream surfaces immediately on the first `Next()` call.
- `OnDecision` is synchronous + panic-recovered + nil-safe.
- `go test -race ./policy/...` is green.
- `policy/doc.go` records Q1-Q5 ratifications + cites CC-2, KC-3, KC-5.
- `go.mod` unchanged; no `go.sum` created.
- No edit to `llm.ChatModel` / `agents.Agent` / `memory.Memory` / `orchestrate.NodeFunc[S]` (KC-5).
- `go list -deps ./policy/` shows only stdlib + `github.com/costa92/llm-agent/llm`.
- All `<verify>` commands pass.
</acceptance>

<success_criteria>
A v0.6.0 caller compiles unchanged when they add `policy.Wrap(model)`
to their existing `model` construction (KC-5 additive). The wrapped
model preserves every capability assertion (`ToolCaller`, `Embedder`,
`StructuredOutputs`) and every `WithTools`/`WithSchema` rebind keeps
the policy chain. Custom gates can deny, redact, or replace requests
via the 4-action `Decision` enum; `OnDecision` provides a sync audit
log hook. The package is pure stdlib + the in-repo `llm` import; no
new module added to `go.mod`. This slice's surface is the foundation
the 3 built-in gates (36-02), the compose-with-otel test (36-03), and
the example (36-04) all build on.
</success_criteria>

<output>
Create `.planning/phases/36-policy-safety-middleware/36-01-SUMMARY.md`
when done.
</output>
