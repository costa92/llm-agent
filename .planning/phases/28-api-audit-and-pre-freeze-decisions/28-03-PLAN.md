---
phase: 28-api-audit-and-pre-freeze-decisions
plan: 03
type: execute
wave: 2
repo: llm-agent-rag
depends_on: ["28-01"]
files_modified:
  - README.md
  - docs/api-audit-v1.0.md
requirements: [RAG-API-02]
must_haves:
  truths:
    - "the README no longer claims shipped features are unimplemented"
    - "the zero-TODO/replace/dead-code state is recorded as a verified release-readiness fact"
  artifacts:
    - path: "README.md"
      provides: "a README whose Not-implemented list is factually correct for v0.6.0"
      contains: |
        Not implemented yet: lists only genuine non-goals (HTTP service, CLI);
        no stale feedback-workflow or contract-gate claims
    - path: "docs/api-audit-v1.0.md"
      provides: "a Release-readiness section recording the clean-codebase audit"
      contains: |
        verified zero TODO/FIXME/replace/dead-code; grep evidence
---

<objective>
Correct the stale `README.md` "Not implemented yet" claims and record the
verified clean-codebase state as a release-readiness fact in
`docs/api-audit-v1.0.md`. RAG-API-02.
</objective>

<context>
- `README.md` line ~147 `Not implemented yet:` lists four items:
  `HTTP service layer`, `CLI`, `online-to-offline production-feedback
  workflow (planned in slice 13-03)`, `cross-repo contract-drift CI gates
  (planned in slice 13-04)`.
  - The **feedback workflow shipped** — the `feedback` package exists.
  - The **contract gate shipped** — `contract/contract_test.go` exists.
  - `HTTP service layer` and `CLI` are genuine deferred non-goals — they
    **stay** on the list.
- The README **status line** (line ~48 `Current status: production-ready
  core, evolving ecosystem.`) and the compatibility-policy link are **Phase
  29** work (29-03) — do **not** touch the status line here. This slice
  fixes factual staleness only.
- `docs/api-audit-v1.0.md` was created by slice 28-01 — this slice appends
  to it.
- The milestone audit found zero `TODO`/`FIXME`/`XXX`/`HACK`/`Deprecated`
  in non-test code and no `replace` directives — 28-03 *re-verifies and
  records* this as a release-readiness fact.
- See `28-RESEARCH.md` Decision 3.
</context>

<tasks>
1. `README.md` — edit the `Not implemented yet:` list: **remove** the
   `online-to-offline production-feedback workflow (planned in slice 13-03)`
   line and the `cross-repo contract-drift CI gates (planned in slice
   13-04)` line. Keep `HTTP service layer` and `CLI`. Do not touch any
   other part of the README (the status line is 29-03).
2. Verify each surviving README claim against the codebase before closing:
   confirm there is no HTTP service package and no CLI entry point
   (`cd /tmp/llm-agent-rag && ls cmd/ 2>/dev/null; grep -rln 'net/http' --include=*.go . | grep -v _test`).
   If anything contradicts the surviving list, surface it rather than
   silently editing.
3. Append a **Release-readiness** section to `docs/api-audit-v1.0.md`
   recording, with the grep commands as evidence:
   - zero `TODO`/`FIXME`/`XXX`/`HACK`/`Deprecated` markers in non-test code:
     `cd /tmp/llm-agent-rag && grep -rn 'TODO\|FIXME\|XXX\|HACK\|Deprecated' --include=*.go . | grep -v _test.go`
     (expected: empty);
   - no `replace` directives: `grep -n '^replace\|	replace' go.mod` (empty);
   - the README "Not implemented yet" list is now factually accurate as of
     this slice.
4. Run every command in `<verify>`.

Out of scope:
- The README status line + compatibility link — 29-03.
- Any Go code change.
- `docs/compatibility.md` — Phase 29.
</tasks>

<verify>
- stale claims gone from the README:
  `cd /tmp/llm-agent-rag && ! grep -n 'slice 13-03\|slice 13-04\|contract-drift CI gates\|online-to-offline production-feedback' README.md`
- genuine non-goals retained:
  `cd /tmp/llm-agent-rag && grep -n 'HTTP service layer' README.md && grep -n 'CLI' README.md`
- the audit doc gained the Release-readiness section:
  `cd /tmp/llm-agent-rag && grep -n 'Release-readiness' docs/api-audit-v1.0.md`
- clean-state still true:
  `cd /tmp/llm-agent-rag && ! grep -rn 'TODO\|FIXME\|XXX\|HACK\|Deprecated' --include=*.go . | grep -v _test.go`
- no Go code touched — build/test still green:
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
</verify>

<acceptance>
- The README "Not implemented yet" list names only genuine non-goals (HTTP
  service layer, CLI); the shipped feedback workflow and contract gate are
  removed.
- `docs/api-audit-v1.0.md` has a Release-readiness section recording the
  verified zero-`TODO`/`replace`/dead-code state with grep evidence.
- The README status line is unchanged (deferred to 29-03).
- No Go code changed; no new module dependency; all `<verify>` commands pass.
</acceptance>
