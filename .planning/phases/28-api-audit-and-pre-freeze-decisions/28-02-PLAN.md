---
phase: 28-api-audit-and-pre-freeze-decisions
plan: 02
type: execute
wave: 2
repo: llm-agent-rag
depends_on: ["28-01"]
files_modified:
  - eval/eval.go
  - eval/graph.go
  - eval/eval_test.go
  - eval/drift.go
  - eval/global.go
  - doc.go
requirements: [RAG-API-02]
must_haves:
  truths:
    - "the eval base evaluator is named RetrievalEvaluator/RetrievalResult, symmetric with the prefixed answer-path evaluators"
    - "the ragkit root package comment documents it as a deliberate doc-anchor, not an accidental name mismatch"
    - "the renames are contained to the eval package and break no contract-pinned symbol"
  artifacts:
    - path: "eval/eval.go"
      provides: "the renamed retrieval evaluator surface"
      contains: |
        type RetrievalResult struct; type RetrievalEvaluator struct;
        (RetrievalEvaluator).Run returns RetrievalResult
    - path: "doc.go"
      provides: "the ragkit doc-anchor package comment"
      contains: |
        package ragkit; comment states it is a documentation anchor and
        callers import the sub-packages
---

> Archived planning record only.
> Do not use as current development guide.
> Current development follows live code and `llm-agent-rag` current docs.


<objective>
Apply the two ratified pre-freeze breaking changes: rename the `eval` base
evaluator `Evaluator`/`Result` → `RetrievalEvaluator`/`RetrievalResult`
(KS-4), and rewrite the `ragkit` `doc.go` package comment to document the
root as a deliberate documentation anchor (KS-3). RAG-API-02.

After `v1.0.0` these are impossible without a `/v2` — this slice is the
last opportunity.
</objective>

<context>
- `eval/eval.go`: `type Result struct` (~line 65), `type Evaluator struct`
  (~line 81), `func (e Evaluator) Run(...) (Result, error)` (~line 88) with
  internal `Result{}` / `Result{...}` returns.
- Reference sites verified repo-wide by grep (`28-RESEARCH.md`):
  - `eval/graph.go:26,32` — `RunGraphAB` builds `Evaluator{Retriever:..., Options:...}`.
  - `eval/eval_test.go` — `eval.Evaluator{...}` at lines ~99, ~144, ~151;
    test func names `TestEvaluatorRunRejectsNilRetriever`,
    `TestEvaluatorRunRejectsZeroTopK`.
  - `eval/drift.go:51`, `eval/global.go:48` — doc-comment text
    "RunGraphAB / Evaluator measure that".
  - **No references in `examples/`, `contract/`, or any package outside
    `eval`** — the rename is contained.
- The three later evaluators stay as-is: `GlobalEvaluator`,
  `DriftEvaluator`, `TriadEvaluator` (already prefixed). `(Evaluator).Run`
  keeps the method name `Run`.
- `doc.go` declares `package ragkit`, holds one package comment and **no
  exported symbols**. Keep the name `ragkit` and the file; only the comment
  text changes.
- See `28-RESEARCH.md` Decision 2; the recorded decisions in
  `docs/api-audit-v1.0.md` (slice 28-01).
</context>

<tasks>
1. `eval/eval.go` — rename the type `Result` → `RetrievalResult` and
   `Evaluator` → `RetrievalEvaluator`. Update the method receiver
   (`func (e RetrievalEvaluator) Run`), the return type on `Run`, and every
   internal `Result{}` / `Result{...}` literal. Update the doc comments on
   both types so they start with the new name (Go convention) and keep
   describing the retrieval recall/MRR/precision role.
2. `eval/graph.go` — update the two `Evaluator{...}` constructions in
   `RunGraphAB` (lines ~26, ~32) to `RetrievalEvaluator{...}`.
3. `eval/eval_test.go` — update `eval.Evaluator{...}` → `eval.RetrievalEvaluator{...}`
   at every site; rename the affected test funcs for consistency
   (`TestRetrievalEvaluatorRunRejectsNilRetriever`,
   `TestRetrievalEvaluatorRunRejectsZeroTopK`); update any
   `eval.Result`-typed variables.
4. `eval/drift.go`, `eval/global.go` — update the doc-comment mentions of
   `Evaluator` to `RetrievalEvaluator` so the prose stays accurate.
5. Defensive grep — confirm no `eval.Evaluator` / `eval.Result` (or bare
   `Evaluator`/`Result` referring to the renamed types) remains anywhere:
   `cd /tmp/llm-agent-rag && grep -rn '\bEvaluator\b\|\bResult\b' --include=*.go . | grep -v 'Retrieval\|Global\|Drift\|Triad'`
   — the only acceptable hits are unrelated identifiers; eyeball the output.
6. `doc.go` — keep `package ragkit`; rewrite the package comment to state:
   `ragkit` is the SDK's short name and **the root package is a
   documentation anchor only** — it exports nothing and callers should
   import the sub-packages (`rag`, `retrieve`, `store`, …) directly. Keep
   it concise (a short paragraph). The package name stays `ragkit`.
7. Run every command in `<verify>`.

Out of scope:
- Any rename beyond the two ratified ones — KS-2: v1.0 freezes, no redesign.
- The `eval` four-evaluator unification refactor — explicitly rejected.
- The README fix — 28-03. `docs/compatibility.md` — Phase 29.
- The `CHANGELOG.md` `[v1.0.0]` entry — 30-03.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./eval/... -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- new names present, old names gone:
  `cd /tmp/llm-agent-rag && go doc ./eval RetrievalEvaluator && go doc ./eval RetrievalResult`
  `cd /tmp/llm-agent-rag && ! grep -rn 'eval\.Evaluator\b\|eval\.Result\b' --include=*.go .`
- `ragkit` comment rewritten: `cd /tmp/llm-agent-rag && go doc . | head -5`
  (shows the doc-anchor wording)
- no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
- contract gate green: `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./contract/... -count=1`
- core facade smoke (run from the core repo `llm-agent`):
  `GOWORK=off go vet ./rag/... && go test ./rag/...`
</verify>

<acceptance>
- `eval.RetrievalEvaluator` and `eval.RetrievalResult` exist; `eval.Evaluator`
  and `eval.Result` no longer exist; `(RetrievalEvaluator).Run` returns
  `RetrievalResult`.
- `RunGraphAB`, the `eval` tests, and the `drift.go`/`global.go` doc
  comments all reference the new names.
- `doc.go` keeps `package ragkit` and its comment documents the root as a
  deliberate documentation anchor.
- No symbol renamed beyond the two ratified ones; no new dependency.
- The `contract` gate and the core-facade smoke both pass.
- All `<verify>` commands pass.
</acceptance>
