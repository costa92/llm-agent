---
phase: 28-api-audit-and-pre-freeze-decisions
plan: 01
type: execute
wave: 1
repo: llm-agent-rag
depends_on: []
files_modified:
  - docs/api-audit-v1.0.md
requirements: [RAG-API-01]
must_haves:
  truths:
    - "every exported symbol of llm-agent-rag is classified keep/rename/unexport in one written, reviewed doc"
    - "the audit confirms in writing there are no accidental exports"
    - "the Ask*/*Options naming and the two pre-freeze renames are recorded as ratified decisions"
  artifacts:
    - path: "docs/api-audit-v1.0.md"
      provides: "the v1.0 freeze-time exported-surface inventory and naming-decision record"
      contains: |
        per-package exported-symbol tables; keep/rename/unexport column;
        no-accidental-exports confirmation; ratified Ask*/*Options + rename decisions
---

<objective>
Produce `docs/api-audit-v1.0.md` — a written, reviewed inventory of
`llm-agent-rag`'s entire exported surface, classifying every exported
symbol keep / rename / unexport, confirming no accidental exports, and
recording the ratified pre-freeze naming decisions. RAG-API-01.

This slice writes **no Go code** — it is a documentation + audit artifact.
</objective>

<context>
- v1.0 is a freeze, not a feature milestone (KS-2). The audit *records keep
  decisions* — it does not trim or redesign the surface.
- `go list ./...` (default tags) yields 22 packages: root (`doc.go`),
  `advanced`, `agentic`, `contract`, `embed`, `eval`, `examples`,
  `feedback`, `generate`, `graph`, `guard`, `ingest`, `obs`, `pack`,
  `postgres`, `prompt`, `rag`, `rerank`, `retrieve`, `store`,
  `store/storetest`, `tree`. `adapter/llmagent` is additional, behind
  `-tags llmagent`.
- `examples` is a test-only worked-examples package — note it as such; it
  has no first-class exported API to freeze.
- The two ratified renames (recorded here, *applied* in 28-02):
  `eval.Evaluator`→`RetrievalEvaluator`, `eval.Result`→`RetrievalResult`
  (KS-4). The `ragkit` `doc.go` comment rewrite (KS-3) is also 28-02.
- The `Ask`/`AskGlobal`/`AskDrift` vs `AskOptions`/`GlobalOptions`/
  `DriftOptions` naming is ratified **as-is** — record the rationale.
- `pack.TokenCounter`/`SimpleCounter` and the `retrieve` concrete-struct
  surface are deliberate seams — record as keep.
- See `28-RESEARCH.md` Decision 1.
</context>

<tasks>
1. Enumerate the exported surface. For each importable package, capture the
   exported symbols:
   - `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go list ./...`
     for the package list;
   - `GOWORK=off GOCACHE=/tmp/go-build go doc <import-path>` per package for
     the exported types/funcs/vars/consts;
   - `GOWORK=off GOCACHE=/tmp/go-build go doc <import-path>.<Type>` to
     expand exported struct fields / interface methods where the
     keep/rename judgement needs them;
   - include `adapter/llmagent`:
     `go doc` works under the build tag — inspect its source directly
     (`adapter/llmagent/*.go`) since `go doc` honors build tags.
2. Create `docs/api-audit-v1.0.md` in the `llm-agent-rag` repo. Structure:
   - **Header** — purpose (the v1.0 freeze-time audit), the audited tag
     (`git describe --tags`), date, and a one-line pointer that
     `docs/compatibility.md` (Phase 29) is the *living* policy while this is
     a *point-in-time* record.
   - **Per-package inventory** — one subsection per package, a table:
     `Symbol | Kind (type/func/method/var/const/interface) | Disposition
     (keep / rename→X / unexport) | Note`. Every exported symbol appears.
   - **Accidental-export confirmation** — a written statement that the
     surface was reviewed symbol-by-symbol and the seam interfaces
     (`EntityLinker`, `QueryDecomposer`, `SectionPlanner`, `PathRanker`,
     `pack.TokenCounter`/`SimpleCounter`, the `retrieve` concrete
     retrievers, the `store` capability interfaces, …) are deliberate
     plug-points, not accidents — disposition keep.
   - **Ratified naming decisions** — a section recording:
     - the `Ask`/`AskGlobal`/`AskDrift` vs `AskOptions`/`GlobalOptions`/
       `DriftOptions` naming **ratified as-is**, with the rationale (the
       option structs name the answer *mode*, the set is internally
       consistent, renaming is churn);
     - the two ratified renames `eval.Evaluator`→`RetrievalEvaluator` and
       `eval.Result`→`RetrievalResult` (KS-4) — recorded as decisions
       *applied in slice 28-02*, with the symmetry rationale and the
       grep-confirmed reference scope (`eval` package only);
     - the `ragkit` `doc.go` comment rewrite (KS-3) — recorded as a
       decision *applied in slice 28-02*.
   - **Contract-gate cross-check** — confirm in writing (cite
     `contract/contract_test.go`) that neither rename touches a
     contract-pinned symbol, so no coordinated core-repo PR is required.
3. Run every command in `<verify>`.

Out of scope:
- Applying any rename or the `doc.go` rewrite — 28-02.
- The README stale-claim fix — 28-03.
- `docs/compatibility.md` — Phase 29.
- Any Go code change.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && test -f docs/api-audit-v1.0.md && echo OK`
- audit doc names every package from `go list ./...` plus `adapter/llmagent`:
  `cd /tmp/llm-agent-rag && for p in $(GOWORK=off GOCACHE=/tmp/go-build go list ./... | sed 's#.*llm-agent-rag/\?##'); do grep -q "${p:-root}" docs/api-audit-v1.0.md || echo "MISSING: ${p:-root}"; done` (no MISSING lines)
- no Go code touched — build/vet/test still green:
  `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./... && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
- only `docs/api-audit-v1.0.md` is added:
  `cd /tmp/llm-agent-rag && git status --short` (one untracked file, no modified `.go`)
</verify>

<acceptance>
- `docs/api-audit-v1.0.md` exists and classifies every exported symbol of
  every importable package + `adapter/llmagent` as keep / rename / unexport.
- It confirms in writing there are no accidental exports.
- It records the `Ask*`/`*Options` naming as ratified-as-is and the two
  renames + the `ragkit` comment rewrite as decisions for 28-02.
- It confirms the renames touch no `contract`-pinned symbol.
- No Go code changed; no new module dependency; all `<verify>` commands pass.
</acceptance>
