---
phase: 09-source-aware-ingestion-and-index-lifecycle
plan: 01
type: execute
wave: 1
repo: llm-agent-rag
depends_on: []
files_modified:
  - ingest/
  - rag/
autonomous: true
requirements: [RAG-INGEST-01]
must_haves:
  truths:
    - "source identity and version lineage live at the ingest document boundary"
    - "chunk and stored-chunk metadata automatically preserve lineage fields"
  artifacts:
    - path: "ingest/"
      provides: "source-aware document lineage contract"
      contains: "document fields + metadata propagation tests"
---

<objective>
Add source identity, version, checksum, and embedding-version lineage to the
standalone ingestion path so later retrieval and backend work can rely on
stable metadata instead of ad hoc caller conventions.
</objective>

<tasks>
- Extend `ingest.Document` with additive lineage fields.
- Propagate lineage into chunk metadata during splitting.
- Preserve lineage through the `rag.System.Import(...)` path into stored
  chunks.
- Add tests covering metadata propagation from document -> chunk -> store.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./ingest ./rag -count=1`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
</verify>
