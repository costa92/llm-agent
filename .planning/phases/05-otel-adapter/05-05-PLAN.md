---
phase: 05-otel-adapter
plan: 05
type: execute
wave: 2
repo: llm-agent-otel
depends_on: [01, 02, 03, 04]
files_modified:
  - compose/
  - README.md
autonomous: true
requirements: [OTEL-09, OTEL-10]
must_haves:
  truths:
    - "exporter wiring and compose example prove end-to-end visibility"
    - "README documents opt-in semantics, defaults, and demo workflow"
  artifacts:
    - path: "compose/"
      provides: "Grafana/Tempo demo"
      contains: "otel-lgtm example"
---

<objective>
Wire exporters and ship a minimal compose-based demonstration that shows
wrapped traces end-to-end.
</objective>

<tasks>
- Add OTLP HTTP exporter defaults and any opt-in gRPC support.
- Create a `compose/` example using `grafana/otel-lgtm`.
- Document wrapper usage, semconv opt-in, content-capture defaults, and the
  demo verification flow in `README.md`.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./... -count=1`
- `GOCACHE=/tmp/go-build go build ./...`
</verify>
