---
phase: 05-otel-adapter
plan: 02
type: execute
wave: 1
repo: llm-agent-otel
depends_on: []
files_modified:
  - otelagent/
autonomous: true
requirements: [OTEL-02, OTEL-07]
must_haves:
  truths:
    - "otelagent.Wrap preserves the agent contract"
    - "agent runs produce invoke_agent/chat/execute_tool span structure"
  artifacts:
    - path: "otelagent/"
      provides: "agent wrapper"
      contains: "Wrap(Agent)"
---

<objective>
Build the agent wrapper that instruments high-level agent runs and step
execution without changing agent semantics.
</objective>

<tasks>
- Add `otelagent.Wrap(Agent) Agent`.
- Emit the `invoke_agent` span tree and map agent steps into child spans or
  bounded events.
- Add tests against representative Simple/ReAct-style traces.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./otelagent/... -count=1`
- `GOCACHE=/tmp/go-build go build ./otelagent/...`
</verify>
