---
phase: 05-otel-adapter
plan: 03
type: execute
wave: 1
repo: llm-agent-otel
depends_on: []
files_modified:
  - otelmetrics/
  - semconv_gen_ai.go
autonomous: true
requirements: [OTEL-03, OTEL-04, OTEL-05, OTEL-06]
must_haves:
  truths:
    - "semconv constants are centralized"
    - "metrics stay low-cardinality and content capture defaults OFF"
  artifacts:
    - path: "semconv_gen_ai.go"
      provides: "centralized gen_ai constants"
      contains: "stability opt-in gate"
---

<objective>
Centralize semconv emission and land metrics/content-capture guardrails before
exporter wiring.
</objective>

<tasks>
- Add centralized `gen_ai.*` constants and opt-in checks.
- Emit token/duration/TTFT metrics with a strict attribute allowlist.
- Add tests proving 1000 distinct user IDs do not explode metric cardinality
  and that content capture is disabled by default.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./... -run 'Test(Metrics|ContentCapture|Cardinality)' -count=1`
- `GOCACHE=/tmp/go-build go build ./...`
</verify>
