---
phase: 05-otel-adapter
plan: 01
type: execute
wave: 1
repo: llm-agent-otel
depends_on: []
files_modified:
  - otelmodel/
autonomous: true
requirements: [OTEL-01, OTEL-03, OTEL-07]
must_haves:
  truths:
    - "otelmodel.Wrap preserves llm capability interfaces"
    - "Generate and Stream instrumentation emits one span per operation"
  artifacts:
    - path: "otelmodel/"
      provides: "capability-preserving model wrapper"
      contains: "Wrap(ChatModel)"
---

<objective>
Build the provider wrapper that instruments `llm.ChatModel` without erasing any
optional capability interfaces.
</objective>

<tasks>
- Add `otelmodel.Wrap(ChatModel) ChatModel`.
- Re-expose `ToolCaller`, `Embedder`, and `StructuredOutputs` when present on
  the inner model.
- Instrument `Generate` and `Stream` with one span per operation and bounded
  streaming events.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./otelmodel/... -count=1`
- `GOCACHE=/tmp/go-build go build ./otelmodel/...`
</verify>
