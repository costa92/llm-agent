---
phase: 05-otel-adapter
plan: 04
type: execute
wave: 1
repo: llm-agent-otel
depends_on: []
files_modified:
  - otelslog/
autonomous: true
requirements: [OTEL-08]
must_haves:
  truths:
    - "slog handler bridges trace/span ids and gen_ai fields"
    - "log bridge composes with the tracing wrapper config"
  artifacts:
    - path: "otelslog/"
      provides: "slog bridge"
      contains: "Handler"
---

<objective>
Add the slog bridge so wrapped models and agents can emit correlated structured
logs.
</objective>

<tasks>
- Implement `slog.Handler` integration with trace/span correlation.
- Preserve structured fields and route `gen_ai.*` keys cleanly.
- Add focused tests for attribute propagation and correlation IDs.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./otelslog/... -count=1`
- `GOCACHE=/tmp/go-build go build ./otelslog/...`
</verify>
