---
phase: 21-graph-storage
plan: 02
type: execute
wave: 2
repo: llm-agent-rag
depends_on: [21-01]
files_modified:
  - postgres/postgres.go
  - postgres/graph.go
  - postgres/postgres_conformance_test.go
requirements: [RAG-GRAPH-03]
must_haves:
  truths:
    - "the postgres store persists and traverses the graph too — the same GraphStore capability, recursive-CTE traversal, no graph database"
    - "the postgres and in-memory graph stores pass the same shared conformance suite"
    - "recursive traversal is bounded in SQL — by an explicit depth predicate and a row limit"
  artifacts:
    - path: "postgres/graph.go"
      provides: "postgres GraphStore implementation"
      contains: |
        UpsertGraph / RemoveGraphBySource / Neighborhood (WITH RECURSIVE) /
        FindEntities over entities + relations tables
---

> Archived planning record only.
> Do not use as current development guide.
> Current development follows live code and `llm-agent-rag` current docs.


<objective>
Complete RAG-GRAPH-03's storage half: `postgres.Store` implements
`store.GraphStore` over `entities`/`relations` tables with recursive-CTE
traversal — no graph database — and passes the same `RunGraphConformance`
suite as the in-memory store.
</objective>

<context>
- 21-01 delivered `store.GraphStore`, `graph.Subgraph`, the in-memory
  implementation, and `storetest.RunGraphConformance`.
- `postgres.Store` — `Migrate(ctx)` runs idempotent DDL (the place to add
  the new tables); `isSafeIdent` guards configurable identifiers;
  `LexicalSearch` + `var _ store.LexicalSearcher = (*Store)(nil)` is the
  capability-implementation precedent. pgx is already a dependency.
- `postgres/postgres_conformance_test.go` — env-gated tests
  (`openTestPool`, `newTableStore`); `TestPostgresLexicalConformance` runs
  the shared lexical suite. The graph suite wires up the same way.
- `graph.Graph`, `graph.Entity`, `graph.Relation`, `graph.Subgraph`.
- See `21-RESEARCH.md` Decisions 4 and 5.
</context>

<tasks>
1. Extend `postgres.Store.Migrate` with two idempotent `CREATE TABLE IF NOT
   EXISTS` statements (namespaced; provenance as a `text[]` column):
   - `entities` — id, namespace, name, type, description, source_chunk_ids
     `text[]`, metadata `jsonb`; primary key `(namespace, id)`.
   - `relations` — id, namespace, source, target, relation, description,
     source_chunk_ids `text[]`, weight; primary key `(namespace, id)`.
   - Indexes: an index on `(namespace, source)` and `(namespace, target)`
     for traversal; one on `(namespace, lower(name))` for `FindEntities`.
   Keep the existing migration's style (idempotent, `isSafeIdent` for any
   configurable identifier).
2. Create `postgres/graph.go` — `postgres.Store` methods implementing
   `store.GraphStore`:
   - `UpsertGraph` — `INSERT ... ON CONFLICT (namespace, id) DO UPDATE`
     union-merging description and `source_chunk_ids` (array union);
     entities and relations in one transaction.
   - `RemoveGraphBySource(chunkIDs)` — remove the given ids from every
     row's `source_chunk_ids` (`array_remove` / anti-join), then
     `DELETE` rows whose `source_chunk_ids` became empty (GC).
   - `Neighborhood(seedIDs, depth)` — a `WITH RECURSIVE` CTE walking
     `relations` from the seeds (both directions), bounded by an explicit
     depth predicate (`depth` clamped to `[0, 2]` — KG-7) **and** a row
     `LIMIT` (the explosion guard); return `graph.Subgraph` with the
     reached entities, their relations, and the `Depth` map.
   - `FindEntities(names)` — `SELECT ... WHERE namespace = $1 AND
     lower(name) = ANY($2)`.
   - Compile-time `var _ store.GraphStore = (*Store)(nil)`.
3. Add `TestPostgresGraphConformance` to
   `postgres/postgres_conformance_test.go` — env-gated, reusing
   `openTestPool`/`newTableStore`, running `storetest.RunGraphConformance`.
4. Run every command in `<verify>`.

Out of scope:
- `Import` wiring — 21-03.
- Graph retrieval — Phase 22.
- Community detection / global search — v0.8.
</tasks>

<verify>
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go build ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go vet ./...`
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./postgres ./store/... -count=1`
  (the postgres graph conformance test is env-gated — it skips cleanly
  without a live database; compile + skip is the bar in this environment)
- `cd /tmp/llm-agent-rag && GOWORK=off GOCACHE=/tmp/go-build go test ./... -count=1`
- confirm no new dep: `cd /tmp/llm-agent-rag && git diff --stat go.mod go.sum` (must be empty)
- core facade smoke (run from the core repo `llm-agent`):
  `GOWORK=off go vet ./rag/... && go test ./rag/...`
</verify>

<acceptance>
- `postgres.Store` implements `store.GraphStore` (compile-time asserted).
- `Migrate` creates the `entities`/`relations` tables idempotently;
  `Neighborhood` traverses with a `WITH RECURSIVE` CTE bounded by depth
  (≤ 2) and a row limit.
- `TestPostgresGraphConformance` runs `RunGraphConformance` env-gated and
  compiles + skips cleanly without a live database.
- No new module dependency (`pgx` already present).
- All `<verify>` commands pass.

## Notes

The `postgres` graph path cannot be verified against a live database in the
current environment — it compiles, vets, and skips clean. Live-Postgres CI
wiring is carried-forward v0.5 debt; report the unverified-live status
honestly in the summary.
