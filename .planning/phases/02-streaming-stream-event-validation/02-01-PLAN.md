---
phase: 02-streaming-stream-event-validation
plan: 01
type: execute
wave: 1
repo: llm-agent-providers
depends_on: []
files_modified:
  - openai/openai.go
  - openai/map.go
  - openai/openai_test.go
autonomous: true
requirements: [OAI-02, OAI-06, OAI-07]
must_haves:
  truths:
    - "OpenAI adapter implements real Stream() over openai-go/v3"
    - "Outbound streaming requests always set stream_options.include_usage=true"
    - "Retry state machine never retries after first byte is delivered"
    - "Per-index accumulation infrastructure exists for interleaved tool-call deltas"
  artifacts:
    - path: "openai/openai.go"
      provides: "Stream implementation and internal retry boundary"
      contains: "func (o *OpenAI) Stream"
    - path: "openai/openai_test.go"
      provides: "stream ordering, usage, and retry tests"
      contains: "TestStream_OpenAI"
---

<objective>
Implement OpenAI streaming in the sister repo using the same adapter package
landed in Phase 1. Preserve the Generate contract, add a real Stream method,
enforce `stream_options.include_usage`, and verify the pre-first-byte-only retry
rule in tests.
</objective>

<context>
@.planning/phases/02-streaming-stream-event-validation/02-CONTEXT.md
@.planning/phases/02-streaming-stream-event-validation/02-RESEARCH.md
@.planning/phases/02-streaming-stream-event-validation/02-PATTERNS.md
@/tmp/llm-agent-providers/openai/openai.go
@/tmp/llm-agent-providers/openai/map.go
@/tmp/llm-agent-providers/openai/openai_test.go
</context>

<tasks>
- Add `Stream(ctx, req)` using the OpenAI SDK streaming API.
- Emit ordered `StreamEvent` values and final usage.
- Include request-body assertions that `stream_options.include_usage` is present.
- Add unit tests for ordered text deltas, cancel-mid-stream, pre-first-byte retry,
  and no retry after first byte.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./openai/...`
- `GOCACHE=/tmp/go-build go build ./openai/...`
</verify>
