---
phase: 02-streaming-stream-event-validation
plan: 03
type: execute
wave: 1
repo: llm-agent-providers
depends_on: []
files_modified:
  - ollama/ollama.go
  - ollama/map.go
  - ollama/ollama_test.go
autonomous: true
requirements: [OLL-02, OLL-06]
must_haves:
  truths:
    - "Ollama adapter implements real Stream() over the callback-based SDK"
    - "ctx cancellation propagates to the outward iterator promptly"
    - "Stream bridging introduces no goroutine leaks"
    - "Usage.Source semantics match the shared reported-vs-unknown rule"
  artifacts:
    - path: "ollama/ollama.go"
      provides: "callback-to-iterator stream bridge"
      contains: "func (o *Ollama) Stream"
    - path: "ollama/ollama_test.go"
      provides: "cancel and leak-safety tests"
      contains: "TestStream_Ollama"
---

<objective>
Implement Ollama streaming by adapting the SDK callback API into the shared
iterator contract. This plan is mainly about correct cancellation, cleanup, and
usage semantics rather than transport complexity.
</objective>

<context>
@.planning/phases/02-streaming-stream-event-validation/02-CONTEXT.md
@.planning/phases/02-streaming-stream-event-validation/02-RESEARCH.md
@.planning/phases/02-streaming-stream-event-validation/02-PATTERNS.md
@/tmp/llm-agent-providers/ollama/ollama.go
@/tmp/llm-agent-providers/ollama/map.go
@/tmp/llm-agent-providers/ollama/ollama_test.go
</context>

<tasks>
- Add `Stream(ctx, req)` using the Ollama callback stream API.
- Bridge callbacks into `StreamReader.Next()` safely.
- Ensure cancellation returns quickly and leaks no goroutines.
- Add tests for text-delta ordering, cancellation, and unknown-usage on abort.
</tasks>

<verify>
- `GOCACHE=/tmp/go-build go test ./ollama/...`
- `GOCACHE=/tmp/go-build go build ./ollama/...`
</verify>
