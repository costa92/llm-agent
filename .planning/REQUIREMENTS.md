# Requirements: awaiting next milestone

**Status:** no active milestone requirements as of 2026-05-12
**Core Value:** the core `llm-agent` module remains stdlib-only and zero-dep;
providers, telemetry, and reference services stay opt-in sister repos

The full `v0.3` requirement set has been archived to
`.planning/milestones/v0.3-REQUIREMENTS.md`.

## Active

- None. `v0.3` has been closed and archived.

## Carry-forward Notes

- `DEPRC-01..04` remain future work, but they are not active milestone
  requirements until the Phase 7 calendar gate is intentionally opened.
- Any new feature work should begin by writing a fresh milestone requirement
  set instead of reusing the archived `v0.3` scope.

## Next Action

- Use `.planning/ROADMAP.md` as the active forward-work view.
- When a new milestone starts, replace this file with a fresh requirements
  document scoped to that milestone.
