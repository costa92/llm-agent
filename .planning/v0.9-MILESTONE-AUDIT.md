# v0.9 Milestone Audit — GraphRAG refinements: DRIFT search and path-ranking

> Archived audit record only.
> This file records historical milestone verification and must not be used as
> a current development guide.

**Audited:** 2026-05-20
**Milestone:** v0.9 — GraphRAG refinements for `llm-agent-rag`
**Phases:** 26-27 (path-ranking / DRIFT search), all executed
**Verdict:** ✅ **PASS** — all 4 requirements (`RAG-GRAPH4-01..04`) delivered
and verified green; milestone is code-complete, pending the close steps in
the last section.

## Verification gate (re-run at audit time)

- `llm-agent-rag`: `GOWORK=off go build ./... && go vet ./... && go test
  ./... -count=1` — all 19 packages **ok**, no FAIL. `git diff --stat
  go.mod go.sum` — **empty**: the standalone SDK gained **no new
  dependency** across both phases.
- Core facade `github.com/costa92/llm-agent/rag` — `go vet ./rag/...` +
  `go test ./rag/...` **ok**. The additive v0.9 work does not perturb the
  facade.

## Requirement-by-requirement verdict

| Req | Phase | Delivered artifact | Verdict |
|-----|-------|--------------------|---------|
| RAG-GRAPH4-01 | 26 | `graph.RankedPath` type + `PathRanker` seam; deterministic pure-stdlib `WeightedPathRanker` — bounded-DFS simple-path enumeration over a `Subgraph`, composite score (length + `Relation.Weight` + provenance overlap), total entity-ID-sequence tie-break; golden tests | ✅ |
| RAG-GRAPH4-02 | 26 | opt-in `GraphRetriever.PathRanker` field; additive `GraphTrace.Paths` + `EvidenceSubgraph` surfaced through `rag.Diagnostics`; `Retrieve` byte-identical (hits/scores/RRF) when path mode off; worked example + docs | ✅ |
| RAG-GRAPH4-03 | 27 | `rag.System.AskDrift` + `DriftOptions` + `Diagnostics.Drift` — a third answer path: global primer (reusing `AskGlobal`'s helpers) → hard-bounded local follow-up loop (round cap 3, terminate on no-new-entities) → synthesis; not a `Retriever`, not a mode flag; graceful degradation | ✅ |
| RAG-GRAPH4-04 | 27 | `eval.DriftEvaluator` — DRIFT-answer RAG-Triad/`LLMJudge` harness (groundedness vs consulted reports + answer-relevance, no chunk recall@k); deterministic worked example; `docs/graphrag.md` finalized with the v1.0+ deferral list | ✅ |

## Keystone-decision compliance (KG4-1..KG4-7)

- **KG4-1** v0.9 ships DRIFT + path-ranking; incremental community
  maintenance deferred again, documented in `docs/graphrag.md` "Deferred to
  v1.0+". ✅
- **KG4-2** DRIFT is a third answer path — `AskDrift` orchestrates the
  global primer + local traversal; it does not implement `retrieve.Retriever`
  and is not a mode flag on `Ask`/`AskGlobal`. ✅
- **KG4-3** the DRIFT local loop is hard-bounded — round cap 3,
  termination on no-new-follow-up-entities; verified by the
  round-cap/early-termination tests. ✅
- **KG4-4** path ranking is deterministic, pure stdlib, an opt-in additive
  mode — `Retrieve` byte-identical when `PathRanker` is nil; verified by an
  explicit identical-hits test. ✅
- **KG4-5** incremental community maintenance deferred — v0.8's full
  re-detection stays; documented with a profiling trigger. ✅
- **KG4-6** determinism — path ranking golden-tested; DRIFT orchestration
  scripted-model golden-tested; `eval` gates use scripted model + judge. ✅
- **KG4-7** no new dependency; no existing seam changed shape —
  `AskGlobal`, `GraphRetriever` (path mode off), `GraphStore`,
  `CommunityStore`, `LouvainDetector` untouched; `go.mod` diff empty. ✅

## Carry-forward debt

- **Live-Postgres CI wiring** (from v0.5): the `postgres` `tsvector`,
  graph, and `_communities`/`_community_reports` paths remain env-gated and
  not runtime-verified against a live database. v0.9 added no `postgres`
  code — this debt is unchanged.
- **Incremental community maintenance** is a deliberate v1.0+ deferral
  (KG4-5), with a documented profiling trigger — not debt.

## Close steps (pending operator ask)

1. Commit v0.9 changes — `llm-agent-rag` working tree + the `.planning/`
   tree in this core repo.
2. Merge `llm-agent-rag` to `master`, tag `v0.6.0` (SDK version line:
   `v0.5.0` → `v0.6.0`, additive — no new dependency, no breaking change),
   update `CHANGELOG.md`.
3. Push `llm-agent-rag`. (`llm-agent-otel` needs no `require` bump — v0.9
   adds no API its consumers depend on.)
4. `/gsd-transition` — archive v0.9 ROADMAP/REQUIREMENTS to
   `.planning/milestones/`, mark traceability `Done`, move to
   between-milestones state.
